---
phase: 10-osc-remote-users-and-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - juce/osc/OscServer.h
  - juce/osc/OscServer.cpp
  - src/core/njclient.h
  - src/core/njclient.cpp
  - docs/osc.md
autonomous: true
requirements: [OSC-04, OSC-05, OSC-08]
must_haves:
  truths:
    - "Remote user volume/pan/mute controllable via /JamWide/remote/{idx}/volume, /pan, /mute OSC addresses"
    - "Remote user group solo via /JamWide/remote/{idx}/solo sets solo on ALL sub-channels simultaneously"
    - "Sub-channel volume/pan/mute/solo controllable via /JamWide/remote/{idx}/ch/{n}/volume where {n} is sequential 1-based (NOT sparse NINJAM bit index)"
    - "Remote user names broadcast as string OSC messages only when roster changes (dirty-flag, NOT every tick)"
    - "Connect trigger /JamWide/session/connect with string host:port dispatches ConnectCommand with validation"
    - "Disconnect trigger /JamWide/session/disconnect dispatches DisconnectCommand"
    - "Remote user VU meters broadcast every 100ms tick"
    - "All cached send-state and echo suppression flags reset when roster membership changes"
  artifacts:
    - path: "juce/osc/OscServer.cpp"
      provides: "sendDirtyRemoteUsers, sendRemoteVuMeters, sendRemoteRoster, handleRemoteUserOsc, handleOscStringOnMessageThread"
      contains: "sendDirtyRemoteUsers"
    - path: "juce/osc/OscServer.h"
      provides: "New method declarations and remote user dirty-tracking state"
      contains: "sendRemoteRoster"
    - path: "src/core/njclient.h"
      provides: "Extended RemoteUserInfo with volume and pan fields"
      contains: "float volume"
    - path: "src/core/njclient.cpp"
      provides: "Snapshot populates user-level volume and pan"
      contains: "info.volume = user->volume"
    - path: "docs/osc.md"
      provides: "Complete OSC address reference including remote users, roster, connect/disconnect, with explicit sub-channel semantics"
      contains: "/JamWide/remote/"
  key_links:
    - from: "juce/osc/OscServer.cpp (sendDirtyRemoteUsers)"
      to: "processor.cachedUsers"
      via: "Direct iteration of RemoteUserInfo vector"
      pattern: "processor\\.cachedUsers"
    - from: "juce/osc/OscServer.cpp (handleRemoteUserOsc)"
      to: "processor.cmd_queue"
      via: "SetUserStateCommand and SetUserChannelStateCommand dispatch"
      pattern: "cmd_queue\\.try_push"
    - from: "juce/osc/OscServer.cpp (handleOscStringOnMessageThread)"
      to: "processor.cmd_queue"
      via: "ConnectCommand and DisconnectCommand dispatch"
      pattern: "ConnectCommand"
---

<objective>
Extend the Phase 9 OscServer with remote user parameter control (send + receive), roster name broadcast, remote VU meters, and connect/disconnect triggers via OSC.

Purpose: Enable users to control all remote participants from a TouchOSC surface with bidirectional feedback, and to connect/disconnect from NINJAM servers via OSC triggers.

Output: Extended OscServer.h/.cpp with 5 new methods, extended RemoteUserInfo snapshot with user-level volume/pan, updated docs/osc.md with remote user address tables and explicit semantics.

Key design decisions addressing review concerns:
- Sub-channel `/ch/{n}` uses SEQUENTIAL 1-based indexing (not sparse NINJAM bit index). Code resolves to NINJAM bit index via `channels[n-1].channel_index`.
- Group-bus solo has NO native NJClient primitive. Implementation: sets solo on ALL sub-channels simultaneously. Documented as authoritative behavior.
- Slot index resolution happens on the message thread where `cachedUsers` is updated, so parse-time and execution-time roster are guaranteed identical (single-threaded serialization).
- Roster change resets ALL cached send-state and echo suppression flags to avoid stale/inherited state from previous slot occupants.
- String OSC support is holistic: both incoming (connect trigger) and outgoing (roster names, sub-channel names) handled in a unified extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-osc-server-core/09-01-SUMMARY.md
@.planning/phases/09-osc-server-core/09-02-SUMMARY.md
@.planning/phases/10-osc-remote-users-and-template/10-CONTEXT.md
@.planning/phases/10-osc-remote-users-and-template/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From juce/osc/OscServer.h (current -- to be extended):
```cpp
class OscServer : public juce::OSCReceiver::Listener<juce::OSCReceiver::RealtimeCallback>,
                  private juce::Timer
{
public:
    explicit OscServer(JamWideJuceProcessor& proc);
    ~OscServer() override;
    void oscMessageReceived(const juce::OSCMessage& msg) override;
    void oscBundleReceived(const juce::OSCBundle& bundle) override;
    bool start(int receivePort, const juce::String& sendIP, int sendPort);
    void stop();
    bool isEnabled() const;
    bool hasError() const;
    juce::String getErrorMessage() const;
    JamWideJuceProcessor& getProcessor();
private:
    void timerCallback() override;
    void handleOscOnMessageThread(const juce::String& address, float value);
    void sendDirtyApvtsParams(juce::OSCBundle& bundle, bool& hasContent);
    void sendDirtyNonApvtsParams(juce::OSCBundle& bundle, bool& hasContent);
    void sendDirtyTelemetry(juce::OSCBundle& bundle, bool& hasContent);
    void sendVuMeters(juce::OSCBundle& bundle, bool& hasContent);
    float getCurrentValue(int index);
    JamWideJuceProcessor& processor;
    OscAddressMap addressMap;
    juce::OSCReceiver receiver;
    juce::OSCSender sender;
    static constexpr int kMaxParams = 64;
    std::array<float, kMaxParams> lastSentValues{};
    std::array<bool, kMaxParams> oscSourced{};
    float lastSentBpm = -1.0f;
    int lastSentBpi = -1;
    int lastSentBeat = -1;
    int lastSentStatus = -999;
    int lastSentUsers = -1;
    float lastSentSampleRate = -1.0f;
    juce::String lastSentCodec;
    std::atomic<bool> enabled{false};
    std::atomic<bool> receiverError{false};
    bool senderConnected = false;
    bool receiverConnected = false;
    juce::String errorMsg;
    int currentReceivePort = 9000;
    juce::String currentSendIP{"127.0.0.1"};
    int currentSendPort = 9001;
    std::shared_ptr<std::atomic<bool>> alive;
};
```

From src/threading/ui_command.h:
```cpp
namespace jamwide {
struct ConnectCommand { std::string server; std::string username; std::string password; };
struct DisconnectCommand {};
struct SetUserStateCommand {
    int user_index = 0;
    bool set_vol = false; float volume = 1.0f;
    bool set_pan = false; float pan = 0.0f;
    bool set_mute = false; bool mute = false;
    // NOTE: NO set_solo field — solo only exists at sub-channel level
};
struct SetUserChannelStateCommand {
    int user_index = 0; int channel_index = 0;
    bool set_sub = false; bool subscribed = false;
    bool set_vol = false; float volume = 1.0f;
    bool set_pan = false; float pan = 0.0f;
    bool set_mute = false; bool mute = false;
    bool set_solo = false; bool solo = false;
    bool set_outch = false; int outchannel = 0;
};
}
```

From src/core/njclient.h (lines 173-197):
```cpp
// NJClient::SetUserState -- group bus control (vol, pan, mute only, NO solo)
void SetUserState(int idx, bool setvol, float vol, bool setpan, float pan, bool setmute, bool mute);

struct RemoteChannelInfo {
    char name[kRemoteNameMax + 1] = {};
    int name_len = 0;
    int channel_index = -1;   // NINJAM bit index (NOT sequential!)
    bool subscribed = false;
    float volume = 1.0f;
    float pan = 0.0f;
    bool mute = false;
    bool solo = false;
    float vu_left = 0.0f;
    float vu_right = 0.0f;
    int out_chan_index = 0;
    unsigned int codec_fourcc = 0;
};

struct RemoteUserInfo {
    char name[kRemoteNameMax + 1] = {};
    int name_len = 0;
    bool mute = false;
    // NOTE: Currently MISSING volume and pan fields!
    // Task 1 must add: float volume = 1.0f; float pan = 0.0f;
    std::vector<RemoteChannelInfo> channels;
};
```

From juce/JamWideJuceProcessor.h:
```cpp
class JamWideJuceProcessor : public juce::AudioProcessor {
    std::unique_ptr<OscServer> oscServer;
    jamwide::SpscRing<jamwide::UiCommand, 256> cmd_queue;
    std::vector<NJClient::RemoteUserInfo> cachedUsers;
    std::atomic<int> userCount{0};
    juce::String lastServerAddress{"ninbot.com"};
    juce::String lastUsername{"anonymous"};
    NJClient* getClient();
};
```

From juce/ui/ChannelStripArea.cpp (reference dispatch patterns, lines 522-560):
```cpp
// Group bus dispatch (user-level): uses SetUserStateCommand
jamwide::SetUserStateCommand cmd;
cmd.user_index = u;  // 0-based
cmd.set_vol = true;
cmd.volume = vol;    // raw float from fader (range matches NJClient)
processorRef.cmd_queue.try_push(std::move(cmd));

// Sub-channel dispatch: uses SetUserChannelStateCommand
jamwide::SetUserChannelStateCommand cmd;
cmd.user_index = uIdx;
cmd.channel_index = cIdx;  // NINJAM bit index from channels[n].channel_index
cmd.set_vol = true;
cmd.volume = vol;
processorRef.cmd_queue.try_push(std::move(cmd));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RemoteUserInfo snapshot, add remote user OSC send methods, and wire into timerCallback</name>
  <files>
    src/core/njclient.h
    src/core/njclient.cpp
    juce/osc/OscServer.h
    juce/osc/OscServer.cpp
  </files>
  <read_first>
    juce/osc/OscServer.h
    juce/osc/OscServer.cpp
    src/core/njclient.h
    src/core/njclient.cpp
    juce/osc/OscAddressMap.h
    juce/JamWideJuceProcessor.h
    juce/ui/ChannelStripArea.cpp
  </read_first>
  <action>
**1. Extend RemoteUserInfo (src/core/njclient.h, inside the RemoteUserInfo struct, after `bool mute = false;`):**

Add two fields:
```cpp
float volume = 1.0f;
float pan = 0.0f;
```

These mirror the private `RemoteUser::volume` and `RemoteUser::pan` fields. The snapshot needs them so OscServer can read user-level volume/pan feedback without touching NJClient directly.

Addresses review concern: "RemoteUserInfo extension may be wrong layer" -- ACCEPTED because volume and pan ARE authoritative mix state already on RemoteUser. Adding to snapshot is the correct pattern, consistent with how mute is already exposed. OscServer should not maintain shadow state for values that live in NJClient.

**2. Populate new fields in GetRemoteUsersSnapshot (src/core/njclient.cpp):**

Find the `GetRemoteUsersSnapshot` function. After the line that sets `info.mute = user->muted;`, add:
```cpp
info.volume = user->volume;
info.pan = user->pan;
```

**3. Add new method declarations to OscServer.h (private section, after `sendVuMeters` declaration):**

```cpp
// Phase 10: Remote user send methods (D-01 through D-08, D-17)
void sendDirtyRemoteUsers(juce::OSCBundle& bundle, bool& hasContent);
void sendRemoteVuMeters(juce::OSCBundle& bundle, bool& hasContent);
void sendRemoteRoster(juce::OSCBundle& bundle, bool& hasContent);

// Phase 10: Remote user receive dispatch (D-18)
void handleRemoteUserOsc(const juce::String& address, float value);

// Phase 10: String-argument OSC handler (D-09, D-10)
void handleOscStringOnMessageThread(const juce::String& address, const juce::String& value);
```

**4. Add member variables to OscServer.h (after `lastSentCodec` declaration):**

```cpp
// ── Phase 10: Remote user dirty tracking ──
static constexpr int kMaxRemoteSlots = 16;   // per D-05
static constexpr int kMaxSubChannels = 8;    // practical upper bound per user

// Group bus last-sent state per remote user
struct RemoteUserLastSent {
    float volume = -999.0f;
    float pan = -999.0f;
    float mute = -999.0f;
};
std::array<RemoteUserLastSent, kMaxRemoteSlots> lastSentRemoteUsers{};

// Sub-channel last-sent state
struct RemoteChannelLastSent {
    float volume = -999.0f;
    float pan = -999.0f;
    float mute = -999.0f;
    float solo = -999.0f;
};
std::array<std::array<RemoteChannelLastSent, kMaxSubChannels>, kMaxRemoteSlots> lastSentRemoteChannels{};

// Per-slot echo suppression (cleared on roster change to prevent inherited state)
struct RemoteUserOscSourced {
    bool volume = false;
    bool pan = false;
    bool mute = false;
};
std::array<RemoteUserOscSourced, kMaxRemoteSlots> remoteOscSourced{};

struct RemoteChannelOscSourced {
    bool volume = false;
    bool pan = false;
    bool mute = false;
    bool solo = false;
};
std::array<std::array<RemoteChannelOscSourced, kMaxSubChannels>, kMaxRemoteSlots> remoteChOscSourced{};

// Roster change detection (per D-07 — do NOT broadcast every tick)
int lastSentRosterCount = -1;
int lastSentRosterHash = -1;
```

**5. Implement sendDirtyRemoteUsers in OscServer.cpp:**

Follow the existing `sendVuMeters` pattern. Iterate `processor.cachedUsers`, cap at 16 (per D-05). Use 1-based OSC index (per D-02).

```cpp
void OscServer::sendDirtyRemoteUsers(juce::OSCBundle& bundle, bool& hasContent)
{
    const auto& users = processor.cachedUsers;
    const int count = juce::jmin(static_cast<int>(users.size()), kMaxRemoteSlots);

    for (int i = 0; i < count; ++i)
    {
        const auto& user = users[static_cast<size_t>(i)];
        auto& last = lastSentRemoteUsers[static_cast<size_t>(i)];
        auto& src = remoteOscSourced[static_cast<size_t>(i)];
        juce::String prefix = "/JamWide/remote/" + juce::String(i + 1);

        // Group bus volume: normalize NJClient 0-2 -> OSC 0-1
        // (NJClient user volume default 1.0 = unity; APVTS local channels use 0-2 range)
        float oscVol = user.volume * 0.5f;  // divide by 2
        if (!src.volume && oscVol != last.volume)
        {
            bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/volume"), oscVol));
            bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/volume/db"),
                OscAddressMap::linearToDb(user.volume)));
            last.volume = oscVol;
            hasContent = true;
        }
        src.volume = false;

        // Group bus pan: NJClient -1..1 -> OSC 0-1
        float oscPan = OscAddressMap::apvtsPanToOsc(user.pan);
        if (!src.pan && oscPan != last.pan)
        {
            bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/pan"), oscPan));
            last.pan = oscPan;
            hasContent = true;
        }
        src.pan = false;

        // Group bus mute
        float oscMute = user.mute ? 1.0f : 0.0f;
        if (!src.mute && oscMute != last.mute)
        {
            bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/mute"), oscMute));
            last.mute = oscMute;
            hasContent = true;
        }
        src.mute = false;

        // Per sub-channel (per D-03)
        // {n} is SEQUENTIAL 1-based, matching array index in user.channels
        const int chCount = juce::jmin(static_cast<int>(user.channels.size()), kMaxSubChannels);
        for (int c = 0; c < chCount; ++c)
        {
            const auto& ch = user.channels[static_cast<size_t>(c)];
            auto& chLast = lastSentRemoteChannels[static_cast<size_t>(i)][static_cast<size_t>(c)];
            auto& chSrc = remoteChOscSourced[static_cast<size_t>(i)][static_cast<size_t>(c)];
            juce::String chPrefix = prefix + "/ch/" + juce::String(c + 1);

            float chOscVol = ch.volume * 0.5f;
            if (!chSrc.volume && chOscVol != chLast.volume)
            {
                bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(chPrefix + "/volume"), chOscVol));
                bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(chPrefix + "/volume/db"),
                    OscAddressMap::linearToDb(ch.volume)));
                chLast.volume = chOscVol;
                hasContent = true;
            }
            chSrc.volume = false;

            float chOscPan = OscAddressMap::apvtsPanToOsc(ch.pan);
            if (!chSrc.pan && chOscPan != chLast.pan)
            {
                bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(chPrefix + "/pan"), chOscPan));
                chLast.pan = chOscPan;
                hasContent = true;
            }
            chSrc.pan = false;

            float chOscMute = ch.mute ? 1.0f : 0.0f;
            if (!chSrc.mute && chOscMute != chLast.mute)
            {
                bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(chPrefix + "/mute"), chOscMute));
                chLast.mute = chOscMute;
                hasContent = true;
            }
            chSrc.mute = false;

            float chOscSolo = ch.solo ? 1.0f : 0.0f;
            if (!chSrc.solo && chOscSolo != chLast.solo)
            {
                bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(chPrefix + "/solo"), chOscSolo));
                chLast.solo = chOscSolo;
                hasContent = true;
            }
            chSrc.solo = false;
        }
    }
}
```

NOTE: Group bus solo feedback is intentionally NOT sent per-tick. There is no single group-solo boolean in NJClient. The receive path (Task 2) sets solo on ALL sub-channels, and the per-sub-channel solo feedback above reflects the result. The template shows per-user solo button that maps to `/JamWide/remote/{idx}/solo` on the RECEIVE path only. On the SEND path, the group solo state is derived: if ALL sub-channels are soloed, the group is "soloed". This derivation is added in sendDirtyRemoteUsers to send feedback for the group solo button:

```cpp
        // Group bus solo feedback: derived from sub-channel state
        // "Soloed" if ALL sub-channels have solo=true (matches the group solo receive semantics)
        bool allSoloed = !user.channels.empty();
        for (const auto& ch : user.channels)
            if (!ch.solo) { allSoloed = false; break; }
        float groupSoloVal = allSoloed ? 1.0f : 0.0f;
        // Use lastSentRemoteUsers[i] with an additional field... or use a separate array:
```

Actually, to keep it simpler, add a `float solo = -999.0f;` field to `RemoteUserLastSent` and track it:

Update the `RemoteUserLastSent` struct to include:
```cpp
struct RemoteUserLastSent {
    float volume = -999.0f;
    float pan = -999.0f;
    float mute = -999.0f;
    float solo = -999.0f;  // derived: 1.0 if ALL sub-channels soloed, 0.0 otherwise
};
```

And in `sendDirtyRemoteUsers`, after the sub-channel loop, add:
```cpp
        // Group bus solo feedback (derived -- see design note in objective)
        bool allSoloed = !user.channels.empty();
        for (const auto& ch : user.channels)
            if (!ch.solo) { allSoloed = false; break; }
        float oscGroupSolo = allSoloed ? 1.0f : 0.0f;
        if (oscGroupSolo != last.solo)
        {
            bundle.addElement(juce::OSCMessage(
                juce::OSCAddressPattern(prefix + "/solo"), oscGroupSolo));
            last.solo = oscGroupSolo;
            hasContent = true;
        }
```

**6. Implement sendRemoteVuMeters in OscServer.cpp:**

Always-dirty pattern (same as `sendVuMeters`). Iterate `processor.cachedUsers`, cap at 16.

```cpp
void OscServer::sendRemoteVuMeters(juce::OSCBundle& bundle, bool& hasContent)
{
    const auto& users = processor.cachedUsers;
    const int count = juce::jmin(static_cast<int>(users.size()), kMaxRemoteSlots);

    for (int i = 0; i < count; ++i)
    {
        const auto& user = users[static_cast<size_t>(i)];
        juce::String prefix = "/JamWide/remote/" + juce::String(i + 1);

        // Aggregate VU: max across sub-channels
        float maxL = 0.0f, maxR = 0.0f;
        for (const auto& ch : user.channels)
        {
            maxL = juce::jmax(maxL, ch.vu_left);
            maxR = juce::jmax(maxR, ch.vu_right);
        }
        bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/vu/left"), maxL));
        bundle.addElement(juce::OSCMessage(juce::OSCAddressPattern(prefix + "/vu/right"), maxR));

        // Per sub-channel VU
        const int chCount = juce::jmin(static_cast<int>(user.channels.size()), kMaxSubChannels);
        for (int c = 0; c < chCount; ++c)
        {
            juce::String chPrefix = prefix + "/ch/" + juce::String(c + 1);
            bundle.addElement(juce::OSCMessage(
                juce::OSCAddressPattern(chPrefix + "/vu/left"), user.channels[static_cast<size_t>(c)].vu_left));
            bundle.addElement(juce::OSCMessage(
                juce::OSCAddressPattern(chPrefix + "/vu/right"), user.channels[static_cast<size_t>(c)].vu_right));
        }
        hasContent = true;
    }
}
```

**7. Implement sendRemoteRoster in OscServer.cpp:**

Dirty-flag pattern. Compute hash each tick, only broadcast when changed. Addresses review concern: "cached send-state reset on slot occupancy change" -- when roster changes, reset ALL per-slot dirty tracking, echo suppression, and last-sent state.

```cpp
void OscServer::sendRemoteRoster(juce::OSCBundle& bundle, bool& hasContent)
{
    const auto& users = processor.cachedUsers;
    const int count = juce::jmin(static_cast<int>(users.size()), kMaxRemoteSlots);

    // Compute hash: size + sum of name lengths (simple, sufficient for change detection)
    int hash = count;
    for (int i = 0; i < count; ++i)
    {
        hash += static_cast<int>(strlen(users[static_cast<size_t>(i)].name));
        for (const auto& ch : users[static_cast<size_t>(i)].channels)
            hash += static_cast<int>(strlen(ch.name));
    }

    if (hash == lastSentRosterHash && count == lastSentRosterCount)
        return;  // No change

    // ── ROSTER CHANGED: Reset all per-slot cached state ──
    // Addresses review concern: "stale slot index handling" and "cached send-state reset"
    // This prevents a new user in slot N from inheriting the previous occupant's
    // echo suppression flags or last-sent values.
    for (auto& u : lastSentRemoteUsers) u = RemoteUserLastSent{};
    for (auto& u : lastSentRemoteChannels) for (auto& c : u) c = RemoteChannelLastSent{};
    for (auto& u : remoteOscSourced) u = RemoteUserOscSourced{};
    for (auto& u : remoteChOscSourced) for (auto& c : u) c = RemoteChannelOscSourced{};

    // Broadcast names for active slots
    for (int i = 0; i < count; ++i)
    {
        juce::String idx(i + 1);
        juce::String prefix = "/JamWide/remote/" + idx;

        // Strip @IP suffix from username (same pattern as ChannelStripArea.cpp)
        juce::String fullName(users[static_cast<size_t>(i)].name);
        int atIdx = fullName.lastIndexOfChar('@');
        juce::String displayName = (atIdx > 0) ? fullName.substring(0, atIdx) : fullName;

        bundle.addElement(juce::OSCMessage(
            juce::OSCAddressPattern(prefix + "/name"), displayName));

        // Sub-channel names (per D-08)
        const auto& user = users[static_cast<size_t>(i)];
        for (size_t c = 0; c < user.channels.size() && c < static_cast<size_t>(kMaxSubChannels); ++c)
        {
            juce::String chName(user.channels[c].name);
            bundle.addElement(juce::OSCMessage(
                juce::OSCAddressPattern(prefix + "/ch/" + juce::String(static_cast<int>(c) + 1) + "/name"),
                chName));
        }
    }

    // Clear empty slots (per D-06: send empty string)
    for (int i = count; i < kMaxRemoteSlots; ++i)
    {
        juce::String idx(i + 1);
        bundle.addElement(juce::OSCMessage(
            juce::OSCAddressPattern("/JamWide/remote/" + idx + "/name"), juce::String()));
    }

    lastSentRosterCount = count;
    lastSentRosterHash = hash;
    hasContent = true;
}
```

**8. Wire new send methods into timerCallback:**

In `OscServer::timerCallback()`, after the existing `sendVuMeters(bundle, hasContent);` call, add:
```cpp
    sendRemoteRoster(bundle, hasContent);     // BEFORE dirty params (roster change resets cache)
    sendDirtyRemoteUsers(bundle, hasContent);
    sendRemoteVuMeters(bundle, hasContent);
```

NOTE: `sendRemoteRoster` must come BEFORE `sendDirtyRemoteUsers` so that the cache reset from a roster change takes effect before dirty-checking remote user params in the same tick.

**9. Reset remote dirty tracking in start() and stop():**

In `OscServer::start()`, after the existing reset block, add:
```cpp
for (auto& u : lastSentRemoteUsers) u = RemoteUserLastSent{};
for (auto& u : lastSentRemoteChannels) for (auto& c : u) c = RemoteChannelLastSent{};
for (auto& u : remoteOscSourced) u = RemoteUserOscSourced{};
for (auto& u : remoteChOscSourced) for (auto& c : u) c = RemoteChannelOscSourced{};
lastSentRosterCount = -1;
lastSentRosterHash = -1;
```
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide/build-juce && cmake --build . --target JamWide_VST3 --parallel 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/core/njclient.h RemoteUserInfo struct contains `float volume = 1.0f;`
    - src/core/njclient.h RemoteUserInfo struct contains `float pan = 0.0f;`
    - src/core/njclient.cpp GetRemoteUsersSnapshot contains `info.volume = user->volume;`
    - src/core/njclient.cpp GetRemoteUsersSnapshot contains `info.pan = user->pan;`
    - juce/osc/OscServer.h contains `void sendDirtyRemoteUsers(`
    - juce/osc/OscServer.h contains `void sendRemoteVuMeters(`
    - juce/osc/OscServer.h contains `void sendRemoteRoster(`
    - juce/osc/OscServer.h contains `void handleRemoteUserOsc(`
    - juce/osc/OscServer.h contains `void handleOscStringOnMessageThread(`
    - juce/osc/OscServer.h contains `kMaxRemoteSlots = 16`
    - juce/osc/OscServer.h contains `RemoteUserOscSourced` (per-slot echo suppression struct)
    - juce/osc/OscServer.h contains `RemoteChannelOscSourced` (per-channel echo suppression struct)
    - juce/osc/OscServer.cpp contains `sendRemoteRoster(bundle, hasContent)` BEFORE `sendDirtyRemoteUsers`
    - juce/osc/OscServer.cpp sendDirtyRemoteUsers contains `/JamWide/remote/`
    - juce/osc/OscServer.cpp sendRemoteRoster contains `lastSentRosterHash` dirty check
    - juce/osc/OscServer.cpp sendRemoteRoster contains `RemoteUserLastSent{}` (cache reset on roster change)
    - juce/osc/OscServer.cpp sendRemoteRoster contains `RemoteUserOscSourced{}` (echo suppression reset)
    - juce/osc/OscServer.cpp sendDirtyRemoteUsers contains `allSoloed` (derived group solo feedback)
    - Build target JamWide_VST3 compiles with zero errors
  </acceptance_criteria>
  <done>
    RemoteUserInfo extended with volume/pan. Three new send methods (sendDirtyRemoteUsers, sendRemoteVuMeters, sendRemoteRoster) wired into timerCallback. Roster broadcasts only on change (not every tick). Roster change resets ALL per-slot cached state and echo suppression flags. Group solo is derived from sub-channel state. All 16 remote slots supported with group bus and per-sub-channel feedback. Build compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add remote user receive path with prefix dispatch, string OSC handling, connect/disconnect triggers, and docs update</name>
  <files>
    juce/osc/OscServer.cpp
    juce/osc/OscServer.h
    docs/osc.md
  </files>
  <read_first>
    juce/osc/OscServer.h
    juce/osc/OscServer.cpp
    src/threading/ui_command.h
    src/core/njclient.h
    juce/JamWideJuceProcessor.h
    juce/ui/ChannelStripArea.cpp
    juce/NinjamRunThread.cpp
    docs/osc.md
  </read_first>
  <action>
**1. Extend oscMessageReceived to handle string arguments (addresses review concern: "string handling should be reviewed holistically"):**

The current `oscMessageReceived()` returns early on non-float/int arguments. Add a comprehensive string branch BEFORE the existing float/int handling. This covers both the connect trigger (incoming string) and any future string-argument addresses:

```cpp
void OscServer::oscMessageReceived(const juce::OSCMessage& msg)
{
    auto address = msg.getAddressPattern().toString();

    // Phase 10: String argument path (connect trigger per D-09, holistic string support)
    if (msg.size() > 0 && msg[0].isString())
    {
        juce::String strValue = msg[0].getString();
        auto aliveFlag = alive;
        juce::MessageManager::callAsync([this, aliveFlag,
                                         addr = std::move(address),
                                         str = std::move(strValue)]()
        {
            if (!aliveFlag->load(std::memory_order_acquire))
                return;
            handleOscStringOnMessageThread(addr, str);
        });
        return;
    }

    // Existing float/int handling follows unchanged...
    // (keep all existing code after this point)
}
```

**2. Add prefix-based dispatch in handleOscOnMessageThread (per D-18):**

At the TOP of `handleOscOnMessageThread`, BEFORE the existing `addressMap.resolve()` call, add two blocks:

```cpp
void OscServer::handleOscOnMessageThread(const juce::String& address, float value)
{
    // Phase 10: remote user prefix dispatch (per D-18)
    // Addresses review concern: "stale slot index" -- resolution happens HERE on the
    // message thread, which is the same thread that updates cachedUsers. This guarantees
    // the roster snapshot is current at dispatch time (single-threaded serialization).
    if (address.startsWith("/JamWide/remote/"))
    {
        handleRemoteUserOsc(address, value);
        return;
    }

    // Phase 10: session disconnect float trigger (per D-09)
    // Disconnect accepts: any float >= 0.5, or int 1
    if (address == "/JamWide/session/disconnect")
    {
        if (value >= 0.5f)
        {
            jamwide::DisconnectCommand cmd;
            processor.cmd_queue.try_push(std::move(cmd));
        }
        return;
    }

    // Existing static map lookup follows...
    int idx = addressMap.resolve(address);
    // ... (keep existing code unchanged)
}
```

**3. Implement handleRemoteUserOsc with explicit sub-channel semantics:**

Addresses review concerns:
- "Sub-channel `/ch/{n}` semantics" -- `{n}` is SEQUENTIAL 1-based (array index into `user.channels[]`), resolved to NINJAM bit index via `channels[n-1].channel_index`
- "Group-bus solo semantics" -- group solo dispatches `SetUserChannelStateCommand` with `set_solo=true` to ALL sub-channels. This is the AUTHORITATIVE behavior: group solo = set ALL sub-channels to the same solo state.

```cpp
void OscServer::handleRemoteUserOsc(const juce::String& address, float value)
{
    // Parse: /JamWide/remote/{idx}/...
    // "/JamWide/remote/" is 16 characters
    juce::String remainder = address.substring(16);

    int slashPos = remainder.indexOf("/");
    if (slashPos < 0) return;  // malformed

    int oscIdx = remainder.substring(0, slashPos).getIntValue();
    if (oscIdx < 1 || oscIdx > kMaxRemoteSlots) return;  // T-10-01: bounds check

    // Resolve against CURRENT roster (message thread -- same thread that updates cachedUsers)
    const auto& users = processor.cachedUsers;
    int userIndex = oscIdx - 1;  // 1-based OSC -> 0-based NJClient (Pitfall 1)
    if (userIndex >= static_cast<int>(users.size())) return;  // slot empty

    juce::String control = remainder.substring(slashPos + 1);

    // Clamp value for all numeric controls
    float clamped = juce::jlimit(0.0f, 1.0f, value);

    // ── Sub-channel path: /ch/{n}/... ──
    if (control.startsWith("ch/"))
    {
        juce::String chRemainder = control.substring(3);  // after "ch/"
        int chSlash = chRemainder.indexOf("/");
        if (chSlash < 0) return;

        // {n} is SEQUENTIAL 1-based (review concern #3: explicitly defined)
        int oscChIdx = chRemainder.substring(0, chSlash).getIntValue();
        if (oscChIdx < 1) return;
        int seqIdx = oscChIdx - 1;  // 0-based sequential index

        // Resolve NINJAM bit index from sequential index (Pitfall 2)
        const auto& user = users[static_cast<size_t>(userIndex)];
        if (seqIdx >= static_cast<int>(user.channels.size())) return;
        int njChannelIdx = user.channels[static_cast<size_t>(seqIdx)].channel_index;

        juce::String chControl = chRemainder.substring(chSlash + 1);

        jamwide::SetUserChannelStateCommand cmd;
        cmd.user_index = userIndex;
        cmd.channel_index = njChannelIdx;

        if (chControl == "volume")
        {
            cmd.set_vol = true;
            cmd.volume = clamped * 2.0f;  // OSC 0-1 -> NJClient 0-2
            // Set echo suppression
            if (seqIdx < kMaxSubChannels)
                remoteChOscSourced[static_cast<size_t>(userIndex)][static_cast<size_t>(seqIdx)].volume = true;
        }
        else if (chControl == "volume/db")
        {
            cmd.set_vol = true;
            cmd.volume = OscAddressMap::dbToLinear(juce::jlimit(-100.0f, 6.0f, value));
            if (seqIdx < kMaxSubChannels)
                remoteChOscSourced[static_cast<size_t>(userIndex)][static_cast<size_t>(seqIdx)].volume = true;
        }
        else if (chControl == "pan")
        {
            cmd.set_pan = true;
            cmd.pan = OscAddressMap::oscPanToApvts(clamped);
            if (seqIdx < kMaxSubChannels)
                remoteChOscSourced[static_cast<size_t>(userIndex)][static_cast<size_t>(seqIdx)].pan = true;
        }
        else if (chControl == "mute")
        {
            cmd.set_mute = true;
            cmd.mute = (value >= 0.5f);
            if (seqIdx < kMaxSubChannels)
                remoteChOscSourced[static_cast<size_t>(userIndex)][static_cast<size_t>(seqIdx)].mute = true;
        }
        else if (chControl == "solo")
        {
            cmd.set_solo = true;
            cmd.solo = (value >= 0.5f);
            if (seqIdx < kMaxSubChannels)
                remoteChOscSourced[static_cast<size_t>(userIndex)][static_cast<size_t>(seqIdx)].solo = true;
        }
        else return;  // Unknown sub-channel control, silently ignore

        processor.cmd_queue.try_push(std::move(cmd));
    }
    // ── Group bus path: volume, pan, mute, solo ──
    else
    {
        if (control == "volume")
        {
            jamwide::SetUserStateCommand cmd;
            cmd.user_index = userIndex;
            cmd.set_vol = true;
            cmd.volume = clamped * 2.0f;  // OSC 0-1 -> NJClient 0-2
            processor.cmd_queue.try_push(std::move(cmd));
            remoteOscSourced[static_cast<size_t>(userIndex)].volume = true;
        }
        else if (control == "volume/db")
        {
            jamwide::SetUserStateCommand cmd;
            cmd.user_index = userIndex;
            cmd.set_vol = true;
            cmd.volume = OscAddressMap::dbToLinear(juce::jlimit(-100.0f, 6.0f, value));
            processor.cmd_queue.try_push(std::move(cmd));
            remoteOscSourced[static_cast<size_t>(userIndex)].volume = true;
        }
        else if (control == "pan")
        {
            jamwide::SetUserStateCommand cmd;
            cmd.user_index = userIndex;
            cmd.set_pan = true;
            cmd.pan = OscAddressMap::oscPanToApvts(clamped);
            processor.cmd_queue.try_push(std::move(cmd));
            remoteOscSourced[static_cast<size_t>(userIndex)].pan = true;
        }
        else if (control == "mute")
        {
            jamwide::SetUserStateCommand cmd;
            cmd.user_index = userIndex;
            cmd.set_mute = true;
            cmd.mute = (value >= 0.5f);
            processor.cmd_queue.try_push(std::move(cmd));
            remoteOscSourced[static_cast<size_t>(userIndex)].mute = true;
        }
        else if (control == "solo")
        {
            // GROUP SOLO: Authoritative behavior (addresses review concern #1)
            // NJClient has NO group-level solo primitive. SetUserStateCommand has no set_solo.
            // Implementation: set solo on ALL sub-channels to the same state.
            // This means: toggling group solo ON solos every sub-channel of this user.
            //             toggling group solo OFF un-solos every sub-channel.
            // Individual sub-channel solo states are OVERRIDDEN by group solo toggle.
            const auto& user = users[static_cast<size_t>(userIndex)];
            bool soloState = (value >= 0.5f);
            for (const auto& ch : user.channels)
            {
                jamwide::SetUserChannelStateCommand cmd;
                cmd.user_index = userIndex;
                cmd.channel_index = ch.channel_index;  // NINJAM bit index
                cmd.set_solo = true;
                cmd.solo = soloState;
                processor.cmd_queue.try_push(std::move(cmd));
            }
        }
        // else: unknown group control, silently ignore (T-10-05)
    }
}
```

**4. Implement handleOscStringOnMessageThread with robust connect validation:**

Addresses review concern: "Connect trigger validation needs failure behavior for missing port, invalid port, IPv6, whitespace."

```cpp
void OscServer::handleOscStringOnMessageThread(const juce::String& address, const juce::String& value)
{
    if (address == "/JamWide/session/connect")
    {
        // Defensive parsing (per D-09, addresses review concern #7)
        juce::String trimmed = value.trim();
        if (trimmed.isEmpty() || trimmed.length() > 256) return;  // T-10-03: length limit

        juce::String host;
        int port = 2049;  // NINJAM default

        // Handle IPv6 bracketed notation: [::1]:2049
        if (trimmed.startsWithChar('['))
        {
            int closeBracket = trimmed.indexOf("]");
            if (closeBracket < 0) return;  // malformed IPv6
            host = trimmed.substring(1, closeBracket);  // strip brackets
            // Check for :port after closing bracket
            if (closeBracket + 1 < trimmed.length() && trimmed[closeBracket + 1] == ':')
            {
                juce::String portStr = trimmed.substring(closeBracket + 2);
                int parsed = portStr.getIntValue();
                if (parsed >= 1 && parsed <= 65535)
                    port = parsed;
                else
                    return;  // invalid port
            }
        }
        else
        {
            // Standard host:port or host-only
            int colonIdx = trimmed.lastIndexOfChar(':');
            if (colonIdx > 0)
            {
                juce::String portStr = trimmed.substring(colonIdx + 1);
                int parsed = portStr.getIntValue();
                if (parsed >= 1 && parsed <= 65535)
                {
                    host = trimmed.substring(0, colonIdx);
                    port = parsed;
                }
                else
                {
                    // Port part is not a valid number -- treat entire string as host
                    host = trimmed;
                }
            }
            else
            {
                host = trimmed;
                // port stays at default 2049
            }
        }

        if (host.isEmpty()) return;

        juce::String serverStr = host + ":" + juce::String(port);

        // Use stored credentials (per D-09)
        // Fallback to "anonymous" if lastUsername empty (per Pitfall 5)
        std::string username = processor.lastUsername.toStdString();
        if (username.empty())
            username = "anonymous";

        jamwide::ConnectCommand cmd;
        cmd.server = serverStr.toStdString();
        cmd.username = username;
        cmd.password = "";
        processor.cmd_queue.try_push(std::move(cmd));
    }
    // else: unknown string-argument address, silently ignore
}
```

**5. Update docs/osc.md with complete Phase 10 address tables:**

This makes `docs/osc.md` the SINGLE SOURCE OF TRUTH for all OSC addresses (addresses review concern #5). The template in Plan 02 must match this file exactly.

Apply the following changes to `docs/osc.md`:

**5a. Remove the "Future (Phase 10)" section** at the bottom of the file.

**5b. Add "Remote Users (1-16)" section** after the "Local Channels (1-4)" section under "Controllable Parameters":

```markdown
### Remote Users (1-16)

Remote user slots use **positional indexing**: slot 1 is the first connected remote user, slot 2 is the second, etc. When a user disconnects, subsequent slots shift down. The roster broadcast (below) tells your control surface which user is in each slot.

Maximum 16 controllable remote user slots. Users beyond 16 appear in the plugin UI but are not OSC-controllable.

#### Group Bus (User-Level)

| Address | Type | Range | Direction | Description |
|---------|------|-------|-----------|-------------|
| `/JamWide/remote/{idx}/volume` | float | 0 -- 1 | bidirectional | User volume (0.5 = unity / 0 dB) |
| `/JamWide/remote/{idx}/volume/db` | float | -100 -- 6 | bidirectional | User volume in dB |
| `/JamWide/remote/{idx}/pan` | float | 0 -- 1 | bidirectional | User pan (0=left, 0.5=center, 1=right) |
| `/JamWide/remote/{idx}/mute` | float | 0 or 1 | bidirectional | User mute |
| `/JamWide/remote/{idx}/solo` | float | 0 or 1 | bidirectional | User solo (see note below) |

**Solo behavior:** NJClient has no group-level solo primitive. Group solo sets solo on ALL sub-channels simultaneously. Feedback reads 1.0 only when ALL sub-channels are soloed. Individual sub-channel solo states are overridden by group solo toggle.

#### Sub-Channels

Multi-channel users expose individual sub-channels. `{n}` is a **sequential 1-based index** into the user's channel list (NOT the sparse NINJAM bit index). The server resolves sequential to internal channel index automatically.

| Address | Type | Range | Direction | Description |
|---------|------|-------|-----------|-------------|
| `/JamWide/remote/{idx}/ch/{n}/volume` | float | 0 -- 1 | bidirectional | Sub-channel volume (0.5 = unity) |
| `/JamWide/remote/{idx}/ch/{n}/volume/db` | float | -100 -- 6 | bidirectional | Sub-channel volume in dB |
| `/JamWide/remote/{idx}/ch/{n}/pan` | float | 0 -- 1 | bidirectional | Sub-channel pan |
| `/JamWide/remote/{idx}/ch/{n}/mute` | float | 0 or 1 | bidirectional | Sub-channel mute |
| `/JamWide/remote/{idx}/ch/{n}/solo` | float | 0 or 1 | bidirectional | Sub-channel solo |
```

**5c. Add "Session Control" section** after "Session Telemetry":

```markdown
## Session Control

| Address | Argument Type | Payload | Description |
|---------|---------------|---------|-------------|
| `/JamWide/session/connect` | string | `"host:port"` | Connect to NINJAM server. Port defaults to 2049 if omitted. IPv6: `"[::1]:2049"`. Uses last UI-configured username/password (defaults to "anonymous"). |
| `/JamWide/session/disconnect` | float | >= 0.5 | Disconnect from current server |

**Connect validation:** Empty host is rejected. Port must be 1-65535 (non-numeric port treated as hostname). String length limit: 256 characters. Invalid input is silently ignored.
```

**5d. Add "Remote User VU Meters" subsection** in the VU Meters section:

```markdown
### Remote Users (1-16)

| Address | Type | Description |
|---------|------|-------------|
| `/JamWide/remote/{idx}/vu/left` | float | Aggregate left VU (max across sub-channels) |
| `/JamWide/remote/{idx}/vu/right` | float | Aggregate right VU |
| `/JamWide/remote/{idx}/ch/{n}/vu/left` | float | Sub-channel left VU |
| `/JamWide/remote/{idx}/ch/{n}/vu/right` | float | Sub-channel right VU |

**Template note:** The shipped TouchOSC template omits remote VU meters intentionally (too dense for 8 slots). The server broadcasts them; advanced users can add VU controls to custom templates.
```

**5e. Add "Roster Broadcast" section** after VU Meters:

```markdown
## Roster Broadcast

Sent only when user list changes (not every update tick).

| Address | Type | Description |
|---------|------|-------------|
| `/JamWide/remote/{idx}/name` | string | Username for slot (empty `""` if empty). `@IP` suffix stripped. |
| `/JamWide/remote/{idx}/ch/{n}/name` | string | Sub-channel name |

Slot names update when any user joins, leaves, or changes channels. All 16 slot names are re-broadcast on each change (empty slots get `""`).
```
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide/build-juce && cmake --build . --target JamWide_VST3 --parallel 2>&1 | tail -5 && grep -c "/JamWide/remote/" /Users/cell/dev/JamWide/docs/osc.md</automated>
  </verify>
  <acceptance_criteria>
    - juce/osc/OscServer.cpp oscMessageReceived contains `msg[0].isString()`
    - juce/osc/OscServer.cpp oscMessageReceived contains `handleOscStringOnMessageThread`
    - juce/osc/OscServer.cpp handleOscOnMessageThread contains `startsWith("/JamWide/remote/")`
    - juce/osc/OscServer.cpp handleOscOnMessageThread contains `handleRemoteUserOsc`
    - juce/osc/OscServer.cpp handleOscOnMessageThread contains `/JamWide/session/disconnect`
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `SetUserStateCommand`
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `SetUserChannelStateCommand`
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `oscIdx - 1` (1-based to 0-based)
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `ch.channel_index` (NINJAM bit index resolution)
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains comment "GROUP SOLO" or "group solo" documenting the derived behavior
    - juce/osc/OscServer.cpp handleOscStringOnMessageThread contains `ConnectCommand`
    - juce/osc/OscServer.cpp handleOscStringOnMessageThread contains `256` (string length limit)
    - juce/osc/OscServer.cpp handleOscStringOnMessageThread contains `startsWithChar('[')` or `indexOf("]")` (IPv6 handling)
    - juce/osc/OscServer.cpp handleOscStringOnMessageThread contains `65535` (port range validation)
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `remoteOscSourced` (echo suppression)
    - juce/osc/OscServer.cpp handleRemoteUserOsc contains `remoteChOscSourced` (sub-channel echo suppression)
    - docs/osc.md contains `/JamWide/remote/{idx}/volume`
    - docs/osc.md contains `/JamWide/remote/{idx}/ch/{n}/volume`
    - docs/osc.md contains `/JamWide/remote/{idx}/vu/left`
    - docs/osc.md contains `/JamWide/remote/{idx}/name`
    - docs/osc.md contains `/JamWide/session/connect`
    - docs/osc.md contains `/JamWide/session/disconnect`
    - docs/osc.md contains "sequential 1-based index" (explicit sub-channel semantics)
    - docs/osc.md contains "Solo behavior" (explicit group solo documentation)
    - docs/osc.md contains "Roster Broadcast" section heading
    - docs/osc.md contains "Session Control" section heading
    - docs/osc.md contains "Template note" documenting intentional remote VU omission
    - docs/osc.md does NOT contain "Future (Phase 10)" section
    - grep count for "/JamWide/remote/" in docs/osc.md returns at least 20
    - Build target JamWide_VST3 compiles with zero errors
  </acceptance_criteria>
  <done>
    Remote user OSC receive works: prefix-based dispatch for /JamWide/remote/ addresses, group bus and sub-channel control with correct index mapping (sequential-to-sparse resolution), group solo authoritative behavior documented and implemented, string argument handling for connect trigger with IPv6/port validation, disconnect float trigger, per-slot echo suppression. docs/osc.md is the single source of truth for all addresses with explicit sub-channel and solo semantics. All dispatches go through cmd_queue preserving SPSC invariant. Build compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OSC network -> OscServer | Untrusted UDP packets arrive from local network (any device can send OSC) |
| OscServer -> cmd_queue | Validated commands cross into NJClient control path |
| OscServer -> NJClient (connect) | String argument triggers network connection to user-specified server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | handleRemoteUserOsc | mitigate | Bounds check: `oscIdx >= 1 && oscIdx <= min(cachedUsers.size(), 16)` before any dispatch. Out-of-range indices silently ignored. |
| T-10-02 | Tampering | handleRemoteUserOsc (sub-channel) | mitigate | Bounds check: `seqIdx < user.channels.size()`. Resolve NINJAM bit index via `channels[seqIdx].channel_index`, never pass raw OSC index to NJClient. |
| T-10-03 | Denial of Service | handleOscStringOnMessageThread | mitigate | String length limit: `trimmed.length() > 256` returns early. Port validation: 1-65535 range check. IPv6 bracket validation. Empty host rejection. |
| T-10-04 | Information Disclosure | connect trigger | accept | Connect uses stored credentials (same attack surface as plugin UI). No new credential exposure. Documented in docs/osc.md. |
| T-10-05 | Tampering | oscMessageReceived | mitigate | Malformed addresses silently ignored by prefix check and control name parsing. No crash paths on garbage input. Value clamping via juce::jlimit on all numeric inputs. |
| T-10-06 | Denial of Service | sendRemoteRoster | mitigate | Roster broadcast only on change (hash comparison), not every 100ms tick. Prevents string message flooding. Cache reset on roster change prevents stale-state inheritance. |
</threat_model>

<verification>
1. Build: `cd build-juce && cmake --build . --target JamWide_VST3 --parallel` -- zero errors
2. Existing tests: `cd build-juce && ctest --test-dir . --output-on-failure` -- all pass
3. Send methods: `grep -n "sendDirtyRemoteUsers\|sendRemoteVuMeters\|sendRemoteRoster\|handleRemoteUserOsc\|handleOscStringOnMessageThread" juce/osc/OscServer.cpp` -- all 5 present
4. Snapshot extension: `grep "info.volume = user->volume" src/core/njclient.cpp` -- present
5. docs/osc.md: `grep -c "/JamWide/remote/" docs/osc.md` -- returns 20+
6. Explicit semantics: `grep "sequential 1-based" docs/osc.md` -- present
7. Solo docs: `grep "Solo behavior" docs/osc.md` -- present
8. Cache reset: `grep "RemoteUserLastSent{}" juce/osc/OscServer.cpp` -- present in sendRemoteRoster
</verification>

<success_criteria>
- OscServer sends bidirectional feedback for up to 16 remote users (group bus + sub-channels)
- Remote user VU meters broadcast every 100ms tick
- Roster names broadcast only when user list changes, with full cache reset on change
- Incoming /JamWide/remote/ messages dispatch correct SetUserState/SetUserChannelState commands
- Sub-channel {n} is sequential 1-based, resolved to NINJAM bit index internally
- Group solo sets all sub-channels; feedback is derived (all-soloed = group soloed)
- Connect trigger dispatches ConnectCommand with robust validation (IPv6, port range, length limit)
- Disconnect trigger dispatches DisconnectCommand
- Per-slot echo suppression flags reset on roster change
- All index conversions correct (1-based OSC to 0-based NJClient)
- docs/osc.md is the single source of truth with explicit semantics
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-osc-remote-users-and-template/10-01-SUMMARY.md`
</output>
