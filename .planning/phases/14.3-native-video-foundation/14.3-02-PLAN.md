---
plan_id: 14.3-02
phase: 14.3
slug: rawdata-send-api
type: execute
wave: 2
depends_on: ["14.3-01"]
files_modified:
  - src/threading/spsc_payloads.h
  - src/core/njclient.h
  - src/core/njclient.cpp
  - tests/test_rawdata_send.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - SC-4
  - SC-5
  - SC-8
must_haves:
  truths:
    - "NJClient::RawDataSendBegin(unsigned char outGuid[16], unsigned int fourcc, int chidx, int estsize) public method exists and is callable from any thread; populates outGuid via WDL_RNG_bytes and enqueues a RawDataItem with type=0 (SC-4)"
    - "NJClient::RawDataSendWrite(const unsigned char guid[16], const void *data, int dataLen, bool isEnd) public method exists and is callable from any thread; enqueues a RawDataItem with type=1 and flags=(isEnd?1:0) (SC-4)"
    - "NJClient::RawData_Callback slot + NJClient::RawData_User void* slot exist on the public interface alongside ChatMessage_Callback (SC-5)"
    - "is_video_fourcc(unsigned int) file-static helper exists in src/core/njclient.cpp; returns true for MAKE_NJ_FOURCC('H','2','6','4'), MAKE_NJ_FOURCC('V','P','8',' ') (trailing space), MAKE_NJ_FOURCC('M','J','P','G'); returns false for NJ_ENCODER_FMT_TYPE and NJ_ENCODER_FMT_FLAC (Landmine L1 byte-order discipline)"
    - "Send queue is SpscRing<jamwide::RawDataItem, RAWDATA_SEND_QUEUE_CAPACITY> with capacity 64 — peer-paced traffic, generous headroom (Pattern A)"
    - "Producer-side try_push failure increments m_rawdata_sendq_overflows (atomic, relaxed); accessor GetRawDataSendQueueOverflowCount() exists mirroring GetBlockQueueDropCount()"
    - "Drain loop lives lexically inside NJClient::Run() — same thread context as the existing audio-encoder Send calls at njclient.cpp:2502 / 2549 (Landmine L2 — Net_Connection::Send not thread-safe)"
    - "Drain loop is guarded by if (!m_netcon) at entry; on null netcon, drain-and-discard each queued item (delete owned payload heap-buf) and increment overflow counter (Landmine L3; Pattern C)"
    - "Drain-and-send block mirrors ninjamzap-core/njclient.cpp:1984-2039 verbatim: type==0 → mpb_client_upload_interval_begin; type==1 → split owned payload into MAX_ENC_BLOCKSIZE chunks and emit mpb_client_upload_interval_write per chunk, with flags=1 only on the last chunk when item->flags&1 is set"
    - "tests/test_rawdata_send.cpp covers: Begin then Write(N) then Write(isEnd=true) populates SPSC; drain produces exactly 1 BEGIN + N WRITE + 1 END mpb message stream; 2×MAX_ENC_BLOCKSIZE payload splits into 3 chunks (2 mid + 1 end); is_video_fourcc test vectors pass; overflow counter increments on queue full; payload heap-bufs freed on Disconnect drain-and-discard"
    - "RawData send path does NOT touch the existing audio-encoder Send sites at lines 2502 / 2549; existing audio xmit MUST remain functional under the same NJClient::Run() body (SC-7 indirect: no regression)"
  artifacts:
    - path: src/threading/spsc_payloads.h
      provides: "RawDataItem POD payload + RAWDATA_SEND_QUEUE_CAPACITY constant"
      contains: ["struct RawDataItem", "guid[16]", "fourcc", "chidx", "estsize", "flags", "payload", "is_trivially_copyable", "RAWDATA_SEND_QUEUE_CAPACITY"]
    - path: src/core/njclient.h
      provides: "Public RawDataSendBegin/Write/Callback API + private SPSC + overflow counter + accessor + RawDataDownloadTracker"
      contains: ["RawDataCallback", "RawData_Callback", "RawData_User", "RawDataSendBegin", "RawDataSendWrite", "m_rawdata_sendq", "m_rawdata_sendq_overflows", "GetRawDataSendQueueOverflowCount", "m_rawdata_downloads", "RawDataDownloadTracker"]
    - path: src/core/njclient.cpp
      provides: "is_video_fourcc helper + RawDataSendBegin/Write impls + drain block in NJClient::Run + destructor cleanup"
      contains: ["is_video_fourcc", "RawDataSendBegin", "RawDataSendWrite", "m_rawdata_sendq.try_push", "m_rawdata_sendq.drain", "mpb_client_upload_interval_begin", "mpb_client_upload_interval_write"]
    - path: tests/test_rawdata_send.cpp
      provides: "In-process unit test for the send queue + drain + chunking + is_video_fourcc"
      contains: ["test_rawdata_begin_write_end_roundtrip", "test_payload_chunking_at_max_enc_blocksize", "test_is_video_fourcc_h264", "test_is_video_fourcc_excludes_oggv_flac", "test_disconnect_drain_and_discard", "test_send_queue_overflow_counter_increments"]
    - path: CMakeLists.txt
      provides: "Test target wiring (add_executable + target_link_libraries njclient + add_test)"
      contains: ["add_executable(test_rawdata_send", "target_link_libraries(test_rawdata_send", "njclient", "add_test(NAME rawdata_send_roundtrip"]
  key_links:
    - from: "src/core/njclient.cpp NJClient::Run drain block"
      to: "m_netcon->Send via mpb_client_upload_interval_*"
      via: "drain pops, if (!m_netcon) discard else build mpb message and Send"
      pattern: "m_rawdata_sendq\\.drain.*mpb_client_upload_interval"
    - from: "tests/test_rawdata_send.cpp"
      to: "NJClient public RawDataSendBegin/Write"
      via: "linker resolves to njclient static lib (same as test_encryption)"
      pattern: "target_link_libraries\\(test_rawdata_send PRIVATE njclient\\)"
    - from: "src/core/njclient.cpp RawDataSendWrite producer"
      to: "m_rawdata_sendq.try_push + m_rawdata_sendq_overflows.fetch_add on full"
      via: "any-thread producer; SPSC contract"
      pattern: "m_rawdata_sendq_overflows\\.fetch_add"
estimated_loc: 450
---

<objective>
Port NinjamZap's RawDataSendBegin/RawDataSendWrite/RawDataCallback public API into JamWide's NJClient, adapting the send queue from NinjamZap's unbounded WDL_PtrList<RawDataQueueItem>+WDL_Mutex to JamWide's locked-in SPSC + overflow-counter discipline established by Phase 15.1. Add the is_video_fourcc(unsigned int) file-static helper (verbatim port from ninjamzap-server/usercon.cpp:104-116, with byte-order conventions matching JamWide's existing MAKE_NJ_FOURCC macro at njclient.cpp:212). Drain block lives inside NJClient::Run()'s body, lexically adjacent to existing audio-encoder Send sites; Disconnect-safety guard at entry mirrors the drainBroadcastBlocks pattern at njclient.cpp:3806-3815.

Purpose: Deliver the send half of the codec-agnostic transport scaffolding required by the v1.3 Native Video milestone (per SPEC section What this phase delivers item 2). Provides the is_video_fourcc helper that 14.3-03 will reference.

Output: A public NJClient API that any thread can call to upload an opaque-byte stream on a NINJAM channel; a passing in-process unit test that exercises the begin/write/end + chunking semantics; no behavioral change to the existing audio xmit path or the existing VDO.Ninja stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14.3-native-video-foundation/14.3-SPEC.md
@.planning/phases/14.3-native-video-foundation/14.3-RESEARCH.md
@.planning/phases/14.3-native-video-foundation/14.3-PATTERNS.md
@.planning/phases/14.3-native-video-foundation/14.3-VALIDATION.md
@.planning/quick/260515-0pc-investigate-jamtaba-video-implementation/260515-0pc-RESEARCH-ADDENDUM.md
@src/core/njclient.h
@src/core/njclient.cpp
@src/threading/spsc_ring.h
@src/threading/spsc_payloads.h
@tests/test_encryption.cpp
@CMakeLists.txt

Reference implementation (verbatim port source):
@/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h
@/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp
@/Users/cell/dev/ninjamzap-server/ninjam/server/usercon.cpp

<interfaces>
JamWide-side existing types and macros the executor must compose with. All extracted from the source files above; do not re-explore.

From src/threading/spsc_ring.h (already read): template SpscRing has try_push, try_pop, drain(callback), size(), empty(). T must be std::is_trivially_copyable per line 29. N must be power of 2 (static_assert at line 34); 64 is valid.

From src/threading/spsc_payloads.h header comment (lines 27-30): SPSC element-type contract — every payload MUST be std::is_trivially_copyable. Each variant + POD locks this contract via a static_assert. Therefore RawDataItem MUST be trivially copyable. Variable-length opaque payload is carried via a WDL_HeapBuf* owned-pointer field (drain deletes after sending). Pattern matches the DecodeState* ownership-transfer used by PeerNextDsUpdate.

From src/core/njclient.cpp:154-155 + :212 (file-local macros, NOT in njclient.h — verified). NJ_ENCODER_FMT_TYPE is MAKE_NJ_FOURCC('O','G','G','v'); NJ_ENCODER_FMT_FLAC is MAKE_NJ_FOURCC('F','L','A','C'); MAKE_NJ_FOURCC(A,B,C,D) is ((A) | ((B)<<8) | ((C)<<16) | ((D)<<24)). Therefore is_video_fourcc MUST live in njclient.cpp (file-static) since both fourcc constants are cpp-local. Decoding convention: c[0] = fourcc & 0xff etc. (little-endian, Landmine L1).

From src/core/njclient.cpp:831: #define MAX_ENC_BLOCKSIZE (8192+1024). This is the chunking constant for the drain loop. RawData WRITE chunks MUST split at this boundary (per ninjamzap-core/njclient.cpp:2022).

From src/core/njclient.h:592-602 (ChatMessage neighborhood — where new RawData decls land):
  void ChatMessage_Send(const char *parm1, ..., const char *parm5=NULL);
  void (*ChatMessage_Callback)(void *userData, NJClient *inst, const char **parms, int nparms);
  void *ChatMessage_User;

New RawData decls land RIGHT AFTER this block (Pattern Map section "src/core/njclient.h — public API addition").

From src/core/njclient.h:651-659 (overflow-counter accessor template):
  uint64_t GetBlockQueueDropCount() const noexcept {
      return m_block_queue_drops.load(std::memory_order_relaxed);
  }
GetRawDataSendQueueOverflowCount() mirrors this idiom verbatim.

From src/core/njclient.h:833-842 (SPSC + overflow-counter declaration template). The new m_rawdata_sendq + m_rawdata_sendq_overflows mirror this idiom verbatim (Pattern A) — comment block names producer ("any thread"), consumer ("run thread inside NJClient::Run drain block"), capacity rationale ("≤ 2-4 RawData items/sec/peer; 64 capacity is comfortable"), post-UAT invariant ("drop count == 0").

From src/core/njclient.cpp:3806-3825 (drainBroadcastBlocks — the if (!m_netcon) discard guard template, Pattern C). The new RawData drain mirrors this pattern. For RawData the queued payload owns a WDL_HeapBuf* — the discard path MUST delete item.payload to avoid leaking.

From src/core/njclient.cpp:2708-2720 (the ChatMessage_Send mpb-build + Send template — what NOT to do for RawData, since RawData producer is any-thread). ChatMessage_Send calls m_netcon->Send directly because it is invoked from the run thread; RawDataSend producer is any thread, so direct-Send is wrong (Landmine L2). RawDataSendBegin/Write MUST instead enqueue to SPSC and return.

From ninjamzap-core/njclient.cpp:2047-2082 (verbatim port source for the send-side impls; JamWide replaces WDL_Mutex+WDL_PtrList.Add with SpscRing.try_push + overflow bump):
  void NJClient::RawDataSendBegin(unsigned char outGuid[16], unsigned int fourcc, int chidx, int estsize) {
    WDL_RNG_bytes(outGuid, 16);
    RawDataQueueItem *item = new RawDataQueueItem;
    item->type = 0; memcpy(item->guid, outGuid, 16);
    item->fourcc = fourcc; item->chidx = chidx; item->estsize = estsize; item->flags = 0;
    m_rawdata_sendq_cs.Enter(); m_rawdata_sendq.Add(item); m_rawdata_sendq_cs.Leave();
  }
  void NJClient::RawDataSendWrite(const unsigned char guid[16], const void *data, int dataLen, bool isEnd) {
    RawDataQueueItem *item = new RawDataQueueItem;
    item->type = 1; memcpy(item->guid, guid, 16);
    item->fourcc = 0; item->chidx = 0; item->estsize = 0;
    item->flags = isEnd ? 1 : 0;
    if (data && dataLen > 0) { item->data.Resize(dataLen); memcpy(item->data.Get(), data, dataLen); }
    m_rawdata_sendq_cs.Enter(); m_rawdata_sendq.Add(item); m_rawdata_sendq_cs.Leave();
  }

From ninjamzap-core/njclient.cpp:1984-2039 (verbatim port source for the drain block; JamWide replaces the mutex+while-GetSize loop with SpscRing.drain and the discard branch with the discard-guard Pattern C). Logical shape: if (m_netcon) loop over queue; for type==0 build mpb_client_upload_interval_begin (guid/fourcc/chidx/estsize) and Send; for type==1 chunk-split payload at MAX_ENC_BLOCKSIZE (8192+1024), build mpb_client_upload_interval_write per chunk (guid/audio_data/audio_data_len/flags), final chunk sets flags=1 when item.flags&1. Empty-payload + isEnd writes a single empty WRITE with flags=item.flags. delete item.payload after each item.

From ninjamzap-server/ninjam/server/usercon.cpp:104-116 (verbatim port source for is_video_fourcc):
  static bool is_video_fourcc(unsigned int fourcc) {
    unsigned char c[4];
    c[0] = fourcc & 0xff; c[1] = (fourcc >> 8) & 0xff;
    c[2] = (fourcc >> 16) & 0xff; c[3] = (fourcc >> 24) & 0xff;
    return (c[0]=='H' && c[1]=='2' && c[2]=='6' && c[3]=='4') ||
           (c[0]=='V' && c[1]=='P' && c[2]=='8') ||
           (c[0]=='M' && c[1]=='J' && c[2]=='P' && c[3]=='G');
  }
JamWide port: tighten the VP8 check to require trailing-space byte3 (PATTERNS Locked Decision; NinjamZap server is lenient but wire-format spec calls for trailing space). Document this 1-char divergence inline.

From tests/test_encryption.cpp:1-90 (test scaffold template): TEST(name)/PASS()/FAIL(msg) macros at lines 28-44 — copy verbatim into tests/test_rawdata_send.cpp; static int tests_run, tests_passed; int main() invokes each test_* function and prints pass/fail summary.

From CMakeLists.txt:407-413 (test wiring template). Mirror verbatim for test_rawdata_send: add_executable, target_include_directories(... ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/src), target_link_libraries(... PRIVATE njclient), add_test(NAME rawdata_send_roundtrip COMMAND test_rawdata_send). Land under the same if(JAMWIDE_BUILD_TESTS) block at line 395 (NOT under JAMWIDE_VIDEO_SPIKE — Landmine L8, pure-C++ in-process test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — define RawDataItem payload + RAWDATA_SEND_QUEUE_CAPACITY in src/threading/spsc_payloads.h; ship the tests/test_rawdata_send.cpp scaffold with stubbed RED tests; wire test target in CMakeLists.txt</name>
  <files>src/threading/spsc_payloads.h, tests/test_rawdata_send.cpp, CMakeLists.txt</files>
  <read_first>
    - src/threading/spsc_payloads.h (entire file — for header style, the trivially-copyable static_assert idiom, and where new payload structs land)
    - src/threading/spsc_ring.h (entire file — for the trivially-copyable contract at line 29)
    - tests/test_encryption.cpp:1-90 (test scaffold template — TEST/PASS/FAIL macros and main() structure)
    - CMakeLists.txt:395-413 (the JAMWIDE_BUILD_TESTS block — for add_executable + target_link_libraries syntax)
    - .planning/phases/14.3-native-video-foundation/14.3-PATTERNS.md (section "tests/test_rawdata_send.cpp (NEW)" and "src/core/njclient.h — private SPSC + run-thread registry" for payload struct template)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:311-330 (verbatim port source for RawDataQueueItem + RawDataDownloadTracker shapes)
    - .planning/phases/14.3-native-video-foundation/14.3-RESEARCH.md (section "JamWide Analogs" — payload type pattern rationale; section "Open Questions" #1 — capacity = 64)
  </read_first>
  <behavior>
    - src/threading/spsc_payloads.h: NEW struct RawDataItem placed in namespace jamwide near the bottom of the header (after DecodeArmRequest at line 302, before the closing namespace closure at line 340). Trivially copyable. Fields: int type (0=begin, 1=data/end), unsigned char guid[16], unsigned int fourcc, int chidx, int estsize, int flags, class ::WDL_HeapBuf *payload (owned by item; drain or discard path deletes it).
    - src/threading/spsc_payloads.h: static_assert std::is_trivially_copyable_v<RawDataItem> matches the contract at line 27-29. The owned-pointer WDL_HeapBuf* makes the struct trivially copyable while keeping the variable-length payload off the SPSC inline buffer (matches Pattern A — owned-pointer ownership transfer).
    - src/threading/spsc_payloads.h: NEW constant inline constexpr std::size_t RAWDATA_SEND_QUEUE_CAPACITY = 64 with a comment naming the producer (any thread — RawDataSendBegin/Write callers), consumer (run thread inside NJClient::Run drain block), traffic rationale (video keyframe upload runs at the NINJAM interval cadence — under 2-4 items/sec/peer; capacity 64 absorbs 16x worst-case headroom), and post-UAT invariant (GetRawDataSendQueueOverflowCount() == 0 after standard UAT).
    - src/threading/spsc_payloads.h: header comment at lines 1-30 updated to mention the 14.3 carve-out.
    - tests/test_rawdata_send.cpp: file scaffold compiles with stubbed-FAIL test bodies. Each test function exists, calls TEST(name), and immediately calls FAIL("RED — not yet implemented") so the test executable exits non-zero. The bodies are filled in Task 3.
    - CMakeLists.txt: new add_executable(test_rawdata_send tests/test_rawdata_send.cpp) + target_link_libraries(test_rawdata_send PRIVATE njclient) + add_test(NAME rawdata_send_roundtrip COMMAND test_rawdata_send) mirroring test_encryption wiring exactly. Lands under the existing if(JAMWIDE_BUILD_TESTS) block at line 395, NOT under JAMWIDE_VIDEO_SPIKE (Landmine L8 — pure-C++ in-process test).
    - Verification: ctest -R rawdata_send_roundtrip --output-on-failure exits NON-ZERO (RED — the test bodies all FAIL by design until Task 3 implements them).
  </behavior>
  <action>
    Edit src/threading/spsc_payloads.h. Update header comment at lines 1-30 with a new bullet recording the 14.3 carve-out (RawDataItem + RAWDATA_SEND_QUEUE_CAPACITY for the NinjamZap-compatible video transport scaffolding; run-thread consumer; any-thread producer). Add forward declaration class ::WDL_HeapBuf adjacent to the existing DecodeState/RemoteUser/Local_Channel forward decls at line 53. After the DecodeArmRequest definition at line 302, add a new section header comment block titled "(h) RawDataItem — Phase 14.3 video transport scaffolding" followed by the struct RawDataItem (fields named in <behavior> above) and the static_assert std::is_trivially_copyable_v<RawDataItem>. Below the struct, add inline constexpr std::size_t RAWDATA_SEND_QUEUE_CAPACITY = 64 with the producer/consumer/rationale/post-UAT-invariant comment block matching the style of lines 833-842 of njclient.h (15.1-07a comment idiom).

    Create tests/test_rawdata_send.cpp (~80 LOC scaffold; bodies filled in Task 3). Includes: cassert, cstdio, cstring, vector, plus the njclient public header (core/njclient.h) and spsc_payloads (threading/spsc_payloads.h). Copy the TEST(name)/PASS()/FAIL(msg) macros verbatim from tests/test_encryption.cpp:28-44 along with the static int counters tests_run and tests_passed. Empty test functions (each calls TEST(name) then FAIL("RED — not yet implemented") so the function returns having registered a failure): test_rawdata_begin_write_end_roundtrip, test_payload_chunking_at_max_enc_blocksize, test_is_video_fourcc_h264, test_is_video_fourcc_vp8_trailing_space, test_is_video_fourcc_mjpg, test_is_video_fourcc_excludes_oggv_flac, test_disconnect_drain_and_discard, test_send_queue_overflow_counter_increments. The int main() invokes all eight test_* functions, prints "Total: X passed, Y failed", returns (tests_passed == tests_run) ? 0 : 1.

    Edit CMakeLists.txt under the existing if(JAMWIDE_BUILD_TESTS) block at line 395 (after the existing test_encryption block at lines 407-413). Add add_executable(test_rawdata_send tests/test_rawdata_send.cpp); target_include_directories(test_rawdata_send PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/src); target_link_libraries(test_rawdata_send PRIVATE njclient); add_test(NAME rawdata_send_roundtrip COMMAND test_rawdata_send). Mirror the test_encryption block style verbatim.
  </action>
  <verify>
    <automated>cd build-juce && cmake .. -DJAMWIDE_BUILD_TESTS=ON -G Ninja && ninja test_rawdata_send 2>&1 | tee /tmp/14.3-02-task1-build.log; ! grep -E 'error:|undefined' /tmp/14.3-02-task1-build.log; ctest -R rawdata_send_roundtrip --output-on-failure 2>&1 | tee /tmp/14.3-02-task1-ctest.log; grep -qE 'RED — not yet implemented' /tmp/14.3-02-task1-ctest.log && ! grep -q 'Total: 8 passed, 0 failed' /tmp/14.3-02-task1-ctest.log</automated>
  </verify>
  <done>
    src/threading/spsc_payloads.h declares RawDataItem + RAWDATA_SEND_QUEUE_CAPACITY with the trivially-copyable static_assert passing at compile time; tests/test_rawdata_send.cpp builds and runs but exits non-zero with "RED — not yet implemented" failure lines for all 8 tests; CMakeLists.txt has the test_rawdata_send wiring under JAMWIDE_BUILD_TESTS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RawData public API declarations + private SPSC + overflow counter + accessor + RawDataDownloadTracker to src/core/njclient.h; add file-static is_video_fourcc helper + RawDataSendBegin/Write impls + drain block + destructor cleanup + constructor init to src/core/njclient.cpp</name>
  <files>src/core/njclient.h, src/core/njclient.cpp</files>
  <read_first>
    - src/core/njclient.h (entire file — current state; especially lines 580-670 for the public-API neighborhood and lines 700-900 for the private member neighborhood)
    - src/core/njclient.cpp (lines 1-160 for include block; lines 150-220 for fourcc macros + MAKE_NJ_FOURCC; lines 700-900 for #defines like MAX_ENC_BLOCKSIZE; lines 2400-2575 for existing audio-encoder Send sites; lines 3790-3840 for drainBroadcastBlocks pattern; lines 945-1010 for constructor and _reinit; lines 1010-1200 for destructor and Disconnect)
    - .planning/phases/14.3-native-video-foundation/14.3-PATTERNS.md (sections "src/core/njclient.h — public API addition", "src/core/njclient.h — private SPSC + run-thread registry", "src/core/njclient.cpp — RawDataSendBegin/Write implementations", "src/core/njclient.cpp — drain block inside NJClient::Run()")
    - .planning/phases/14.3-native-video-foundation/14.3-RESEARCH.md (section "Landmines" L1, L2, L3 — byte-order discipline, Net_Connection thread-safety, Disconnect tear-down)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:205-216 (verbatim port source for public API decls)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:311-330 (verbatim port source for RawDataDownloadTracker shape)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:2047-2082 (verbatim port source for the send-side impls)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1984-2039 (verbatim port source for the drain block)
    - /Users/cell/dev/ninjamzap-server/ninjam/server/usercon.cpp:104-116 (verbatim port source for is_video_fourcc)
  </read_first>
  <behavior>
    - src/core/njclient.h: add public RawData API decls IMMEDIATELY AFTER the ChatMessage_Callback block at lines 600-602. Specifically: typedef RawDataCallback, slot RawData_Callback, void* slot RawData_User, then methods RawDataSendBegin(unsigned char outGuid[16], unsigned int fourcc, int chidx, int estsize) and RawDataSendWrite(const unsigned char guid[16], const void *data, int dataLen, bool isEnd). Names verbatim from ninjamzap (API consistency tax — see PATTERNS.md "Apply across all plans").
    - src/core/njclient.h: add GetRawDataSendQueueOverflowCount() const noexcept accessor immediately adjacent to the existing GetBlockQueueDropCount() accessor at lines 651-659. Mirror the comment-block style verbatim with 14.3-02 + Codex M-8 prefix.
    - src/core/njclient.h: add RawDataDownloadTracker POD struct (fields: unsigned char guid[16], unsigned int fourcc, char username[256], int chidx — verbatim from ninjamzap njclient.h:323-330) and the private member WDL_PtrList<RawDataDownloadTracker> m_rawdata_downloads adjacent to the existing m_downloads at line 746. Comment block names this as 14.3-03 prep; touched ONLY inside NJClient::Run's MESSAGE_SERVER_DOWNLOAD_INTERVAL_{BEGIN,WRITE} branches.
    - src/core/njclient.h: add private SPSC + overflow counter declarations in the m_remoteuser_update_q neighborhood (after line 842). Specifically jamwide::SpscRing<jamwide::RawDataItem, jamwide::RAWDATA_SEND_QUEUE_CAPACITY> m_rawdata_sendq and std::atomic<uint64_t> m_rawdata_sendq_overflows{0}. Each gets a comment block in the 15.1-XX style naming the producer (any thread — RawDataSendBegin/Write callers), consumer (run thread, NJClient::Run drain block), capacity rationale, and post-UAT invariant.
    - src/core/njclient.cpp: define file-static is_video_fourcc(unsigned int fourcc) helper near the existing fourcc macros at line 212 (after MAKE_NJ_FOURCC). Verbatim from ninjamzap-server/usercon.cpp:104-116 but with VP8 trailing-space tightening (PATTERNS Locked Decisions #1) documented inline as a 1-char divergence.
    - src/core/njclient.cpp: implement NJClient::RawDataSendBegin and NJClient::RawDataSendWrite near the existing ChatMessage_Send impl at lines 2708-2720. Each function: build a jamwide::RawDataItem POD with the relevant fields populated; for Write copy data into a new WDL_HeapBuf (only when dataLen > 0); call m_rawdata_sendq.try_push(item). On try_push failure: delete payload (if any); m_rawdata_sendq_overflows.fetch_add(1, std::memory_order_relaxed); writeLog warning. No mutex anywhere — the SPSC is the synchronization primitive.
    - src/core/njclient.cpp: add drain block inside NJClient::Run() body. Lexically, it goes just AFTER the existing audio-encoder Send loop block in the Run body (specifically: after the close of the #ifndef NJCLIENT_NO_XMIT_SUPPORT block that ends around line 2575, but BEFORE the next loop construct). The drain block uses m_rawdata_sendq.drain([this](jamwide::RawDataItem&& item){ ... }) with the discard branch (if (!m_netcon)) running first per Pattern C.
    - src/core/njclient.cpp NJClient destructor: locate the m_downloads cleanup near line 1100-1200 (search for m_downloads.Empty(true) or equivalent). Add adjacent m_rawdata_downloads.Empty(true) and a final drain that frees any queued payload heap-bufs (m_rawdata_sendq.drain with a lambda that deletes item.payload).
    - src/core/njclient.cpp NJClient constructor / _reinit (search near line 970-1000): initialize RawData_Callback=0; RawData_User=0; (mirroring the ChatMessage_Callback init at line 972-973).
    - Existing audio-encoder Send sites at lines 2502 / 2549 MUST NOT be touched — pure additive change, no edits within the audio path's encoder loop.
  </behavior>
  <action>
    Edit src/core/njclient.h:

    First, after line 602 (end of "void *ChatMessage_User;" block), insert the new public RawData API block. The block declares: the comment "Phase 14.3: codec-agnostic transport scaffolding" naming send-side as SPSC-queued and run-thread-drained (citing Landmine L2 / src/core/netmsg.cpp:289) and receive-side as dispatched at MESSAGE_SERVER_DOWNLOAD_INTERVAL_BEGIN when fourcc is non-OGGv/non-FLAC and a callback is registered (forward-reference to 14.3-03); the typedef RawDataCallback with eventType comment "0=begin, 1=data, 2=end" and the parameter list (void *userData, int eventType, const unsigned char *guid, unsigned int fourcc, const char *username, int chidx, const void *data, int dataLen); the public slots RawData_Callback and RawData_User; the two method decls RawDataSendBegin and RawDataSendWrite with the signatures exactly as in <interfaces> above. Document inline that both methods are callable from any thread, internally serialized via SPSC + run-thread drain.

    Second, after the existing GetBlockQueueDropCount() accessor at lines 657-659, add GetRawDataSendQueueOverflowCount() mirroring the comment-block style verbatim (prefix "14.3-02 + Codex M-8:", names the producer as any thread, the bump site as the producer-side try_push failure path, post-UAT invariant == 0, relaxed semantics observability-only).

    Third, after line 746 (the existing WDL_PtrList<RemoteDownload> m_downloads declaration), add the RawDataDownloadTracker POD struct + private member WDL_PtrList<RawDataDownloadTracker> m_rawdata_downloads. Fields of the tracker (verbatim from ninjamzap-core/njclient.h:323-330): unsigned char guid[16]; unsigned int fourcc; char username[256]; int chidx. Comment block names this as 14.3-03 prep with the architectural invariant "touched ONLY inside NJClient::Run's MESSAGE_SERVER_DOWNLOAD_INTERVAL_{BEGIN,WRITE} branches; no audio-thread access".

    Fourth, after line 842 (the existing m_remoteuser_update_overflows declaration), add: jamwide::SpscRing<jamwide::RawDataItem, jamwide::RAWDATA_SEND_QUEUE_CAPACITY> m_rawdata_sendq; std::atomic<uint64_t> m_rawdata_sendq_overflows{0}. The comment block (prefix "14.3-02:") names the producer (any thread — RawDataSendBegin/Write callers including UI thread and future audio thread for QueueVideoFrame), the consumer (run thread inside NJClient::Run drain block lexically adjacent to existing audio-encoder Send loop), and explicitly cites Landmine L2 (Net_Connection::Send not thread-safe) as the rationale for not direct-Send'ing from the producer. Capacity rationale: 64 — video keyframe upload runs at NINJAM interval cadence under 2-4 items/sec/peer. Post-UAT invariant: GetRawDataSendQueueOverflowCount() == 0.

    Edit src/core/njclient.cpp:

    Fifth, after line 212 (the MAKE_NJ_FOURCC macro definition), insert the file-static is_video_fourcc helper. Decoding via c[0] = fourcc & 0xff, c[1] = (fourcc >> 8) & 0xff, c[2] = (fourcc >> 16) & 0xff, c[3] = (fourcc >> 24) & 0xff (little-endian convention matching MAKE_NJ_FOURCC at line 212). Returns true when c spells H264, when c spells VP8 with trailing space (intentional — divergence from ninjamzap-server which is lenient), or when c spells MJPG. Document the VP8 trailing-space tightening inline.

    Sixth, edit the NJClient constructor / _reinit block near lines 970-1000. After the existing ChatMessage_Callback=0; ChatMessage_User=0; initialization, add RawData_Callback = 0; RawData_User = 0;.

    Seventh, add the RawDataSendBegin and RawDataSendWrite implementations adjacent to ChatMessage_Send at lines 2708-2720. RawDataSendBegin: calls WDL_RNG_bytes(outGuid, 16); builds a jamwide::RawDataItem with type=0, guid=outGuid, fourcc=fourcc, chidx=chidx, estsize=estsize, flags=0, payload=nullptr; on m_rawdata_sendq.try_push failure increments m_rawdata_sendq_overflows.fetch_add and writeLog warns. RawDataSendWrite: builds an item with type=1, guid=guid, fourcc=0, chidx=0, estsize=0, flags=(isEnd?1:0); if data and dataLen > 0 allocates a new WDL_HeapBuf, sizes it to dataLen via Resize, memcpys data into Get(), assigns to item.payload; on m_rawdata_sendq.try_push failure deletes item.payload (if any), increments m_rawdata_sendq_overflows.fetch_add, writeLog warns.

    Eighth, add the drain block inside NJClient::Run(). Locate the existing audio-encoder Send loop block that ends after the closing brace of the #ifndef NJCLIENT_NO_XMIT_SUPPORT block near line 2575 (search for "lc->m_curwritefile_writelen+=sz" and walk forward to the matching #endif). Place the drain block AFTER that #endif but BEFORE the next loop construct in NJClient::Run's body. The drain block shape: if (!m_netcon) call m_rawdata_sendq.drain with a lambda that for each item deletes item.payload (if any) and increments m_rawdata_sendq_overflows.fetch_add — this is the Pattern C discard guard mirroring lines 3806-3815. Else (m_netcon valid) call m_rawdata_sendq.drain with a lambda that for each item: if item.type==0 builds mpb_client_upload_interval_begin with guid, fourcc, chidx, estsize from the item, then m_netcon->Send(build); if item.type==1 splits item.payload into MAX_ENC_BLOCKSIZE-sized chunks, builds mpb_client_upload_interval_write per chunk with guid, audio_data ptr, audio_data_len, flags=(remaining<=0 && item.flags&1)?1:0, then m_netcon->Send(build). After the per-item send: delete item.payload if non-null. Empty-payload case: if item.payload is null OR GetSize()==0, send a single mpb_client_upload_interval_write with audio_data=NULL, audio_data_len=0, flags=item.flags (matches ninjamzap-core/njclient.cpp:2007-2015).

    Ninth, in the NJClient destructor (search for "m_downloads.Empty(true)" near line 1100-1200), add adjacent: m_rawdata_downloads.Empty(true); and a final drain of the send queue: m_rawdata_sendq.drain with a lambda that deletes item.payload for any queued items. Rationale: prevents heap-buf leak on shutdown when callers enqueue right before destructor runs.

    Tenth: do NOT touch the existing audio-encoder Send sites at lines 2502 and 2549; they belong to the audio xmit path and must remain byte-identical for SC-7 indirect (no audio regression).
  </action>
  <verify>
    <automated>cd build-juce && ninja njclient 2>&1 | tee /tmp/14.3-02-task2-build.log && ! grep -E 'error:|undefined symbol' /tmp/14.3-02-task2-build.log && grep -qE 'is_video_fourcc|RawDataSendBegin|RawDataSendWrite' src/core/njclient.cpp && grep -qE 'm_rawdata_sendq|m_rawdata_downloads|GetRawDataSendQueueOverflowCount' src/core/njclient.h</automated>
  </verify>
  <done>
    src/core/njclient.h declares RawData_Callback, RawData_User, RawDataSendBegin, RawDataSendWrite, GetRawDataSendQueueOverflowCount, m_rawdata_sendq, m_rawdata_sendq_overflows, m_rawdata_downloads, RawDataDownloadTracker. src/core/njclient.cpp defines is_video_fourcc (file-static), RawDataSendBegin (any-thread producer + try_push + overflow on full), RawDataSendWrite (same, with heap-buf payload), drain block inside NJClient::Run with Pattern C discard guard, destructor frees queued payloads, constructor inits RawData_Callback=0 RawData_User=0. ninja njclient builds clean with no warnings on the new code.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill in the eight test bodies in tests/test_rawdata_send.cpp; verify ctest rawdata_send_roundtrip exits 0 (GREEN); confirm RawDataItem static_assert + SPSC capacity invariant under TSan</name>
  <files>tests/test_rawdata_send.cpp</files>
  <read_first>
    - tests/test_rawdata_send.cpp (current state from Task 1 — scaffold with stubbed FAILs)
    - src/core/njclient.h (current state from Task 2 — has the public RawData API + GetRawDataSendQueueOverflowCount)
    - src/core/njclient.cpp (current state from Task 2 — has is_video_fourcc + impls + drain block)
    - tests/test_encryption.cpp (the established test pattern for linking against njclient + exercising public methods)
    - tests/test_block_queue_spsc.cpp (existing test for the SPSC pattern; mirror its style for the in-process producer/consumer tests)
    - .planning/phases/14.3-native-video-foundation/14.3-PATTERNS.md (section "tests/test_rawdata_send.cpp (NEW)" — the test coverage requirements; section "tests/test_video_fourcc.cpp" — the is_video_fourcc test vectors)
    - .planning/phases/14.3-native-video-foundation/14.3-VALIDATION.md (Per-Task Verification Map row 14.3-02-* — the test must answer "ctest -R rawdata_send_roundtrip" GREEN; covers SC-4, SC-5, SC-8 send-side)
  </read_first>
  <behavior>
    - test_rawdata_begin_write_end_roundtrip: instantiate a stack NJClient (default-constructible per public header); call RawDataSendBegin(outGuid, MAKE_NJ_FOURCC('H','2','6','4'), 0, 0); call RawDataSendWrite(outGuid, "AAAA", 4, false); call RawDataSendWrite(outGuid, "BB", 2, true); assert GetRawDataSendQueueOverflowCount() == 0; assert that the send queue holds exactly 3 items at this point (verified via public-API-only — actually, simply assert that none of the three Begin/Write calls bumped the overflow counter; the drain itself is exercised via the NJClient::Run path which requires a mock Net_Connection — see test_encryption.cpp for the mocking pattern; if mocking is impractical, expose a friend-test accessor m_rawdata_sendq.size() via a JAMWIDE_BUILD_TESTS-gated getter, similar to other tests).
    - test_payload_chunking_at_max_enc_blocksize: construct a 2 * MAX_ENC_BLOCKSIZE payload (size = 2 * (8192+1024) = 18432 bytes), call RawDataSendBegin + RawDataSendWrite(payload, size, isEnd=true); assert GetRawDataSendQueueOverflowCount() == 0; the drain (when m_netcon is mocked) MUST emit exactly 3 mpb_client_upload_interval_write messages where the first two have flags=0 and the third has flags=1, and the cumulative bytes equal the input payload. If the Net_Connection mock is non-trivial, assert at minimum that the queued item carries the full payload (size==18432, byte-identity check first 64 bytes + last 64 bytes).
    - test_is_video_fourcc_h264: is_video_fourcc(MAKE_NJ_FOURCC('H','2','6','4')) returns true. Since is_video_fourcc is file-static in njclient.cpp, the test exercises it indirectly via a public NJClient method that uses it — e.g. invoke RawDataSendBegin with fourcc 'H264' and verify it doesn't fail, OR add a JAMWIDE_BUILD_TESTS-gated public helper bool NJClient::IsVideoFourcc(unsigned int) const that wraps the file-static. Recommendation: add the gated public wrapper, similar to the pattern used by other in-process tests.
    - test_is_video_fourcc_vp8_trailing_space: IsVideoFourcc(MAKE_NJ_FOURCC('V','P','8',' ')) returns true; IsVideoFourcc(MAKE_NJ_FOURCC('V','P','8',0)) returns false (verifies the trailing-space tightening, divergence from ninjamzap server).
    - test_is_video_fourcc_mjpg: IsVideoFourcc(MAKE_NJ_FOURCC('M','J','P','G')) returns true.
    - test_is_video_fourcc_excludes_oggv_flac: IsVideoFourcc('OGGv' = MAKE_NJ_FOURCC('O','G','G','v')) returns false; IsVideoFourcc('FLAC' = MAKE_NJ_FOURCC('F','L','A','C')) returns false.
    - test_disconnect_drain_and_discard: construct NJClient without connecting (so m_netcon is null); call RawDataSendBegin/Write a few times to populate the queue; manually invoke NJClient::Run once (or the equivalent test-only drain hook); after Run, assert GetRawDataSendQueueOverflowCount() has incremented by the number of items pushed (since the discard branch increments per item per the drain lambda above). Verify no memory leak via Valgrind-style sentinel (heap-bufs should be deleted in the discard branch).
    - test_send_queue_overflow_counter_increments: call RawDataSendBegin RAWDATA_SEND_QUEUE_CAPACITY + 1 times rapidly without intervening Run/drain; assert GetRawDataSendQueueOverflowCount() == 1 (the final extra push fails); the first 64 pushes must succeed.
    - All 8 tests pass; ctest -R rawdata_send_roundtrip exits 0; the executable prints "Total: 8 passed, 0 failed".
  </behavior>
  <action>
    Fill the eight test_* function bodies in tests/test_rawdata_send.cpp following the <behavior> rubric above. Add a JAMWIDE_BUILD_TESTS-gated friend declaration or a JAMWIDE_BUILD_TESTS-gated public wrapper method NJClient::IsVideoFourcc(unsigned int) const that delegates to the file-static is_video_fourcc — gate it under #ifdef JAMWIDE_BUILD_TESTS so production builds don't ship the wrapper. The gating macro is already defined as a target_compile_definition on njclient at CMakeLists.txt:136-138 (verified). Place the IsVideoFourcc decl in njclient.h adjacent to the existing test-only debug-counter accessor m_chinfo_applies_observed (already JAMWIDE_BUILD_TESTS-style gated via the surrounding test infrastructure).

    For tests that need to observe drain behavior (test_rawdata_begin_write_end_roundtrip, test_payload_chunking_at_max_enc_blocksize, test_disconnect_drain_and_discard): add a JAMWIDE_BUILD_TESTS-gated public method NJClient::DrainRawDataSendQueueForTest(std::vector<jamwide::RawDataItem>& out) that pops every queued item into the out vector for inspection. This avoids needing a mock Net_Connection — the test inspects the queued POD payloads directly. The DrainRawDataSendQueueForTest method must transfer ownership of the WDL_HeapBuf* payload to the caller (vector elements carry the raw owned-pointer; test main() at end iterates the vector and deletes payloads).

    For test_payload_chunking_at_max_enc_blocksize: this test specifically exercises the chunking logic which lives in the drain block (not in RawDataSendWrite — which just enqueues the whole payload as a single RawDataItem). To exercise chunking the test needs to invoke the drain. Two options: (a) inject a mock Net_Connection that captures Send(mpb*) calls (mirrors test_encryption.cpp's mocking pattern); (b) factor the chunking into a static helper NJClient::ChunkRawDataItem(const RawDataItem&, std::function<void(const mpb_client_upload_interval_write&)>) callable from tests. Option (b) is simpler and stays within the in-process pattern; choose (b). The drain block in Run() calls the static helper for each item.

    For test_send_queue_overflow_counter_increments: simply call RawDataSendBegin 65 times in a tight loop and assert exactly the 65th call bumps the counter; the first 64 succeed.

    Re-run ctest -R rawdata_send_roundtrip --output-on-failure to confirm GREEN.
  </action>
  <verify>
    <automated>cd build-juce && cmake .. -DJAMWIDE_BUILD_TESTS=ON -G Ninja && ninja njclient test_rawdata_send 2>&1 | tee /tmp/14.3-02-task3-build.log && ! grep -E 'error:|undefined' /tmp/14.3-02-task3-build.log && ctest -R rawdata_send_roundtrip --output-on-failure 2>&1 | tee /tmp/14.3-02-task3-ctest.log && grep -q 'Total: 8 passed, 0 failed' /tmp/14.3-02-task3-ctest.log && grep -q 'rawdata_send_roundtrip.*Passed' /tmp/14.3-02-task3-ctest.log</automated>
  </verify>
  <done>
    All 8 test_rawdata_send tests pass; ctest -R rawdata_send_roundtrip exits 0; overflow counter behaves correctly (0 in roundtrip, 1 after 65 pushes, N after N discards on null-netcon drain); chunking helper splits correctly at MAX_ENC_BLOCKSIZE; is_video_fourcc test vectors all pass; the existing full ctest suite (ctest --output-on-failure) still passes with no regressions in test_encryption, test_block_queue_spsc, test_spsc_state_updates, etc.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RawDataSendBegin/Write caller → NJClient SPSC | Any thread (UI, future audio, plugin host) submits opaque payloads; ownership of the heap-buf payload transfers to the SPSC; drain or discard path owns deletion |
| SPSC consumer (run thread) → Net_Connection::Send | Single-thread invariant locks here — Net_Connection::Send is the unsynchronized site (src/core/netmsg.cpp:289); breaking this invariant by adding a second producer to m_sendq corrupts the wire stream |
| Drain block ↔ existing audio-encoder Send loop | Both live inside NJClient::Run's body, both call m_netcon->Send sequentially — the drain block MUST NOT interleave concurrently with the audio-encoder Send sites at lines 2502 / 2549 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.3-08 | Tampering / Memory-Safety | RawDataItem.payload (owned WDL_HeapBuf*) | mitigate | Ownership protocol locked: producer allocates on RawDataSendWrite; SPSC carries the pointer; drain branch deletes after Send; discard branch (null m_netcon) deletes without sending; destructor drains and deletes; test test_disconnect_drain_and_discard exercises the discard branch. No double-delete possible because the drain pops items (try_pop transfers ownership out of the ring). |
| T-14.3-09 | Tampering | Net_Connection::Send concurrent mutation (Landmine L2) | mitigate | Drain MUST live lexically inside NJClient::Run; PATTERNS.md flags any task that moves the drain to a JUCE timer or MessageManager::callAsync as scope deviation. Code review gate: grep for "m_netcon->Send" in any callback or async context must show the drain stays in Run's body. |
| T-14.3-10 | Denial of Service | Producer fills SPSC during Disconnect window | mitigate | If (!m_netcon) drain-and-discard branch increments overflow counter per discarded item; counter is observable via GetRawDataSendQueueOverflowCount() so 14.3 phase verification can assert it. |
| T-14.3-11 | Information Disclosure | RawData payloads carry opaque bytes (in v1.3, video frames containing user's camera) | accept | Phase 14.3 only ships transport scaffolding; payload content (camera video) is v1.3 scope; current threat: zero (no consumer of the API ships in 14.3 except tests). For v1.3, this resurfaces as needing E2E encryption coupling with Phase 15's AES-256-CBC layer; tracked in deferred-items. |
| T-14.3-12 | Repudiation / Auditability | Send-queue overflow silently drops payloads | mitigate | writeLog warning on every overflow (RawDataSendBegin and RawDataSendWrite producer-side bumps); GetRawDataSendQueueOverflowCount() accessor for programmatic observability; test_send_queue_overflow_counter_increments verifies the bump path. |
| T-14.3-13 | Elevation of Privilege | Default-zero NJClient::RawData_Callback fires before assignment | accept | Producer code calls RawData_Callback only when non-null; checked at every invocation site in 14.3-03. Same pattern as ChatMessage_Callback (used since v1.0, no incidents). Risk: low. |
</threat_model>

<verification>

Post-task: run from repo root.

1. **Send API exists with the right signatures (SC-4):**
   ```
   grep -n 'void NJClient::RawDataSendBegin\|void NJClient::RawDataSendWrite' src/core/njclient.cpp | wc -l
   # Expect: 2

   grep -n 'RawDataSendBegin\|RawDataSendWrite' src/core/njclient.h | wc -l
   # Expect: 2+
   ```

2. **Public RawData_Callback typedef + slots exist (SC-5):**
   ```
   grep -n 'RawDataCallback\|RawData_Callback\|RawData_User' src/core/njclient.h | wc -l
   # Expect: 3+
   ```

3. **SPSC + overflow counter wired correctly (Pattern A):**
   ```
   grep -nE 'm_rawdata_sendq.*RAWDATA_SEND_QUEUE_CAPACITY|m_rawdata_sendq_overflows' src/core/njclient.h | wc -l
   # Expect: 2+

   grep -nE 'm_rawdata_sendq.try_push|m_rawdata_sendq.drain' src/core/njclient.cpp | wc -l
   # Expect: 3+ (one try_push for Begin, one for Write, at least one drain)

   grep -n 'm_rawdata_sendq_overflows.fetch_add' src/core/njclient.cpp | wc -l
   # Expect: 3+ (Begin overflow, Write overflow, discard-branch increments)
   ```

4. **Drain is lexically inside NJClient::Run, not a callback (Landmine L2):**
   ```
   grep -nE 'NJClient::Run|m_rawdata_sendq.drain' src/core/njclient.cpp
   # Verify: every m_rawdata_sendq.drain line falls after `int NJClient::Run(` and before its matching closing brace.
   ```

5. **Discard guard exists (Landmine L3, Pattern C):**
   ```
   grep -B2 -A6 'm_rawdata_sendq.drain' src/core/njclient.cpp | head -50
   # Expect: an if (!m_netcon) branch that drains-and-discards with delete payload + fetch_add(1)
   ```

6. **is_video_fourcc tightening (Landmine L1 + PATTERNS Locked Decisions #1):**
   ```
   grep -A12 'static inline bool is_video_fourcc' src/core/njclient.cpp
   # Expect: VP8 case requires c[3]==' ' (trailing space)
   ```

7. **Tests GREEN (SC-4, SC-5, SC-8 send-side):**
   ```
   cd build-juce && ctest -R rawdata_send_roundtrip --output-on-failure
   # Expect: 1 test passed; exit 0; output includes "Total: 8 passed, 0 failed"
   ```

8. **Full suite no regression (SC-7 indirect):**
   ```
   cd build-juce && ctest --output-on-failure
   # Expect: all existing tests still pass (encryption, block_queue_spsc, spsc_state_updates, etc.)
   ```

9. **VDO.Ninja byte-identity (SC-7):**
   ```
   git diff HEAD -- juce/video companion | wc -l
   # Expect: 0
   ```

10. **Audio-encoder Send sites untouched:**
    ```
    git diff HEAD -- src/core/njclient.cpp | grep -E '^[-+].*MAX_ENC_BLOCKSIZE|m_enc->Encode|m_enc->Available' | wc -l
    # Expect: 0 (the existing encoder loop must be byte-identical)
    ```

</verification>

<success_criteria>

- [ ] SC-4: NJClient::RawDataSendBegin + RawDataSendWrite exist as public methods; both callable from any thread (SPSC-mediated)
- [ ] SC-5: NJClient::RawDataCallback typedef + RawData_Callback + RawData_User slots on public interface
- [ ] SC-8 (send-side half): ctest rawdata_send_roundtrip exits 0; covers begin/write/end roundtrip, MAX_ENC_BLOCKSIZE chunking, overflow counter, discard-on-Disconnect, is_video_fourcc vectors
- [ ] is_video_fourcc helper exists in njclient.cpp file-static; recognizes H264, VP8 (trailing space), MJPG; excludes OGGv and FLAC
- [ ] Pattern A: m_rawdata_sendq SPSC capacity 64; m_rawdata_sendq_overflows atomic; GetRawDataSendQueueOverflowCount accessor mirrors GetBlockQueueDropCount
- [ ] Pattern C: drain has if (!m_netcon) discard-and-delete-payload branch; mirrors drainBroadcastBlocks
- [ ] Drain lives lexically inside NJClient::Run, not a callback/timer (Landmine L2)
- [ ] Existing audio xmit path bytes-identical (no regression in encrypted/FLAC/Vorbis tests)
- [ ] VDO.Ninja stack byte-identical (SC-7)

</success_criteria>

<output>
After completion, create `.planning/phases/14.3-native-video-foundation/14.3-02-SUMMARY.md` documenting:
- Final RAWDATA_SEND_QUEUE_CAPACITY value used (64 per RESEARCH; deviate only with rationale)
- Whether the JAMWIDE_BUILD_TESTS-gated NJClient::IsVideoFourcc + NJClient::DrainRawDataSendQueueForTest helpers landed (recommended) or were replaced with friend-class access
- Drain placement line number inside NJClient::Run (which existing audio-encoder Send is it adjacent to)
- Any divergence from ninjamzap-core/njclient.cpp:1984-2082 verbatim port (expected: zero except SPSC/mutex swap + heap-buf-pointer ownership)
- ctest -R rawdata_send_roundtrip output (8 passed, 0 failed)
- Full ctest run output showing no regression
</output>
</content>
