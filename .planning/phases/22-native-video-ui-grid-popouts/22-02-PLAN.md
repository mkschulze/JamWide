---
phase: 22
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - juce/ui/video/VideoGridBand.h
  - juce/ui/video/VideoGridBand.cpp
  - juce/ui/ConnectionBar.h
  - juce/ui/ConnectionBar.cpp
  - juce/JamWideJuceEditor.h
  - juce/JamWideJuceEditor.cpp
  - CMakeLists.txt
autonomous: false
requirements: [DISP-01, DISP-04]
must_haves:
  truths:
    - "User can click the new ConnectionBar 'Grid' button and the in-main-view video grid band toggles visibility between sessionInfoStrip and channelStripArea"
    - "When the band is visible and self is broadcasting (D-07), the self-tile appears as the first slot"
    - "When the band is visible and a remote peer's PeerVideoSink exists (D-12), that peer's `RemotePeerTile` is mounted in the band via the 30Hz Timer poll (D-13 Option a)"
    - "The first time any peer's `first_frame_seen` flips false→true, the band auto-opens once per session (D-05); user explicit close is sticky and not undone by subsequent peer first-frames"
    - "Toggling the grid band off and on again does NOT call any NJClient connection/disconnect API — the audio session continues uninterrupted (DISP-04)"
    - "Channel strip area's vertical height shrinks by exactly the band height when the band is open (D-02 stacked-above-mixer layout)"
    - "ConnectionBar fits inside the 1200 px kBaseWidth budget — the new Grid button + existing Camera + status dots + Connect/Browse + logo do not overlap"
  artifacts:
    - path: "juce/ui/video/VideoGridBand.h"
      provides: "Container for self-tile + per-peer RemotePeerTile flow layout; owns 30Hz Timer for sink-poll + self-broadcast atomic poll"
      contains: "class VideoGridBand"
    - path: "juce/ui/video/VideoGridBand.cpp"
      provides: "Hand-rolled resized() using computeGridLayout; timerCallback() with D-13 Option a sink polling; D-07 self-tile gating"
      contains: "computeGridLayout"
    - path: "juce/ui/ConnectionBar.h"
      provides: "Grid button member + accessor + onGridToggleClicked callback"
      contains: "onGridToggleClicked"
    - path: "juce/JamWideJuceEditor.cpp"
      provides: "gridBand_ member, resized() insertion, timerCallback() auto-open latch, ConnectionBar grid-click wiring"
      contains: "gridBand_"
  key_links:
    - from: "juce/JamWideJuceEditor.cpp"
      to: "juce/ui/video/VideoGridBand.h"
      via: "unique_ptr<VideoGridBand> gridBand_ constructed in editor ctor; setBounds in resized() between sessionInfoStrip and channelStripArea"
      pattern: "gridBand_->setBounds"
    - from: "juce/ui/video/VideoGridBand.cpp"
      to: "juce/video/distributor/JamWideRemoteFrameDistributor.h"
      via: "30Hz Timer calls findSink(name, 1) per cached user; mounts RemotePeerTile when sink first appears"
      pattern: "findSink"
    - from: "juce/JamWideJuceEditor.cpp"
      to: "juce/video/distributor/PeerVideoSink.h"
      via: "20Hz editor Timer auto-open latch checks first_frame_seen.load() per cached user"
      pattern: "first_frame_seen"
    - from: "juce/ui/ConnectionBar.cpp"
      to: "juce/JamWideJuceEditor.cpp"
      via: "onGridToggleClicked lambda calls editor->toggleGridBand(!processorRef.getVideoGridVisible())"
      pattern: "onGridToggleClicked"
---

<objective>
Build the in-main-view grid band container that hosts the tile substrate from Plan 22-01, wire the new ConnectionBar "Grid" button as the toggle entry point, and integrate the band into `JamWideJuceEditor::resized()` between `sessionInfoStrip` and `channelStripArea`. Implement the D-05 auto-open-on-first-frame latch in the editor's existing 20Hz Timer. Covers DISP-01 (per-user tile grid in main view) and DISP-04 (toggle without disconnecting NINJAM).

Purpose: This is the container + chrome integration layer. It owns the lifecycle of per-peer tiles via the 30Hz sink-poll Timer (D-13 Option a — recommended over distributor event surface to avoid touching Phase 21's now-closed code), and it owns the self-tile gating via `connectionBar.getCameraIsBroadcasting()` (D-07 — symmetric with peer-tile rule). Toggling is a pure UI operation (`setVisible` + `resized()`) — DISP-04's "no disconnect" requirement is satisfied by NOT touching `NJClient` at all in this plan.

Output: 2 new container files + 3 modified surfaces (ConnectionBar, JamWideJuceEditor, CMakeLists). After this plan, with `JamWideJuce_Standalone` running and a peer broadcasting on `video.ninjamzap.com:2049`, clicking the "Grid" button opens the band, the peer's tile renders the live feed, clicking again hides the band without dropping the connection. The popout windows + state persistence still come in Plans 22-03 and 22-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-native-video-ui-grid-popouts/22-CONTEXT.md
@.planning/phases/22-native-video-ui-grid-popouts/22-RESEARCH.md
@.planning/phases/22-native-video-ui-grid-popouts/22-PATTERNS.md
@.planning/phases/22-native-video-ui-grid-popouts/22-VALIDATION.md
@.planning/phases/22-native-video-ui-grid-popouts/22-01-PLAN.md

@juce/ui/video/computeGridLayout.h
@juce/ui/video/VideoTileBase.h
@juce/ui/video/SelfVideoTile.h
@juce/ui/video/RemotePeerTile.h

@juce/ui/ChannelStripArea.h
@juce/ui/ChannelStripArea.cpp
@juce/ui/ConnectionBar.h
@juce/ui/ConnectionBar.cpp
@juce/JamWideJuceEditor.h
@juce/JamWideJuceEditor.cpp
@juce/JamWideJuceProcessor.h
@juce/video/distributor/JamWideRemoteFrameDistributor.h
@juce/video/distributor/PeerVideoSink.h
@juce/video/native/JamWideFrameDistributor.h

<interfaces>
<!-- Existing editor 20Hz Timer + layout slot pattern (Plan 22-02 extends, not replaces): -->
From juce/JamWideJuceEditor.cpp:286: startTimerHz(20);   // editor's existing master timer
From juce/JamWideJuceEditor.cpp:407: void JamWideJuceEditor::timerCallback() { drainEvents(); pollStatus(); ... }
From juce/JamWideJuceEditor.cpp:372-405: resized() — layout order today: connectionBar → beatBar → sessionInfoStrip → channelStripArea (fills remaining).
From juce/JamWideJuceEditor.h:146: static constexpr int kBaseWidth = 1200;
From juce/JamWideJuceEditor.h:148: static constexpr int kConnectionBarHeight = 44;
From juce/JamWideJuceEditor.h:153: static constexpr int kSessionInfoStripHeight = 20;

<!-- Existing ConnectionBar Camera button pattern — analog for Grid button: -->
From juce/ui/ConnectionBar.h:107-108:
  class CameraButton;
  std::unique_ptr<CameraButton> cameraButton;
From juce/ui/ConnectionBar.h:43: std::function<void()> onCameraClicked;
From juce/ui/ConnectionBar.h:65-67: bool getCameraIsBroadcasting() const noexcept { return cameraIsBroadcasting_; }
From juce/ui/ConnectionBar.cpp:265-277: cameraButton = std::make_unique<CameraButton>(*this); ... addAndMakeVisible(*cameraButton);
From juce/ui/ConnectionBar.cpp:359-365: cameraButton->setBounds(rightX - 130, y, 130, h); rightX -= 130 + gap;

<!-- Processor cached-roster contract (run-thread mutates under mutex; UI-thread reads under same mutex): -->
From juce/JamWideJuceProcessor.h:269-270:
  mutable std::mutex cachedUsersMutex;
  std::vector<NJClient::RemoteUserInfo> cachedUsers;

<!-- Existing bot-filter helpers (declared at the top of ChannelStripArea.cpp; this plan re-uses the same filter for tile mounting): -->
From juce/ui/ChannelStripArea.cpp:10: bool isBot(const juce::String& name)
From juce/ui/ChannelStripArea.cpp:26: juce::String stripAtSuffix(const juce::String& name)

<!-- Phase 21 PeerVideoSink first_frame_seen atomic (D-05 trigger): -->
From juce/video/distributor/PeerVideoSink.h: std::atomic<bool> first_frame_seen;

<!-- Phase 19 self-camera distributor + processor accessor: -->
From juce/JamWideJuceProcessor: JamWideFrameDistributor* getFrameDistributor();
From juce/JamWideJuceProcessor: JamWideRemoteFrameDistributor* getRemoteFrameDistributor();

<!-- The new processor state accessors are added in Plan 22-04, NOT this plan. For now, gridBand_ is a direct unique_ptr<VideoGridBand> on the editor; band-visible state lives on the editor as a single bool gridBandVisible_; band height is a constexpr 280 in this plan and is persisted by 22-04. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VideoGridBand container with hand-rolled resized() + 30Hz Timer (D-13 Option a)</name>
  <files>juce/ui/video/VideoGridBand.h, juce/ui/video/VideoGridBand.cpp, CMakeLists.txt</files>
  <read_first>
    - juce/ui/video/computeGridLayout.h (from Plan 22-01 — the signature this band will consume)
    - juce/ui/video/SelfVideoTile.h (from Plan 22-01 — the self-tile class this band conditionally mounts per D-07)
    - juce/ui/video/RemotePeerTile.h (from Plan 22-01 — the per-peer tile class this band mounts under sink-poll)
    - juce/ui/ChannelStripArea.h (lines 1-30 — analog: same 30Hz Timer + roster-snapshot + hand-rolled resized() shape)
    - juce/ui/ChannelStripArea.cpp:317-326 (30Hz Timer setup + dtor stopTimer)
    - juce/ui/ChannelStripArea.cpp:408-417 (cachedUsersMutex roster-snapshot pattern)
    - juce/ui/ChannelStripArea.cpp:848-907 (hand-rolled resized() with area.removeFromTop/Bottom)
    - juce/video/distributor/JamWideRemoteFrameDistributor.h (lines 102-152 — findSink + subscribeToPeer)
    - juce/video/distributor/PeerVideoSink.h (full file — first_frame_seen atomic contract)
    - juce/JamWideJuceProcessor.h:269-270 (cachedUsers + cachedUsersMutex declaration)
    - .planning/phases/22-native-video-ui-grid-popouts/22-RESEARCH.md (sections "Code Examples > Example 4" lines 883-940 AND "Architecture Patterns > Pattern 4: Self-broadcast atomic observation" lines 486-551)
    - .planning/phases/22-native-video-ui-grid-popouts/22-PATTERNS.md (section "juce/ui/video/VideoGridBand.{h,cpp}" lines 244-329)
  </read_first>
  <behavior>
    - Behaviour 1: Constructed with `(JamWideJuceProcessor& processor, ConnectionBar& bar, JamWideFrameDistributor* selfDist, JamWideRemoteFrameDistributor* remoteDist)`. After construction the band has zero child tiles (no peers yet).
    - Behaviour 2: `startTimerHz(30)` in constructor; `stopTimer()` as the FIRST line of destructor. Timer body: poll self-broadcast atomic + poll remote distributor sink map.
    - Behaviour 3: On `timerCallback()` edge transition `connectionBar.getCameraIsBroadcasting()` false→true, the band mounts a fresh `SelfVideoTile(*selfDist)` as the first child (D-07). Reverse transition unmounts it.
    - Behaviour 4: On `timerCallback()`, walks `processor.cachedUsers` under `cachedUsersMutex`; for each non-bot, non-self user, checks `remoteDist->findSink(name, 1)`. If a sink exists for a peer not already in `peerTiles_`, constructs a fresh `RemotePeerTile(*remoteDist, name, 1)`, mounts it, marks layoutChanged. If a peer in `peerTiles_` is no longer in cachedUsers OR has no sink, removes the tile.
    - Behaviour 5: `resized()` calls `computeGridLayout(numVisibleTiles, tileAreaW, tileAreaH)` and positions each tile in row-major order, accounting for the 24px header band + 4px bottom resizer strip.
    - Behaviour 6: `paint()` draws the 24px header strip with `kSurfaceStrip` background, a `↗ ×` icon pair on the right side (planner picks exact pixel placement; ~16px buttons with 4px gap), optional `[N peers]` badge on the left.
    - Behaviour 7: Mouse down on the header `×` triggers `onCloseRequested_()` callback (editor wires this to `toggleGridBand(false)`). Mouse down on the header `↗` triggers `onDetachRequested_()` callback (editor wires this in Plan 22-03; for this plan, the callback may be unset/no-op).
    - Behaviour 8: Each mounted `RemotePeerTile`'s `onPopoutClicked_` callback is wired by VideoGridBand to invoke `onPeerPopoutRequested_(username)` callback exposed to the editor (editor wires in Plan 22-03; for this plan, callback may be unset/no-op).
    - Behaviour 9: Mouse down/drag on the bottom 4px resizer strip drags the band height; calls `onHeightChangeRequested_(newHeight)` callback (editor wires to a setter; for this plan, integer member `currentBandHeight_` default 280 is used by editor's resized()).
  </behavior>
  <action>
    Create `juce/ui/video/VideoGridBand.h` declaring `class VideoGridBand : public juce::Component, private juce::Timer` in `jamwide` namespace. Forward-declare `class JamWideJuceProcessor;` `class ConnectionBar;` `class JamWideFrameDistributor;` `class JamWideRemoteFrameDistributor;` `class SelfVideoTile;` `class RemotePeerTile;`. Constructor signature `VideoGridBand(JamWideJuceProcessor& processor, ConnectionBar& connectionBar, JamWideFrameDistributor* selfDistributor, JamWideRemoteFrameDistributor* remoteDistributor)`. Out-of-line destructor declared in header (so unique_ptr<SelfVideoTile>/<RemotePeerTile> deleters resolve in the .cpp where full type is visible — mirror the ConnectionBar pattern). Override `void resized() override;`, `void paint(juce::Graphics& g) override;`, `void mouseDown(const juce::MouseEvent&) override;`, `void mouseDrag(const juce::MouseEvent&) override;`, `void mouseUp(const juce::MouseEvent&) override;`. Public callbacks: `std::function<void()> onCloseRequested;`, `std::function<void()> onDetachRequested;`, `std::function<void(const juce::String&)> onPeerPopoutRequested;`, `std::function<void(int)> onHeightChangeRequested;`. Public accessor `int getCurrentBandHeight() const noexcept { return currentBandHeight_; }` + `void setCurrentBandHeight(int h)` clamped to [140, 800]. Private `void timerCallback() override;`. Private member layout (in this order — none are RAII-detach types here): `JamWideJuceProcessor& processorRef_;`, `ConnectionBar& connectionBarRef_;`, `JamWideFrameDistributor* selfDistributor_;`, `JamWideRemoteFrameDistributor* remoteDistributor_;`, `std::unique_ptr<SelfVideoTile> selfTile_;`, `std::unordered_map<juce::String, std::unique_ptr<RemotePeerTile>> peerTiles_;`, `bool selfBroadcastingLast_ = false;`, `int currentBandHeight_ = 280;` (D-10 default per Claude's Discretion; 22-04 will swap this for the processor accessor), `bool resizing_ = false;`, `int dragStartY_ = 0;`, `int dragStartHeight_ = 0;`.

    Create `juce/ui/video/VideoGridBand.cpp`. Include the JUCE message-thread helpers + `computeGridLayout.h` + `SelfVideoTile.h` + `RemotePeerTile.h` + `juce/JamWideJuceProcessor.h` + `juce/ui/ConnectionBar.h` + `juce/ui/ChannelStripArea.h` (for isBot/stripAtSuffix helpers — declare them via `extern bool isBot(const juce::String&); extern juce::String stripAtSuffix(const juce::String&);` since ChannelStripArea.cpp has them as free functions at the top of the TU). Constructor: stores refs, starts `startTimerHz(30)`. Destructor: FIRST line `stopTimer();` then `peerTiles_.clear();` then `selfTile_.reset();` (explicit reverse-construction order).

    Implement `timerCallback()` per RESEARCH Example 4 / Pattern 4:
    Step A — self-broadcast observation:
    - `const bool nowBroadcasting = connectionBarRef_.getCameraIsBroadcasting() && selfDistributor_ != nullptr;`
    - If `nowBroadcasting != selfBroadcastingLast_`:
      - If now true: `selfTile_ = std::make_unique<SelfVideoTile>(*selfDistributor_); addAndMakeVisible(*selfTile_); selfTile_->onPopoutClicked_ = [this]{ if (onPeerPopoutRequested) onPeerPopoutRequested(juce::String(jamwide::kSelfTileSentinel)); };` — **W7 (per checker iter 1):** use the named constant `jamwide::kSelfTileSentinel` declared in `juce/ui/video/VideoTileBase.h` (Plan 22-01), NOT the bare `"__self__"` literal. Plan 22-03's editor controller dispatch (`openOrToggleRemotePopout`) checks `if (username == jamwide::kSelfTileSentinel)` and routes to `drivePreviewWindowVisibility` per D-09.
      - If now false: `removeChildComponent(selfTile_.get()); selfTile_.reset();`
      - `selfBroadcastingLast_ = nowBroadcasting;`
      - Set `layoutChanged = true;`

    Step B — per-peer sink poll (D-13 Option a):
    - Collect `std::vector<juce::String> currentNonBotPeers;` under `std::lock_guard lk(processorRef_.cachedUsersMutex);` iterating `processorRef_.cachedUsers`; skip bots via `isBot(juce::String(u.name))`; push `stripAtSuffix(juce::String(u.name))`.
    - For each name in `currentNonBotPeers`: if `peerTiles_.find(name) == end()` AND `remoteDistributor_->findSink(name.toRawUTF8(), 1) != nullptr`, construct `RemotePeerTile(*remoteDistributor_, name, 1)`, wire its `onPopoutClicked_ = [this, name]{ if (onPeerPopoutRequested) onPeerPopoutRequested(name); };`, `addAndMakeVisible(...)`, store in `peerTiles_[name]`, mark layoutChanged.
    - For each entry in `peerTiles_`: if name no longer in `currentNonBotPeers` OR `findSink(name, 1) == nullptr`, removeChildComponent + erase, mark layoutChanged.
    - If `layoutChanged`, call `resized()`.

    Implement `resized()`:
    - `auto bounds = getLocalBounds();`
    - `auto headerArea = bounds.removeFromTop(24);` (unused — paint uses getLocalBounds().removeFromTop(24))
    - `auto resizerArea = bounds.removeFromBottom(4);` (unused — paint draws the resizer)
    - `auto tileArea = bounds;`
    - Collect ordered tile vector: `std::vector<juce::Component*> visibleTiles; if (selfTile_) visibleTiles.push_back(selfTile_.get()); for (auto& kv : peerTiles_) visibleTiles.push_back(kv.second.get());` (self is always first slot per D-07; peer iteration order is determined by `unordered_map` which is fine — peer order stability is Claude's Discretion).
    - `auto layout = computeGridLayout((int) visibleTiles.size(), tileArea.getWidth(), tileArea.getHeight());`
    - For each tile in `visibleTiles`: compute col, row, set bounds inside `tileArea` accounting for `layout.marginX`/`layout.marginY`/`layout.spacing` per RESEARCH Pattern 2 final lines.

    Implement `paint(g)`:
    - 24px header strip with `kSurfaceStrip` bg, `[N peers]` badge on left (planner-tunable), `↗` and `×` glyphs on right with ~16px hit regions (planner records exact x-coords as constexpr `kHeaderDetachX_ = getWidth() - 38; kHeaderCloseX_ = getWidth() - 18;`).
    - 4px bottom strip with subtle `kBorderSubtle` colour to hint the resizer affordance.

    Implement `mouseDown`/`mouseDrag`/`mouseUp` for the 4px bottom resizer strip:
    - On mouseDown inside bottom 4px: `resizing_ = true; dragStartY_ = e.getScreenY(); dragStartHeight_ = currentBandHeight_;`. Also set `setMouseCursor(juce::MouseCursor::UpDownResizeCursor)` (or similar).
    - On mouseDrag while `resizing_`: `int newHeight = juce::jlimit(140, 800, dragStartHeight_ + (e.getScreenY() - dragStartY_));` (negative delta means making band taller? planner picks sign convention — for bottom edge drag, dragging down grows the band, dragging up shrinks). Call `setCurrentBandHeight(newHeight); if (onHeightChangeRequested) onHeightChangeRequested(newHeight);` (editor's callback re-calls editor's `resized()` to redistribute the available area).
    - On mouseUp: `resizing_ = false;`.
    - On mouseDown inside header `×` region: invoke `onCloseRequested` callback. On mouseDown inside header `↗` region: invoke `onDetachRequested` callback.

    Modify `CMakeLists.txt`: extend the existing Plan 22-01 `target_sources(JamWideJuce PRIVATE ...)` block with `juce/ui/video/VideoGridBand.h` + `juce/ui/video/VideoGridBand.cpp`. No additional link libraries.

    Per CONTEXT.md D-12: only mount peer tiles when `findSink` returns non-null AND the user is in `cachedUsers`. Per CONTEXT.md D-13 Option a: 30Hz Timer polls. Per CONTEXT.md D-10: default band height 280px (planner's discretion baseline). Per RESEARCH Anti-Pattern: tile pointers in `peerTiles_` are message-thread-owned, and the cached `PeerVideoSink*` is re-queried per-paint by the tile itself (already implemented in Plan 22-01).
  </action>
  <verify>
    <automated>cd build-juce && cmake --build . --target JamWideJuce_VST3 2>&1 | tail -40</automated>
  </verify>
  <done>
    `VideoGridBand.h/.cpp` exist under `juce/ui/video/`. CMakeLists.txt registers them in the `JamWideJuce` target. `JamWideJuce_VST3` builds cleanly. The band's `timerCallback()` follows D-13 Option a (Timer-poll). The band's `resized()` invokes `computeGridLayout` from Plan 22-01.
  </done>
  <acceptance_criteria>
    - Source assertion: `grep -q "class VideoGridBand" juce/ui/video/VideoGridBand.h` succeeds
    - Source assertion: `grep -q "private juce::Timer" juce/ui/video/VideoGridBand.h` succeeds
    - Source assertion: `grep -q "startTimerHz(30)" juce/ui/video/VideoGridBand.cpp` succeeds
    - Source assertion: `grep -q "stopTimer()" juce/ui/video/VideoGridBand.cpp` succeeds (in destructor)
    - Source assertion: `grep -q "cachedUsersMutex" juce/ui/video/VideoGridBand.cpp` succeeds (D-13 sink-poll respects roster lock)
    - Source assertion: `grep -q "computeGridLayout" juce/ui/video/VideoGridBand.cpp` succeeds (uses Plan 22-01's helper)
    - Source assertion: `grep -q "findSink" juce/ui/video/VideoGridBand.cpp` succeeds (D-13 Option a polling)
    - Source assertion: `grep -q "getCameraIsBroadcasting" juce/ui/video/VideoGridBand.cpp` succeeds (D-07 self-tile gating)
    - Source assertion: `grep -q "isBot" juce/ui/video/VideoGridBand.cpp` succeeds (bot filter — per memory project_ninbot_still_visible)
    - **W7 sentinel reference:** `grep -q "kSelfTileSentinel" juce/ui/video/VideoGridBand.cpp` succeeds AND `! grep -q '"__self__"' juce/ui/video/VideoGridBand.cpp` (no bare literal — use the named constant from VideoTileBase.h)
    - CMake assertion: `grep -q "juce/ui/video/VideoGridBand.cpp" CMakeLists.txt` succeeds
    - Build assertion: `cd build-juce && cmake --build . --target JamWideJuce_VST3` exits 0
    - Member-order assertion: in `juce/ui/video/VideoGridBand.h`, the `unique_ptr<SelfVideoTile> selfTile_` and `unordered_map<...> peerTiles_` members are declared BEFORE `bool selfBroadcastingLast_` — verified via `awk '/class VideoGridBand/,/^};/' juce/ui/video/VideoGridBand.h | grep -n "selfTile_\|peerTiles_\|selfBroadcastingLast_"` (selfTile_ line < selfBroadcastingLast_ line). This is so child component destructors run before the bool helper data.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConnectionBar GridButton + JamWideJuceEditor wiring (resized() insertion + 20Hz auto-open latch)</name>
  <files>juce/ui/ConnectionBar.h, juce/ui/ConnectionBar.cpp, juce/JamWideJuceEditor.h, juce/JamWideJuceEditor.cpp, CMakeLists.txt</files>
  <read_first>
    - juce/ui/ConnectionBar.h (full file — Camera button placement pattern)
    - juce/ui/ConnectionBar.cpp:23-71 (file-local CameraButton subclass definition pattern)
    - juce/ui/ConnectionBar.cpp:265-277 (camera button construction + onClick wiring)
    - juce/ui/ConnectionBar.cpp:359-365 (camera button placement in resized())
    - juce/JamWideJuceEditor.h (lines 1-160 — editor member list, layout constants, kBaseWidth=1200 — verify the right-cluster pixel budget per RESEARCH Pitfall 7)
    - juce/JamWideJuceEditor.cpp:230-253 (existing CameraPreviewWindow construction + callback wiring — analog for the new gridBand_ constructor + callback wiring)
    - juce/JamWideJuceEditor.cpp:286 (startTimerHz(20) — the 20Hz Timer this plan extends with the D-05 auto-open latch)
    - juce/JamWideJuceEditor.cpp:372-405 (resized() — slot to extend with gridBand_->setBounds insertion between sessionInfoStrip and channelStripArea)
    - juce/JamWideJuceEditor.cpp:407-450 (timerCallback() entry — extension point for D-05 auto-open latch)
    - juce/ui/video/VideoGridBand.h (the new class from Task 1 — its callbacks)
    - juce/JamWideJuceProcessor.h:269-270 (cachedUsers + cachedUsersMutex)
    - juce/video/distributor/PeerVideoSink.h (first_frame_seen atomic for D-05)
    - .planning/phases/22-native-video-ui-grid-popouts/22-RESEARCH.md (section "Architecture Patterns > Pattern 5: Auto-open-on-first-frame latch (D-05)" lines 552-583)
    - .planning/phases/22-native-video-ui-grid-popouts/22-RESEARCH.md (section "Common Pitfalls > Pitfall 7: ConnectionBar GridButton overlaps right cluster at default 1200px width" lines 762-770)
  </read_first>
  <behavior>
    - Behaviour 1: `ConnectionBar` gets a new public member `std::function<void()> onGridToggleClicked;` and a new `setGridVisible(bool)` / `getGridVisible() const noexcept` accessor pair mirroring `setCameraIsBroadcasting`/`getCameraIsBroadcasting`. The GridButton is a `std::unique_ptr<GridButton>` member with `GridButton` defined file-local in `ConnectionBar.cpp` as `: public juce::TextButton` (mirror of CameraButton — no right-click menu needed).
    - Behaviour 2: GridButton label is `"Grid"`; tooltip is `"Toggle video grid"`. Button is placed in the right cluster to the LEFT of the Camera button at 60px wide (revised from 80px per checker W1 — measurement-driven shrink so the new button fits inside the existing left-vs-right pixel budget at default 1200px window width without bumping kBaseWidth). Right cluster grows from ~514px to ~580px; left cluster ends near x=835 with password visible; remaining buffer ~32px is acceptable for the v1.3 beta. If UAT cell A in Task 3 still shows visible overlap or crowding, the fallback is to bump `kBaseWidth = 1380` in `juce/JamWideJuceEditor.h` line 146 in the same Task 2 edit (DO NOT silently widen — document the decision in the SUMMARY).
    - Behaviour 3: When user clicks the GridButton, `onGridToggleClicked()` fires → editor's wired lambda toggles the band visibility.
    - Behaviour 4: `JamWideJuceEditor` gains `std::unique_ptr<jamwide::VideoGridBand> gridBand_;` constructed in the editor ctor after `connectionBar` is constructed (so `connectionBar` ref is valid). Constructed only if both `processorRef.getFrameDistributor()` AND `processorRef.getRemoteFrameDistributor()` return non-null pointers. Wires `gridBand_->onCloseRequested = [this]{ toggleGridBand(false); };`. Wires `gridBand_->onHeightChangeRequested = [this](int h){ /* stored on editor member gridBandHeight_; for now, just call resized() */ };`. Leaves `gridBand_->onDetachRequested` and `gridBand_->onPeerPopoutRequested` UNSET in this plan (Plan 22-03 wires them).
    - Behaviour 5: Editor adds a private member `bool gridBandVisible_ = false;` and `int gridBandHeight_ = 280;` plus method `void toggleGridBand(bool visible);` that sets `gridBandVisible_ = visible;`, calls `gridBand_->setVisible(visible);`, and calls `resized();`. Method is callable from the GridButton click lambda AND from the auto-open latch in timerCallback.
    - Behaviour 6: Editor's `resized()` gets a new conditional block AFTER the existing `chatPanel.setBounds(area.removeFromRight(chatWidth));` (juce/JamWideJuceEditor.cpp:389) but BEFORE `channelStripArea.setBounds(area);` (line 399). **W2 decision (per checker iteration 1):** insert AFTER `chatPanel.removeFromRight` so the band spans only the MIXER WIDTH (the column to the LEFT of the chat panel). The chat panel stays adjacent to the mixer column at its full editor-height; the band feels "above mixer" which matches D-02's "stacked between SessionInfoStrip and ChannelStripArea" interpretation when SessionInfoStrip+ChannelStripArea = the mixer column. Only `channelStripArea` shrinks vertically when the band opens; chat panel is unaffected. Insertion shape:
      ```
      sessionInfoStrip.setBounds(area.removeFromTop(kSessionInfoStripHeight));      // existing
      int chatWidth = chatSidebarVisible ? kChatPanelWidth : 0;                     // existing
      if (chatSidebarVisible) chatPanel.setBounds(area.removeFromRight(chatWidth)); // existing
      // ... existing chatToggleButton setBounds ...                                 // existing
      if (gridBand_ && gridBandVisible_) {                                          // NEW Phase 22
          gridBand_->setBounds(area.removeFromTop(gridBandHeight_));
      }
      channelStripArea.setBounds(area);                                             // existing
      ```
      Acceptance is verifiable by visual inspection in Task 3 cell A: with the band visible and the chat panel visible, the band's top edge sits below SessionInfoStrip; the band's right edge sits at the chat panel's LEFT edge (NOT extending across the chat panel); chat panel runs the full editor height unaffected by the band.
    - Behaviour 7: Editor's `timerCallback()` gets an additive auto-open latch per RESEARCH Pattern 5 (lines 552-583). Uses `std::atomic<bool> gridAutoOpenLatchFired_{false};` member. On each tick, if latch is false: walks `processorRef.cachedUsers` under `cachedUsersMutex`; for each non-bot peer, queries `getRemoteFrameDistributor()->findSink(name, 1)`; if any sink has `first_frame_seen.load() == true` and `!gridAutoOpenLatchFired_.exchange(true)` succeeds, call `toggleGridBand(true);` and break the loop.
    - Behaviour 8: NO NJClient API call inside `toggleGridBand` — the toggle is pure UI (DISP-04). Verifiable by `grep -L "client->\|NJClient::" toggleGridBand` (the method body should not reference these symbols).
    - Behaviour 9: The auto-open latch is per-session (resets to false only on plugin reload — i.e., editor reconstruction). Once fired, subsequent peer first-frames do not re-open the band even if the user explicitly closed it (per D-05 — "Once auto-opened, the band stays open until the user explicitly closes it").
  </behavior>
  <action>
    Modify `juce/ui/ConnectionBar.h`:
    - Add `std::function<void()> onGridToggleClicked;` public field, alongside the existing `onCameraClicked` block (~line 43).
    - Add public accessor pair `void setGridVisible(bool v) noexcept { gridVisible_ = v; if (gridButton) gridButton->setToggleState(v, juce::dontSendNotification); }` and `bool getGridVisible() const noexcept { return gridVisible_; }` adjacent to the existing `setCameraIsBroadcasting` (~lines 62-67).
    - Add forward declaration `class GridButton;` and `std::unique_ptr<GridButton> gridButton;` private member, adjacent to existing `cameraButton` (~lines 107-108).
    - Add private `bool gridVisible_{false};` adjacent to existing `cameraIsBroadcasting_` (~line 120).

    Modify `juce/ui/ConnectionBar.cpp`:
    - Add file-local subclass definition `class ConnectionBar::GridButton : public juce::TextButton { public: explicit GridButton(ConnectionBar& p) : parent(p) {} private: ConnectionBar& parent; };` adjacent to the existing `CameraButton` definition (~lines 23-71). No right-click menu (D-04 always-visible — no submenu needed).
    - In the ConnectionBar constructor body (adjacent to where `cameraButton` is constructed, ~line 265): construct `gridButton = std::make_unique<GridButton>(*this); gridButton->setButtonText("Grid"); gridButton->setColour(juce::TextButton::buttonColourId, juce::Colour(JamWideLookAndFeel::kSurfaceStrip)); gridButton->setColour(juce::TextButton::textColourOffId, juce::Colour(JamWideLookAndFeel::kTextSecondary)); gridButton->setTooltip("Toggle video grid"); gridButton->setClickingTogglesState(true); gridButton->onClick = [this]() { if (onGridToggleClicked) onGridToggleClicked(); }; addAndMakeVisible(*gridButton);`
    - In `ConnectionBar::resized()` (~line 359-365 area where camera button is placed): add Grid button placement BEFORE the camera button placement (so Grid sits to the LEFT of Camera per CONTEXT.md D-05 wording "next to the existing Camera button" — placing it to the left preserves the natural mental order "open camera" → "show grid of cameras"). Specifically: `if (gridButton) { gridButton->setBounds(rightX - 60, y, 60, h); rightX -= 60 + gap; }` BEFORE the Camera button block. **W1 pre-emptive decision (per checker iteration 1):** Width-budget arithmetic at kBaseWidth=1200 (area reduced 8 each side, effective width 1184): existing right cluster from `area.getRight()` runs fit(36)→debug(36)→camera(130)→midi(44)→osc(44)→sync(44 cond.)→route(52)→codec(80) plus six gap(6)s = ~514px → starts at x≈670. Left cluster ends near x≈835 with password visible (logo+server+username+pwdToggle+password+connect+browse+status). Buffer today is therefore already negative when password is visible. Adding Grid at 60px+gap(6) increases the right cluster by 66px → right cluster now starts at x≈604. **The right cluster MUST NOT collide with the left cluster.** Two acceptable resolutions: **(A)** Grid at 60px wide (recommended baseline); UAT cell A confirms. **(B)** If A overlaps observably, bump `kBaseWidth` from 1200 to 1380 in `juce/JamWideJuceEditor.h:146` AS PART OF THIS SAME TASK (same commit). DO NOT silently widen — record the decision in the plan SUMMARY. Either resolution is acceptable; checker iteration 1 declined to mandate one specifically because the real measurement is observable only at runtime.

    Modify `juce/JamWideJuceEditor.h`:
    - Add `#include "ui/video/VideoGridBand.h"` near the existing video include (juce/video/native/CameraPreviewWindow.h).
    - Add new private members near the existing `previewWindow_` group (~lines 81-82): `std::unique_ptr<jamwide::VideoGridBand> gridBand_;`, `bool gridBandVisible_ = false;`, `int gridBandHeight_ = 280;`, `std::atomic<bool> gridAutoOpenLatchFired_{false};`.
    - Add private method `void toggleGridBand(bool visible);` near the existing `drivePreviewWindowVisibility` declaration (~line 49).

    Modify `juce/JamWideJuceEditor.cpp`:
    - In the editor constructor body (adjacent to where `previewWindow_` is constructed at lines 236-253): construct `gridBand_` only if both distributors are available:
      `if (auto* selfDist = processorRef.getFrameDistributor()) { if (auto* remoteDist = processorRef.getRemoteFrameDistributor()) { gridBand_ = std::make_unique<jamwide::VideoGridBand>(processorRef, connectionBar, selfDist, remoteDist); gridBand_->onCloseRequested = [this]() { toggleGridBand(false); }; gridBand_->onHeightChangeRequested = [this](int h) { gridBandHeight_ = juce::jlimit(140, 800, h); resized(); }; addChildComponent(*gridBand_); /* hidden until user toggles */ } }`. Use `addChildComponent` not `addAndMakeVisible` so band starts hidden (D-14 mirror — no auto-open on plugin load; D-05 latch opens it on first peer frame within the session).
    - Wire ConnectionBar's `onGridToggleClicked`: `connectionBar.onGridToggleClicked = [this]() { toggleGridBand(!gridBandVisible_); };` adjacent to the existing `connectionBar.onCameraClicked = ...` wiring.
    - Implement `void JamWideJuceEditor::toggleGridBand(bool visible) { if (!gridBand_) return; gridBandVisible_ = visible; gridBand_->setVisible(visible); connectionBar.setGridVisible(visible); resized(); }`. ZERO calls to `client.*` or `NJClient::` in this method body — pure UI toggle (DISP-04).
    - In `resized()` (~lines 372-405 of juce/JamWideJuceEditor.cpp): insert the band block AFTER `chatPanel.setBounds(area.removeFromRight(chatWidth));` (line 389) and the existing `chatToggleButton.setBounds(...)` block at lines 391-396, but BEFORE `channelStripArea.setBounds(area);` (line 399). This gives the band the MIXER WIDTH (W2 decision above — chat panel stays adjacent to mixer). The exact new block:
      ```
      if (gridBand_ && gridBandVisible_) {
          gridBand_->setBounds(area.removeFromTop(gridBandHeight_));
      }
      ```
      Only `channelStripArea` shrinks vertically by `gridBandHeight_` when the band is open; `chatPanel` is unaffected.
    - In `timerCallback()` (~lines 407-450): after the existing `drainEvents(); pollStatus();` block, append the D-05 auto-open latch per RESEARCH Pattern 5 lines 561-580:
      ```
      if (!gridAutoOpenLatchFired_.load(std::memory_order_acquire)) {
        if (auto* dist = processorRef.getRemoteFrameDistributor()) {
          std::lock_guard<std::mutex> lk(processorRef.cachedUsersMutex);
          for (const auto& u : processorRef.cachedUsers) {
            if (isBot(juce::String(u.name))) continue;
            if (auto* sink = dist->findSink(u.name, /*chidx*/1)) {
              if (sink->first_frame_seen.load(std::memory_order_acquire)) {
                if (!gridAutoOpenLatchFired_.exchange(true)) {
                  // Defer the actual visibility flip out of the mutex scope to
                  // avoid re-entering cachedUsersMutex from any callback.
                  juce::MessageManager::callAsync([this]{ toggleGridBand(true); });
                }
                break;
              }
            }
          }
        }
      }
      ```
      Note the `MessageManager::callAsync` deferral — we are already on the message thread (timerCallback is message-thread) so the asynchronous shim is purely to escape the cachedUsersMutex hold before `toggleGridBand` runs `resized()` which may touch other UI state.
    - Include `extern bool isBot(const juce::String&);` near the top of the .cpp (it lives in ChannelStripArea.cpp at file scope) OR include the relevant header that exposes it. If no header exists, declare `extern bool isBot(const juce::String&);` at the top of JamWideJuceEditor.cpp inside an anonymous-namespace `namespace { extern bool isBot(const juce::String&); }` or convert ChannelStripArea.cpp's bot filter into a small public helper header. Cleanest: add a new tiny header `juce/ui/BotFilter.h` that declares both `isBot` and `stripAtSuffix` as free functions; update ChannelStripArea.cpp to include it (and remove the local definitions — keep them as the implementation). Both VideoGridBand.cpp and JamWideJuceEditor.cpp can then `#include "ui/BotFilter.h"`.

    Add `juce/ui/BotFilter.h` (new file) declaring `bool isBot(const juce::String& name);` and `juce::String stripAtSuffix(const juce::String& name);`. Move the implementations from ChannelStripArea.cpp:10-30 into a new file `juce/ui/BotFilter.cpp` OR keep them in ChannelStripArea.cpp and just expose via the header. Recommended: leave implementations in ChannelStripArea.cpp (avoiding new .cpp) and just create the header — works because they were already extern by virtue of being free functions at namespace scope; adding the header is just declaring them officially.

    Modify `CMakeLists.txt`: extend the existing `target_sources(JamWideJuce PRIVATE ...)` block to include `juce/ui/BotFilter.h`. (The implementations are still in ChannelStripArea.cpp which is already in the target.)
  </action>
  <verify>
    <automated>cd build-juce && cmake --build . --target JamWideJuce_Standalone JamWideJuce_VST3 2>&1 | tail -40</automated>
  </verify>
  <done>
    `ConnectionBar` has a GridButton. `JamWideJuceEditor` owns `gridBand_`, wires the toggle callback, inserts the band in `resized()`, and implements the D-05 auto-open latch in `timerCallback()`. `JamWideJuce_Standalone` + `JamWideJuce_VST3` both build cleanly. The `toggleGridBand` method body has zero references to `client.*` or `NJClient::` (DISP-04).
  </done>
  <acceptance_criteria>
    - Source assertion: `grep -q "class GridButton" juce/ui/ConnectionBar.h` succeeds
    - Source assertion: `grep -q "onGridToggleClicked" juce/ui/ConnectionBar.h` succeeds
    - Source assertion: `grep -q "setGridVisible\|getGridVisible" juce/ui/ConnectionBar.h` succeeds (accessor pair)
    - Source assertion: `grep -q "gridButton" juce/ui/ConnectionBar.cpp` finds at least 5 hits (declaration, ctor body, addAndMakeVisible, resized() setBounds, onClick)
    - Source assertion: `grep -q "gridBand_" juce/JamWideJuceEditor.cpp` finds at least 4 hits (ctor construction, onCloseRequested wire, resized() setBounds, toggleGridBand body)
    - Source assertion: `grep -q "gridAutoOpenLatchFired_" juce/JamWideJuceEditor.cpp` succeeds AND the auto-open block uses `exchange(true)` (atomic latch fire-once)
    - Source assertion: `grep -q "first_frame_seen.load" juce/JamWideJuceEditor.cpp` succeeds (D-05 latch reads the Phase 21 atomic)
    - DISP-04 assertion: `awk '/^void JamWideJuceEditor::toggleGridBand/,/^}/' juce/JamWideJuceEditor.cpp | grep -cE 'client\.|NJClient::|disconnect|reconnect' ` returns 0 — toggle MUST NOT touch NJClient.
    - Source assertion: `grep -q "addChildComponent(\*gridBand_)" juce/JamWideJuceEditor.cpp` (band starts hidden, NOT addAndMakeVisible)
    - Source assertion: `grep -q "MessageManager::callAsync" juce/JamWideJuceEditor.cpp` succeeds inside the auto-open latch block (deferred toggle out of mutex scope)
    - BotFilter header: `test -f juce/ui/BotFilter.h && grep -q "isBot\|stripAtSuffix" juce/ui/BotFilter.h` succeeds
    - Editor includes: `grep -q '#include "ui/video/VideoGridBand.h"\|#include "ui/video/VideoGridBand.h"' juce/JamWideJuceEditor.h` OR equivalent in the .cpp succeeds
    - Build assertion: `cd build-juce && cmake --build . --target JamWideJuce_Standalone JamWideJuce_VST3` exits 0
    - Width budget assertion (visual only — operator confirms in Task 3 checkpoint that ConnectionBar fits at default 1200px window width with no overlap)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual verification — band toggles, tile renders, ConnectionBar fits</name>
  <what-built>
    Plan 22-02 wired the in-main-view grid band. Clicking the new "Grid" button in ConnectionBar should now toggle a 280-pixel band between SessionInfoStrip and ChannelStripArea. When connected to `video.ninjamzap.com:2049` with a peer broadcasting, that peer's tile should render the live video feed inside the band. When self is broadcasting (Phase 20-03 broadcast toggle on), a "You" tile should appear as the first slot.

    The auto-open-on-first-frame latch (D-05) should fire once per session: connect to a server with the band closed, wait for a peer to start broadcasting H264, and the band should auto-open within ~50-100ms after the first decoded frame.
  </what-built>
  <how-to-verify>
    Build: `cd build-juce && cmake --build . --target JamWideJuce_Standalone JamWideJuce_VST3`

    Test cell A — ConnectionBar fit at default width:
    1. Launch `./build-juce/JamWideJuce_artefacts/Debug/Standalone/JamWide.app/Contents/MacOS/JamWide` (or open in Standalone host on Windows / via DAW)
    2. Confirm the default window is 1200px wide and the right cluster is: `Grid | Camera | MIDI dot | OSC dot | Sync | Route | Codec | Debug | Fit` with no overlap onto the left cluster (`Logo | server field | username | password | Connect | Browse`)
    3. If overlap exists, RECORD AS ISSUE and we bump kBaseWidth to 1280 in revision

    Test cell B — Grid button toggle (DISP-04 happy path):
    1. Connect to a server that does NOT host any video peers (or stay disconnected). Click "Grid" — band should appear empty (no tiles, just header chrome + ~280px empty band). Click "Grid" again — band should hide. Channel strip area should fill the space again. Connection state (if any) MUST NOT have dropped — verify ConnectionBar status stays the same throughout.
    2. Repeat the toggle 5× rapidly. Audio should remain uninterrupted.

    Test cell C — Self-tile broadcast gating (D-07):
    1. With band visible, enable broadcasting via Camera button right-click menu → "Start Broadcast". Within ~50ms the self-tile labelled "You" should appear as the first slot.
    2. "Stop Broadcast" → self-tile disappears within ~50ms.

    Test cell D — Peer tile renders live feed (DISP-01 partial):
    1. Connect to `video.ninjamzap.com:2049` with the JamWide standalone. Coordinate with at least 1 collaborator broadcasting H264 video via JamWide (Phase 20) or NinjamZap mobile.
    2. Open the band via Grid button. Wait for the peer's first frame.
    3. Confirm the peer's `RemotePeerTile` appears in the band with the username strip at the bottom and the live video rendering in the centre.
    4. Confirm the popout `↗` icon is always-visible in the top-right of each tile (D-04). Clicking it should be a no-op for now (popout wiring lands in Plan 22-03).

    Test cell E — Auto-open latch (D-05):
    1. Close the band (Grid button toggle off). Disconnect. Reconnect with the band closed.
    2. Wait for first peer frame from collaborator. Band should auto-open within ~100ms.
    3. Close the band manually. Wait for the same peer to stop+restart broadcasting. Band should NOT re-auto-open (latch is once-per-session sticky).

    Test cell F — Band height resizer (D-10):
    1. With the band visible, hover near the bottom edge — cursor should change to the up-down resize cursor.
    2. Drag down — band should grow taller; mixer should shrink. Drag up — band shrinks. Constrained between 140 and 800 pixels.

    Test cell G — DISP-04 (no disconnect on toggle):
    1. With an ACTIVE NINJAM connection (audio flowing), toggle Grid button ON. Confirm audio continues without glitch.
    2. Toggle OFF. Confirm audio continues.
    3. In Activity Monitor / Task Manager, confirm CPU profile of JamWide does NOT spike when toggling.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 test cells pass.
    Type "issues: [description]" if any cells fail — we will diagnose and revise the plan (likely a width-budget bump, a latch ordering issue, or a sink-poll bug).
    For cells B/D/E that require a live server with peers, type "deferred: [list]" if you cannot run them now — we will move them to Plan 22-04's manual UAT report and they become BLOCKED until UAT is run.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConnectionBar UI click → editor controller | onGridToggleClicked lambda is wired by the editor at construction; runs on the message thread. No race. |
| Editor 20Hz Timer → cachedUsers + remote distributor | Reads `cachedUsers` under `cachedUsersMutex` (existing pattern) and `findSink` (thread-safe via distributor's `mu_`). Auto-open latch is a `std::atomic<bool>::exchange` once-fire — no race. |
| VideoGridBand 30Hz Timer → cachedUsers + sink lifecycle | Same mutex pattern as ChannelStripArea. The tile's cached PeerVideoSink* is re-queried per-paint (RESEARCH Pitfall 6 mitigation already in tile code from Plan 22-01). |
| Tile destruction → distributor in-flight | RAII Subscription destructor blocks for in-flight handleAsyncUpdate (Phase 21 contract). Member-order proven by Plan 22-01 Task 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-CB (LOW, from gate) | Tampering | ConnectionBar Grid button state desync — saved gridVisible=true but no peers, band renders empty | accept | Empty band renders gracefully (just header + empty area); no crash; user can close manually. Persisted state (gridVisible) is handled in Plan 22-04, not this plan. |
| T-22-RE-1 | Denial of Service | Editor 20Hz Timer auto-open latch enters an infinite re-entry loop if `toggleGridBand` indirectly retriggers the latch | mitigate | Latch uses `atomic<bool>::exchange(true)` — once set, the condition `!load(acquire)` is false, no re-entry. Also the `toggleGridBand` callback is deferred via `MessageManager::callAsync` out of the cachedUsersMutex scope. |
| T-22-RE-2 | Denial of Service | VideoGridBand 30Hz Timer flapping on rapid peer join/leave causes UI thrash | accept | RESEARCH Open Question 2 — no debounce for v1.3 beta; revisit per UAT feedback. The 30Hz cadence is already the codebase-wide standard (ChannelStripArea uses it). |
| T-22-MM-1 | Information Disclosure | Mounting a peer tile renders the latest decoded `image_front` even if the peer left the room mid-frame | accept | Phase 21's removeSink protocol clears the sink before the tile can re-query findSink → tile shows "video starting..." overlay (no live frame). No PII exposure path beyond what was already on-screen. |
| T-22-AP (HIGH-equivalent, from gate's T-22-SP) | Tampering | Plugin state injection (popout-count cap, bounds clamping, username 256-char cap) | N/A in this plan | Deferred to Plan 22-04 (state persistence layer). Persistence is OUT OF SCOPE for 22-02; the gridBand starts hidden via `addChildComponent` and a session bool, not a persisted field. |
</threat_model>

<verification>
1. Plan 22-01's two unit tests still pass: `cd build-juce && ctest -R "video_grid_layout|video_tile_member_order" --output-on-failure` exits 0
2. `JamWideJuce_Standalone` + `JamWideJuce_VST3` build cleanly with no compiler warnings beyond pre-existing baseline
3. The `toggleGridBand` method body in `JamWideJuceEditor.cpp` contains zero references to `client.*` or `NJClient::` symbols (DISP-04 hard requirement)
4. Manual visual verification (Task 3 checkpoint) confirms 7 test cells pass; if any cell fails we revise and re-run
</verification>

<success_criteria>
- DISP-01 advanced — band container renders peer tiles consumed from Phase 21's distributor
- DISP-04 satisfied — `toggleGridBand` makes zero NJClient API calls; manual cell G confirms audio uninterrupted on toggle
- ConnectionBar width budget respected (Pitfall 7 — visual check in Task 3 cell A)
- Auto-open latch (D-05) fires once per session, sticky after user-close
- 30Hz sink-poll (D-13 Option a) correctly mounts/unmounts tiles on peer add/remove
- Self-tile (D-07) gating works on Broadcast on/off
- All 3 tasks complete; visual checkpoint signed off
</success_criteria>

<output>
Create `.planning/phases/22-native-video-ui-grid-popouts/22-02-SUMMARY.md` when done. Include: files modified, build output, manual verification results for each of the 7 test cells (PASS / FAIL / DEFERRED), and any kBaseWidth bump applied (only if width-budget overlap was observed).
</output>
</content>
</invoke>