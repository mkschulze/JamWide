# Phase 15.1 — Continue Here (Wave 2 resume)

**Saved:** 2026-04-26
**Last commit:** `738ef99` (15.1-05 deferred-delete SUMMARY/STATE/ROADMAP)
**Build:** 243

## Status

**Wave 1 complete + Wave 2 plan 1 (15.1-05) complete.** 5 of 11 plans done.

| Wave | Plan | Status |
|------|------|--------|
| 1 | 15.1-02 atomic-promotion | ✓ done |
| 1 | 15.1-03 audio-path log removal | ✓ done |
| 1 | 15.1-04 SPSC infrastructure | ✓ done |
| 2 | 15.1-05 deferred-delete | ✓ done |
| 2 | 15.1-06 locchan-cs-snapshot | ▶ **NEXT** |
| 3 | 15.1-07a remote-user-mirror | pending |
| 3 | 15.1-07b buffer-queue | pending |
| 3 | 15.1-07c decode-media-buffer | pending |
| 4 | 15.1-09 codec-call-site-integration | pending |
| 4 | 15.1-08 prealloc-hardening | pending |
| 5 | 15.1-10 phase-verification | pending |

## What landed (last session)

**Audio-thread state, post-Wave-1 + 15.1-05:**

- `m_misc_cs` no longer acquired anywhere on audio path (15.1-02)
- `m_bpm` / `m_bpi` / `m_beatinfo_updated` / `m_interval_pos` are `std::atomic<>`; `m_beatinfo_updated` documented as edge-triggered best-effort per Codex L-10 (15.1-02)
- `writeLog` / `writeUserChanLog` / `JAMWIDE_DEV_BUILD` `fopen` block all removed from audio path (15.1-03)
- `src/threading/spsc_payloads.h` — final Wave-0 payload shapes for ALL downstream waves (DecodeArmRequest, LocalChannelAddedUpdate, PeerAddedUpdate, BlockRecord, etc.); Codex M-9 grep gate passes (15.1-04)
- `--tsan` build flag + `JAMWIDE_TSAN` CMake option live; SPSC primitive 10/10 tests pass under TSan with zero races (15.1-04)
- `MAX_BLOCK_SAMPLES = 2048` contract documented (enforcement lives in 15.1-08) (15.1-04)
- All 7 audit-cited `delete ds;` sites on audio path now publish to `m_deferred_delete_q`; `NinjamRunThread::run()` drains every 20ms; `GetDeferredDeleteOverflowCount()` accessor live for Codex M-8 (15.1-05)

**Audit findings closed so far:** CR-03, CR-04, CR-05, CR-06, CR-07, H-01, H-02, L-02, L-10. **9 of 21.**

## Next action — `/gsd-execute-phase 15.1`

The standard execute-phase command will resume at 15.1-06. To execute only the next plan: `/gsd-execute-phase 15.1 --wave 2`. To execute all remaining: bare `/gsd-execute-phase 15.1`.

## Critical Anti-Patterns

| Severity | Pattern | Why it matters |
|----------|---------|----------------|
| blocking | DO NOT introduce locks, heap allocations, or I/O on the audio path | The whole phase exists to remove these — re-adding any one negates the work. Use the `realtime-audio-reviewer` agent at `.claude/agents/realtime-audio-reviewer.md` to verify any audio-path code change. |
| blocking | DO NOT call `delete` on `DecodeState*` from any function reachable from `processBlock`/`AudioProc`/`process_samples`/`mixInChannel`/`on_new_interval` | 15.1-05 just converted all 7 audit-cited sites to deferred-delete. New code touching these functions must use the same pattern, not direct `delete`. |
| blocking | DO NOT introduce `lc_ptr` / `user_ptr` raw back-pointers in payload definitions | Codex HIGH-2 — payloads must NOT carry pointers back into run-thread-owned canonical objects. 15.1-04 has a grep gate that fails the build if this regresses. |
| blocking | DO NOT block the audio thread waiting for the run-thread to drain a queue | Use `tryPush` (non-blocking, returns false on full); increment overflow counter on failure; never spin or sleep on the audio thread. |

## Codex review concerns still ahead (per 15.1-REVIEWS.md)

- **HIGH-1** — `15.1-09` (Wave 4) carries the redesigned file-reader refill loop that closes H-04 in steady state. This is the single highest-risk plan; do not collapse its scope.
- **HIGH-2/HIGH-3** — `15.1-06` (NEXT) and `15.1-07a` (Wave 3) carry the `lc_ptr`/`user_ptr` elimination + deferred-free for `Local_Channel`/`RemoteUser`. Plan-checker confirmed the fixes are in the plan; execution is where they actually land.
- **HIGH-4** — `15.1-07` was split into 07a/07b/07c. Each has its own scope and dependency edges.
- **M-5/M-6/M-8** — verification gates (TSan automation, perf budget, drop-counter assertions) all live in `15.1-10` (Wave 5).

## Pre-existing issues (do NOT fix in this phase)

Tracked in `.planning/phases/15.1-rt-safety-hardening/deferred-items.md`:

- `tests/test_encryption.cpp` — pre-existing build break (`encrypt_payload_with_iv` undeclared)
- `tests/test_flac_codec` — pre-existing roundtrip failures
- Various clangd unused-include warnings (`njclient.h:71-77`, `njclient.cpp:40`, `spsc_payloads.h:30`, `test_spsc_state_updates.cpp:30,36`) — cleanup, low priority
- `NinjamRunThread.cpp:335,339,605,607` — pre-existing `-Wsign-conversion` warnings (not introduced by 15.1-05; surfaced because file recompiled)

## Build / Test commands

```bash
# Standard release build (x86_64 macOS, per project memory)
cd build-juce && ./scripts/build.sh

# TSan build for SPSC verification
./scripts/build.sh --tsan

# Run the new 15.1 test suite
cd build-test && ctest -R 'test_njclient_atomics|test_spsc_state_updates|test_deferred_delete' --output-on-failure
```

## Key files for resuming

- `.planning/phases/15.1-rt-safety-hardening/15.1-06-locchan-cs-snapshot-PLAN.md` — the next plan
- `.planning/phases/15.1-rt-safety-hardening/15.1-REVIEWS.md` — Codex's HIGH/MEDIUM concerns
- `.planning/phases/15.1-rt-safety-hardening/15.1-05-SUMMARY.md` — what just landed
- `src/threading/spsc_payloads.h` — Wave-0 payloads (don't extend; use as-is)
- `src/core/njclient.h` / `njclient.cpp` — current audio-thread state
