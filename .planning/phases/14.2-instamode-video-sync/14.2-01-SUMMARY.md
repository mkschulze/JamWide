---
phase: 14.2-instamode-video-sync
plan: 01
subsystem: audio, video
tags: [ninjam, instamode, latency-measurement, state-machine, ptt, websocket, video-sync]

# Dependency graph
requires:
  - phase: 12-video-sync-and-roster-discovery
    provides: VideoCompanion WebSocket server, broadcastBufferDelay, bufferDelay message format
  - phase: 11-video-companion-foundation
    provides: VideoCompanion class, SessionSnapshot, companion page infrastructure
provides:
  - NJClient measurement state machine (IDLE/INSTA_CAPTURED/MEASURED/CONSUMED)
  - Instatalk channel (ch 4, flags=0x02) with PTT silence callback
  - broadcastMeasuredDelay with syncMode JSON field
  - Three-tier delay priority (manual > measured > calculated)
  - RemoteChannelInfo.flags exposed to run thread
affects: [14.2-02, companion-page, video-sync]

# Tech tracking
tech-stack:
  added: []
  patterns: [state-machine-atomics, non-capturing-lambda-c-callback, remoteuser-pointer-identity]

key-files:
  created: []
  modified:
    - src/core/njclient.h
    - src/core/njclient.cpp
    - juce/JamWideJuceProcessor.h
    - juce/JamWideJuceProcessor.cpp
    - juce/NinjamRunThread.h
    - juce/NinjamRunThread.cpp
    - juce/video/VideoCompanion.h
    - juce/video/VideoCompanion.cpp

key-decisions:
  - "Measurement state machine lives entirely in NJClient (single owner, audio thread writes, run thread reads)"
  - "RemoteUser* pointer comparison for same-user identity (stable within m_users_cs lock scope)"
  - "Non-capturing lambda decays to C function pointer for SetLocalChannelProcessor"
  - "syncMode JSON field added to bufferDelay messages (measured vs calculated)"

patterns-established:
  - "State machine atomics: enum states + atomic<int> for cross-thread state machines in NJClient"
  - "PTT callback pattern: non-capturing lambda with void* inst pointer to JamWideJuceProcessor"
  - "Three-tier delay: manual > measured > calculated priority in VideoCompanion"

requirements-completed: [VID-13]

# Metrics
duration: 16min
completed: 2026-04-16
---

# Phase 14.2 Plan 01: Instamode Video Sync Summary

**NJClient state-machine latency measurement via Instatalk channel with PTT callback and VideoCompanion three-tier delay broadcast**

## Performance

- **Duration:** 16 min
- **Started:** 2026-04-16T12:40:05Z
- **Completed:** 2026-04-16T12:56:02Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Implemented 4-state measurement state machine (IDLE/INSTA_CAPTURED/MEASURED/CONSUMED) in NJClient with atomic cross-thread safety
- Created Instatalk channel (ch 4, flags=0x02, 64kbps) on normal connect with PTT silence/voice callback
- Added three-tier delay broadcast: broadcastMeasuredDelay (syncMode:"measured") overrides broadcastBufferDelay (syncMode:"calculated")
- Exposed RemoteChannelInfo.flags field for run thread to detect remote instamode channels
- Added pollInstamodeDelay run-thread method consuming measurement state machine and broadcasting to VideoCompanion

## Task Commits

Each task was committed atomically:

1. **Task 1: NJClient measurement state machine, RemoteChannelInfo.flags, processor PTT atomic** - `a81238d` (feat)
2. **Task 2: Instatalk channel setup, PTT callback, VideoCompanion three-tier delay, run-thread polling** - `0283a42` (feat)

## Files Created/Modified
- `src/core/njclient.h` - InstaMeasState enum, measurement atomics, resetInstaMeasurement(), RemoteChannelInfo.flags
- `src/core/njclient.cpp` - currentMillis() helper, t_insta hook in mixInChannel, t_interval hook in on_new_interval, flags in GetRemoteUsersSnapshot
- `juce/JamWideJuceProcessor.h` - pttActive and instaMeasurementBroadcast atomics, THREADING CONTRACT update
- `juce/JamWideJuceProcessor.cpp` - instaMeasurementBroadcast reset in releaseResources
- `juce/NinjamRunThread.h` - pollInstamodeDelay declaration
- `juce/NinjamRunThread.cpp` - Instatalk channel setup, PTT callback, disconnect/reconnect measurement reset, pollInstamodeDelay implementation, run loop integration
- `juce/video/VideoCompanion.h` - broadcastMeasuredDelay declaration, cachedSyncMode_ member, SessionSnapshot.cachedSyncMode
- `juce/video/VideoCompanion.cpp` - broadcastMeasuredDelay implementation, syncMode in broadcastBufferDelay and sendConfigToClient

## Decisions Made
- Measurement state consolidated in NJClient only (not split across Processor + NJClient) per review HIGH concern
- RemoteUser* pointer comparison used instead of user index for cross-timestamp matching per review HIGH concern
- t_interval hook placed at ds assignment line (not function entry) per review HIGH concern
- Re-measurement allowed on reconnect AND video activation per review MEDIUM concern
- Channel 4 is system channel not shown in local channel strip UI (ChannelStripArea only renders 0-3)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added #include <cstring> to NinjamRunThread.cpp**
- **Found during:** Task 2 (PTT callback implementation)
- **Issue:** memset used in PTT callback lambda but <cstring> not explicitly included
- **Fix:** Added `#include <cstring>` to NinjamRunThread.cpp includes
- **Files modified:** juce/NinjamRunThread.cpp
- **Verification:** Build compiles cleanly
- **Committed in:** 0283a42 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Minor include addition for correctness. No scope creep.

## Issues Encountered
- Pre-existing build failure in test_encryption.cpp (undeclared `encrypt_payload_with_iv`) -- unrelated to this plan, not addressed (out of scope)

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plugin-side measurement pipeline complete, ready for companion page integration (Plan 02)
- broadcastMeasuredDelay sends syncMode:"measured" JSON for companion to display
- Cached sync mode sent to new WS clients on connect for late-joining companions
- PTT button/key binding still needed (Claude's discretion per CONTEXT.md)

## Self-Check: PASSED

All 8 modified files verified present on disk. Both task commits (a81238d, 0283a42) verified in git log. SUMMARY.md created successfully. Build compiles cleanly for all plugin targets (VST3, AU, CLAP).

---
*Phase: 14.2-instamode-video-sync*
*Completed: 2026-04-16*
