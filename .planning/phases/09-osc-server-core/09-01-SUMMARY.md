---
phase: 09-osc-server-core
plan: 01
subsystem: osc
tags: [juce_osc, osc, udp, bidirectional, realtime, remote-control]

# Dependency graph
requires:
  - phase: 04-core-ui-panels
    provides: APVTS parameters, UiAtomicSnapshot, cmd_queue SPSC ring
provides:
  - OscServer bidirectional OSC engine with dirty-flag sender
  - OscAddressMap address-to-parameter mapping with 44 entries
  - juce_osc module linkage and loopback test
  - Echo suppression for OSC feedback prevention
  - Session telemetry and VU meter broadcast
affects: [09-02-osc-config-ui, touchosc-layouts]

# Tech tracking
tech-stack:
  added: [juce_osc]
  patterns: [RealtimeCallback-to-callAsync dispatch, dirty-flag bundle sending, echo suppression bitmask]

key-files:
  created:
    - juce/osc/OscServer.h
    - juce/osc/OscServer.cpp
    - juce/osc/OscAddressMap.h
    - juce/osc/OscAddressMap.cpp
    - tests/test_osc_loopback.cpp
  modified:
    - CMakeLists.txt

key-decisions:
  - "RealtimeCallback listener with callAsync dispatch preserves SPSC single-producer invariant"
  - "HashMap-based O(1) address resolution for 44 OSC address entries"
  - "Standard linear dB conversion (not VbFader curve) for OSC volume/db variants"
  - "OSC pan uses 0-1 range, converted to/from APVTS -1..1 internally"
  - "VU meters always sent every tick (100ms) since they change continuously"
  - "Solo state tracked via atomic bitmask for outgoing feedback"

patterns-established:
  - "OSC receive pattern: RealtimeCallback -> callAsync -> handleOscOnMessageThread"
  - "OSC send pattern: 100ms Timer -> dirty check -> bundle accumulation -> single send"
  - "Echo suppression: oscSourced flag set on receive, cleared on next send tick"

requirements-completed: [OSC-01, OSC-02, OSC-06, OSC-07]

# Metrics
duration: 19min
completed: 2026-04-06
---

# Phase 9 Plan 1: OSC Server Core Summary

**Bidirectional OSC server engine with juce_osc: 44 address mappings, dirty-flag bundle sender at 100ms, echo suppression, session telemetry, and VU meter broadcast**

## Performance

- **Duration:** 19 min
- **Started:** 2026-04-06T08:06:36Z
- **Completed:** 2026-04-06T08:25:19Z
- **Tasks:** 3
- **Files created/modified:** 6

## Accomplishments
- Linked juce_osc module and verified with loopback smoke test (send + receive on localhost)
- OscAddressMap covers 27 controllable + 17 read-only addresses with HashMap O(1) lookup
- OscServer implements full IEM pattern: RealtimeCallback receive, callAsync dispatch, 100ms Timer dirty-flag bundle send, echo suppression
- All APVTS params (master vol/mute, metro vol/mute, 4x local vol/pan/mute) controllable via OSC
- Metro pan writes to NJClient atomic directly (non-APVTS path)
- Local solo dispatches via cmd_queue with bitmask tracking for outgoing feedback
- Session telemetry (bpm, bpi, beat, status, users, codec, samplerate) broadcast from uiSnapshot atomics
- VU meters (master + 4 local channels) sent at 100ms rate

## Task Commits

Each task was committed atomically:

1. **Task 1: Build infrastructure -- link juce_osc and create loopback test** - `14fe276` (feat)
2. **Task 2: OscAddressMap -- address-to-parameter mapping with dual namespace** - `e89f2dc` (feat)
3. **Task 3: OscServer -- bidirectional OSC with dirty-flag sender, echo suppression, and telemetry** - `1bb6853` (feat)

## Files Created/Modified
- `juce/osc/OscAddressMap.h` - Address-to-parameter mapping class with OscParamType enum (68 lines)
- `juce/osc/OscAddressMap.cpp` - 44 address entries, HashMap lookup, dB/pan conversions (181 lines)
- `juce/osc/OscServer.h` - Bidirectional OSC server class declaration (84 lines)
- `juce/osc/OscServer.cpp` - Full server implementation: receive, dispatch, dirty send, telemetry, VU (491 lines)
- `tests/test_osc_loopback.cpp` - Loopback smoke test verifying juce_osc linkage (107 lines)
- `CMakeLists.txt` - Added juce::juce_osc link, OSC source files, test target

## Decisions Made
- Used `juce_add_console_app` for the OSC test target (provides proper JUCE header generation)
- Used `RealtimeCallback` in loopback test (avoids needing JUCE_MODAL_LOOPS_PERMITTED for message pump)
- Standard linear dB conversion (20*log10) rather than VbFader perceptual curve for OSC dB variants
- Pan conversion: OSC 0-1 mapped to APVTS -1..1 (industry standard)
- MAKE_NJ_FOURCC macro defined locally (matches existing codebase pattern in ui_local.cpp, ui_remote.cpp)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed test compilation: JuceHeader.h not available for plain add_executable**
- **Found during:** Task 1 (loopback test build)
- **Issue:** Plan specified `add_executable` for test, but JuceHeader.h is only generated by `juce_add_plugin`
- **Fix:** Used `juce_add_console_app` + `juce_generate_juce_header` inside the JUCE CMake section
- **Files modified:** CMakeLists.txt
- **Verification:** Build succeeds, test runs

**2. [Rule 3 - Blocking] Fixed test runtime: runDispatchLoopUntil requires JUCE_MODAL_LOOPS_PERMITTED**
- **Found during:** Task 1 (loopback test build)
- **Issue:** `runDispatchLoopUntil` is guarded by `#if JUCE_MODAL_LOOPS_PERMITTED`
- **Fix:** Switched listener to `RealtimeCallback` (calls on network thread, no message loop needed) and used `Thread::sleep` polling
- **Files modified:** tests/test_osc_loopback.cpp
- **Verification:** Test passes, value round-trips correctly

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary for test infrastructure. No scope creep. Core functionality unaffected.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- OscServer and OscAddressMap are ready for Plan 02 to wire into the processor
- Plan 02 will add: OscServer member to JamWideJuceProcessor, config dialog UI, state persistence (version 1->2), and start/stop lifecycle
- All 44 OSC addresses are defined and the server compiles clean against the processor API

## Self-Check: PASSED

- All 6 files exist on disk
- All 3 task commits found in git log
- All min_lines requirements met (OscServer.cpp: 491, OscAddressMap.cpp: 181, test: 107)

---
*Phase: 09-osc-server-core*
*Completed: 2026-04-06*
