---
phase: 21-h-264-decoder-receive-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - juce/video/distributor/JamWideRemoteFrameDistributor.h
  - juce/video/distributor/JamWideRemoteFrameDistributor.cpp
  - juce/video/distributor/PeerVideoSink.h
  - juce/video/distributor/PeerVideoSink.cpp
  - juce/JamWideJuceProcessor.h
  - juce/JamWideJuceProcessor.cpp
  - src/core/njclient.h
  - src/core/njclient.cpp
  - tests/test_remote_frame_distributor.cpp
  - tests/test_video_sync_e2e.cpp
  - tests/uat/phase-21-receive-uat-procedure.md
  - tests/uat/phase-21-receive-uat-report.md
  - CMakeLists.txt
autonomous: false
requirements:
  - WIRE-02
  - COD-03
user_setup:
  - service: "video.ninjamzap.com:2049 (public ninjamzap-server reference instance)"
    why: "UAT Cells 1-4 require a populated NINJAM room with another JamWide instance (or a NinjamZap mobile peer) broadcasting H.264 video. The community-operated public instance is the validated v1.3 beta reference."
    env_vars: []
    dashboard_config:
      - task: "Configure JamWide standalone's existing NINJAM server browser with host video.ninjamzap.com port 2049 (no preset entry needed; manual address entry is the supported v1.3 path per SRV-01)"
        location: "JamWide standalone Main view → Connect → Server browser → Manual entry"
      - task: "Coordinate a second JamWide instance OR an existing NinjamZap mobile peer to broadcast video into the same room for the 5+ minute UAT cells"
        location: "Out-of-band (Slack / SMS) with a beta-tester collaborator"

must_haves:
  truths:
    - "JamWideRemoteFrameDistributor service is constructed on JamWideJuceProcessor BEFORE the NJClient (parallel to the existing JamWideFrameDistributor camera-side; lifetime bound to the processor)"
    - "PeerVideoSink owns a double-buffered juce::Image (image_front + image_back) + atomic generation counter + AsyncUpdater + listener vector + atomic status fields (hold_count, decode_error_count, drop_resync_count, synced, first_frame_seen)"
    - "subscribeToPeer(username, chidx, onRepaint) returns a move-only RAII Subscription; ~Subscription detaches the listener and blocks until any in-flight handleAsyncUpdate dispatch returns (Phase 19 HIGH-2 mirror)"
    - "Multiple subscribers per peer (DISP-03 prep): two listeners on the same (username, chidx) both receive frame signals (grid tile + popout can coexist in Phase 22)"
    - "Lazy decoder + sink lifecycle: on first H264 BEGIN observed by RawData_Callback for a (username, chidx), NJClient creates VideoRecvState's decoder + PeerVideoSink + registers the sink with the distributor. Tear-down on user leave (m_remoteusers Delete site) joins decoder and removes sink."
    - "Audio-thread runVideoReceiveBlock_ pushes a VideoRecvBufferView onto vs->decoderSlotQ (Plan 21-02 wiring per CONTEXT.md D-12 revised — B-1 resolution); decoder thread's parseSlotAndFeed_ walks the slot bytes in-thread, feeds libavcodec, and triggers sink->triggerAsyncUpdate after sws_scale completes; UI thread reads image_front under brief bufferLock"
    - "Per-peer isolation: 3 standalone JamWide instances broadcasting + receiving simultaneously, one peer's decode error does not affect the other two (success criterion 4)"
    - "tests/test_remote_frame_distributor.cpp covers Subscription RAII (~Subscription blocks in-flight) + multi-listener fan-out + atomic generation semantics"
    - "tests/test_video_sync_e2e.cpp ties Plan 21-01 + Plan 21-02 + Plan 21-03 together: drives the full pipeline in-process via the test dispatchers, asserts per-peer isolation via three_peers_isolated_decode_errors"
    - "tests/uat/phase-21-receive-uat-procedure.md documents 4 UAT cells (one per ROADMAP success criterion); tests/uat/phase-21-receive-uat-report.md is populated by the UAT checkpoint task at runtime"
  artifacts:
    - path: "juce/video/distributor/JamWideRemoteFrameDistributor.h"
      provides: "JamWideRemoteFrameDistributor class declaration + Subscription RAII handle (move-only, ~Subscription calls unsubscribe_ which blocks in-flight)"
      contains: "class JamWideRemoteFrameDistributor"
    - path: "juce/video/distributor/JamWideRemoteFrameDistributor.cpp"
      provides: "Implementation: unordered_map<string key, unique_ptr<PeerVideoSink>>; subscribeToPeer; findOrCreateSink; removeSink; deferred-listener support for subscribe-before-peer-exists race (RESEARCH §Open Question 2)"
      contains: "JamWideRemoteFrameDistributor::subscribeToPeer"
    - path: "juce/video/distributor/PeerVideoSink.h"
      provides: "PeerVideoSink class: juce::AsyncUpdater subclass, double-buffered image, atomic generation, listener vector + addListener/removeListener, atomic status fields (D-20)"
      contains: "class PeerVideoSink"
    - path: "juce/video/distributor/PeerVideoSink.cpp"
      provides: "Implementation: handleAsyncUpdate iterates listeners under brief lock; addListener/removeListener thread-safe; image_front double-buffer ctor at (width, height)"
      contains: "PeerVideoSink::handleAsyncUpdate"
    - path: "juce/JamWideJuceProcessor.h"
      provides: "std::unique_ptr<JamWideRemoteFrameDistributor> remoteFrameDistributor member + JamWideRemoteFrameDistributor* getRemoteFrameDistributor() accessor; lifetime parallel to existing frameDistributor (line 132)"
      contains: "remoteFrameDistributor"
    - path: "juce/JamWideJuceProcessor.cpp"
      provides: "Constructor body: remoteFrameDistributor = make_unique<...> BEFORE NJClient construction (NJClient receives a non-owning pointer for the lazy lifecycle hooks); destructor: distributor destroyed AFTER NJClient (so NJClient's m_video_streams destructor can call removeSink without dangling-pointer)"
      contains: "remoteFrameDistributor = std::make_unique"
    - path: "src/core/njclient.h"
      provides: "Setter NJClient::SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor*) wired at JamWideJuceProcessor ctor; lazy-lifecycle hook NJClient::ensureVideoDecoderForPeer_(VideoRecvState* vs, int width, int height) private helper that spins up decoder + sink + distributor registration on first BEGIN for the peer"
      contains: "SetRemoteFrameDistributor"
    - path: "src/core/njclient.cpp"
      provides: "BEGIN handler (Plan 21-01 territory): after findOrCreateVideoStream, call ensureVideoDecoderForPeer_ to lazy-spin-up; user-leave (Plan 21-01 territory): call distributor->removeSink before resetting VideoRecvState"
      contains: "ensureVideoDecoderForPeer_"
    - path: "tests/test_remote_frame_distributor.cpp"
      provides: "Unit tests: two_listeners_same_peer_both_called + subscription_dtor_blocks_in_flight + atomic_generation_lock_free_read"
      contains: "test_two_listeners_same_peer_both_called"
    - path: "tests/test_video_sync_e2e.cpp"
      provides: "Integration test: three_peers_isolated_decode_errors (drives 3 simultaneous fake-peer broadcasts via DispatchTest* helpers; one peer gets corrupt NALs; asserts only that peer's decode_error_count climbs)"
      contains: "test_three_peers_isolated_decode_errors"
    - path: "tests/uat/phase-21-receive-uat-procedure.md"
      provides: "4 UAT cells mapping 1:1 to ROADMAP success criteria 1-4 (wall-clock alignment, mid-session join ≤2 intervals, hold→freeze→recover, 3-peer per-peer isolation); each cell has reproducible setup steps + acceptance criteria + counter readout commands"
      contains: "Cell 1: Wall-clock audio-video alignment"
    - path: "tests/uat/phase-21-receive-uat-report.md"
      provides: "Initial empty report template; populated during the Plan 21-03 checkpoint:human-verify task with PASS/FAIL/blocked results + diagnostic counter values"
      contains: "Phase 21 receive UAT report"
    - path: "CMakeLists.txt"
      provides: "JamWideJuce target gains juce/video/distributor/*.cpp + JamWideRemoteFrameDistributor.h + PeerVideoSink.h sources; test_remote_frame_distributor + test_video_sync_e2e add_executable + add_test entries with jamwide_use_ffmpeg linkage"
      contains: "test_remote_frame_distributor"
  key_links:
    - from: "JamWideJuceProcessor ctor"
      to: "remoteFrameDistributor = std::make_unique<JamWideRemoteFrameDistributor>(); then m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get())"
      via: "construction order: distributor BEFORE njClient (since njClient's lazy lifecycle hooks reference it)"
      pattern: "SetRemoteFrameDistributor"
    - from: "BEGIN handler (Plan 21-01 augmentation site)"
      to: "ensureVideoDecoderForPeer_(vs, default_width=320, default_height=240)"
      via: "called once per first BEGIN per peer; idempotent (early-return if vs->decoder != nullptr)"
      pattern: "ensureVideoDecoderForPeer_"
    - from: "User-leave site (Plan 21-01 user-leave reset block)"
      to: "distributor->removeSink(theuser->name, video_chidx)"
      via: "called BEFORE the existing VideoRecvState slot resets; releases distributor's sink ownership and detaches any live subscriptions"
      pattern: "removeSink"
    - from: "Decoder thread scaleAndSwapImage_"
      to: "PeerVideoSink::triggerAsyncUpdate → handleAsyncUpdate → onRepaint callbacks"
      via: "sink_->bufferLock briefly held for image swap + generation bump; then triggerAsyncUpdate dispatches on message thread"
      pattern: "triggerAsyncUpdate"
---

<objective>
Complete Phase 21 by:
1. Landing `JamWideRemoteFrameDistributor` (symmetric inverse of Phase 19's `JamWideFrameDistributor`) and `PeerVideoSink` (double-buffered juce::Image + atomic-generation + AsyncUpdater + listener vector + atomic status fields), constructed on JamWideJuceProcessor parallel to the existing camera-side distributor
2. Wiring the lazy decoder+sink lifecycle: on first H264 BEGIN per peer, NJClient creates the decoder thread + PeerVideoSink + registers the sink with the distributor; on user leave, NJClient joins the decoder + removes the sink
3. Validating via 2 unit-test sub-tests + 1 integration test (per-peer isolation under 3 fake peers) + a 4-cell UAT against `video.ninjamzap.com:2049` proving the 4 ROADMAP success criteria

Purpose: Phase 21 closure — Phase 22 tiles need a `PeerVideoSink*` per peer with a stable `juce::Image` they can paint() lock-free, hold_count/first_frame_seen/synced atomics they can read each repaint, and a Subscription RAII handle they can hold as a member. This plan ships exactly that surface. The UAT cells prove the ROADMAP success criteria are met against the canonical reference server.

Output: Phase 22-ready PeerVideoSink + distributor surface + green tests + populated UAT report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-VALIDATION.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-01-PLAN.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-02-PLAN.md

<interfaces>
<!-- Phase 19 distributor — Plan 21-03 mirrors its shape inverse. -->

From `juce/video/native/JamWideFrameDistributor.h` (Phase 19 send-side distributor):
```cpp
// CAMERA-SIDE: publish(juce::Image) from camera-callback thread; subscribers
// onFrame(const juce::Image&). One subscriber-list, blanket-fanout, no per-key
// routing — every subscriber sees every frame.

class JamWideFrameDistributor {
public:
    class Subscriber { ... };          // interface
    class Subscription { ... };        // move-only RAII; ~Subscription calls unregisterAndWait
    Subscription registerSubscriber(Subscriber* s);
    void publish(const juce::Image& image);    // camera-thread caller
    float getPeakFps() const noexcept;
private:
    void unregisterAndWait(std::uint64_t id) noexcept;
    // mutex + cv + map of id → shared_ptr<Entry{Subscriber*, inFlight atomic}>
};
```

**Phase 21 INVERSE shape:**
- Multiple sinks (one per peer); each sink has its own listener list (DISP-03 grid-and-popout coexist)
- Lookup by (username, chidx) string key, not blanket fanout
- Subscription holds the key + id; ~Subscription removes the listener from THAT sink only

From `juce/JamWideJuceProcessor.h` (existing):
```cpp
// line 17:
#include "video/native/JamWideFrameDistributor.h"
// line 132:
std::unique_ptr<jamwide::JamWideFrameDistributor> frameDistributor;
// line 170:
jamwide::JamWideFrameDistributor* getFrameDistributor() { return frameDistributor.get(); }
```

From `juce/JamWideJuceProcessor.cpp` (existing):
```cpp
// line 66:
frameDistributor = std::make_unique<jamwide::JamWideFrameDistributor>();
```

Plan 21-03 adds parallel `remoteFrameDistributor` member + `getRemoteFrameDistributor()` accessor + ctor make_unique line, both touching the existing processor.

From Plan 21-02 `juce/video/decoder/Openh264Decoder.h`:
```cpp
class Openh264Decoder {
    void setSink(jamwide::PeerVideoSink* sink);    // called after construction by Plan 21-03 lifecycle helper
    ...
};
```
</interfaces>

<verbatim_port_sources>
- /Users/cell/dev/JamWide/juce/video/native/JamWideFrameDistributor.h + .cpp (entire) — Phase 21-03 Subscription / Entry / unregisterAndWait pattern verbatim mirror
- /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 626-732 — Pattern 3 PeerVideoSink + Pattern 4 JamWideRemoteFrameDistributor; lines 1132-1156 — Code Example 4 subscribeToPeer with deferred-listener handling)
- /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-01..D-08 sink shape; D-17..D-20 atomic status fields)
- /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 1212-1232 — Open Question 2 (subscribe-before-peer-exists race) and 3 (sink dimension policy) — implement per the RESEARCH recommendation: defer the listener-add until run-thread BEGIN handler creates the sink)
</verbatim_port_sources>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PeerVideoSink + JamWideRemoteFrameDistributor + JamWideJuceProcessor wiring</name>
  <files>juce/video/distributor/JamWideRemoteFrameDistributor.h, juce/video/distributor/JamWideRemoteFrameDistributor.cpp, juce/video/distributor/PeerVideoSink.h, juce/video/distributor/PeerVideoSink.cpp, juce/JamWideJuceProcessor.h, juce/JamWideJuceProcessor.cpp, CMakeLists.txt</files>
  <read_first>
    - /Users/cell/dev/JamWide/juce/video/native/JamWideFrameDistributor.h (entire — Subscription RAII pattern template)
    - /Users/cell/dev/JamWide/juce/video/native/JamWideFrameDistributor.cpp (entire — unregisterAndWait blocking-on-inFlight implementation template)
    - /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.h (entire — existing frameDistributor member at line 132; Plan 21-03 adds parallel remoteFrameDistributor member)
    - /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.cpp (lines 50-100 — existing ctor body where frameDistributor is constructed at line 66; existing dtor body)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 626-732 — Patterns 3 + 4; lines 1132-1156 — subscribe-before-peer-exists deferred-listener pattern)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-01..D-08 sink shape, D-17..D-20 status fields)
    - /Users/cell/dev/JamWide/juce/video/decoder/Openh264Decoder.h (Plan 21-02 — declared setSink slot needs definition here)
  </read_first>
  <behavior>
    - `juce/video/distributor/PeerVideoSink.h` declares `class PeerVideoSink : public juce::AsyncUpdater { ... };` with:
      - Constructor `PeerVideoSink(int width, int height)` initializes `image_front(juce::Image::ARGB, width, height, true)` and `image_back(...)` similarly (cleared on construction so the placeholder paint shows transparent until first frame arrives per D-19)
      - Public members: `juce::Image image_front; juce::Image image_back; juce::CriticalSection bufferLock; std::atomic<std::uint64_t> generation{0};`
      - Atomic status fields per D-20: `std::atomic<int> hold_count{0}, decode_error_count{0}, drop_resync_count{0}; std::atomic<bool> synced{false}, first_frame_seen{false};`
      - Listener API: `std::uint64_t addListener(std::function<void()> cb)` returns a new listener id; `void removeListener(std::uint64_t id)` removes and blocks until any in-flight handleAsyncUpdate dispatch returns
      - `void handleAsyncUpdate() override` iterates listeners under a brief lock (snapshot the listener pointers + inFlight bump, release lock, fan out, decrement inFlight, signal cv)
      - Private members: `juce::CriticalSection listenerLock_; std::unordered_map<std::uint64_t, std::shared_ptr<ListenerEntry>> listeners_; std::uint64_t nextId_{1}; std::condition_variable_any listenerCv_;` (or juce::SpinLock + AbstractFifo if existing JamWide style — match Phase 19 distributor's `std::mutex regMu_; std::condition_variable cv_;` exactly to keep the audit story trivial)
    - `juce/video/distributor/JamWideRemoteFrameDistributor.h` declares the distributor + Subscription per Pattern 4 from RESEARCH:
      - `class Subscription { ... };` move-only, holds a back-pointer + key + id, ~Subscription calls `dist_->unsubscribe_(key_, id_)` which blocks any in-flight handleAsyncUpdate for that sink
      - Public API: `[[nodiscard]] Subscription subscribeToPeer(const std::string& username, int chidx, std::function<void()> onRepaint);`
      - Public lifecycle: `PeerVideoSink* findOrCreateSink(const std::string& username, int chidx, int width, int height);` (called by NJClient run-thread on first BEGIN per peer); `void removeSink(const std::string& username, int chidx);` (called by NJClient run-thread on user-leave); `PeerVideoSink* findSink(const std::string& username, int chidx) noexcept;` (called by Phase 22 tile if it wants to bind without a Subscription — unusual; mostly the Subscription path is used)
      - Private: `juce::CriticalSection mu_; std::unordered_map<std::string, std::unique_ptr<PeerVideoSink>> sinks_; std::unordered_map<std::string, std::vector<std::pair<std::uint64_t, std::function<void()>>>> deferredListeners_;` (the deferred-listener pattern per RESEARCH §Open Question 2)
    - `juce/video/distributor/JamWideRemoteFrameDistributor.cpp` implements:
      - `subscribeToPeer(username, chidx, onRepaint)`: take mu_; look up sink by key; if exists, call sink->addListener; if not, push (id, onRepaint) into deferredListeners_[key]; return Subscription wrapping {this, key, id}
      - `findOrCreateSink(username, chidx, width, height)`: take mu_; look up key; if not present, construct unique_ptr<PeerVideoSink>(width, height), insert; flush deferredListeners_[key] onto sink->addListener; return raw pointer
      - `removeSink(username, chidx)`: take mu_; look up key; if present, the unique_ptr destructor handles sink teardown (handleAsyncUpdate completion is the sink's own responsibility via its inFlight gate); erase from sinks_ map; do NOT erase from deferredListeners_ (a later subscribeToPeer call may re-defer; but the same Subscription instance still has the same id — it's a no-op when the sink is removed). Wait — that creates a memory leak in deferredListeners_ if a user joins, leaves, never rejoins. Acceptable for v1.3; document.
      - `unsubscribe_(key, id)`: take mu_; look up sink by key; if present, sink->removeListener(id) (blocks if in-flight); else look up deferredListeners_[key], remove the (id, cb) entry if present (no in-flight to block — sink never existed)
    - `juce/JamWideJuceProcessor.h` gains:
      - `#include "video/distributor/JamWideRemoteFrameDistributor.h"` after the existing line 17 `#include "video/native/JamWideFrameDistributor.h"`
      - `std::unique_ptr<jamwide::JamWideRemoteFrameDistributor> remoteFrameDistributor;` immediately after the existing `frameDistributor` member (line 132)
      - `jamwide::JamWideRemoteFrameDistributor* getRemoteFrameDistributor() { return remoteFrameDistributor.get(); }` immediately after the existing `getFrameDistributor()` accessor (line 170)
    - `juce/JamWideJuceProcessor.cpp` ctor body adds `remoteFrameDistributor = std::make_unique<jamwide::JamWideRemoteFrameDistributor>();` immediately after the existing `frameDistributor = std::make_unique<...>();` (line 66). After NJClient is constructed (search ctor for `m_njClient.` use), add `m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get());` to hand the pointer to NJClient (Task 2 lands the setter).
    - `juce/JamWideJuceProcessor.cpp` dtor: lifetime resolution per W-1 (member-ordering decision now PINNED — no executor discretion):
      - **Read confirmed at planning time:** `juce/JamWideJuceProcessor.h:132` declares `std::unique_ptr<jamwide::JamWideFrameDistributor> frameDistributor;` in the PUBLIC section. `juce/JamWideJuceProcessor.h:346` declares `std::unique_ptr<NJClient> client;` in the PRIVATE section. Member destruction order is LIFO based on declaration order → `client` (later) is destroyed BEFORE `frameDistributor` (earlier) under implicit destruction. The explicit dtor at `juce/JamWideJuceProcessor.cpp:74-110` REVERSES this with `frameDistributor.reset()` at line 104 BEFORE `client.reset()` at line 109.
      - **Placement chosen:** declare `std::unique_ptr<jamwide::JamWideRemoteFrameDistributor> remoteFrameDistributor;` IMMEDIATELY AFTER `frameDistributor` at line 132 (still PUBLIC section, alongside the camera-side distributor symmetrically). This makes the camera-side and peer-side distributors structurally parallel, and source-grep for `frameDistributor` continues to surface the closely-related field.
      - **Explicit dtor sequencing (mirror the existing `frameDistributor.reset()` call at JamWideJuceProcessor.cpp:104):** add `remoteFrameDistributor.reset();` IMMEDIATELY AFTER `frameDistributor.reset();` at line 104, and BEFORE `client.reset();` at line 109. Lifecycle invariant: when `client.reset()` fires NJClient::~NJClient → m_video_streams.Empty(true) → VideoRecvState::~VideoRecvState → `decoder->close()`, the decoder dtor MUST NOT call into the (already-destroyed) distributor. Plan 21-02 Task 2's VideoRecvState dtor only calls `decoder->close()` (which joins the thread and frees ffmpeg context); the decoder destructor does NOT call sink_->triggerAsyncUpdate (the run() loop has already exited by then) and does NOT call distributor->removeSink (Plan 21-03's user-leave path is the only caller of removeSink, and that path runs from NJClient::Run() on the run thread, not from NJClient::~NJClient). Therefore `remoteFrameDistributor.reset()` BEFORE `client.reset()` is safe.
      - **W-1 rationale (line-number cited):** NJClient is a `std::unique_ptr<NJClient> client` PRIVATE MEMBER at JamWideJuceProcessor.h:346 (NOT an inherited base class — JamWideJuceProcessor inherits from `juce::AudioProcessor` and `jamwide::VideoEncoderListener` only, per JamWideJuceProcessor.h:61-63). LIFO destruction of members would tear down `client` BEFORE `remoteFrameDistributor`, but the explicit dtor at JamWideJuceProcessor.cpp:74-110 already establishes the convention of explicit reset() calls in the desired safe order. We follow that convention.
      - **JamWideFrameDistributor lifetime model copied verbatim:** Phase 19's camera-side distributor has the exact same shape (declared at JamWideJuceProcessor.h:132, reset at JamWideJuceProcessor.cpp:104, before client.reset() at :109). Phase 21's peer-side distributor is symmetric. This matches the W-1 instruction to "copy its lifetime model exactly".
    - CMakeLists.txt: add the new `juce/video/distributor/*.cpp` + headers to JamWideJuce target_sources. New directory; mirror the encoder/ source block (line 251-254).
  </behavior>
  <action>
    Per D-01..D-08, D-17..D-20, Patterns 3+4 from RESEARCH:

    1. **Create `juce/video/distributor/PeerVideoSink.h`:** ~80 LOC. Inherits `juce::AsyncUpdater`. Header includes `<juce_events/juce_events.h>` (for AsyncUpdater), `<juce_graphics/juce_graphics.h>` (for juce::Image), `<juce_core/juce_core.h>` (for CriticalSection), `<atomic>`, `<functional>`, `<unordered_map>`, `<memory>`. Class declaration as above. Public domain comment block referencing D-01..D-08 and D-17..D-20.

    2. **Create `juce/video/distributor/PeerVideoSink.cpp`:** ~80 LOC implementation. handleAsyncUpdate body iterates listeners with inFlight tracking exactly mirroring Phase 19 JamWideFrameDistributor.cpp's `publish` method (snapshot under lock, bump inFlight, release lock, fan-out outside lock, decrement inFlight, signal cv). addListener / removeListener bodies the same shape as Phase 19's registerSubscriber / unregisterAndWait.

    3. **Create `juce/video/distributor/JamWideRemoteFrameDistributor.h`:** ~80 LOC. Forward-decl PeerVideoSink. Class declaration as per Pattern 4 + Behavior section. Move-only Subscription nested class.

    4. **Create `juce/video/distributor/JamWideRemoteFrameDistributor.cpp`:** ~120 LOC. Implement the 4 lifecycle methods + Subscription dtor. The deferredListeners_ pattern handles RESEARCH §Open Question 2 (subscribe-before-peer-exists).

    5. **Update `juce/JamWideJuceProcessor.h`:**
       - Add `#include "video/distributor/JamWideRemoteFrameDistributor.h"` after line 17
       - Add `std::unique_ptr<jamwide::JamWideRemoteFrameDistributor> remoteFrameDistributor;` member IMMEDIATELY AFTER the existing `frameDistributor` declaration at JamWideJuceProcessor.h:132 (in the PUBLIC section, alongside the camera-side distributor). **W-1 member ordering RESOLVED — no executor discretion:** the camera-side `frameDistributor` is the precedent — copy its lifetime model EXACTLY. The relevant facts:
         - `frameDistributor` is at JamWideJuceProcessor.h:132 (public section)
         - `client` (std::unique_ptr<NJClient>) is at JamWideJuceProcessor.h:346 (private section)
         - The explicit dtor at JamWideJuceProcessor.cpp:104 already calls `frameDistributor.reset()` BEFORE `client.reset()` at :109
         - JamWideJuceProcessor inherits from `juce::AudioProcessor` and `jamwide::VideoEncoderListener` (JamWideJuceProcessor.h:61-63) — NJClient is NOT an inherited base class; it is a private std::unique_ptr member
         - VideoRecvState::~VideoRecvState (Plan 21-02 Task 2) only calls `decoder->close()`; the decoder destructor does NOT transitively call distributor methods, so `remoteFrameDistributor.reset()` BEFORE `client.reset()` is safe
         The new declaration goes at JamWideJuceProcessor.h:133 (one line below the existing frameDistributor at :132). The new explicit reset() call goes at JamWideJuceProcessor.cpp:104.5 (immediately after the existing `frameDistributor.reset();` at line 104, before `videoCompanion.reset();` at line 106).
       - Add `jamwide::JamWideRemoteFrameDistributor* getRemoteFrameDistributor() { return remoteFrameDistributor.get(); }` accessor at line 170 alongside getFrameDistributor.

    6. **Update `juce/JamWideJuceProcessor.cpp`:** ctor at line 66 adds `remoteFrameDistributor = std::make_unique<jamwide::JamWideRemoteFrameDistributor>();`. After this line, search for where the existing camera `frameDistributor` is given to NJClient (or where the NJClient is constructed) and add the parallel `m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get());` call. **NJClient::SetRemoteFrameDistributor is added in Task 2** — for now, in Task 1, we can just leave a TODO comment and add the call once Task 2 lands the setter. Recommended: do this Task 1 + Task 2 together at the .cpp level since the SetRemoteFrameDistributor reference will fail to compile otherwise. Practical: declare the SetRemoteFrameDistributor stub in njclient.h in Task 1 too (with an empty body), then Task 2 fills the body. Or: gate Task 1's call site with the setter declared in Task 1 but bodied in Task 2.

       To keep the build green at Task 1 end: in this Task 1, add the SetRemoteFrameDistributor declaration to njclient.h with a no-op inline body (`void SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor*) {}`). Task 2 replaces the inline body with the real one. This keeps Tasks 1 + 2 cleanly separated.

    7. **CMakeLists.txt:** After the existing Phase 20-01 encoder block (line 251-254), add:
       ```cmake
       # Plan 21-03 Task 1: JamWideRemoteFrameDistributor + PeerVideoSink (symmetric inverse of Phase 19's camera-side distributor).
       target_sources(JamWideJuce PRIVATE
           juce/video/distributor/JamWideRemoteFrameDistributor.h
           juce/video/distributor/JamWideRemoteFrameDistributor.cpp
           juce/video/distributor/PeerVideoSink.h
           juce/video/distributor/PeerVideoSink.cpp
       )
       ```

    8. **Verification:** `./scripts/build.sh JamWideJuce_Standalone` builds cleanly. Existing test suite green: `cd build-juce && ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder"` exits 0.
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh JamWideJuce_Standalone test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state test_video_decoder &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder"</automated>
    <grep>test -f /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h &amp;&amp; test -f /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.cpp &amp;&amp; test -f /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h &amp;&amp; test -f /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.cpp</grep>
    <grep>grep -c "remoteFrameDistributor\|getRemoteFrameDistributor" /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.h /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.cpp</grep>
    <grep>grep -c "class PeerVideoSink\|class JamWideRemoteFrameDistributor" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h</grep>
  </verify>
  <acceptance_criteria>
    - All 4 new files exist in `juce/video/distributor/`
    - `grep -q "class PeerVideoSink.*public juce::AsyncUpdater" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h` (inheritance per D-02)
    - `grep -q "juce::Image image_front" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h` AND `grep -q "juce::Image image_back" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h` (D-08 double-buffer)
    - `grep -q "std::atomic&lt;.*&gt; generation" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h` (D-01 atomic generation)
    - `grep -q "first_frame_seen\|hold_count\|decode_error_count\|drop_resync_count\|synced" /Users/cell/dev/JamWide/juce/video/distributor/PeerVideoSink.h` (D-20 atomic status fields)
    - `grep -q "class Subscription" /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h` (D-03 RAII handle)
    - `grep -q "subscribeToPeer\|findOrCreateSink\|removeSink" /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h` (D-05/D-06 API)
    - `grep -q "deferredListeners_" /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.cpp` (RESEARCH §Open Question 2 — defer pattern)
    - `grep -q "remoteFrameDistributor" /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.h` (processor member added)
    - `grep -q "remoteFrameDistributor = std::make_unique" /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.cpp` (ctor wiring)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder"` exits 0 (no regression)
    - JamWideJuce_Standalone builds under -Werror
  </acceptance_criteria>
  <done>PeerVideoSink + JamWideRemoteFrameDistributor land in the new juce/video/distributor/ directory. Symmetric inverse of Phase 19 camera-side distributor: same Subscription RAII pattern + same inFlight blocking dtor. JamWideJuceProcessor owns the distributor as a heap dependency, lifetime parallel to camera distributor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire lazy decoder + sink lifecycle (NJClient::SetRemoteFrameDistributor + ensureVideoDecoderForPeer_ + user-leave removeSink)</name>
  <files>src/core/njclient.h, src/core/njclient.cpp, juce/video/decoder/Openh264Decoder.h, juce/video/decoder/Openh264Decoder.cpp</files>
  <read_first>
    - /Users/cell/dev/JamWide/src/core/njclient.h (after Plan 21-01 + 21-02 — VideoRecvState has decoder/decoderSlotQ/sink fields per CONTEXT.md D-12 revised; SetRemoteFrameDistributor was declared as inline no-op in Plan 21-03 Task 1)
    - /Users/cell/dev/JamWide/src/core/njclient.cpp (Plan 21-01 BEGIN handler at line ~2360 + user-leave site + Plan 21-02 vs->decoder->pushSlotView(makeSlotView_(...)) call inside runVideoReceiveBlock_ per CONTEXT.md D-12 revised — parser is on the decoder thread, NOT on the audio thread)
    - /Users/cell/dev/JamWide/juce/video/decoder/Openh264Decoder.h + .cpp (Plan 21-02 — setSink method already declared; verify the body either stores the pointer or simply records it for scaleAndSwapImage_ to read)
    - /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h (Task 1 — findOrCreateSink / removeSink API)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-10 lazy lifecycle — "spin-up on first H264 BEGIN per peer; tear-down on peer leave")
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 1212-1232 — Open Question 2 / 3 — RESEARCH recommendation: sink dims = first-seen peer resolution; no upscaling on later resolution change; sink dims default to 320×240 for Plan 21 v1 since we don't know peer's first-frame resolution at BEGIN — verify via Plan 21-02 sws_ behavior)
  </read_first>
  <behavior>
    - `NJClient::SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor*)` stores the pointer in a new `m_remote_frame_distributor` member; can be set once at JamWideJuceProcessor ctor. Replaces the Task 1 no-op inline body.
    - `NJClient::ensureVideoDecoderForPeer_(VideoRecvState* vs, int width, int height)` private helper: idempotent (early-return if vs->decoder != nullptr). Otherwise:
      1. Call `m_remote_frame_distributor->findOrCreateSink(vs->stream_username, vs->stream_chidx, width, height)` to get the PeerVideoSink* (this also flushes any deferred listeners onto the sink)
      2. Assign `vs->sink = sinkPtr`
      3. Construct `vs->decoder = std::make_unique<jamwide::Openh264Decoder>(vs->decoderSlotQ);` (decoder constructor takes the slot-view SpscRing by reference per CONTEXT.md D-12 revised; sink is set via setSink in step 5 below)
      4. Call `vs->decoder->open(width, height)`; on failure log via `juce::Logger::writeToLog` (Plan 21 D-Discretion logging surface — open/close events) AND null out vs->decoder (so the lazy-lifecycle null-guard in runVideoReceiveBlock_ stays effective)
      5. Optionally also call `vs->decoder->setSink(vs->sink)` if the ctor signature didn't take a sink; pick one or the other consistently
    - BEGIN handler augmentation (extending Plan 21-01 Task 2's existing block at `src/core/njclient.cpp:2360` BEGIN handler): after `findOrCreateVideoStream` populates vs, call `ensureVideoDecoderForPeer_(vs, 320, 240)` to spin up the decoder + sink. 320×240 is the v1.3 baseline (Phase 19 default capture preset is Medium = 640×480 but Phase 20 default IDR rate of 10fps at 320×240 ~98 kbps is the validated baseline; sink dims at 320×240 lets sws_scale downscale Higher peers and stay sharp). RESEARCH §Open Question 3 confirms: no upscaling (Phase 22 paint draws image at target rect, that's where upscale-for-display happens).
    - User-leave (extending Plan 21-01 Task 2's user-leave reset block): BEFORE the existing `vs->next.reset(); vs->pending.reset(); vs->accumulating.reset();` calls, also call `if (m_remote_frame_distributor) m_remote_frame_distributor->removeSink(theuser->name.Get(), vs->stream_chidx);` to release the sink ownership. After Plan 21-02's vs->decoder destructor (via VideoRecvState dtor when m_video_streams.Empty(true) runs) the decoder is joined; the sink's listeners are detached as `~Subscription` calls fire from Phase 22 tiles being torn down. Order: removeSink first (distributor drops shared ownership), then vs->next/pending/accumulating reset.
    - Decoder construction signature: Plan 21-02 Task 1 declared `Openh264Decoder(SpscRing<VideoRecvBufferView, 4>& slotInputQ)` + `setSink(PeerVideoSink*)` per CONTEXT.md D-12 revised. Task 2 (this) constructs with the slot-view queue ref only, then calls setSink(sinkPtr) after construction. This keeps test mode (sink = nullptr until setSink is called) trivially working — test_video_decoder.cpp never calls setSink and exercises the test_capture_image_ path.
    - JamWideJuceProcessor.cpp ctor: replace the Task 1 TODO with the real `m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get())` call.
  </behavior>
  <action>
    Per D-10 lazy lifecycle:

    1. **Update `src/core/njclient.h`:**
       - Add private member `jamwide::JamWideRemoteFrameDistributor* m_remote_frame_distributor = nullptr;` alongside the existing distributor-related members (search for `JamWideFrameDistributor` — there is no member, just a forward decl in the include; place the new member near other run-thread members like `m_rawdata_downloads`)
       - Add forward decl `namespace jamwide { class JamWideRemoteFrameDistributor; }` near the top of njclient.h (after the existing forward decls)
       - Replace the Task 1 inline no-op `void SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor*) {}` with a declaration: `void SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor* d) noexcept;`
       - Add private declaration `void ensureVideoDecoderForPeer_(VideoRecvState* vs, int width, int height);`

    2. **Update `src/core/njclient.cpp`:**
       - Implement `void NJClient::SetRemoteFrameDistributor(jamwide::JamWideRemoteFrameDistributor* d) noexcept { m_remote_frame_distributor = d; }` (single line; the pointer is non-owning, just a hand-off)
       - Implement `void NJClient::ensureVideoDecoderForPeer_(VideoRecvState* vs, int width, int height)`:
         ```cpp
         if (vs->decoder) return;     // idempotent
         if (!m_remote_frame_distributor) {
             juce::Logger::writeToLog("Plan 21-03: lazy decoder skipped — no distributor wired");
             return;
         }
         jamwide::PeerVideoSink* sinkPtr = m_remote_frame_distributor->findOrCreateSink(
             vs->stream_username, vs->stream_chidx, width, height);
         if (!sinkPtr) return;
         vs->sink = sinkPtr;
         vs->decoder = std::make_unique<jamwide::Openh264Decoder>(vs->decoderSlotQ);
         vs->decoder->setSink(sinkPtr);
         if (!vs->decoder->open(width, height)) {
             juce::Logger::writeToLog("Plan 21-03: decoder open failed for peer " +
                                       juce::String(vs->stream_username));
             vs->decoder.reset();
             m_remote_frame_distributor->removeSink(vs->stream_username, vs->stream_chidx);
             vs->sink = nullptr;
         }
         ```
       - Update BEGIN handler in `src/core/njclient.cpp:2360-2378` raw-data branch (Plan 21-01 Task 2's augmentation): IMMEDIATELY AFTER the `vs->accumulating.interval_seq = m_sync_interval_cnt;` line, add the call `ensureVideoDecoderForPeer_(vs, 320, 240);` (still under m_video_recv_cs). The decoder+sink spin-up is run-thread-only and adds ~5-50 ms of allocation + thread creation, which is fine on the run thread (which is not RT-critical).
       - Update user-leave reset block (Plan 21-01 Task 2's m_remoteusers Delete site augmentation): immediately BEFORE the existing `vs->next.reset(); vs->pending.reset(); vs->accumulating.reset();` calls, add:
         ```cpp
         if (m_remote_frame_distributor) {
             m_remote_frame_distributor->removeSink(theuser->name.Get(), vs->stream_chidx);
         }
         vs->sink = nullptr;
         // vs->decoder unique_ptr is reset by the VideoRecvState dtor in m_video_streams.Empty(true);
         // for user-leave-without-state-removal (matching upstream), explicitly reset:
         vs->decoder.reset();
         ```
       - Verify: the order of operations is: removeSink (distributor releases its ref) → vs->sink = null (clear stale pointer in VideoRecvState) → vs->decoder.reset() (decoder ~Openh264Decoder runs R4 H9 7-step destructor, including stopping the thread). The decoder dtor calling sink_->triggerAsyncUpdate on its last frame is FINE because by the time the dtor runs, the sink ownership has moved off the distributor (no Phase 22 tile holds a Subscription that fires) — and even if a Subscription's onRepaint was mid-flight, the sink's own destructor handles the inFlight gate.

    3. **Update Plan 21-02 Openh264Decoder.h to add `setSink(PeerVideoSink*)` method (if not already added).** Already mentioned in Plan 21-02 Task 1 behavior; this Task 2 just verifies it's wired and replaces the ctor's sink parameter with the setter pattern.

    4. **Update `juce/JamWideJuceProcessor.cpp`:** Replace the Task 1 TODO comment with the actual `m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get());` call. Place IMMEDIATELY AFTER the existing `remoteFrameDistributor = std::make_unique<...>();` line at ctor line ~67.

    5. **Verification:** `./scripts/build.sh JamWideJuce_Standalone` builds clean. Existing tests green. Manual smoke test: connect JamWide to localhost ninjamzap-server (or video.ninjamzap.com:2049 — but that's the UAT, defer to Task 4); inspect logs for "decoder open succeeded for peer X" — should NOT fire if no peer broadcasts; SHOULD fire once a peer broadcasts H.264 BEGIN.
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh JamWideJuce_Standalone test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state test_video_decoder &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder"</automated>
    <grep>grep -c "SetRemoteFrameDistributor\|ensureVideoDecoderForPeer_" /Users/cell/dev/JamWide/src/core/njclient.h /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "findOrCreateSink\|removeSink" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "m_njClient.SetRemoteFrameDistributor" /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.cpp</grep>
  </verify>
  <acceptance_criteria>
    - `grep -q "void NJClient::SetRemoteFrameDistributor" /Users/cell/dev/JamWide/src/core/njclient.cpp` (real setter body, not the Task 1 inline stub)
    - `grep -q "void NJClient::ensureVideoDecoderForPeer_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (lazy-lifecycle helper)
    - `grep -q "ensureVideoDecoderForPeer_(vs" /Users/cell/dev/JamWide/src/core/njclient.cpp` (called in BEGIN handler)
    - `grep -q "m_remote_frame_distributor-&gt;removeSink" /Users/cell/dev/JamWide/src/core/njclient.cpp` (called in user-leave block)
    - `grep -q "vs-&gt;decoder.reset()" /Users/cell/dev/JamWide/src/core/njclient.cpp` (decoder torn down on user leave)
    - `grep -q "vs-&gt;sink = nullptr" /Users/cell/dev/JamWide/src/core/njclient.cpp` (stale sink pointer cleared)
    - `grep -q "m_njClient.SetRemoteFrameDistributor(remoteFrameDistributor.get())" /Users/cell/dev/JamWide/juce/JamWideJuceProcessor.cpp` (processor wires distributor to njClient)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder"` exits 0 (no regression)
  </acceptance_criteria>
  <done>Lazy decoder+sink lifecycle wired: BEGIN handler spawns decoder + sink + distributor registration on first H264 BEGIN per peer; user-leave joins decoder + removes sink. JamWideJuceProcessor hands the distributor pointer to NJClient at ctor time. Whole stack builds cleanly and existing tests stay green. The plugin is now FUNCTIONAL — a peer broadcasting H.264 will create a decoder, decode frames, and trigger sink AsyncUpdates that Phase 22 tiles will subscribe to.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write test_remote_frame_distributor.cpp (3 unit sub-tests) + test_video_sync_e2e.cpp (3-peer isolation integration) + wire CMake</name>
  <files>tests/test_remote_frame_distributor.cpp, tests/test_video_sync_e2e.cpp, CMakeLists.txt</files>
  <read_first>
    - /Users/cell/dev/JamWide/juce/video/distributor/JamWideRemoteFrameDistributor.h, PeerVideoSink.h (Task 1 outputs)
    - /Users/cell/dev/JamWide/tests/test_video_state_machine.cpp (test-style template)
    - /Users/cell/dev/JamWide/tests/test_video_decoder.cpp (Plan 21-02 Task 3 — fixture-driven decoder test pattern)
    - /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp (Plan 21-01 Task 3 — DispatchTestVideoRecv* dispatcher usage; test_video_sync_e2e uses these to drive 3 peers in parallel)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-VALIDATION.md (lines 56-58 — 3 sub-tests for Plan 21-03: two_listeners_same_peer_both_called, subscription_dtor_blocks_in_flight, three_peers_isolated_decode_errors)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 1373-1382 — Success criterion 4 mechanism: per-peer thread + per-peer SpscRing + per-peer codec context = no shared state; three_peers_isolated_decode_errors detects breach)
  </read_first>
  <behavior>
    - **test_remote_frame_distributor.cpp** has 3 sub-tests:
      1. `test_two_listeners_same_peer_both_called` (D-06): Construct a JamWideRemoteFrameDistributor; call findOrCreateSink("alice", 1, 320, 240) — sink exists. Subscribe twice: `sub1 = dist->subscribeToPeer("alice", 1, [&]{cnt1++;});` and `sub2 = dist->subscribeToPeer("alice", 1, [&]{cnt2++;});`. Call sink->triggerAsyncUpdate() then `juce::MessageManager::callAsync` cycle (or use `juce::MessageManager::getInstance()->runDispatchLoopUntil(100)` in test mode to flush async queue). Assert cnt1 == 1 and cnt2 == 1.
      2. `test_subscription_dtor_blocks_in_flight` (HIGH-2 mirror): Construct distributor + sink. Subscribe with a callback that sleeps 200ms. Call sink->triggerAsyncUpdate() → handleAsyncUpdate fires the callback in flight. Have another thread call `~Subscription` (move-then-drop, or scope-destroy). The dtor must block until the callback returns. Use `auto t0 = now(); { auto sub = move(...); } auto elapsed = now() - t0;` and assert elapsed ≥ 150ms (close to the 200ms callback sleep). Verifies HIGH-2 unregisterAndWait semantics.
      3. `test_subscribe_before_peer_exists_then_create` (RESEARCH §Open Question 2 deferred-listener path): Construct distributor (no sinks yet). Subscribe before any sink exists: `sub = dist->subscribeToPeer("bob", 1, [&]{cnt++;});`. Now lazy-create: `sink = dist->findOrCreateSink("bob", 1, 320, 240);` — this should flush the deferred listener onto the sink. Call sink->triggerAsyncUpdate() + runDispatchLoopUntil. Assert cnt == 1.

    - **test_video_sync_e2e.cpp** has 1 sub-test (Plan 21-VALIDATION.md test-id 21-03-03):
      `test_three_peers_isolated_decode_errors`: Instantiate one NJClient + distributor wiring. Add 3 fake remote users via Plan 21-01 Task 3's AddTestRemoteUserMirrorWithDs helper: ("alice", slot=0, guid 0xAA), ("bob", slot=1, guid 0xBB), ("charlie", slot=2, guid 0xCC). Drive 3 BEGIN+marker+SPS-PPS+IDR cycles via DispatchTestVideoRecvBegin/Write/End:
         - Peer alice: valid SPS/PPS fixture + valid IDR fixture (Plan 21-02 Task 3 fixtures reused)
         - Peer bob: valid SPS/PPS + valid IDR
         - Peer charlie: valid SPS/PPS + **CORRUPT** IDR (e.g., random 1024 bytes with Annex-B start code prefix)
      Run audio-thread RunOnNewIntervalReceiveBlockForTest 2 times so both intervals see swap → DS-match → playing → vs->decoder->pushSlotView (per CONTEXT.md D-12 revised — slot-view push on audio thread, decoder thread parses). Poll each peer's sink via `dist->findSink("alice", 1)->first_frame_seen` etc. Assert:
         - alice.sink.first_frame_seen == true, alice.sink.decode_error_count == 0
         - bob.sink.first_frame_seen == true, bob.sink.decode_error_count == 0
         - charlie.sink.first_frame_seen == false (or true if a partial decode emerged — accept either), charlie.sink.decode_error_count >= 1 (the corrupt NAL was dropped)
         - charlie's decode error did NOT cause alice or bob's decode_error_count to climb

      This is the **per-peer isolation gate (success criterion 4)** verified in-process.

    - CMakeLists.txt:
      ```cmake
      # Plan 21-03 Task 3: distributor unit tests.
      add_executable(test_remote_frame_distributor tests/test_remote_frame_distributor.cpp)
      target_compile_definitions(test_remote_frame_distributor PRIVATE JAMWIDE_BUILD_TESTS=1)
      target_include_directories(test_remote_frame_distributor PRIVATE
          ${CMAKE_CURRENT_SOURCE_DIR}/src
          ${CMAKE_CURRENT_SOURCE_DIR}
          ${CMAKE_CURRENT_SOURCE_DIR}/juce
      )
      target_link_libraries(test_remote_frame_distributor PRIVATE
          njclient
          juce::juce_graphics
          juce::juce_events
      )
      add_test(NAME remote_frame_distributor COMMAND test_remote_frame_distributor)

      # Plan 21-03 Task 3: 3-peer per-peer isolation integration test.
      add_executable(test_video_sync_e2e tests/test_video_sync_e2e.cpp)
      target_compile_definitions(test_video_sync_e2e PRIVATE JAMWIDE_BUILD_TESTS=1)
      target_include_directories(test_video_sync_e2e PRIVATE
          ${CMAKE_CURRENT_SOURCE_DIR}/src
          ${CMAKE_CURRENT_SOURCE_DIR}
          ${CMAKE_CURRENT_SOURCE_DIR}/juce
      )
      target_link_libraries(test_video_sync_e2e PRIVATE
          njclient
          juce::juce_graphics
          juce::juce_events
      )
      jamwide_use_ffmpeg(test_video_sync_e2e)
      add_test(NAME video_sync_e2e COMMAND test_video_sync_e2e
               WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
      ```
  </behavior>
  <action>
    1. **Create `tests/test_remote_frame_distributor.cpp`:** Mirror test_video_state_machine.cpp's header block. Include `juce/video/distributor/JamWideRemoteFrameDistributor.h`, `juce/video/distributor/PeerVideoSink.h`, `<juce_events/juce_events.h>` (for MessageManager + runDispatchLoopUntil), `<thread>`, `<chrono>`, `<atomic>`. TEST/PASS/FAIL macros. Three sub-tests as defined in Behavior section. main() runs juce::ScopedJuceInitialiser_GUI then invokes the three. Each test cleans up its Subscription before exiting (Subscriptions are RAII move-only, scope handles).

    2. **Create `tests/test_video_sync_e2e.cpp`:** Mirror test_video_decoder.cpp's structure. Include njclient.h, JamWideRemoteFrameDistributor.h, PeerVideoSink.h, Openh264Decoder.h. test_three_peers_isolated_decode_errors() body:
       - Construct std::unique_ptr<NJClient> client + std::unique_ptr<JamWideRemoteFrameDistributor> dist
       - Call client->SetRemoteFrameDistributor(dist.get())
       - Use Plan 21-01's AddTestRemoteUserMirrorWithDs to add 3 fake remote-user mirror entries (alice/bob/charlie with distinct guids)
       - Read fixture bytes for SPS/PPS + IDR (Plan 21-02 Task 3 fixture files at tests/fixtures/)
       - For each peer p in {alice, bob, charlie}: construct a fake interval BEGIN+WRITE sequence via client->DispatchTestVideoRecvBegin/Write/End that builds:
         - 24-byte marker frame: `[00 00 00 14][BE swap=N][16B guid_X]` where guid_X matches the peer's AddTestRemoteUserMirrorWithDs guid (so the GUID-pair tree will DS-match)
         - SPS/PPS chunk: `[2B BE sps_len][SPS_no_start_code][2B BE pps_len][PPS_no_start_code]`
         - For alice and bob: valid IDR fixture bytes; for charlie: 1024 random bytes (corrupt)
         Each chunk wrapped with `[4B BE chunk_inner_length]` per the wire format (Plan 21-01 4-byte BE length prefix multi-write reassembly)
       - Call client->DispatchTestVideoRecvBegin/Write/End for each peer to push the bytes into the run-thread state
       - Call client->RunOnNewIntervalReceiveBlockForTest() to run the audio-thread SWAP (Plan 21-01 helper). The decoder threads pick up parsed NalChunks. Wait up to 5 seconds via juce::WaitableEvent or polling for first_frame_seen on alice and bob; assert charlie's decode_error_count >= 1
       - Strong: assert alice.sink.decode_error_count == 0 AND bob.sink.decode_error_count == 0 AND charlie.sink.decode_error_count >= 1 — that's the per-peer isolation proof

       Helper for chunk byte construction: a local lambda `chunk(uint32_t len, const std::vector<uint8_t>& body)` that prepends 4 BE bytes.

    3. **CMakeLists.txt:** Add the two add_executable + add_test blocks as in Behavior section. WORKING_DIRECTORY for test_video_sync_e2e is the source root so tests/fixtures/* paths resolve.

    4. **Verification:** `./scripts/build.sh test_remote_frame_distributor test_video_sync_e2e` builds. `cd build-juce && ctest --output-on-failure -R "remote_frame_distributor|video_sync_e2e"` runs both; all 4 sub-tests pass. Full suite green.
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh test_remote_frame_distributor test_video_sync_e2e test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state test_video_decoder &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder|remote_frame_distributor|video_sync_e2e"</automated>
    <grep>grep -c "test_two_listeners_same_peer_both_called\|test_subscription_dtor_blocks_in_flight\|test_subscribe_before_peer_exists_then_create" /Users/cell/dev/JamWide/tests/test_remote_frame_distributor.cpp</grep>
    <grep>grep -c "test_three_peers_isolated_decode_errors" /Users/cell/dev/JamWide/tests/test_video_sync_e2e.cpp</grep>
    <grep>grep -c "add_test(NAME remote_frame_distributor\|add_test(NAME video_sync_e2e" /Users/cell/dev/JamWide/CMakeLists.txt</grep>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/cell/dev/JamWide/tests/test_remote_frame_distributor.cpp` AND `test -f /Users/cell/dev/JamWide/tests/test_video_sync_e2e.cpp`
    - All 3 test_remote_frame_distributor sub-tests defined (verified via grep)
    - `test_three_peers_isolated_decode_errors` defined in test_video_sync_e2e.cpp
    - Both CMake targets wired with add_test entries
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "^remote_frame_distributor$"` exits 0
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "^video_sync_e2e$"` exits 0
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder|remote_frame_distributor|video_sync_e2e"` exits 0 (whole suite green)
  </acceptance_criteria>
  <done>3 distributor unit sub-tests prove D-06 multi-listener + HIGH-2 mirror dtor-blocking + RESEARCH §Open Question 2 deferred-listener path. 1 integration test proves the 3-peer per-peer isolation gate (success criterion 4) in-process via the test dispatchers. All 14 Wave-1+2+3 tests pass; whole Phase 21 test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Author phase-21-receive-uat-procedure.md (4 UAT cells) + add CMake LOCAL_DEPLOY hooks if needed</name>
  <files>tests/uat/phase-21-receive-uat-procedure.md, tests/uat/phase-21-receive-uat-report.md</files>
  <read_first>
    - /Users/cell/dev/JamWide/tests/uat/phase-20-broadcast-uat-report.md (Phase 20 UAT report — Plan 21-03 mirrors structure; same 5-min populated-server protocol)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-VALIDATION.md (lines 83-90 — Manual-Only Verifications: 4 UAT cells mapped to success criteria 1-4)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (success criteria 1-4 + D-17/D-18/D-19/D-20 atomic counter semantics — UAT report records these values via lldb)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 1337-1382 — Proof-by-Construction Architecture for each success criterion)
    - .claude/projects/-Users-cell-dev-JamWide/memory/feedback_uat_scope_redflags.md (UAT discipline — every user-visible happy path covered)
  </read_first>
  <action>
    Per ROADMAP success criteria 1-4 + VALIDATION manual cells + `feedback_uat_scope_redflags.md`:

    Create `tests/uat/phase-21-receive-uat-procedure.md` mirroring `phase-20-broadcast-uat-report.md` structure. Four cells:

    **Cell 1: Wall-clock audio-video alignment (5+ min, no 'video one interval early')** — ROADMAP success criterion 1
    Setup: Two JamWide standalone instances. Instance A on local Mac (this dev machine); instance B is either (a) another JamWide build on a second Mac OR (b) a NinjamZap mobile peer (preferred — proves cross-platform wire format compat). Both connect to `video.ninjamzap.com:2049` via JamWide's existing NINJAM server browser → manual address entry → join room "jamwide-uat-21". Instance A camera ON + broadcast ON (uses Phase 19 + 20 stack). Instance B receives.
    Steps: B watches A's video tile for 5 minutes. Lip-sync check: A speaks a counted phrase ("one one one... two two two..."). B confirms the spoken count matches the lip movement in the video (no 1-interval-early lag).
    Acceptance: PASS if B reports zero perceived audio-video drift over 5 min AND lldb readout of `vs->drop_resync_count == 0` AND `vs->hold_count == 0` at T+5:00 (via JAMWIDE_BUILD_TESTS-only `GetVideoStreamForTest` accessor exposed in lldb).
    Failure modes: video appears EARLIER than the lip movement (the original "video one interval early" bug); video lags AUDIO by ≥1 interval (would indicate the 1-swap-defer is firing on PREV when it shouldn't).

    **Cell 2: Mid-session join sees video within ≤2 NINJAM intervals** — ROADMAP success criterion 2
    Setup: same as Cell 1. Instance A starts broadcasting first; instance B is offline. After 30 seconds (≥3 intervals into A's broadcast), B starts and joins the same room.
    Steps: B watches the timestamp of when its tile for A transitions from "video starting…" overlay to the first decoded frame. Wall-clock from B's join button click to first frame: should be ≤ 2 × NINJAM interval duration (typically ~12 seconds at 4 bars BPI).
    Acceptance: PASS if first frame arrives within 2 intervals (measured via lldb on B's GetPeerSinkForTest("alice", 1)->first_frame_seen first true).

    **Cell 3: Peer audio stops → freeze → kHoldCapDrop=4 → resume cleanly** — ROADMAP success criterion 3
    Setup: same as Cell 1. After steady broadcast established, instance A toggles audio channel 1 mute (Cmd-1 or click the mute button on A's Local Channel). Audio stream stops, video continues (different streams, asymmetric).
    Steps: B watches A's tile. Expected: tile freezes on last-good-frame for 1-2 intervals (no overlay yet); at 3rd hold interval the 'syncing…' overlay (D-17) appears as a subtle bottom-right label; at 4th hold the kHoldCapDrop fires, drop_resync_count increments, next.reset() — but the LAST-GOOD-FRAME remains painted (no black tile). Instance A un-mutes audio. Within 1 interval boundary, B's tile rejoins cleanly (next-DS-match plays after the 1-swap defer through pending).
    Acceptance: PASS if (a) tile freezes (not black, not crash), (b) 'syncing…' overlay appears at 2nd hold (verify Phase 22 UI manually or read sink->hold_count from lldb), (c) drop_resync_count increments by exactly 1 after the 4th hold, (d) post-resume, tile renders cleanly within 1 interval.

    **Cell 4: 3+ peers simultaneously, per-peer isolation, ≥5 min** — ROADMAP success criterion 4
    Setup: 3 standalone JamWide instances on 3 separate machines (mix of Macs + Windows + NinjamZap mobile preferred to also exercise cross-platform). All 3 join `video.ninjamzap.com:2049` room "jamwide-uat-21-3way". All 3 enable camera + broadcast.
    Steps: All 3 users watch the other 2 users' tiles for 5 minutes. Speak randomly to verify no audio glitches per listener. Counter readout via lldb at T+5:00 on at least one instance: each peer's sink->decode_error_count, sink->drop_resync_count, vs->slot_drop_count must all be 0 (or near 0).
    Acceptance: PASS if 5 min completes without any of: (a) frozen tile other than the brief lip-sync micro-stops (b) audio glitches (c) decoder errors flowing to a different peer's counters (cross-contamination), (d) a process crash on any of the 3 instances.

    Each cell records the JamWide build number (visible in About dialog) and the test date. Failure modes are documented per Cell — Cell 4 is the most likely to surface CPU starvation under macOS QoS (Pitfall 4 from RESEARCH).

    Create `tests/uat/phase-21-receive-uat-report.md` as an empty template (no PASS/FAIL marks yet — the checkpoint:human-verify task in Task 5 fills these in based on actual UAT execution).
  </action>
  <verify>
    <automated>test -f /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-procedure.md &amp;&amp; test -f /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-report.md</automated>
    <grep>grep -c "Cell 1:\|Cell 2:\|Cell 3:\|Cell 4:" /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-procedure.md</grep>
    <grep>grep -c "success criterion 1\|success criterion 2\|success criterion 3\|success criterion 4" /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-procedure.md</grep>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-procedure.md` (procedure exists)
    - `test -f /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-report.md` (report template exists)
    - Procedure contains exactly 4 Cell N: sections (verified via grep — Cell 1/2/3/4)
    - Each cell maps to one ROADMAP success criterion (1/2/3/4) — referenced in cell text
    - Each cell has Setup + Steps + Acceptance sections with reproducible instructions
    - Counter-readout instructions reference lldb / GetVideoStreamForTest / GetPeerSinkForTest accessors (JAMWIDE_BUILD_TESTS gates)
    - Procedure names `video.ninjamzap.com:2049` as the recommended UAT server (per SRV-01 / Phase 24 reference policy)
    - Report template has empty Cell 1/2/3/4 status entries ready for Task 5 to fill
  </acceptance_criteria>
  <done>UAT procedure documents 4 cells mapping 1:1 to ROADMAP success criteria. Procedure follows Phase 20's tested format. Per `feedback_uat_scope_redflags.md`, every user-visible happy path (alignment, mid-join, freeze-then-recover, multi-peer isolation) has an explicit cell. Counter-readout commands gated by JAMWIDE_BUILD_TESTS only require lldb attach — no production-build mode changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run UAT cells 1-4 against video.ninjamzap.com:2049, populate phase-21-receive-uat-report.md</name>
  <files>tests/uat/phase-21-receive-uat-report.md</files>
  <read_first>
    - /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-procedure.md (Task 4 output — procedure to follow)
    - /Users/cell/dev/JamWide/tests/uat/phase-21-receive-uat-report.md (Task 4 output — empty report template to fill)
  </read_first>
  <what-built>
    All Plan 21-01 + 21-02 + 21-03 Tasks 1-3 land:
    - VideoRecvBuffer + VideoRecvState verbatim port + audio-thread SWAP (Plan 21-01)
    - Openh264Decoder + AVCC parser + Annex-B SPS/PPS path (Plan 21-02)
    - PeerVideoSink + JamWideRemoteFrameDistributor + lazy lifecycle wiring (Plan 21-03 Task 1+2)
    - 17 automated sub-tests across 5 test executables pass (Plan 21-01 Task 3: 9; Plan 21-02 Task 3: 5; Plan 21-03 Task 3: 3 unit + 1 e2e = 4)
    - test_video_sync_e2e proves per-peer isolation in-process

    What remains: validation against a real populated NINJAM session.
  </what-built>
  <how-to-verify>
    1. Build a fresh JamWideJuce_Standalone with all Plan 21-01/02/03 changes:
       ```
       cd /Users/cell/dev/JamWide
       ./scripts/build.sh JamWideJuce_Standalone
       ```
       Build number visible in About dialog should increment from the Plan 21-02 build.
    2. Open `tests/uat/phase-21-receive-uat-procedure.md`. Run each cell in order:
       - Cell 1 (wall-clock alignment, 5+ min) — coordinate with a second peer (another JamWide instance or NinjamZap mobile peer)
       - Cell 2 (mid-session join ≤2 intervals)
       - Cell 3 (audio-stop → hold → kHoldCapDrop=4 → resume)
       - Cell 4 (3+ peers per-peer isolation, ≥5 min)
    3. For each cell, record PASS / FAIL / BLOCKED in `tests/uat/phase-21-receive-uat-report.md` with:
       - Result line (PASS/FAIL/BLOCKED + 1-line summary)
       - Counter readouts from lldb (if applicable): drop_resync_count, hold_count, decode_error_count, slot_drop_count per peer
       - Build number tested
       - Test date
       - Observed behavior notes (especially for any FAIL)
    4. If any cell FAILS, do NOT close the phase — surface the failure to the user with:
       - Symptom description
       - Suspected root cause (relate to D-XX / pitfall N / open question N)
       - Proposed fix scope (does it need a Plan 21-04, or a quick task?)

    All four cells PASSING is the gate for closing Phase 21. BLOCKED cells (e.g., no mobile peer available, no second Mac, no Windows machine) should be carried forward as Phase 24 BETA-XX scope per the existing precedent (Phase 19 UAT cells 4/6/7/8 were blocked on environments and deferred).
  </how-to-verify>
  <resume-signal>Reply with "All UAT cells PASS — phase 21 closed" OR a list of which cells failed with diagnostic detail (counter values, observed symptoms) so we can decide if a Plan 21-04 fix-up plan is needed before closing the phase.</resume-signal>
  <acceptance_criteria>
    - phase-21-receive-uat-report.md has explicit PASS/FAIL/BLOCKED markers for Cells 1-4
    - At least 2 cells PASS (Cell 1 wall-clock + Cell 4 multi-peer isolation are the highest-priority gates; Cell 2 mid-join and Cell 3 audio-stop are valuable but acceptable as BLOCKED if a real audio-stop scenario can't be reproduced cleanly)
    - Any FAIL cell has a documented root-cause hypothesis + proposed remediation scope
    - User has explicitly signaled the resume signal
  </acceptance_criteria>
  <done>UAT cells 1-4 are exercised against video.ninjamzap.com:2049; results documented in tests/uat/phase-21-receive-uat-report.md; failures (if any) are surfaced with root-cause hypotheses for a follow-up plan. User has explicitly approved phase closure or directed a fix-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NJClient run thread → JamWideRemoteFrameDistributor (run thread caller) | findOrCreateSink / removeSink called from run thread under m_video_recv_cs; distributor mu_ is a separate lock (no nesting concerns) |
| Decoder thread → PeerVideoSink::triggerAsyncUpdate → message thread handleAsyncUpdate | Phase 19 HIGH-4 verbatim pattern; AsyncUpdater coalesces |
| Message thread (Phase 22 tile) → Subscription dtor | HIGH-2 mirror: ~Subscription calls unsubscribe_ which blocks if in-flight |
| JamWideJuceProcessor::~JamWideJuceProcessor → unique_ptr<JamWideRemoteFrameDistributor> | LIFO member destruction; distributor outlives any internal sinks (sinks owned by distributor's map). NJClient must have already torn down its m_video_streams (which call removeSink during dtor reset) BEFORE distributor's map destructs — verify member ordering carefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | Crash | Subscription outliving the distributor (e.g., Phase 22 tile destroyed after JamWideJuceProcessor) | mitigate | Subscription stores a raw back-pointer to the distributor. If distributor destructs first, Subscription's ~Subscription dereferences a freed pointer. JamWideJuceProcessor's normal lifecycle has the editor (which owns tiles) destruct BEFORE the processor (so all Subscriptions are released first), but if a tile escapes that ordering (e.g., a popout DocumentWindow lifetime exceeds the editor), it could outlive the distributor. Mitigation: Plan 21-03 Task 1 uses raw back-pointer; if Phase 22 introduces a popout that can outlive editor/processor, switch to std::weak_ptr<Distributor>. Phase 22 owns this constraint. |
| T-21-15 | Information Disclosure / Crash | findOrCreateSink racing concurrent subscribeToPeer for same peer | mitigate | Both operations take distributor mu_; serialised. findOrCreateSink flushes deferredListeners_[key] under the same lock; no listener can be lost. |
| T-21-16 | Crash | PeerVideoSink::handleAsyncUpdate iterating listeners_ while ~Subscription removes a listener | mitigate | inFlight refcount pattern (verbatim Phase 19): handleAsyncUpdate snapshots listener shared_ptrs under listenerLock_, bumps inFlight, releases lock; ~Subscription waits via cv until inFlight is 0 for the removed listener. Test test_subscription_dtor_blocks_in_flight verifies. |
| T-21-17 | Denial of Service | Lazy-lifecycle thread creation storm under burst-BEGIN scenarios | mitigate | ensureVideoDecoderForPeer_ is idempotent (early-return on vs->decoder != nullptr). A single peer's burst BEGIN/END/BEGIN within one interval (Plan 21-01 burst-detect path) does NOT trigger new decoder creation; the existing decoder picks up the new accumulator. Decoder creation cost ~5-50 ms is amortised over the peer's session. |
| T-21-18 | Tampering / Crash | Adversarial 4-byte BE chunk length in WRITE handler exceeding the 4 MB cap | mitigate | Inherited from Plan 21-01 T-21-01: pre-allocated WDL_HeapBuf clamps; Plan 21-03 inherits the same gate at the distributor layer (sink dims fixed at first findOrCreateSink call; can't be inflated by a peer). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 21 installs zero new packages (all deps vendored as Phase 14.3-01). Slopcheck N/A. |
</threat_model>

<verification>
After all five tasks land (Task 5 is the human-verify checkpoint):

```bash
# Automated suite
cd /Users/cell/dev/JamWide && ./scripts/build.sh JamWideJuce_Standalone test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state test_video_decoder test_remote_frame_distributor test_video_sync_e2e
cd build-juce && ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state|video_decoder|remote_frame_distributor|video_sync_e2e"
```

8 test executables exit 0 (5 from Plans 21-01/02 + 3 from Plan 21-03 Task 3).

Manual verification: Task 5 populates `tests/uat/phase-21-receive-uat-report.md` with at least PASS/FAIL/BLOCKED markers for each of Cells 1-4. Phase 21 closure gate: Cells 1 + 4 PASS (minimum), Cells 2 + 3 PASS or accept BLOCKED-for-Phase-24 (per the Phase 19 UAT precedent where BLOCKED cells deferred to Phase 23/24 environment).
</verification>

<success_criteria>
- JamWideRemoteFrameDistributor + PeerVideoSink land in juce/video/distributor/ with Subscription RAII + listener vector + atomic generation + atomic status fields (D-01..D-08, D-17..D-20)
- JamWideJuceProcessor owns the distributor lifecycle parallel to the camera-side frameDistributor
- Lazy decoder + sink lifecycle: BEGIN handler spins up decoder + sink + registers with distributor on first H264 BEGIN per peer; user-leave joins decoder + removes sink (D-10)
- Test suite green across all 8 Phase 21 test targets (test_rawdata_send + test_video_state_machine + test_video_fourcc + test_video_encoder + test_video_recv_state + test_video_decoder + test_remote_frame_distributor + test_video_sync_e2e)
- test_video_sync_e2e three_peers_isolated_decode_errors PASSES, proving per-peer isolation (success criterion 4) in-process
- tests/uat/phase-21-receive-uat-procedure.md authored with 4 cells mapping to ROADMAP success criteria 1-4
- tests/uat/phase-21-receive-uat-report.md populated by Task 5 checkpoint with at least Cells 1 + 4 PASS (Cells 2 + 3 PASS or acceptable BLOCKED-for-Phase-24)
- Phase 21 closure: ROADMAP entry advances 0/3 → 3/3 plans complete; STATE.md updates; v1.3 phases mark Phase 21 Complete
</success_criteria>

<output>
Create `.planning/phases/21-h-264-decoder-receive-pipeline/21-03-SUMMARY.md` when done (after Task 5 checkpoint resolves).
</output>
