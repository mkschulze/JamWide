---
phase: 14.3-native-video-foundation
fixed_at: 2026-05-15T00:00:00Z
review_path: .planning/phases/14.3-native-video-foundation/14.3-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 4
skipped: 1
status: partial
---

# Phase 14.3: Code Review Fix Report

**Fixed at:** 2026-05-15
**Source review:** `.planning/phases/14.3-native-video-foundation/14.3-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (CR-01, CR-02, WR-01, WR-02, WR-03)
- Fixed: 4 (CR-02, WR-01, WR-02, WR-03)
- Skipped: 1 (CR-01 — already fixed by prior commit)

## Fixed Issues

### CR-02: is_video_fourcc comment corrected — false caller attribution removed

**Files modified:** `src/core/njclient.cpp`
**Commit:** `adae28e`
**Applied fix:** Replaced the block comment at lines 230-233 that falsely claimed the
run-thread drain and the 14.3-03 receive-dispatch INSERT branch call `is_video_fourcc`.
Neither does. The comment now accurately states the function is used only from the
`JAMWIDE_BUILD_TESTS` `IsVideoFourcc` wrapper, and explains why the production dispatch
is intentionally codec-agnostic (fourcc != NJ_ENCODER_FMT_TYPE && != NJ_ENCODER_FMT_FLAC)
rather than video-specific.

### WR-01: CI ctest filter broadened to run all Phase 14.3 unit tests

**Files modified:** `.github/workflows/juce-build.yml`
**Commit:** `8fffe27`
**Applied fix:** Changed the `ctest -R ffmpeg_link_smoke` invocation to
`ctest -R "ffmpeg_link_smoke|rawdata_send_roundtrip|video_fourcc" --output-on-failure`.
Renamed the CI step from "ffmpeg link smoke test" to "Run Phase 14.3 unit tests".
The 14.3-02 and 14.3-03 suites (SPSC round-trip, chunking, overflow counting, Landmine L4
exclusion, disconnect discard) are now executed on every CI push.

### WR-02: MINGW case pattern in build_ffmpeg_lgpl.sh fixed

**Files modified:** `scripts/build_ffmpeg_lgpl.sh`
**Commit:** `c33543d`
**Applied fix:** Changed the ambiguous case arm from
`MINGW64_NT-*"|x86_64"|MSYS_NT-*"|x86_64"|CYGWIN_NT-*"|x86_64"` to
`"MINGW64_NT-"*"|x86_64"|"MSYS_NT-"*"|x86_64"|"CYGWIN_NT-"*"|x86_64"`. Each arm now
properly enforces the `|x86_64` arch suffix as a literal match requirement. Verified via
bash test: MINGW64_NT-* x86_64 matches; MINGW64_NT-* arm64 correctly falls to the error
arm. Syntax checked with `bash -n`.

### WR-03: Overflow and discard counters split in RawData send queue

**Files modified:** `src/core/njclient.cpp`, `src/core/njclient.h`, `tests/test_rawdata_send.cpp`
**Commit:** `c9214c3`
**Applied fix:**
- Added `m_rawdata_sendq_discards` atomic member in `njclient.h` with explanatory comment
- Added `GetRawDataSendQueueDiscardCount()` accessor in `njclient.h`
- Updated `GetRawDataSendQueueOverflowCount()` doc comment to state it covers try_push failures only
- Changed `njclient.cpp` Pattern C drain branch to bump `m_rawdata_sendq_discards` instead of `m_rawdata_sendq_overflows`
- Updated `test_disconnect_drain_and_discard` to assert overflow counter is NOT touched by Pattern C, and discard counter IS bumped by exactly 3

The post-UAT `GetRawDataSendQueueOverflowCount() == 0` invariant is now actionable even
after Disconnect/Reconnect cycles.

## Skipped Issues

### CR-01: RawDataSendWrite — WDL_HeapBuf::Resize() unchecked OOM

**File:** `src/core/njclient.cpp:3025-3041`
**Reason:** skipped: already-fixed — commit `8e594f8` (T-14.3-08 security audit)
independently applied the identical fix prior to this review-fix run. The code at
lines 3026-3041 already uses `ResizeOK` with NULL guard, OOM drop, and log message.
No re-application needed.
**Original issue:** `WDL_HeapBuf::Resize()` unchecked; OOM causes `memcpy(NULL)` crash.

---

_Fixed: 2026-05-15_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
