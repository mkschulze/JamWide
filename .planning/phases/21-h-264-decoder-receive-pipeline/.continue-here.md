---
context: phase
phase: 21-h-264-decoder-receive-pipeline
task: planning-complete-release-in-flight
total_tasks: n/a
status: in_progress
last_updated: 2026-05-17T16:40:22.812Z
---

<current_state>
**Two parallel threads on branch `quick/260515-0pc-native-video-port` (pushed to origin).**

**Thread A — Phase 21 planning (DONE, ready to execute):**
H.264 Decoder & Receive Pipeline plans are fully revised per the codex cross-AI review pass + plan-checker iteration 2. All 10 HIGH codex clusters addressed, B-1 (research RESOLVED) + W-1 (responsibility map) + W-3 (back-to-back push regression test) closed. STATE.md says "Ready to execute". Next action for Phase 21: `/gsd-execute-phase 21`.

**Thread B — v1.1-beta.20.6 release (IN FLIGHT, blocked on CI):**
First attempt (release `v1.1-beta.20.6` at HEAD `1ac8ab2`) was rolled back after all 3 CI platforms failed on pre-existing Phase 14.3 ffmpeg-gate vs incomplete CI wiring. Six CI fix commits later, the workflow now:

- Skips `build-windows` (Phase 23 — `if: ${{ false }}`)
- Skips `build-linux` (post-v1.3 — Linux V4L2 wrapper)
- Builds `build-macos` arm64-only (Phase 23 owns universal stitching)
- Drops `JAMWIDE_BUILD_TESTS`/`VIDEO_SPIKE` from macOS CI to match beta.20.5 surface (`test_flac_codec` has pre-existing `WDL_VORBIS_INTERFACE_ONLY`-guard gap in `wdl/vorbisencdec.h:42`)

CI run **`25996593334`** (commit `75e3dd3`) is the decisive run. Background watcher `b554si149` is still active and will notify when it completes. **As of timestamp above, status was `in_progress`.**
</current_state>

<completed_work>

Phase 21 planning (commits committed on `quick/260515-0pc-native-video-port`):
- `8842f4f` — Cross-AI review (codex) — 10 HIGH lifetime/RT-safety findings → `21-REVIEWS.md`
- `feafa73` — Revise plans per codex review (10 HIGH + 7 MEDIUM + 2 LOW)
- `7a3d4db` — Close B-1 + W-1 + W-3 from checker iteration 1
- `1ac8ab2` — Mark planning complete after --reviews replan (STATE.md)

Release engineering (same branch):
- `73c957a` — Created v1.1-beta.20.6 release first time + updated `docs/download.md` (BOTH ROLLED BACK by `228e2a1`)
- `228e2a1` — Reverted `docs/download.md` to point at v1.1-beta.20.5 (after release rollback)
- `d760cb1` — Wire ffmpeg vendoring on Linux+Windows; fix macOS openh264 `OS=darwin` arg (openh264 v2.1.1 only ships `build/platform-darwin.mk`, no `platform-darwin64.mk`)
- `6c21ec6` — Fix `verify_ffmpeg_lgpl` on Linux (LD_LIBRARY_PATH + skip-non-platform) + Windows openh264 import-lib generation (gendef/dlltool)
- `4463542` — Defer build-windows + universal-mac to Phase 23
- `30ee519` — Defer build-linux too (camera code needs V4L2 wrapper)
- `75e3dd3` — Drop `JAMWIDE_BUILD_TESTS` + `VIDEO_SPIKE` from mac CI (matches beta.20.5 surface)

Branch + push:
- Renamed `quick/260515-0pc-jamtaba-video-port` → `quick/260515-0pc-native-video-port`
- Pushed to origin with upstream tracking
- Memory updates: `project_jamtaba_video_port.md` body + `MEMORY.md` index now reference new branch name and protocol attribution (jacinside/ninjamzap-core)

Memory additions:
- `feedback_check_ci_before_tag.md` — Always `gh run list --branch X --limit 1` before pushing a release tag
</completed_work>

<remaining_work>

**Blocked on background watcher `b554si149` (CI run `25996593334`):**

1. **Watch for watcher completion** — when notified, check `gh run view 25996593334 --json conclusion,jobs`
2. **If macOS arm64 build succeeded:**
   - Download the mac artifact: `gh run download 25996593334 -n JamWide-macOS -D /tmp/`
   - Create release: `gh release create v1.1-beta.20.6 --target 75e3dd3 --notes-file /tmp/release-notes-v1.1-beta.20.6.md --prerelease /tmp/JamWide-macOS.tar.gz`
     - **Alternative:** if `/tmp/release-notes-v1.1-beta.20.6.md` is gone (temp file), regenerate from the body in `HANDOFF.json` `context_notes` or from this file's release-notes section below
   - Update `docs/download.md` to point at v1.1-beta.20.6 (single href change, was just done at `73c957a` before rollback — re-do same change)
   - Commit + push the docs change
3. **If macOS arm64 build failed:** Read failure log via `gh run view --job <macos_job_id> --log-failed`, diagnose, fix, repeat.

**Phase 21 execute (orthogonal):** `/gsd-execute-phase 21` — plans verified, ready when you are.
</remaining_work>

<decisions_made>

- **Defer Windows to Phase 23.** Cisco's openh264 prebuilt has no MinGW import library; `gendef` isn't in MSYS2 base-devel; Phase 23 explicitly owns Windows codesign+build. Workflow disabled with `if: ${{ false }}` — one-line flip when Phase 23 is ready.
- **Defer Linux to post-v1.3.** Phase 19's camera code uses `juce::CameraDevice` unconditionally; JUCE has no `juce_CameraDevice_linux.h`. Memory `project_jamtaba_video_port` already locked "Linux capture as Item K post-v1".
- **Defer universal-binary macOS to Phase 23.** `cmake/ffmpeg.cmake` explicitly says "Universal-binary stitching is a Phase 14.3 follow-up" — only loads one per-arch tree. arm64-only for v1.3 betas.
- **Drop `JAMWIDE_BUILD_TESTS` from mac CI.** `test_flac_codec` has pre-existing CMake-vs-header gap (`WDL_VORBIS_INTERFACE_ONLY` compile-def has no matching guard in `wdl/vorbisencdec.h`). beta.20.5 sidestepped by not setting BUILD_TESTS at all — match that surface.
- **Phase 21 Open Questions Q1 resolution: AVCC parser on DECODER thread, NOT audio thread.** Per CONTEXT.md D-12 revised + B-1 + codex Cluster 2 Option A. Audio thread only memcpys slot bytes into `VideoRecvState`-owned `VideoRecvSlotSnapshot` + pushes integer index into `SpscRing<int, 4>`.
- **Phase 21 Option A ownership model.** `VideoRecvState` owns `std::array<VideoRecvSlotSnapshot, 4>` + index-only SPSC. Structurally prevents back-to-back-push frame corruption from the original `SpscRing<VideoRecvBufferView, 4>` design.
- **Phase 21 4-step user-leave shutdown protocol.** `decoder->close()` → `decoder->setSink(nullptr)` → `vs->sink=nullptr` → `distributor->removeSink(...)`. Plus processor destructor reorders `client.reset()` BEFORE `remoteFrameDistributor.reset()`.
- **Release notes credit NinjamZap.** Per user's directive, all NinjamZap mentions cite `https://github.com/jacinside/ninjamzap-core` and `https://github.com/jacinside/ninjamzap-server` with thanks to @jacinside. Notes explicitly say "JamWide reimplements the wire format from scratch in C++/JUCE; no NinjamZap source is vendored." No mention of "jamtaba" anywhere in release notes or branch name (per user).
</decisions_made>

<blockers>

- **CI run `25996593334` still in_progress** at handoff time. Background watcher `b554si149` will notify on completion. Resume by checking that watcher's status first.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state
2. `.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md` — codex review with 10 HIGH findings + remediation map
3. `.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md` D-12 revised + B-1 — locked decoder-thread parsing decision
4. `/tmp/release-notes-v1.1-beta.20.6.md` (if still present) — curated release notes for the beta. **WARNING:** /tmp can be cleared between sessions; the body is also in `HANDOFF.json` `context_notes` field (truncated) and quoted in the release-notes-source section near the bottom of this file (TODO: add if /tmp is unreliable).

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Tagging a release before verifying branch CI is green | Pushed `v1.1-beta.20.6` tag on a branch where CI had been failing for 2 days (Phase 14.3 ffmpeg gate vs incomplete CI workflow). All 3 CI builds failed; public-facing empty release on GitHub for ~5 min until rollback. | blocking | Before any `gh release create v*` or `git push origin v*`, run `gh run list --branch <branch> --limit 1 --json conclusion` and confirm `conclusion: success`. Saved as memory entry `feedback_check_ci_before_tag.md`. |
| Believing a test target compiles because the CMakeLists looks plausible | `test_flac_codec` has `target_compile_definitions(WDL_VORBIS_INTERFACE_ONLY)` but the header `wdl/vorbisencdec.h` has NO matching guard — the compile-def is dead code. Manifested when `JAMWIDE_BUILD_TESTS=ON` was set in CI (it wasn't at beta.20.5). | advisory | Don't assume a `WDL_VORBIS_INTERFACE_ONLY`-style compile-def actually works — grep the header for the macro before relying on it. |
| Assuming CI workflow handles new architectural constraints automatically | Commit `489b03c` (Phase 14.3 cmake gate) made the ffmpeg vendored tree mandatory, but the CI workflow only vendored ffmpeg for macOS. Linux + Windows builds silently broke for 2 days. | advisory | When introducing a new mandatory build dependency, update ALL CI lanes in the same commit, not just the "primary" one. |

## Infrastructure State

- **Branch:** `quick/260515-0pc-native-video-port` (renamed from `quick/260515-0pc-jamtaba-video-port` 2026-05-17)
- **Remote:** `origin` tracks the new branch name
- **Latest commit:** `75e3dd3` (drop BUILD_TESTS + VIDEO_SPIKE from mac CI)
- **In-flight CI runs:**
  - `25996593334` (commit `75e3dd3`, mac-only with all CI fixes) — **DECISIVE, in_progress**, watcher `b554si149`
  - `25996430123` (commit `30ee519`, will fail at vorbis-include since BUILD_TESTS was still on then) — **stale**, watcher `bzd3lbbcr` just completed
- **CI workflow `if: ${{ false }}` deferrals:**
  - `build-windows` → Phase 23
  - `build-linux` → post-v1.3 (Linux capture wrapper)
- **Memory entries updated:**
  - `project_jamtaba_video_port.md` (branch name + attribution)
  - `MEMORY.md` index (entry now "Native video port", new branch name)
  - `feedback_check_ci_before_tag.md` (new — always verify CI before tagging)

## Background Processes

- **`b554si149`** — `gh run watch 25996593334 --exit-status` — watching the decisive CI run. **Will notify when complete.** Do NOT proactively poll.
- **Other earlier watchers (`b6wlqkrtp`, `bg47v34w9`, `b87d6zt53`, `bzd3lbbcr`)** — all completed before pause; their target runs are either superseded or known-failed.

<context>
The session arc:
1. `/gsd-review --phase 21` — codex flagged 10 HIGH bugs (audio-thread RT-safety, VideoRecvBufferView ownership, lifetime triangle, lazy decoder startup, etc.)
2. `/gsd-plan-phase 21 --reviews` — planner revised + plan-checker found 3 follow-up issues (B-1/W-1/W-3) + planner closed them + checker passed
3. User asked: "push and make a small beta release and rename the branch, don't mention jamtaba, credit ninjamzap"
4. Branch renamed, pushed, release created — but CI was already broken (pre-existing Phase 14.3 issue). Release rolled back.
5. 6 CI fix commits + 2 scope-defer commits later: mac-arm64-only CI should work
6. Currently waiting on CI run 25996593334 to confirm

User has been actively steering decisions throughout — asked "if the mac build succeeds then put that on the release" — they want a mac arm64 binary on a clean v1.1-beta.20.6 release once CI is green. They want NinjamZap properly credited, no "jamtaba" anywhere.
</context>

<next_action>
**First, check `b554si149` watcher status** (it's a background `gh run watch 25996593334 --exit-status`). If it completed:

```bash
gh run view 25996593334 --json conclusion,jobs --jq '{conclusion, jobs: [.jobs[] | {name, conclusion}]}'
```

If `build-macos.conclusion == "success"`:

```bash
# Download artifact
gh run download 25996593334 -n JamWide-macOS -D /tmp/
ls /tmp/JamWide-macOS.tar.gz

# Verify release-notes still exist (may be cleared from /tmp)
[ -f /tmp/release-notes-v1.1-beta.20.6.md ] && echo "OK" || echo "REGENERATE — see release-notes-source section below"

# Create release with binary attached
gh release create v1.1-beta.20.6 \
  --target 75e3dd3 \
  --notes-file /tmp/release-notes-v1.1-beta.20.6.md \
  --prerelease \
  /tmp/JamWide-macOS.tar.gz

# Update docs/download.md href (was at 73c957a before rollback — same change)
# Edit: change https://github.com/mkschulze/JamWide/releases/tag/v1.1-beta.20.5 → v1.1-beta.20.6
gsd-sdk query commit "docs(release): point download page at v1.1-beta.20.6" --files docs/download.md
git push origin quick/260515-0pc-native-video-port
```

If CI failed, read the failure log: `gh run view 25996593334 --log-failed | tail -50` and triage.

**Orthogonal next step (no time pressure):** `/gsd-execute-phase 21` to start implementing the Phase 21 plans.
</next_action>
