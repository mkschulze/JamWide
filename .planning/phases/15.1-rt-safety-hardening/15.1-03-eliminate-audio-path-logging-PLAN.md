---
phase: 15.1
plan: 03
slug: eliminate-audio-path-logging
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/njclient.cpp
  - src/core/njclient.h
autonomous: true
requirements:
  - ROADMAP-15.1-S2  # auditor reports zero CRITICAL on audio path (closes CR-04, H-01, H-02, L-02)
  - ROADMAP-15.1-S5  # writeLog/writeUserChanLog/JAMWIDE_DEV_BUILD fopen block removed from audio path
finding_ids: [CR-04, H-01, H-02, L-02]
review_response:
  codex_concerns_addressed:
    - "Per-plan delta for 15.1-03 (Codex review): writeUserChanLog body is removed/compile-guarded after callers go away — no inert dead code retained."
must_haves:
  truths:
    - "writeLog and writeUserChanLog are not called from any audio-thread function"
    - "writeUserChanLog FUNCTION DEFINITION is removed (or wrapped in #if 0) since it has zero remaining callers after this plan; no inert dead code retained per Codex review delta"
    - "JAMWIDE_DEV_BUILD fopen(/tmp/jamwide.log) block at process_samples is deleted unconditionally"
    - "writeLog API itself remains (used by non-audio-thread callers like Disconnect, error paths)"
  artifacts:
    - path: src/core/njclient.cpp
      provides: "audio-path log call sites removed; writeUserChanLog body removed/compile-guarded"
      contains: "// 15.1-03 CR-04"
    - path: src/core/njclient.h
      provides: "writeUserChanLog declaration removed (or marked deprecated) if it was public"
  key_links:
    - from: "src/core/njclient.cpp::on_new_interval"
      to: "(no log calls)"
      via: "deletion"
      pattern: "writeLog\\("
    - from: "src/core/njclient.cpp::mixInChannel"
      to: "(no writeUserChanLog calls)"
      via: "deletion"
      pattern: "writeUserChanLog\\("
threat_model_severity: low
---

<objective>
Per CONTEXT D-03, eliminate audio-thread logging entirely. Delete the `writeLog("interval %d ...")` call at on_new_interval:2693, the `writeUserChanLog` calls at on_new_interval:2753 / mixInChannel:2467 / mixInChannel:2667, and the `JAMWIDE_DEV_BUILD` `fopen("/tmp/jamwide.log")` block at process_samples:2133-2139. Closes AUDIT findings CR-04, H-01, H-02, L-02.

**Codex review delta (incorporated):** the original plan kept `writeUserChanLog`'s function body intact "for now" with `[[maybe_unused]]`. Codex flagged this as inert dead code that should not survive the plan. **This revision removes the body**: after deleting the three audio-path callers, the `writeUserChanLog` definition itself is wrapped in `#if 0 ... #endif` with a comment, AND the prototype declaration in `njclient.h` (if any) is removed. The auxiliary `guidtostr` function — only reached via `writeUserChanLog` — receives the same treatment.

Purpose: Independent of state-sync queues; pure deletion sub-plan with no infrastructure dependency. Per RESEARCH § "Open Questions for Planner" #3, default is no audio-thread logging; planner default applies (no SPSC-routed log path).

Output: Audio-thread call sites of writeLog/writeUserChanLog removed; writeUserChanLog body removed/compile-guarded (no inert code); dev-build fopen block deleted; the writeLog API itself remains in place for non-audio-thread callers (Connect/Disconnect/error paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md
@.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md
@.planning/phases/15.1-rt-safety-hardening/15.1-RESEARCH.md
@.planning/phases/15.1-rt-safety-hardening/15.1-REVIEWS.md

<interfaces>
<!-- writeLog signature (njclient.cpp:683) - DO NOT CHANGE THE API -->
```cpp
void NJClient::writeLog(const char *fmt, ...);
// Used by Connect()/Disconnect()/error paths — must remain callable.
```

<!-- writeUserChanLog signature (njclient.cpp:2675) - WILL BE REMOVED in this plan -->
```cpp
void NJClient::writeUserChanLog(const char *prefix, RemoteUser *user, RemoteUser_Channel *chan, int chanidx);
// Currently has zero non-audio-thread callers; planner removes audio-thread callers AND the body.
```

<!-- guidtostr (njclient.cpp:489) - reached only via writeUserChanLog; remove with it -->
```cpp
static void guidtostr(const unsigned char *guid, char *buf);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete audio-path writeLog/writeUserChanLog call sites, the JAMWIDE_DEV_BUILD fopen block, AND the writeUserChanLog/guidtostr bodies (closes CR-04, H-01, H-02, L-02; addresses Codex per-plan delta)</name>
  <files>src/core/njclient.cpp, src/core/njclient.h</files>
  <read_first>
    - src/core/njclient.cpp lines 489-500 (guidtostr — auxiliary function reached only via writeUserChanLog)
    - src/core/njclient.cpp lines 683-700 (writeLog implementation — DO NOT delete; only audio-path callers go)
    - src/core/njclient.cpp lines 1955-2180 (process_samples, including the JAMWIDE_DEV_BUILD block at 2133-2139)
    - src/core/njclient.cpp lines 2379-2470 (mixInChannel — the writeUserChanLog at 2467)
    - src/core/njclient.cpp lines 2630-2680 (mixInChannel tail recursion — writeUserChanLog at 2667; writeUserChanLog implementation at 2675)
    - src/core/njclient.cpp lines 2690-2770 (on_new_interval — writeLog at 2693 and writeUserChanLog at 2753)
    - src/core/njclient.h (search for writeUserChanLog prototype; remove if present)
    - .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md findings CR-04 (lines 180-186), H-01 (lines 253-256), H-02 (lines 258-261), L-02 (lines 303-305)
    - .planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md D-03 (audio-thread logging eliminated in production paths)
    - .planning/phases/15.1-rt-safety-hardening/15.1-REVIEWS.md (Codex per-plan note for 15.1-03 — "Compile-guard or remove dead writeUserChanLog body after callers go away — don't leave inert code")
  </read_first>
  <action>
    Per CONTEXT D-03, AUDIT findings CR-04/H-01/H-02/L-02, and **Codex per-plan delta for 15.1-03**:

    1. **AUDIT CR-04** — `src/core/njclient.cpp:2693` inside `on_new_interval`. Find the line that looks like:
       ```cpp
       writeLog("interval %d ...", ...);
       ```
       Delete this single statement. Replace with a comment for traceability:
       ```cpp
       // 15.1-03 CR-04: writeLog removed from audio path; was diagnostic noise per CONTEXT D-03.
       ```

    2. **AUDIT H-02** — three writeUserChanLog audio-path call sites:
       - `src/core/njclient.cpp:2467` inside `mixInChannel` llmode advance
       - `src/core/njclient.cpp:2667` inside `mixInChannel` tail-recursion advance
       - `src/core/njclient.cpp:2753` inside `on_new_interval` per-channel loop

       At each site, delete the `writeUserChanLog(...)` call. Replace each with a comment:
       ```cpp
       // 15.1-03 H-02: writeUserChanLog removed from audio path.
       ```

    3. **Codex review delta — remove the writeUserChanLog body, do NOT leave inert code.**

       After step 2, `writeUserChanLog` has zero callers anywhere in the codebase (it was only ever called from audio-path sites; non-audio callers do not exist per the previous read of the file). Per Codex per-plan note: do not retain `[[maybe_unused]]` dead code.

       Remove the function body at `src/core/njclient.cpp:2675-2688` by wrapping it in `#if 0 ... #endif` with a comment:
       ```cpp
       // 15.1-03 H-02 (Codex review delta): body retained as #if 0 to preserve git blame
       // history; not compiled in. If a future need arises, restore via SPSC-mediated logging
       // path (RESEARCH § Open Questions #3) — do NOT restore in-place audio-path call.
       #if 0
       void NJClient::writeUserChanLog(const char *prefix,
                                       RemoteUser *user,
                                       RemoteUser_Channel *chan,
                                       int chanidx)
       {
           // ... ORIGINAL BODY PRESERVED VERBATIM ...
       }
       #endif
       ```

       In `src/core/njclient.h` (if a public prototype declaration exists for writeUserChanLog), remove or compile-guard it. The class member function declaration must not survive in a form that callers could reach. Acceptable patterns:
         (a) Delete the declaration outright (preferred — simplest).
         (b) Wrap in `#if 0 ... #endif` matching the body (acceptable if header history must be preserved).
       Per Codex review, prefer (a). Verify by grep.

    4. **AUDIT L-02 + Codex delta** — `guidtostr` (njclient.cpp:489-495) was only reached via `writeUserChanLog`. After step 3, it has zero callers. Apply the same treatment:
       ```cpp
       // 15.1-03 L-02: guidtostr was reached only via writeUserChanLog; both gone now.
       #if 0
       static void guidtostr(const unsigned char *guid, char *buf) { /* ... */ }
       #endif
       ```

       OR delete outright if the function is `static` and has no other concerns. Prefer outright deletion since it's `static` (file-local scope, no API impact).

    5. **AUDIT H-01** — `src/core/njclient.cpp:2133-2139` inside `process_samples`. The current code is:
       ```cpp
       #ifdef JAMWIDE_DEV_BUILD
       if (!m_debug_logged_remote) {
           FILE* lf = fopen("/tmp/jamwide.log", "a");
           if (lf) {
               fprintf(lf, ...);
               fclose(lf);
           }
           m_debug_logged_remote = 1;
       }
       #endif
       ```
       Delete the **entire** `#ifdef JAMWIDE_DEV_BUILD ... #endif` block unconditionally per CONTEXT D-03 ("delete it unconditionally — it's a one-shot debug log that's been there since dev bringup and has zero ongoing value"). Replace with a comment:
       ```cpp
       // 15.1-03 H-01: JAMWIDE_DEV_BUILD fopen("/tmp/jamwide.log") block removed unconditionally.
       ```

    6. **Verify the writeLog API survives** — `grep -n 'writeLog\\b' src/core/njclient.cpp` should still show the implementation at line ~683 and the call sites in non-audio-thread functions (Connect/Disconnect/error paths). Do NOT delete those.

    7. **Audit-driven sanity checks**:
       - `m_debug_logged_remote` member (declared in njclient.h) is now unused. Remove its declaration (Codex preference: no inert code).
       - Run `grep -nE 'writeUserChanLog|guidtostr|m_debug_logged_remote' src/core/njclient.{cpp,h}` and confirm: ZERO active references; only the `#if 0 ... #endif` blocks for traceability (or none, if you chose outright deletion).
  </action>
  <verify>
    <automated>./scripts/build.sh --tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'writeLog\(|writeUserChanLog\(' src/core/njclient.cpp` returns NO matches inside the line ranges 1955-2280 (process_samples), 2379-2672 (mixInChannel), 2690-2770 (on_new_interval). Allowed matches are inside writeLog implementation (~line 683) and any non-audio-thread callers (Connect/Disconnect/error paths).
    - `grep -n 'fopen.*jamwide\.log\|fopen.*"/tmp/jamwide' src/core/njclient.cpp` returns NO matches.
    - `grep -n 'JAMWIDE_DEV_BUILD' src/core/njclient.cpp` returns NO matches (the block is gone unconditionally).
    - **Codex delta acceptance:** `writeUserChanLog` is either fully deleted OR present only inside `#if 0 ... #endif` blocks. `grep -A 1 'void NJClient::writeUserChanLog' src/core/njclient.cpp` shows the line ABOVE it is `#if 0` (or returns no match if deleted outright).
    - **Codex delta acceptance:** `guidtostr` and `m_debug_logged_remote` are similarly removed or guarded.
    - `grep -nE '\\[\\[maybe_unused\\]\\].*writeUserChanLog|\\[\\[maybe_unused\\]\\].*guidtostr' src/core/njclient.cpp src/core/njclient.h` returns NO matches (no inert-code form retained per Codex).
    - `grep -n '15\.1-03' src/core/njclient.cpp` returns at least three deletion-sentinel comments (one per CR-04, H-02 cluster, H-01).
    - `./scripts/build.sh --tests` exits 0.
    - On a dev-build run, `/tmp/jamwide.log` is NOT created (verified by absence after a 30s standalone session — see UAT row in VALIDATION.md).
  </acceptance_criteria>
  <done>All audio-thread log call sites are gone; writeUserChanLog body and guidtostr are removed or compile-guarded (no inert code per Codex); writeLog API survives for non-RT callers; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio thread → libc stdio | writeLog vfprintf takes libc FILE lock and issues write() syscall — priority inversion source |
| audio thread → filesystem | fopen("/tmp/jamwide.log") may stat-cache-miss on /tmp, causing audio dropout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-03-01 | Denial of Service | writeLog vfprintf on audio thread | mitigate | Delete the call sites entirely. The writeLog API itself remains for non-audio-thread callers, which already accept the cost. Verified by grep on audio-path functions. |
| T-15.1-03-02 | Denial of Service | fopen("/tmp/jamwide.log") on audio thread (dev build) | mitigate | Delete the JAMWIDE_DEV_BUILD block unconditionally per D-03; the diagnostic value is gone. Verified by absence of /tmp/jamwide.log after a session. |
| T-15.1-03-03 | Information Disclosure | loss of post-mortem visibility into interval transitions | accept | Per CONTEXT D-03 the interval-N spam is not actionable; if a future regression needs visibility, an SPSC-routed log path can be added in a follow-up phase (RESEARCH § Open Questions #3). |
| T-15.1-03-04 | Tampering | inert dead code (writeUserChanLog body, guidtostr) misleads future maintainers | mitigate | Per Codex review delta, body is wrapped in `#if 0 ... #endif` (or fully deleted) — never retained as `[[maybe_unused]]` live code. Acceptance criteria includes grep verification that the `[[maybe_unused]]` form does NOT exist. |
</threat_model>

<verification>
- Per VALIDATION.md row 15.1-03:
  - `grep -n 'writeLog\\|writeUserChanLog' src/core/njclient.cpp` shows no callers from AudioProc/process_samples/mixInChannel/on_new_interval; writeUserChanLog body either gone or `#if 0`'d
  - Auditor re-run sees no log calls in the trace map; CR-04, H-01, H-02, L-02 cleared
  - Listening UAT: same protocol as 15.1-02; confirm /tmp/jamwide.log no longer created on dev builds
</verification>

<success_criteria>
- Audio-thread call sites of writeLog/writeUserChanLog are deleted (grep evidence).
- writeUserChanLog body is removed or `#if 0`'d (no `[[maybe_unused]]` inert form per Codex).
- guidtostr and m_debug_logged_remote follow the same fate (removed or guarded).
- JAMWIDE_DEV_BUILD fopen block is deleted unconditionally.
- Build succeeds (no compile errors from dangling references).
- writeLog API itself remains usable by non-RT callers.
- Auditor agent (post sub-plan) reports CR-04, H-01, H-02, L-02 → cleared.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-rt-safety-hardening/15.1-03-SUMMARY.md` capturing:
- Files modified with line ranges of deletions
- Confirmation that writeUserChanLog body and guidtostr were fully removed (or `#if 0`'d) — no inert dead code per Codex review delta
- Confirmation that writeLog API survives for non-audio callers
- Auditor agent re-run result (CR-04, H-01, H-02, L-02 → cleared)
</output>
