---
context: phase
phase: 23-macos-universal-windows-build-codesign
task: linux iteration (3 of 3 cross-platform restorations)
total_tasks: 3
status: paused
last_updated: 2026-05-19T07:17:42.766Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Production helpers wrapped in test-only `#ifdef JAMWIDE_BUILD_TESTS` gate | Phase 21-01 commit `629367b` opened a `#ifdef JAMWIDE_BUILD_TESTS` block at `src/core/njclient.cpp:3509` around `IsVideoFourcc` (a real test wrapper); the matching `#endif` was at line 4679, accidentally enclosing ~1100 lines of Phase 21-01/21-02/21-03 production helpers. Local dev defaulted `BUILD_TESTS=ON` so it compiled; CI with `BUILD_TESTS=OFF` failed at link time with 6 unresolved NJClient symbols. Hidden for ~2 weeks until the first post-commit-629367b CI run with the matching configure. | advisory | When opening a `#ifdef JAMWIDE_BUILD_TESTS` block, write the matching `#endif` immediately and only add functions inside that explicitly belong in the test wrapper bucket. The fix (commit `710a77b`) split the gate into part-1 (just `IsVideoFourcc`) and part-2 (the test-only Dispatch helpers further down). |
| MSYS2 ffmpeg vendoring "works on my laptop" but breaks in CI without explicit toolchain packages | The script `scripts/build_ffmpeg_lgpl.sh` Windows leg requires `gendef` + mingw64 `dlltool` + nasm + autotools. base-devel doesn't include the mingw64-prefixed packages. Each missing piece surfaced as a cryptic error one CI run apart (`openh264 >= 1.3.0 not found using pkg-config` actually meant "gendef didn't produce the import library"). Took 3 commits to clear (`de0e71f` install fix, `ff044ef` MSVC .lib generation, `50b7678` bundling). | advisory | When the script encounters a "WARNING: X not on PATH" message, treat it as a fatal — the downstream symptom will be misleading. Better: post-Phase-23 the script doesn't run on CI at all; bundled trees in `libs/ffmpeg/<platform>/` are the source of truth (commits `a51bb04` mac-arm64, `50b7678` windows-x86_64). Linux next. |

_The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows. No `blocking` constraints in this handoff — both anti-patterns are `advisory` because they've already been structurally fixed (gate split + bundling)._

<current_state>
Phase 23 is "regression-fix scope" per `23-CONTEXT.md`. Two of three cross-platform restorations done + tagged:

- ✅ **macOS Universal** (arm64 + x86_64): shipped in `v1.1-beta.20.7` (commit `7421a7b`) and `v1.1-beta.20.8`. Universal binary verified via `lipo -info` on Standalone + VST3 + AU + CLAP.
- ✅ **Windows x64**: shipped in `v1.1-beta.20.8` (commit `50b7678`). Unsigned (no Windows cert); SmartScreen warning documented.
- 🔜 **Linux receive-only**: not started. `juce::CameraDevice` is referenced unconditionally in `juce/video/native/JamWideCameraDevice.{h,cpp}`; `juce_video` has no `juce_CameraDevice_linux.h`. Linux build also gated `if: ${{ false }}` in the workflow.

Branch `quick/260515-0pc-native-video-port` is ~14 commits ahead of where the day started; HEAD is `d230576` (docs(download): advertise v1.1-beta.20.8).
</current_state>

<completed_work>

### Today's session (2026-05-19 — accidentally a 14-commit, 2-tag day)

**Phase 22 closeout** (started this morning before pivoting to Phase 23):
- `2783455` UAT fix chain — variant C (`kHoldCapDrop` 4 → 32) + Javier @ NinjamZap encoder fixes + 5 Phase-22 + 3 Phase-21 fixes + diag fprintf strip + test sub-test 8 update
- `b0bf430` env-var-gated tx-side `.h264` capture for Javier's ffmpeg sanity test
- `012bf47` High preset 720p30 @ 1500 kbps + matching receive surface

**Phase 23 mac universal restoration:**
- `710a77b` fix(21): split `JAMWIDE_BUILD_TESTS` gate — production helpers were accidentally enclosed (the macOS CI regression that surfaced this morning)
- `b58c97c` ci(mac): upload macos-arm64 ffmpeg tree as one-shot artifact
- `a51bb04` chore(ffmpeg): vendor macos-arm64 prebuilt tree (149 files, 5.5 MB)
- `7421a7b` feat(ci): macOS universal build (arm64 + x86_64) via lipo'd ffmpeg tree — `cmake/ffmpeg.cmake` detects multi-arch CMAKE_OSX_ARCHITECTURES and lipo's per-arch dylibs into universal at configure time

**Phase 23 windows x64 restoration:**
- `4634073` ci: enable Windows-x86_64 build + add to release artifacts
- `de0e71f` ci(windows): install gendef + mingw64 binutils so openh264 link-test succeeds
- `ff044ef` ci(windows): generate MSVC `.lib` import libraries for ffmpeg DLLs — same gendef + dlltool pattern as the openh264 fix, applied to all ffmpeg DLLs
- `f31bb80` ci(windows): upload vendored windows-x86_64 ffmpeg tree as one-shot artifact
- `50b7678` chore(ffmpeg): vendor windows-x86_64 prebuilt tree + strip from-source CI (140 files, 7.3 MB; CI Windows lane now ~6 min instead of 25+)

**Tags shipped:**
- `v1.1-beta.20.7` — macOS Universal (arm64 + x86_64). 48 MB. Signed + notarized.
- `v1.1-beta.20.8` — macOS Universal + Windows x64. 48 MB + 9.7 MB. Mac signed+notarized; Windows unsigned (SmartScreen warning documented).

**Docs:**
- `docs/download.md` advertises v1.1-beta.20.8 as latest; Linux section reduced to a footnote pointing at v1.1-beta.20.5 until 20.9 ships.
- Memory `reference_javier_ninjamzap_encoder_review` added with full review text + VT-compat fix attribution + ffmpeg sanity-test recipe.

**Phase 23 artifacts (commit history earlier today):**
- `23-CONTEXT.md` — regression-fix scope (per user reframe), inventory of 3 platform regressions + locked decisions
- `23-DISCUSSION-LOG.md` — audit trail of discuss-phase
</completed_work>

<remaining_work>

### Linux iteration (the only Phase 23 work left)

1. **Source surgery — `juce/video/native/JamWideCameraDevice.{h,cpp}`:**
   - `.h` line 25: `#include <juce_video/juce_video.h>` → wrap in `#if JUCE_MAC || JUCE_WINDOWS`
   - `.h` line 113: `std::unique_ptr<juce::CameraDevice> juceCamera_;` → conditional (forward-declare `juce::CameraDevice` for Linux so the pointer can be held without the full type)
   - `.h` line 129: `void onOpenResult(juce::CameraDevice* dev, ...)` → conditional
   - `.cpp` line 26: `class CameraListener : public juce::CameraDevice::Listener` → conditional; provide Linux stub class
   - `.cpp` line 320: `juce::CameraDevice::openDeviceAsync(...)` → wrapped; Linux path immediately reports NoHardware
   - `.cpp` line 487: `if (juce::CameraDevice::getAvailableDevices().isEmpty())` → wrapped; Linux returns true (no devices)

2. **Linux UI affordance:** reuse the existing "Camera unavailable" fallback dialog pattern (Phase 19 REAPER fallback) with a Linux-specific message: "Camera not available on Linux (V4L2 capture coming in v1.4)".

3. **Re-enable build-linux CI gate:** remove `if: ${{ false }}` from `.github/workflows/juce-build.yml` build-linux job.

4. **Capture `libs/ffmpeg/linux-x86_64/` via one-shot upload-artifact step** (same pattern as mac-arm64 + windows-x86_64). Then commit, strip from-source build steps from Linux CI job.

5. **Add build-linux to release job `needs:`** + `JamWide-Linux.tar.gz` to upload list.

6. **Tag `v1.1-beta.20.9`** as the first all-three-platform beta. Update `docs/download.md` to drop the Linux footnote pointing at .20.5.
</remaining_work>

<decisions_made>

See `HANDOFF.json` `decisions` array (8 captured) and `23-CONTEXT.md` D-01 through D-08. Highlights:

- **Phase 23 = regression-fix scope, not greenfield.** User reframe locked the framing.
- **Bundle ffmpeg trees in repo** (mac-arm64, mac-x86_64, windows-x86_64 done; linux-x86_64 next). Eliminates ~25 min/run CI cost AND the entire MSYS2/autotools/openh264-source-build toolchain-rot class.
- **MSVC `.lib` import libraries via gendef + dlltool** for Windows. Same pattern proven for openh264 then extended to all ffmpeg DLLs.
- **Ship Windows unsigned for beta**; SmartScreen friction documented inline in release notes + docs/download.md.
- **Universal mac via lipo at CMake-configure-time** (build-tree only, not committed). Local Intel-only path uses macos-x86_64/ directly without lipo overhead.
- **CI is the place where cross-platform happens; local stays Intel-only.** User directive.
- **Iterative fix-and-tag flow** instead of formal Phase 23 plan-phase. CONTEXT.md as regression inventory; no PLAN.md.
</decisions_made>

<blockers>
None. Linux iteration is straightforward; all the toolchain pain is on Windows + macOS-universal which are both done.
</blockers>

## Required Reading (in order)

1. `.planning/phases/23-macos-universal-windows-build-codesign/23-CONTEXT.md` — the regression inventory + 8 locked decisions. Read FIRST; everything that's happened today flows from these decisions.
2. `.planning/phases/23-macos-universal-windows-build-codesign/23-DISCUSSION-LOG.md` — audit trail of how the gray areas got resolved (the user reframe is in here verbatim).
3. `juce/video/native/JamWideCameraDevice.h` lines 25, 113, 129 — the three call sites that need `#if JUCE_MAC || JUCE_WINDOWS` guards (the rest of the .cpp's juce::CameraDevice references cascade from these).
4. `.github/workflows/juce-build.yml` build-linux job (currently `if: ${{ false }}`) — the comment block above the gate documents the reason (`Phase 19's camera-capture code uses juce::CameraDevice unconditionally`). That's exactly what gets fixed in step 1.
5. `git log --oneline 75e3dd3..HEAD` — today's 14-commit chain. The pattern is: iterate fix → push → wait for CI → diagnose next break → repeat. Linux follows the same rhythm.

## Critical Anti-Patterns (do NOT repeat these)

(See the Critical Anti-Patterns table at the top of this file for the full structured record.)

- **Wrapping production helpers in `#ifdef JAMWIDE_BUILD_TESTS`** → split-gate convention from `710a77b` keeps `IsVideoFourcc` in part-1 and the genuinely test-only Dispatch helpers in part-2; production code lives outside both.
- **Assuming MSYS2 `base-devel` covers the mingw64 toolchain** → it doesn't; need `mingw-w64-x86_64-binutils` + `mingw-w64-x86_64-tools-git` for dlltool + gendef. Past this point we're bundling, so the script isn't on the CI hot path anyway.

## Infrastructure State

- **CI on branch `quick/260515-0pc-native-video-port`** — green on both mac (universal) and Windows lanes. Linux skipped (gated). Latest run: `26068665308` for tag `v1.1-beta.20.8` (release published).
- **GitHub Releases** — `v1.1-beta.20.7` and `v1.1-beta.20.8` both live as pre-releases with custom-edited bodies. Test instructions in each.
- **`docs/download.md`** advertises 20.8 as latest. Footnote points Linux users at 20.5.
- **No active background processes** (all CI watches completed; no local servers).
- **Local env**: Intel x86_64 Mac, `build-juce/` warm with the v20.8 build, ffmpeg dylibs at `libs/ffmpeg/macos-x86_64/`.

## Pre-Execution Critique Required

Not applicable — Linux iteration follows the same fix-push-watch-iterate rhythm as the mac + Windows iterations. No pre-execution design artifact to critique.

<context>
The day went like this:

1. Resumed from yesterday's HANDOFF.json (variant C / Javier fixes / HD bump UAT chain pending). User picked variant C → committed `2783455`.
2. Verified Javier's encoder fixes end-to-end via QuickTime VideoToolbox + ffmpeg software decode. Bitstream-clean.
3. User asked to bump preset to 720p30 + matching receive surface → `012bf47`.
4. User asked to make it ARM mac + Windows compatible in CI → triggered Phase 23 discussion → user reframed scope to "restore what worked before Phase 14.3 ffmpeg vendoring broke it" → wrote `23-CONTEXT.md`.
5. User redirected away from formal Phase 23 plan: "iterate, don't run as a full phase". Began fix-push-watch cycle.
6. Macros: gate-fix → universal mac → Windows openh264 → Windows .lib → bundling → docs.
7. User asked "just ship the macOS release now" → tagged `v1.1-beta.20.7`.
8. User said "yes roll" → went after Windows iteration. After 3 commits Windows was green.
9. Tagged `v1.1-beta.20.8` with both platforms.
10. User invoked `/gsd-pause-work` to checkpoint.

The throughline: bundling pre-built ffmpeg trees in the repo is the strategy that makes all this stable. Linux is the last platform that needs it.

Mental model for next session: same iteration rhythm. Linux camera-code conditional is mechanical (~50-100 lines of `#if JUCE_MAC || JUCE_WINDOWS` guards in two files). The CI's Linux job is currently gated off — re-enable, push, see what next breaks. Capture artifact when it builds, commit, strip from-source step. Tag v1.1-beta.20.9.

No surprises expected in the Linux iteration. The hardest part is psyching up to commit ~50-100 lines of #ifdef guards. Use a single edit per file (the .h and the .cpp).
</context>

<next_action>
Open `juce/video/native/JamWideCameraDevice.h`. Wrap the `#include <juce_video/juce_video.h>` at line 25 in `#if JUCE_MAC || JUCE_WINDOWS` / `#else namespace juce { class CameraDevice; }` / `#endif` so the unique_ptr declaration on line 113 still compiles on Linux via forward declaration. Then open `juce/video/native/JamWideCameraDevice.cpp` and wrap every `juce::CameraDevice::*` and `juce::CameraDevice::Listener` reference in matching `#if JUCE_MAC || JUCE_WINDOWS` blocks. On Linux, the class methods (`toggle()`, `recheckPermission()`, `getState()`, etc.) should immediately transition to / report `CameraState::Unavailable` and emit `CameraFallbackCause::NoHardware` (existing enum value). Then in `.github/workflows/juce-build.yml`, remove `if: ${{ false }}` from the build-linux job and push. CI run will tell us what next breaks on Linux.
</next_action>
