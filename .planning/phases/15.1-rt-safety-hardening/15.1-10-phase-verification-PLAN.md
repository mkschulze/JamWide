---
phase: 15.1
plan: 10
slug: phase-verification
type: execute
wave: 5
depends_on:
  - 15.1-02
  - 15.1-03
  - 15.1-04
  - 15.1-05
  - 15.1-06
  - 15.1-07
  - 15.1-08
  - 15.1-09
files_modified:
  - .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md
  - .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md
autonomous: false  # phase-close human-verify checkpoint mandated by CONTEXT D-08
requirements:
  - ROADMAP-15.1-S1  # TSan build of test binaries passes a NINJAM session simulation with peer churn
  - ROADMAP-15.1-S2  # realtime-audio-reviewer agent reports zero CRITICAL findings
  - ROADMAP-15.1-S3  # Manual UAT on populated NINJAM server (3+ peers) — no glitches, no CPU spike
  - ROADMAP-15.1-S4  # m_users_cs and m_locchan_cs no longer acquired from AudioProc or callees
  - ROADMAP-15.1-S5  # writeLog/writeUserChanLog/JAMWIDE_DEV_BUILD fopen block removed from audio path
must_haves:
  truths:
    - "Full ctest suite passes under TSan: cd build-tsan && ctest --output-on-failure exits 0 with no TSan reports"
    - "realtime-audio-reviewer agent re-run on src/core/njclient.cpp and juce/JamWideJuceProcessor.cpp reports CRITICAL count = 0"
    - "30+ minute manual UAT under TSan standalone against populated NINJAM server (3+ peers) shows no audible glitches"
    - "Activity Monitor CPU history shows no recurring spike at the (BPI/BPM)*60 second interval period"
    - "m_users_cs, m_locchan_cs, m_misc_cs are no longer acquired from any audio-thread function (verified by grep + auditor)"
    - "writeLog and writeUserChanLog have zero callers reachable from audio-thread functions (verified by grep)"
    - "fopen / fprintf / fclose calls from JAMWIDE_DEV_BUILD block at the old njclient.cpp:2133 site are deleted (verified by grep)"
    - "VERIFICATION.md captures the closing audit, agent re-run output, UAT log, and any deferred follow-ups"
  artifacts:
    - path: .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md
      provides: "Closing realtime-audio-reviewer agent output: CRITICAL=0, HIGH=0, MEDIUM≤1 (M-03 assertion-only acceptable), LOW informational only"
      min_lines: 50
    - path: .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md
      provides: "Phase verification record: ctest result, TSan ctest result, agent diff vs 15.1-AUDIT.md, UAT log, deferred follow-ups list"
      min_lines: 100
      contains: "## VERIFICATION COMPLETE"
  key_links:
    - from: ".planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md"
      to: ".planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md"
      via: "diff: every CRITICAL from 15.1-AUDIT.md must be marked cleared in 15.1-AUDIT-final.md"
      pattern: "CR-0[1-9]|CR-1[0-2]"
    - from: ".planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md"
      to: ".planning/ROADMAP.md (phase 15.1 success criteria)"
      via: "every success criterion must have an evidence row in VERIFICATION.md"
      pattern: "ROADMAP-15.1-S[1-5]"
threat_model_severity: medium
---

<objective>
Final verification gate per CONTEXT D-07/D-08 + ROADMAP success criteria 1-5. Three-signal verification across the full phase:

1. **Full TSan ctest suite** — `cd build-tsan && ctest --output-on-failure` exits 0 with no TSan reports. Single TSan target (per RESEARCH § "TSan Build Setup (single target)") covers both NJClient core unit tests AND the JUCE callback boundary, closing Codex C-01 from DISCUSSION-LOG.

2. **realtime-audio-reviewer agent re-run** — spawn the auditor over `src/core/njclient.cpp` and `juce/JamWideJuceProcessor.cpp`. Output goes to `15.1-AUDIT-final.md`. Acceptance bar per CONTEXT D-08 + VALIDATION.md row 15.1-10: CRITICAL count = 0, HIGH count = 0, MEDIUM ≤ 1 (M-03 assertion-only is acceptable), LOW are informational.

3. **30+ minute manual UAT under TSan standalone** — `./scripts/build.sh --tsan && open build-tsan/JamWideJuce_artefacts/Standalone/JamWide.app`, connect to a populated NINJAM server with 3+ peers, exercise interval boundaries + peer churn (3+ join/leave cycles) + broadcast on/off cycling on local channels + sessionmode if applicable. Record Activity Monitor CPU history; verify no recurring spike at the `(BPI/BPM)*60` second interval period. Sign off in `15.1-VERIFICATION.md`.

This plan does NOT modify any production code. It only **verifies** the work of 15.1-02 through 15.1-09 and produces two deliverable documents:

- `15.1-AUDIT-final.md` — the closing auditor agent's output (the diff vs `15.1-AUDIT.md` is the proof that the 12 CRITICAL findings are cleared)
- `15.1-VERIFICATION.md` — the goal-backward verification record showing every ROADMAP-15.1-S{1..5} success criterion has evidence (test command + output, UAT log, agent run, grep results)

If any signal is RED — TSan reports, auditor finds CRITICAL, UAT shows glitches/spikes — the phase does NOT close. The blocker is documented in 15.1-VERIFICATION.md and a follow-up sub-plan (or revision) is filed against the failing piece.

Output: phase 15.1 closure with traceability from each ROADMAP success criterion back to its evidence; deferred follow-ups (CR-11 algorithmic residual, H-03 plugin-callback contract, MidiMapper audit) explicitly listed for v1.2 follow-up phases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md
@.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md
@.planning/phases/15.1-rt-safety-hardening/15.1-RESEARCH.md
@.planning/phases/15.1-rt-safety-hardening/15.1-VALIDATION.md
@.planning/phases/15.1-rt-safety-hardening/15.1-DISCUSSION-LOG.md

<interfaces>
<!-- realtime-audio-reviewer agent invocation pattern -->
<!-- Spawn from Claude Code via the Task tool: -->
<!--   Task(subagent_type="realtime-audio-reviewer", -->
<!--        prompt="Re-audit audio path post-15.1. Files: src/core/njclient.cpp, juce/JamWideJuceProcessor.cpp. Compare output to .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md (the pre-fix baseline). Expected: CRITICAL=0, HIGH=0, MEDIUM≤1, LOW informational only.") -->

<!-- TSan build invocation -->
<!-- ./scripts/build.sh --tsan        # configures build-tsan/ + builds standalone (per 15.1-04) -->
<!-- cd build-tsan && ctest --output-on-failure   # runs full ctest under -fsanitize=thread -->

<!-- Standalone TSan UAT -->
<!-- open build-tsan/JamWideJuce_artefacts/Standalone/JamWide.app -->
<!-- (or run via terminal to capture stderr TSan reports) -->

<!-- ROADMAP success criteria (verbatim from ROADMAP.md) — every plan must trace -->
<!--  S1: TSan build of test binaries passes a NINJAM session simulation with peer churn -->
<!--  S2: Auditor reports zero CRITICAL findings -->
<!--  S3: Manual UAT on 3+ peer server: no glitches, no CPU spike -->
<!--  S4: m_users_cs / m_locchan_cs no longer acquired from AudioProc or callees -->
<!--  S5: writeLog / writeUserChanLog / JAMWIDE_DEV_BUILD fopen block removed from audio path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full TSan ctest suite and capture results</name>
  <files>.planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15.1-rt-safety-hardening/15.1-VALIDATION.md row 15.1-10 (acceptance bar)
    - .planning/phases/15.1-rt-safety-hardening/15.1-RESEARCH.md § "TSan Build Setup (single target)" (lines 317-398)
    - scripts/build.sh (verify --tsan flag is in place from 15.1-04)
    - CMakeLists.txt (verify JAMWIDE_TSAN option + codesign gate are in place from 15.1-04)
  </read_first>
  <action>
    Per VALIDATION.md row 15.1-10 (TSan suite command) + RESEARCH § "TSan Build Setup":

    1. Build the TSan target. From the repo root:
    ```bash
    ./scripts/build.sh --tsan
    ```
    This configures `build-tsan/` with `JAMWIDE_BUILD_TESTS=ON + JAMWIDE_BUILD_JUCE=ON + JAMWIDE_TSAN=ON + -fsanitize=thread -g -O1` and builds the standalone + ctest suite. Per RESEARCH "macOS-specific TSan caveats" #1, the codesign block is gated off by JAMWIDE_TSAN=ON.

    Expected result: clean build. If any compile errors, halt and surface as a 15.1-04 regression.

    2. Run the full ctest suite under TSan:
    ```bash
    cd build-tsan && ctest --output-on-failure 2>&1 | tee ../.planning/phases/15.1-rt-safety-hardening/.tsan-ctest.log
    ```

    Capture stdout+stderr to a log file. Expected: every ctest target exits 0; **zero TSan WARNING or DATA RACE reports** in the output. If TSan flags a race, that's a real finding — investigate before phase closure.

    3. Begin authoring `.planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md` with the TSan ctest section:
    ```markdown
    # Phase 15.1 — RT-Safety Hardening — Verification Record

    **Verified:** {date}
    **Status:** {green|red}

    ---

    ## Signal 1 — Full TSan ctest suite (closes ROADMAP-15.1-S1)

    **Command:** `./scripts/build.sh --tsan && cd build-tsan && ctest --output-on-failure`
    **Build target:** `build-tsan/` (JAMWIDE_BUILD_TESTS=ON + JAMWIDE_BUILD_JUCE=ON + -fsanitize=thread -g -O1)
    **Result:** {pass|fail}
    **Tests run:** {N}
    **Tests passed:** {N}
    **TSan reports:** {count, attached log if any}

    Per-test summary:
    | Test name | Status | TSan reports |
    |---|---|---|
    | spsc_state_updates    | pass | 0 |
    | deferred_delete       | pass | 0 |
    | local_channel_mirror  | pass | 0 |
    | remote_user_mirror    | pass | 0 |
    | block_queue_spsc      | pass | 0 |
    | decode_media_buffer_spsc | pass | 0 |
    | decoder_prealloc      | pass | 0 |
    | decode_state_arming   | pass | 0 |
    | njclient_atomics      | pass | 0 |
    | flac_codec            | pass | 0 |
    | encryption            | pass | 0 |
    | video_sync            | pass | 0 |
    | (any JUCE-side ctest tests, e.g., osc_loopback, midi_mapping if registered) | pass | 0 |

    **Closes:** ROADMAP-15.1-S1.
    ```

    Fill the actual N values from the ctest log. If any test failed or TSan flagged a race, document the failure with full context — the phase does not close until resolved.
  </action>
  <verify>
    <automated>cd build-tsan &amp;&amp; ctest --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `build-tsan/` directory exists and was configured with TSan flags (`grep -E 'JAMWIDE_TSAN.*ON' build-tsan/CMakeCache.txt` returns at least one match).
    - `cd build-tsan && ctest --output-on-failure` exits 0.
    - The captured log `.planning/phases/15.1-rt-safety-hardening/.tsan-ctest.log` contains zero "WARNING: ThreadSanitizer" or "data race" lines.
    - `.planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md` exists with Signal 1 section filled out.
    - The Signal 1 section explicitly references ROADMAP-15.1-S1 in the "Closes" line.
  </acceptance_criteria>
  <done>Full TSan ctest suite is green; results captured in 15.1-VERIFICATION.md; ROADMAP-15.1-S1 evidenced.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run realtime-audio-reviewer agent; capture output to 15.1-AUDIT-final.md; diff vs 15.1-AUDIT.md</name>
  <files>.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md, .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md (the pre-fix baseline — the agent must clear every CR-XX finding from this file)
    - .planning/phases/15.1-rt-safety-hardening/15.1-VALIDATION.md row 15.1-10 (auditor severity expectations: CRITICAL=0, HIGH=0, MEDIUM≤1, LOW informational)
    - .planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md D-09/D-10 (auditor-first protocol, agent prompt structure)
    - .claude/agents/realtime-audio-reviewer.md (the agent's own prompt; informs what shape the output takes)
  </read_first>
  <action>
    Per CONTEXT D-09/D-10 + VALIDATION.md row 15.1-10:

    1. Spawn the agent via the Task tool from Claude Code:
    ```
    Task(subagent_type="realtime-audio-reviewer",
         prompt="Re-audit the audio path of NJClient post-15.1 fixes.
                 Scope: src/core/njclient.cpp (especially AudioProc, on_new_interval,
                 process_samples, mixInChannel) and juce/JamWideJuceProcessor.cpp
                 (processBlock and callees up to client->AudioProc).

                 Compare findings to the pre-fix baseline at
                 .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md (12 CRITICAL,
                 4 HIGH, 3 MEDIUM, 2 LOW). For each pre-fix CR-XX/H-XX/M-XX finding,
                 indicate cleared / mitigated / still-present.

                 Acceptance bar per CONTEXT D-08:
                 - CRITICAL count: 0
                 - HIGH count: 0
                 - MEDIUM count: ≤1 (M-03 assertion-only is acceptable per
                   .planning/phases/15.1-rt-safety-hardening/15.1-VALIDATION.md)
                 - LOW: informational only

                 Output structured per the agent's standard format. Save to
                 .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md.")
    ```

    2. Save the agent's output verbatim to `.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md`. The agent should produce a severity-ranked finding list with file:line citations matching the original AUDIT.md format.

    3. **Diff vs 15.1-AUDIT.md:** for each pre-fix finding, the agent's output (or a manual cross-check by the executor) must indicate disposition:

    | Pre-fix Finding | Severity | Sub-plan that closed it | Post-fix status |
    |---|---|---|---|
    | CR-01 (m_users_cs) | CRITICAL | 15.1-07 | cleared |
    | CR-02 (m_locchan_cs) | CRITICAL | 15.1-06 | cleared |
    | CR-03 (m_misc_cs / atomic promotion) | CRITICAL | 15.1-02 | cleared |
    | CR-04 (writeLog audio path) | CRITICAL | 15.1-03 | cleared |
    | CR-05 (delete chan->ds in on_new_interval) | CRITICAL | 15.1-05 | cleared |
    | CR-06 (delete chan->next_ds[0]) | CRITICAL | 15.1-05 | cleared |
    | CR-07 (delete userchan->ds × 5 sites) | CRITICAL | 15.1-05 | cleared |
    | CR-08 (start_decode on audio thread) | CRITICAL | 15.1-09 | cleared |
    | CR-09 (lc->m_bq.AddBlock — BufferQueue) | CRITICAL | 15.1-07 | cleared |
    | CR-10 (m_wavebq->AddBlock) | CRITICAL | 15.1-07 | cleared |
    | CR-11 (decoder internal allocations) | CRITICAL | 15.1-08 + 15.1-09 | mitigated (steady-state covered by Prealloc; algorithmic residual deferred) |
    | CR-12 (DecodeMediaBuffer mutex) | CRITICAL | 15.1-07 | cleared |
    | H-01 (JAMWIDE_DEV_BUILD fopen) | HIGH | 15.1-03 | cleared |
    | H-02 (writeUserChanLog) | HIGH | 15.1-03 | cleared |
    | H-03 (lc->cbf plugin callback) | HIGH | (deferred — API contract) | accepted, follow-up filed |
    | H-04 (fread in runDecode) | HIGH | 15.1-09 | cleared |
    | M-01 (tmpblock.Resize) | MEDIUM | 15.1-08 | cleared |
    | M-02 (Compact realloc) | MEDIUM | 15.1-08 | reclassified LOW (analysis in RESEARCH § "M-02 — borderline") |
    | M-03 (juce::AudioBuffer::setSize assertion) | MEDIUM | 15.1-08 | cleared (jassert added) — acceptable as MEDIUM remaining per VALIDATION.md |
    | L-01 (mixInChannel recursion) | LOW | (informational) | accept |
    | L-02 (snprintf in writeUserChanLog) | LOW | 15.1-03 | cleared (writeUserChanLog removed) |

    4. Append to `15.1-VERIFICATION.md`:
    ```markdown
    ---

    ## Signal 2 — realtime-audio-reviewer agent re-run (closes ROADMAP-15.1-S2)

    **Agent:** `realtime-audio-reviewer`
    **Scope:** src/core/njclient.cpp + juce/JamWideJuceProcessor.cpp
    **Output:** `.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md`
    **Result:** {pass|fail}

    Severity totals (post-fix vs pre-fix from 15.1-AUDIT.md):
    | Severity | Pre-fix (AUDIT.md) | Post-fix (AUDIT-final.md) |
    |---|---|---|
    | CRITICAL | 12 | 0 |
    | HIGH     | 4  | {count, expected ≤1 — H-03 may carry as accepted} |
    | MEDIUM   | 3  | {count, expected ≤1 — M-03 assertion-only acceptable} |
    | LOW      | 2  | {count, informational} |

    Disposition table: see 15.1-AUDIT-final.md (or inline diff above).

    Acceptance per CONTEXT D-08: CRITICAL=0 ✓; HIGH=0 (or H-03 documented as accepted) ✓; MEDIUM ≤1 ✓.

    **Closes:** ROADMAP-15.1-S2.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md &amp;&amp; ! grep -E "^.+CRITICAL" .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md | grep -v "cleared\|mitigated\|accepted"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md` exists with at least 50 lines.
    - Agent output explicitly lists CRITICAL count as 0. Verify with `grep -iE "critical.*[:=]\\s*0|critical count:?\\s*0|0 (?:CRITICAL|critical)" .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT-final.md` returning at least one match, OR the agent's standard output structure showing the CRITICAL section is empty.
    - Every pre-fix CR-XX finding (CR-01 through CR-12) is referenced in the disposition table with status (cleared/mitigated/accepted).
    - `15.1-VERIFICATION.md` Signal 2 section is appended with severity-totals table.
    - The Signal 2 section explicitly references ROADMAP-15.1-S2 in the "Closes" line.
  </acceptance_criteria>
  <done>Auditor agent re-run is green; output saved to AUDIT-final.md; disposition table complete; ROADMAP-15.1-S2 evidenced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT under TSan standalone — 30+ minutes on populated server, peer churn, CPU history</name>
  <files>.planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15.1-rt-safety-hardening/15.1-VALIDATION.md row 15.1-10 + § "Manual-Only Verifications" (the perceptual signals — audible glitches, CPU spike pattern)
    - .planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md D-07 (sign-off bar) + D-08 (acceptance gate)
    - .planning/phases/15.1-rt-safety-hardening/15.1-RESEARCH.md § "Sub-plan validation map" row 15.1-10 (UAT checkpoint specifics: 3+ peers, peer churn, broadcast toggling, sessionmode if applicable)
  </read_first>
  <what-built>
    All of 15.1-02 through 15.1-09 are merged (per CONTEXT D-06, each landed as its own atomic commit). The audio thread is free of mutex acquisitions, heap deallocations, file I/O, and codec-construction paths reachable from `AudioProc`. TSan is clean. Auditor is clean. The remaining signal is the perceptual / runtime UAT — does the actual user-facing audio behave correctly under load?

    Specifically built/changed across the phase:
    - 15.1-02: BPM/BPI/interval-pos atomic promotion (CR-03)
    - 15.1-03: removal of writeLog/writeUserChanLog/JAMWIDE_DEV_BUILD fopen from audio path (CR-04, H-01, H-02, L-02)
    - 15.1-04: SPSC infrastructure (payloads.h, --tsan flag, JAMWIDE_TSAN option)
    - 15.1-05: deferred-delete queue for DecodeState (CR-05, CR-06, CR-07)
    - 15.1-06: m_locchan_cs snapshot (CR-02)
    - 15.1-07: m_users_cs snapshot + BufferQueue replacement + DecodeMediaBuffer SPSC (CR-01, CR-09, CR-10, CR-12)
    - 15.1-08: pre-allocation hardening (M-01, M-02 reclassify, M-03 assertion, CR-11 mitigation)
    - 15.1-09: codec call-site integration — start_decode + runDecode-fread off audio thread (CR-08, H-04)
  </what-built>
  <how-to-verify>
    **Setup (5 minutes):**
    1. Build the TSan standalone if not already built: `./scripts/build.sh --tsan`
    2. Launch standalone: `open build-tsan/JamWideJuce_artefacts/Standalone/JamWide.app`. If macOS Gatekeeper blocks (TSan-instrumented binaries can be unsigned), right-click → Open from Finder.
    3. Open Activity Monitor → CPU → History (View → CPU History or `⌘2`). Pin the JamWide process. The CPU history graph for the JamWide process is the primary diagnostic.
    4. Open a Terminal and tail TSan output: launch the standalone from the terminal so stderr is visible (`./build-tsan/JamWideJuce_artefacts/Standalone/JamWide.app/Contents/MacOS/JamWide`). Watch for any `WARNING: ThreadSanitizer` lines during the session.

    **Connection + populated session (5 minutes):**
    5. Connect to a populated NINJAM server with at least 3 other peers. Suggested servers: `ninjamer.com:2049`, `jam.ninjam.com:2049`, or any test server known to have active users.
    6. Confirm audio is flowing: hear remote audio mixed in, see VU meters moving on remote channels.
    7. Note the BPM and BPI from the connection bar. Compute the interval period: `(BPI / BPM) * 60` seconds. Example: BPM=100, BPI=16 → interval = 9.6s. **Watch the CPU history for spikes recurring at this period.**

    **Peer churn cycle (10 minutes):**
    8. While remaining connected, perform 3+ peer-churn cycles. Each cycle: someone joins (or you toggle visibility), someone leaves (or you toggle a remote channel mute/solo). If no other users join/leave naturally, simulate by:
       - Toggling each remote user's mute/solo
       - Adjusting volume/pan on remote channels
       - Cycling broadcast on/off on your local channels
    9. Listen continuously. Note any pop, click, dropout, or glitch. Mark timestamps.

    **Sustained listening (10+ minutes):**
    10. Continue listening for at least 10 more minutes (total session ≥ 25 minutes). The CPU history should show a flat or smoothly-varying line — NO recurring spike at the interval period.
    11. If applicable: load a sessionmode file (older NINJAM clients used these for asynchronous overdubs); seek randomly 10 times. Watch for glitches at seek points.

    **Capture (5 minutes):**
    12. Take a screenshot of Activity Monitor → CPU → History showing the full JamWide session timeline. Save to `.planning/phases/15.1-rt-safety-hardening/uat-cpu-history.png` (or `.heic`).
    13. Capture the terminal output (TSan reports, if any) to `.planning/phases/15.1-rt-safety-hardening/.uat-stderr.log`.

    **Disconnection / clean exit (2 minutes):**
    14. Disconnect from the server. Quit the standalone. The terminal should show clean exit (return code 0). TSan reports any post-exit findings here — capture them.

    **Append to `15.1-VERIFICATION.md`:**
    ```markdown
    ---

    ## Signal 3 — Manual UAT under TSan standalone (closes ROADMAP-15.1-S3)

    **Build:** `build-tsan/JamWideJuce_artefacts/Standalone/JamWide.app`
    **Server:** {server:port}
    **Peer count:** {N peers, including self}
    **BPM/BPI:** {BPM} / {BPI}, interval period = {seconds} sec
    **Session duration:** {N minutes}, of which {M minutes} active listening
    **Peer churn cycles:** {N cycles, with details}

    **Audible glitches:** {none | timestamps + descriptions}
    **CPU history spike pattern:** {none | description}
    **TSan reports during session:** {0 | description}
    **Clean exit:** {yes | no}
    **Sessionmode test (if applicable):** {N seeks, glitches: none | description}

    **Artifacts:**
    - `.planning/phases/15.1-rt-safety-hardening/uat-cpu-history.png` — CPU history screenshot
    - `.planning/phases/15.1-rt-safety-hardening/.uat-stderr.log` — terminal log

    **Result:** {pass | fail}

    **Closes:** ROADMAP-15.1-S3.
    ```

    **If anything fails** (audible glitches, CPU spikes, TSan reports, crashes): the phase does NOT close. Document the failure with full reproducer steps. The session feedback becomes the trigger for either a revision pass on the relevant sub-plan(s) or a new follow-up sub-plan.
  </how-to-verify>
  <resume-signal>Type "approved" with the UAT timing and any notes (e.g., "approved — 30min, BPM=100 BPI=16, 4 peers, no glitches, no CPU spike, TSan clean"), or describe issues found (e.g., "click at 12:34 during peer-leave; CPU shows one spike at 9.6s mark — investigate").</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Goal-backward grep verification (S4 + S5) and finalize 15.1-VERIFICATION.md</name>
  <files>.planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md (work-in-progress from prior tasks)
    - .planning/ROADMAP.md (phase 15.1 success criteria S1-S5 verbatim)
    - .planning/phases/15.1-rt-safety-hardening/15.1-AUDIT.md (line citations for each violation; the "audio-thread call graph" trace map)
    - .planning/phases/15.1-rt-safety-hardening/15.1-DISCUSSION-LOG.md § "Truly deferred / out-of-scope follow-ups" (the explicit deferred list)
  </read_first>
  <action>
    Per goal-backward methodology — every ROADMAP success criterion needs evidence. Tasks 1-3 closed S1, S2, S3. This task closes S4 and S5 via grep verification, and writes the final phase-closure entry.

    1. **Verify ROADMAP-15.1-S4** ("m_users_cs and m_locchan_cs are no longer acquired from AudioProc or any function it calls"):

    Run these greps and capture results:
    ```bash
    # m_users_cs Enter/Lock from audio-thread functions
    grep -nE "m_users_cs\\.Enter|WDL_MutexLock.*m_users_cs|m_users_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"

    # Same for m_locchan_cs
    grep -nE "m_locchan_cs\\.Enter|WDL_MutexLock.*m_locchan_cs|m_locchan_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"

    # And m_misc_cs (CR-03)
    grep -nE "m_misc_cs\\.Enter|WDL_MutexLock.*m_misc_cs|m_misc_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"
    ```

    All three must return **zero matches**. If any returns matches, the corresponding sub-plan (15.1-02 / 15.1-06 / 15.1-07) is incomplete; phase does not close.

    Note: the greps above filter the function-context match by the second pipe. A more reliable check on the full function bodies: read AudioProc, process_samples, on_new_interval, and mixInChannel ranges (per AUDIT line citations) and confirm no mutex Enter/Leave instances. Cross-check with the auditor agent output from Task 2.

    2. **Verify ROADMAP-15.1-S5** ("writeLog / writeUserChanLog / the JAMWIDE_DEV_BUILD fopen block at njclient.cpp:2133 are removed from the audio path"):

    ```bash
    # writeLog and writeUserChanLog should have NO callers reachable from audio-thread functions
    grep -nE "writeLog\\(|writeUserChanLog\\(" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"

    # JAMWIDE_DEV_BUILD fopen block (originally at AUDIT line 2133-2139) should be deleted
    grep -nE 'fopen.*"\\/tmp\\/jamwide\\.log"|fopenUTF8.*"\\/tmp\\/jamwide\\.log"' src/core/njclient.cpp

    # Catch-all: any fopen / fprintf inside the AudioProc / process_samples / on_new_interval / mixInChannel function ranges
    awk '/^void NJClient::AudioProc/,/^}/' src/core/njclient.cpp | grep -nE "fopen|fprintf|fclose" || true
    awk '/^void NJClient::process_samples/,/^}/' src/core/njclient.cpp | grep -nE "fopen|fprintf|fclose" || true
    awk '/^void NJClient::on_new_interval/,/^}/' src/core/njclient.cpp | grep -nE "fopen|fprintf|fclose" || true
    awk '/^.*NJClient::mixInChannel/,/^}/' src/core/njclient.cpp | grep -nE "fopen|fprintf|fclose" || true
    ```

    All must return **zero matches**. (The catch-all may surface comments referencing fopen — manual inspection if false-positives.)

    Note: the writeLog API itself remains in src/core/njclient.cpp at line ~683 (used by non-audio-thread callers like Disconnect, error paths). That's correct per CONTEXT D-03; we only verify the audio-path callers are gone.

    3. **Append S4 and S5 sections to `15.1-VERIFICATION.md`:**
    ```markdown
    ---

    ## Signal 4 — Mutex elimination grep (closes ROADMAP-15.1-S4)

    **Command:**
    ```bash
    grep -nE "m_users_cs\\.Enter|WDL_MutexLock.*m_users_cs|m_users_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"
    grep -nE "m_locchan_cs\\.Enter|WDL_MutexLock.*m_locchan_cs|m_locchan_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"
    grep -nE "m_misc_cs\\.Enter|WDL_MutexLock.*m_misc_cs|m_misc_cs\\.Lock" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"
    ```
    **Result:** zero matches across all three (verified).

    **Closes:** ROADMAP-15.1-S4.

    ---

    ## Signal 5 — Logging / file I/O elimination grep (closes ROADMAP-15.1-S5)

    **Command:**
    ```bash
    grep -nE "writeLog\\(|writeUserChanLog\\(" src/core/njclient.cpp \
        | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"
    grep -nE 'fopen.*"\\/tmp\\/jamwide\\.log"|fopenUTF8.*"\\/tmp\\/jamwide\\.log"' src/core/njclient.cpp
    ```
    **Result:** zero matches.
    **writeLog API itself:** present at src/core/njclient.cpp:683 — retained for non-audio-thread callers per CONTEXT D-03 (Disconnect, error paths).

    **Closes:** ROADMAP-15.1-S5.

    ---

    ## Deferred Follow-ups (filed as separate v1.2 sub-tasks)

    Per DISCUSSION-LOG.md § "Truly deferred / out-of-scope follow-ups":

    1. **H-03** — `lc->cbf` plugin-callback contract on `SetLocalChannelProcessor`. Not a current-code violation; API-surface concern. Action: document the RT-safety contract on the API or remove the API. Filed as backlog item (no current callers in JamWide per pre-15.1 grep).
    2. **CR-11 algorithmic residual** — libogg / libFLAC internal heap activity during the first 3-5 frames per stream warm-up. Mitigated by 15.1-08 Prealloc on WDL wrappers + 15.1-09 moving construction to run thread; the algorithmic residual is bounded but not eliminated. Filed as v1.2 follow-up if 15.1-10 Instruments trace shows > 1 alloc/sec attributable to audio frames after 60s warm-up.
    3. **MidiMapper RT-safety audit** — `juce/JamWideJuceProcessor.cpp:528-529` calls midiMapper from processBlock. Out of declared phase scope. Filed as backlog item; recommend a dedicated audit pass.

    ---

    ## Phase Closure Summary

    | ROADMAP success criterion | Closing signal | Status |
    |---|---|---|
    | S1 — TSan ctest passes | Signal 1 (Task 1) | {pass} |
    | S2 — Auditor reports zero CRITICAL | Signal 2 (Task 2) | {pass} |
    | S3 — Manual UAT clean | Signal 3 (Task 3) | {pass} |
    | S4 — m_users_cs / m_locchan_cs gone from audio path | Signal 4 (Task 4) | {pass} |
    | S5 — writeLog / fopen gone from audio path | Signal 5 (Task 4) | {pass} |

    **Phase 15.1 — RT-Safety Hardening: VERIFIED.**

    ## VERIFICATION COMPLETE
    ```

    4. Update `.planning/STATE.md` if conventions require: bump `last_activity`, set phase 15.1 to "complete" status; record stopped_at = "Phase 15.1 verification complete". (If STATE.md is auto-updated by the GSD framework on plan completion, skip this manual edit.)

    5. Update `.planning/ROADMAP.md` Phase 15.1 entry: change `[ ] Phase 15.1` to `[x] Phase 15.1` with completion date; mark 15.1 row in the Progress table as Complete.
  </action>
  <verify>
    <automated>grep -c "## VERIFICATION COMPLETE" .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `15.1-VERIFICATION.md` contains all 5 signal sections (Signal 1-5) plus phase-closure summary table.
    - The five "Closes: ROADMAP-15.1-S{1..5}" lines are present.
    - `grep -E "m_users_cs\\.Enter|m_locchan_cs\\.Enter|m_misc_cs\\.Enter" src/core/njclient.cpp | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"` returns zero matches.
    - `grep -E "writeLog\\(|writeUserChanLog\\(" src/core/njclient.cpp | grep -E "AudioProc|process_samples|on_new_interval|mixInChannel"` returns zero matches.
    - `grep -E 'fopen.*jamwide\\.log|fopenUTF8.*jamwide\\.log' src/core/njclient.cpp` returns zero matches.
    - `grep -c "## VERIFICATION COMPLETE" .planning/phases/15.1-rt-safety-hardening/15.1-VERIFICATION.md` returns at least 1.
    - Deferred follow-ups section explicitly names H-03, CR-11 algorithmic residual, MidiMapper.
    - ROADMAP.md phase 15.1 line shows `[x]` (complete) with date.
  </acceptance_criteria>
  <done>All 5 ROADMAP success criteria evidenced; VERIFICATION.md complete with closure summary; deferred follow-ups documented; ROADMAP and STATE updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor agent → planning record | Agent output must be saved verbatim; tampering would falsely close the phase |
| TSan suppressions → false-clean signal | Any TSan suppressions added during 15.1 must be enumerated in VERIFICATION.md so a future review can audit them |
| UAT subjective signal → phase closure | Manual UAT depends on user's perception; absence of glitches over 30 min is statistical, not deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-10-01 | Repudiation | Auditor agent re-run yields a different result on re-invocation; phase-closure record disagrees with reality | mitigate | Save the agent's verbatim output to 15.1-AUDIT-final.md; the file is committed to git, immutable. If a later auditor run disagrees, the diff is auditable against this commit. |
| T-15.1-10-02 | Information disclosure | TSan suppressions file (tsan.supp) silently masks real races | mitigate | Per RESEARCH § "macOS-specific TSan caveats" #4, suppressions are noted only if signal becomes noisy; default is no suppressions. If any are added, VERIFICATION.md "Deferred Follow-ups" must enumerate them and explain why each is a known false-positive (libvorbis / OpenSSL only). |
| T-15.1-10-03 | Denial of Service | UAT signal is anecdotal; rare bug surfaces only at 99th-percentile session length | accept | Phase 15.1's bar is "no observed glitches in 30 min populated UAT" — a strong signal but not a proof. Per CONTEXT D-08, this is the documented bar. Future regressions would surface in user reports; 14.2's CPU-spike issue serves as the prior-art reproducer if symptoms recur. |
| T-15.1-10-04 | Tampering (planning fidelity) | A signal is documented as "pass" when it actually failed | mitigate | Each signal section in VERIFICATION.md cites exact commands, exact output excerpts, and (for S3) attached artifact files. The audit trail is reproducible from the recorded inputs. |
| T-15.1-10-05 | Repudiation | Deferred follow-ups are silently dropped at phase close | mitigate | Task 4 step 3 explicitly enumerates the deferred items (H-03, CR-11 algorithmic residual, MidiMapper) in the VERIFICATION.md "Deferred Follow-ups" section; STATE.md may also note them under "Pending Todos" or "Blockers/Concerns" so they don't get lost. |
</threat_model>

<verification>
- All four tasks complete with their automated verify commands green.
- VERIFICATION.md contains the closing summary table with 5 ✓ rows.
- 15.1-AUDIT-final.md captures auditor agent output; CR-XX disposition table shows zero CRITICAL.
- Manual UAT artifacts (CPU history screenshot + stderr log) attached.
- ROADMAP.md updated; phase 15.1 marked complete.
- All deferred follow-ups (H-03, CR-11 algorithmic, MidiMapper) explicitly listed for v1.2 follow-up.
</verification>

<success_criteria>
- ROADMAP-15.1-S1: TSan ctest exits 0 with zero TSan reports — evidenced in Signal 1.
- ROADMAP-15.1-S2: realtime-audio-reviewer reports CRITICAL=0, HIGH=0 (or H-03 accepted), MEDIUM≤1 — evidenced in Signal 2 with verbatim agent output in AUDIT-final.md.
- ROADMAP-15.1-S3: 30+ min populated UAT under TSan standalone shows no audible glitches and no recurring interval-period CPU spike — evidenced in Signal 3 with attached CPU history screenshot.
- ROADMAP-15.1-S4: greps confirm m_users_cs / m_locchan_cs / m_misc_cs are not acquired from any audio-thread function — evidenced in Signal 4.
- ROADMAP-15.1-S5: greps confirm writeLog / writeUserChanLog / JAMWIDE_DEV_BUILD fopen are not called from audio-thread functions — evidenced in Signal 5.
- 15.1-VERIFICATION.md ends with `## VERIFICATION COMPLETE` and contains all 5 ROADMAP-S references.
- ROADMAP.md phase 15.1 status updated to complete; deferred follow-ups recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-rt-safety-hardening/15.1-10-SUMMARY.md` capturing:
- The four signals' commands and results
- Auditor agent diff: each pre-fix CR-XX/H-XX/M-XX/L-XX with disposition
- UAT details: server, peer count, session length, BPM/BPI, peer-churn cycles, CPU history observation
- Deferred follow-ups (with rationale)
- Phase closure: ROADMAP and STATE updates committed
- (Optional) note on whether the phase needs a /gsd-verify-work pass beyond this plan, or whether this plan IS the verify-work
</output>
