---
phase: 21-h-264-decoder-receive-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/njclient.h
  - src/core/njclient.cpp
  - tests/test_video_recv_state.cpp
  - CMakeLists.txt
  - .claude/agents/realtime-audio-reviewer.md
autonomous: true
requirements:
  - WIRE-02
user_setup: []

must_haves:
  truths:
    - "Run-thread WRITE handler accumulates per-frame bytes into the per-peer VideoRecvBuffer slots and parses the 24-byte marker (extracts audio_ch0_guid + sender_seq)"
    - "Mid-download startPlaying promotes accumulating → next as soon as a complete frame arrives in accumulating"
    - "Audio-thread on_new_interval video receive block performs STAGE-1 promote (pending → playing) followed by the GUID-pair decision tree under m_video_recv_cs"
    - "DS-match defers next → pending; PREV-match plays next → playing immediately; no-match increments hold_count and drops next.reset() with synced=false when hold_count >= kHoldCapDrop=4"
    - "On peer leave (m_remoteusers Delete site), the matching VideoRecvState's prev_ds_guid + hold_count + synced + last_played_* are reset and all four slots are reset()"
    - "BEGIN, WRITE, END, and audio-thread SWAP logic are extracted into private member helpers (handleVideoRecvBegin_, handleVideoRecvWrite_, handleVideoRecvEnd_, runVideoReceiveBlock_) — production dispatchers AND test dispatchers call the SAME helpers (no state-machine duplication per codex Cluster 5)"
    - "Phase 21 audit-allowlist envelope is published in .claude/agents/realtime-audio-reviewer.md covering m_video_recv_cs acquire, WDL_PtrList<VideoRecvState> iteration, VideoRecvBuffer scalar/memcpy access including copyFrom, and m_remoteuser_mirror[s].next_ds[0]->guid read"
    - "Audit envelope wording distinguishes 'accepted for phase parity with NinjamZap source port' from 'safe in general' (codex Cluster 1) — the envelope MUST NOT implicitly authorize future audio-thread heap allocations by analogy"
    - "Timing instrumentation for runVideoReceiveBlock_ lands in Task 2 (NOT deferred to UAT per codex Cluster 1): JAMWIDE_BUILD_TESTS-gated atomic counters track max duration (nanoseconds) and peer count observed during the most recent invocation; accessor exposes both for in-process verification"
    - "tests/test_video_recv_state.cpp exercises all eleven WIRE-02 sub-scenarios via the new DispatchTestVideoRecvBegin/Write/End test helpers (the original nine plus two malformed-length tests per codex Cluster 10) and ctest -R video_recv_state exits 0"
  artifacts:
    - path: "src/core/njclient.h"
      provides: "VideoRecvBuffer + VideoRecvState struct definitions + m_video_streams WDL_PtrList + m_video_recv_cs WDL_Mutex + findVideoStream/findOrCreateVideoStream/findVideoStreamByGUID/removeVideoStream helpers + private handleVideoRecvBegin_/handleVideoRecvWrite_/handleVideoRecvEnd_/runVideoReceiveBlock_ helpers + JAMWIDE_BUILD_TESTS DispatchTestVideoRecvBegin/Write/End helpers + JAMWIDE_BUILD_TESTS timing accessors (getRunVideoReceiveBlockMaxNanosForTest/getRunVideoReceiveBlockLastPeerCountForTest)"
      contains: "struct VideoRecvBuffer"
    - path: "src/core/njclient.cpp"
      provides: "Private handleVideoRecvBegin_/handleVideoRecvWrite_/handleVideoRecvEnd_/runVideoReceiveBlock_ bodies (single source of truth); BEGIN/WRITE/END dispatch sites + audio-thread on_new_interval call those helpers; user-leave video state reset; timing instrumentation in runVideoReceiveBlock_ under JAMWIDE_BUILD_TESTS"
      contains: "m_video_recv_cs.Enter"
    - path: "tests/test_video_recv_state.cpp"
      provides: "11 WIRE-02 sub-tests for ds_match_defers_to_pending, prev_match_plays_immediately, no_match_holds_then_drops_at_4, pending_promotes_to_playing_on_next_swap, mid_download_start_playing, user_leave_resets_video_sync_state, split_frame_reassembles_via_length_prefix, marker_parse_extracts_guid_and_seq, burst_begin_discards_stale_next, zero_length_frame_drops_cleanly, oversize_prefix_clamps_at_4mb_cap"
      contains: "test_ds_match_defers_to_pending"
    - path: "CMakeLists.txt"
      provides: "add_executable(test_video_recv_state ...) + add_test(NAME video_recv_state COMMAND test_video_recv_state) under JAMWIDE_BUILD_TESTS, mirroring the test_video_state_machine block at lines 504-511"
      contains: "test_video_recv_state"
    - path: ".claude/agents/realtime-audio-reviewer.md"
      provides: "New '## Phase 21 audit allowlist envelope' section listing m_video_recv_cs Enter/Leave + WDL_PtrList<VideoRecvState> iteration + VideoRecvBuffer field reads + copyFrom + m_remoteuser_mirror[s].next_ds[0]->guid carve-out sites; wording explicitly scopes acceptance to 'parity with NinjamZap source port' and forbids generalization-by-analogy"
      contains: "Phase 21 audit allowlist envelope"
  key_links:
    - from: "src/core/njclient.cpp MESSAGE_SERVER_DOWNLOAD_INTERVAL_BEGIN raw-data branch (lines ~2363-2378)"
      to: "handleVideoRecvBegin_(...) (private helper that acquires m_video_recv_cs and calls findOrCreateVideoStream + accumulating populate)"
      via: "augmented branch body (runs BEFORE existing RawData_Callback fire)"
      pattern: "handleVideoRecvBegin_"
    - from: "src/core/njclient.cpp MESSAGE_SERVER_DOWNLOAD_INTERVAL_WRITE m_rawdata_downloads loop (lines ~2480-2509)"
      to: "handleVideoRecvWrite_(...) and handleVideoRecvEnd_(...) private helpers (single source of truth; tests also call these via the dispatcher)"
      via: "augmented loop body (runs INSIDE the existing GUID-match block before RawData_Callback fire)"
      pattern: "handleVideoRecvWrite_"
    - from: "src/core/njclient.cpp NJClient::AudioProc on_new_interval (after Phase 20 send-side video block at lines 5256-5258)"
      to: "runVideoReceiveBlock_() private helper (single source of truth; test wrapper RunOnNewIntervalReceiveBlockForTest calls this same helper)"
      via: "adjacent code block following the send-side WDL_MutexLock vlock(&m_video_cs) scope"
      pattern: "runVideoReceiveBlock_"
    - from: "src/core/njclient.cpp m_remoteusers Delete site (existing user-leave code path)"
      to: "Per-username VideoRecvState video sync state reset (prev_ds_guid + hold_count + synced + slot resets)"
      via: "added cleanup block immediately before delete theuser / m_remoteusers.Delete"
      pattern: "memset\\(vs->prev_ds_guid"
---

<objective>
Land the receive-side state machine: port `VideoRecvBuffer` + `VideoRecvState` verbatim from upstream NinjamZap, wire the run-thread BEGIN/WRITE/END handler accumulation (24-byte marker parse, 4-byte BE length-prefix multi-write reassembly, mid-download startPlaying) and the audio-thread `on_new_interval` STAGE-1 promote + GUID-pair decision tree (DS-match defers 1 swap through pending, PREV-match plays immediately, no-match HOLDs and drops at kHoldCapDrop=4). Per codex review Cluster 5: BEGIN, WRITE, END, and audio-thread SWAP logic are extracted into private member helpers (`handleVideoRecvBegin_`, `handleVideoRecvWrite_`, `handleVideoRecvEnd_`, `runVideoReceiveBlock_`) so production dispatchers and test dispatchers call the SAME code — no state-machine duplication. Per codex Cluster 1: timing instrumentation for `runVideoReceiveBlock_` lands in Task 2 (not deferred to UAT). Per codex Cluster 10: two additional malformed-length tests join the original nine (eleven total). Also: publish the Phase 21 audit-allowlist envelope entries in `.claude/agents/realtime-audio-reviewer.md` with wording that explicitly scopes acceptance to "parity with NinjamZap source port" (codex Cluster 1) — the envelope MUST NOT implicitly authorize future audio-thread heap allocations by analogy.

Purpose: WIRE-02 ("Receiving user's video plays in sync with audio at interval boundaries via the GUID-pairing decision tree") — the 1-swap-defer through `pending` is the PROTOCOL-LEVEL fix for the "video one interval early" bug class. The send-side wire format (Phase 20) is byte-identical to upstream; this plan's receive-side state machine is the symmetric inverse, also byte-identical. NO redesign. Per `feedback_proven_over_pure` the upstream NinjamZap behaviour is the canonical correctness target; any deviation IS the bug.

Output: receive-side state machine + audio-thread SWAP block + audit envelope + 11-sub-test suite. No decoder thread yet (lands in Plan 21-02), no PeerVideoSink yet (lands in Plan 21-03). The `playing` slot is left populated with parsed bytes ready for Plan 21-02's slot-snapshot push to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-VALIDATION.md
@.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md

<interfaces>
<!-- Existing NJClient public surface that Plan 21-01 extends. Pulled from src/core/njclient.h. -->
<!-- Executor uses these directly — no codebase scavenger hunt needed. -->

From `src/core/njclient.h` (existing Phase 14.3-03 + Phase 20 surface):
```cpp
// 14.3-03 RawData dispatch (lines ~752-770):
typedef void (*RawDataCallback)(void *userData, int eventType,
                                const unsigned char *guid, unsigned int fourcc,
                                const char *username, int chidx,
                                const void *data, int dataLen);
RawDataCallback RawData_Callback;          // eventType: 0=begin, 1=data, 2=end
void           *RawData_User;

void RawDataSendBegin(unsigned char outGuid[16], unsigned int fourcc,
                      int chidx, int estsize);          // unused by Plan 21-01 — send-side
void RawDataSendWrite(const unsigned char guid[16], const void *data,
                      int dataLen, bool isEnd);          // unused by Plan 21-01 — send-side

// Existing run-thread per-stream tracker (14.3-02; lines 1024-1030):
struct RawDataDownloadTracker {
    unsigned char guid[16];
    unsigned int  fourcc;
    char          username[256];
    int           chidx;
};
WDL_PtrList<RawDataDownloadTracker> m_rawdata_downloads;

// 14.3-03 JAMWIDE_BUILD_TESTS dispatcher methods (line 830-836):
void DispatchTestServerDownloadIntervalBegin(const unsigned char guid[16], ...);
void DispatchTestServerDownloadIntervalWrite(const unsigned char guid[16], ...);

// Phase 15.1-07a HIGH-2 mirror (carve-out for next_ds[0]->guid audio-thread read):
LocalChannelMirror m_locchan_mirror[MAX_LOCAL_CHANNELS];
// Audio thread reads m_remoteuser_mirror[s].next_ds[0]->guid only — never decode_codec / decode_buf.
```

From `tests/test_video_state_machine.cpp` (Plan 21-01 test file mirrors this structure):
```cpp
#include "core/njclient.h"
#include "wdl/heapbuf.h"
static constexpr int MAX_ENC_BLOCKSIZE = 8192 + 1024;
#define MAKE_NJ_FOURCC(A,B,C,D) ((A) | ((B)<<8) | ((C)<<16) | ((D)<<24))
static int tests_run = 0;
static int tests_passed = 0;
#define TEST(name) ...   // see file
#define PASS()    ...
#define FAIL(msg) ...
```

From `CMakeLists.txt` (Plan 21-01 add_test wiring mirrors test_video_state_machine at lines 504-511):
```cmake
add_executable(test_video_state_machine tests/test_video_state_machine.cpp)
target_include_directories(test_video_state_machine PRIVATE ...)
target_compile_definitions(test_video_state_machine PRIVATE JAMWIDE_BUILD_TESTS=1)
target_link_libraries(test_video_state_machine PRIVATE njclient)
add_test(NAME video_state_machine COMMAND test_video_state_machine)
```
</interfaces>

<verbatim_port_sources>
<!-- Byte-faithful port of these upstream line ranges is the WIRE-02 correctness gate. -->
<!-- Cite line numbers in commit messages (per feedback_doc_vs_source_verification). -->

- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:345-414` — `VideoRecvBuffer` + `VideoRecvState` struct definitions
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:416-417` — `WDL_PtrList<VideoRecvState> m_video_streams` + `WDL_Mutex m_video_recv_cs`
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:2124-2173` — `findVideoStream` / `findOrCreateVideoStream` / `findVideoStreamByGUID` / `removeVideoStream` helpers (port verbatim, drop the `@` username-suffix stripping if JamWide does not use the `@host` form — verify against existing m_remoteusers username usage)
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1369-1409` — BEGIN-handler raw-data branch (the augmentation that runs INSIDE the existing JamWide 14.3-03 BEGIN branch at `src/core/njclient.cpp:2360-2378`): create VideoRecvState via findOrCreateVideoStream, stop-appending if append_active, burst-detect if vs->next.active, reset accumulating, populate accumulating.{fourcc,chidx,username,guid,active,interval_seq}
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1439-1592` — WRITE/END handler raw-data branch body: GUID-routed VideoRecvBuffer* selection (appending / playing / pending / accumulating), 4-byte BE length prefix multi-write reassembly via pending_remaining, 24-byte marker parse (extract audio_guid + sender_seq when frameSize == 24 on accumulating's first frame), mid-download startPlaying (accumulating → next when frameCount >= 1 and !vs->next.active), END handling (move accumulating → next, reset accumulating)
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:3084-3256` — audio-thread `on_new_interval` video receive block: m_video_recv_cs.Enter() → for each VideoRecvState run STAGE-1 promote (pending → playing if pending.active && frameCount>=1), then GUID-pair tree on vs->next (audioHasData via m_remoteusers iteration matching vs->next.username, DS-match against senderDs->guid, PREV-match against vs->prev_ds_guid, no-match increments hold_count with kHoldCapDrop=4 force-resync), update vs->prev_ds_guid → release lock
- `/Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1300-1325` — user-leave video sync state reset: when theuser->chanpresentmask==0, take m_video_recv_cs, iterate m_video_streams, for any vs whose stream_username matches theuser->name reset prev_ds_guid + last_played_audio_guid + hold_count + empty_count + synced + last_played_sender_seq + the four slots, release

**JamWide carve-out for the verbatim port (do NOT introduce new design choices):**

- The audio-thread iteration reads `m_remoteusers[].name` + `m_remoteusers[].channels[0].ds` (and then `senderDs->guid`). In JamWide, after Phase 15.1-07a, the audio-thread-accessible canonical handle is `m_remoteuser_mirror[s].next_ds[0]` (NOT `m_remoteusers[].channels[0].ds` — that goes through m_users_cs which would be a CRITICAL on the audio thread). The carve-out: iterate `m_remoteuser_mirror[s]` slots (active && username-match) and read `m_remoteuser_mirror[s].next_ds[0]->guid` for the DS comparison. This is the HIGH-2 carve-out already accepted in Phase 15.1-07a — do NOT read any other field of the DecodeState pointer.
- The upstream `audioHasData` check is `senderDs != NULL`; the JamWide equivalent is `m_remoteuser_mirror[s].active && m_remoteuser_mirror[s].next_ds[0] != nullptr`. If audioHasData == false, behaviour is identical to upstream (skip the match, reset hold_count, log EMPTY/HOLD).
- The upstream `SYNCLOG` macro is a no-op-able log. Per D-16 / D-09: NO writeLog or printf on the audio-thread video path. Convert SYNCLOG sites to `(void)0;` or strip entirely.
- The upstream BEGIN/WRITE/END branches in `MESSAGE_SERVER_DOWNLOAD_INTERVAL_BEGIN` / `_WRITE` already EXIST in JamWide at the 14.3-03 dispatch sites (`src/core/njclient.cpp:2360-2378` BEGIN and `:2480-2509` WRITE/END). Plan 21-01 AUGMENTS these branches (inserts the helper invocations INSIDE the existing block, before the RawData_Callback fire) rather than rewriting them. The existing `RawData_Callback(RawData_User, 0/1/2, ...)` fire is PRESERVED so Phase 21-02 / 21-03 hooks (lazy lifecycle spin-up in 21-03) can still register if needed.
</verbatim_port_sources>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port VideoRecvBuffer + VideoRecvState struct definitions and helpers to njclient.h/cpp</name>
  <files>src/core/njclient.h, src/core/njclient.cpp</files>
  <read_first>
    - /Users/cell/dev/JamWide/src/core/njclient.h (lines 1015-1080 — RawDataDownloadTracker, m_rawdata_downloads, LocalChannelMirror context; lines 735-770 — RawData_Callback surface)
    - /Users/cell/dev/JamWide/src/core/njclient.cpp (lines 2292-2400 — existing BEGIN handler with RawData_Callback dispatch; lines 1100-1330 — NJClient ctor/dtor for member initialization)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.h:345-417 (VideoRecvBuffer + VideoRecvState struct definitions + m_video_streams + m_video_recv_cs — port VERBATIM byte-for-byte)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:2124-2173 (findVideoStream / findOrCreateVideoStream / findVideoStreamByGUID / removeVideoStream helper bodies — port VERBATIM)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (Pattern 1, Pitfall 6 — pre-allocation discipline, Example 1)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-11 4MB pre-allocation, D-14 verbatim port)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md (codex Cluster 5 — extract shared helpers; codex Cluster 6 — wire-format contract; this task lays the struct + helper-declaration foundation that Task 2 then implements as private members)
  </read_first>
  <behavior>
    - Header declares `struct VideoRecvBuffer` with fields {data: WDL_HeapBuf, frameOffsets: WDL_TypedBuf&lt;int&gt;, frameCount, username[256], guid[16], fourcc, chidx, interval_seq, audio_guid[16], active, pending_remaining, sender_seq}, default ctor + reset() + copyFrom() member functions per upstream (Phase 21 D-11 amendment: ctor calls data.Resize(4MB, true) then Resize(0, false) to pre-allocate but report zero logical size)
    - **Wire-format contract pinned (codex Cluster 6):** a multi-line comment above `struct VideoRecvBuffer` explicitly pins the contract that downstream code (Plan 21-02 parser + tests) relies on:
      - `data` stores the frame payloads CONCATENATED. The 4-byte BE outer length prefix that the WRITE handler reads off the wire is CONSUMED by the run-thread accumulator and DOES NOT appear in `data`. Each frame's bytes (24-byte marker, SPS/PPS chunk, or per-frame NAL chunk) appear back-to-back in `data` without the outer prefix.
      - `frameOffsets[i]` is the byte offset INTO `data` of frame `i`'s FIRST payload byte (NOT a pointer to the consumed outer prefix). `frameOffsets[i+1] - frameOffsets[i]` is frame `i`'s payload size in bytes; `frameOffsets[frameCount]` is the total size (so `frameOffsets[frameCount] == data.GetSize()`).
      - The 24-byte marker (first frame at offset 0 when `frameCount == 1`) has THIS layout: `[4B BE outer_swap_count][16B audio_ch0_guid]` — total 20 bytes of payload preceded by the consumed 4B outer prefix. **The outer 4B BE prefix value MUST equal 20** (decimal) — per Phase 20 commit 6d23b5c lesson, this is the regression-gated marker outer-length contract. The marker payload is 20 bytes, NOT 24 — the upstream literature loosely calls it a "24-byte marker" because that's its on-wire footprint including the outer prefix, but the payload IS the 20 bytes that land in `data`.
      - SPS/PPS chunk (when present) has THIS payload layout (no Annex-B start codes — Annex-B wrap is the decoder's job): `[2B BE sps_len][SPS_NAL_bytes][2B BE pps_len][PPS_NAL_bytes]`.
      - Per-frame NAL chunk (the common case) has THIS payload layout: `[NAL_bytes]` — single NAL unit, no inner prefix, no start code.
      - Reference: upstream `/Users/cell/dev/ninjamzap-core/tests/video-sync/harness/TestClient.cpp:120-176` (`sendVideoFrame` + `sendFakeSPSPPS` — byte-exact wire reference per `feedback_doc_vs_source_verification`).
    - Header declares `struct VideoRecvState` with fields {accumulating, next, pending, playing : VideoRecvBuffer, frame_idx, expected_frames, append_active, append_to_next, append_to_pending, append_guid[16], stream_username[256], stream_chidx, key[280], empty_count, hold_count, prev_ds_guid[16], synced, last_played_sender_seq, last_played_audio_guid[16], drop_resync_count}, default ctor (memsets prev_ds_guid + last_played_audio_guid + append_guid; sets sender_seq to -1)
    - NJClient member additions: `WDL_PtrList<VideoRecvState> m_video_streams` + `WDL_Mutex m_video_recv_cs`
    - Public helper declarations: `VideoRecvState* findVideoStream(const char*, int)`, `VideoRecvState* findOrCreateVideoStream(const char*, int)`, `VideoRecvState* findVideoStreamByGUID(const unsigned char*)`, `void removeVideoStream(const char*, int)`
    - **NEW per codex Cluster 5:** private helper declarations (declared in this task; bodied in Task 2): `void handleVideoRecvBegin_(const unsigned char guid[16], unsigned int fourcc, const char *username, int chidx)`, `void handleVideoRecvWrite_(const unsigned char guid[16], const void *data, int dataLen)`, `void handleVideoRecvEnd_(const unsigned char guid[16])`, `void runVideoReceiveBlock_()`. These four helpers are SINGLE SOURCE OF TRUTH — production BEGIN/WRITE/END dispatchers AND test dispatchers AND the audio-thread on_new_interval call these same bodies. The helpers each acquire `m_video_recv_cs` internally (so the caller does not need to). Production callers must NOT pre-acquire `m_video_recv_cs` — these helpers are top-level mutex-acquirers.
    - **NEW per codex Cluster 1:** JAMWIDE_BUILD_TESTS-gated atomic counters for `runVideoReceiveBlock_` timing instrumentation, declared in this task at the bottom of the JAMWIDE_BUILD_TESTS member block: `std::atomic<std::uint64_t> m_run_video_receive_block_max_nanos_{0};` and `std::atomic<int> m_run_video_receive_block_last_peer_count_{0};` and accessors `std::uint64_t getRunVideoReceiveBlockMaxNanosForTest() const noexcept;` + `int getRunVideoReceiveBlockLastPeerCountForTest() const noexcept;` + `void resetRunVideoReceiveBlockTimingForTest() noexcept;`. Production builds DO NOT see these atomics — entire JAMWIDE_BUILD_TESTS block compiles out.
    - .cpp implementation: ctor body for VideoRecvBuffer pre-allocates 4 MB; ctor body for VideoRecvState memsets the per-state scalars
    - .cpp implementation: helper bodies match upstream `njclient.cpp:2124-2173` byte-for-byte (key format `"%.*s:%d"` from username up to `@` if present + chidx; if upstream's `@`-stripping does not match JamWide's username conventions, strip without the `@` handling — JamWide `m_remoteusers[].name` is bare-username, so the strchr-`@` branch is preserved verbatim defensively but never fires)
    - NJClient destructor body: `m_video_streams.Empty(true)` after the existing `m_rawdata_downloads.Empty(true)` call (verify against njclient.cpp:1320 line; m_video_streams owns its entries — Empty(true) calls delete on each)
    - Build target `njclient` compiles cleanly with `JAMWIDE_BUILD_TESTS=1`; existing tests still pass (`ctest -R rawdata_send|video_state_machine|video_fourcc|video_encoder` all green)
  </behavior>
  <action>
    Per D-14 (verbatim upstream port — `ninjamzap-core/njclient.h:345-417` + `njclient.cpp:2124-2173`) + codex Cluster 5 (shared helper extraction) + codex Cluster 6 (wire-format contract pin):

    1. **njclient.h:** Add a new `// Phase 21 receive pipeline (D-14): port VERBATIM of upstream ninjamzap-core/njclient.h:345-417` comment block, then transcribe the `struct VideoRecvBuffer { ... };` and `struct VideoRecvState { ... };` definitions inside `class NJClient { ... };` body. The structs go alongside the existing `struct RawDataDownloadTracker { ... };` at line 1024. Maintain upstream field order EXACTLY (so a `diff` against ninjamzap-core/njclient.h is trivial). Apply ONE amendment per D-11: in `VideoRecvBuffer::VideoRecvBuffer()`, after the existing `memset(audio_guid, 0, 16);`, add the two-line `data.Resize(4 * 1024 * 1024, true); data.Resize(0, false);` pre-allocate-then-shrink-logical-size dance — this is the D-11 4 MB pre-allocation that keeps WRITE-handler accumulation realloc-free. Verify via assumption A3 in 21-RESEARCH.md that `WDL_HeapBuf::Resize(sz, false)` does NOT shrink the underlying allocation (read `libs/wdl/heapbuf.h` first to confirm; if Resize DOES shrink, replace the second line with the equivalent `ResizeOK(0)` or use a private `m_alloc_size` field — but the upstream pattern relies on capacity persisting, so verify behavior, do not assume).

       **Wire-format contract comment (codex Cluster 6) — INSERT IMMEDIATELY ABOVE `struct VideoRecvBuffer`:**
       ```cpp
       // Wire-format contract for VideoRecvBuffer (Plan 21-01, codex review Cluster 6):
       //
       //   `data`             — concatenated frame payloads. The 4-byte BE OUTER length
       //                        prefix that the WRITE handler reads off the wire is
       //                        CONSUMED by the run-thread accumulator and DOES NOT appear
       //                        in `data`. Each frame's payload bytes appear back-to-back.
       //
       //   `frameOffsets[i]`  — byte offset INTO `data` of frame `i`'s FIRST payload byte.
       //                        `frameOffsets[i+1] - frameOffsets[i]` is frame `i`'s payload
       //                        size; `frameOffsets[frameCount]` == data.GetSize().
       //
       //   Frame payload layouts (each frame, after the outer prefix is stripped):
       //
       //     24-byte marker (first frame when frameCount == 1, payload size == 20 bytes):
       //         [4B BE sender_seq][16B audio_ch0_guid]
       //         The OUTER 4B BE prefix value for the marker frame MUST equal 20
       //         (decimal). Phase 20 commit 6d23b5c lost cycles to truncating the marker
       //         to 20 bytes on the wire (missing the outer prefix); this contract is
       //         the regression guard. Reference:
       //         /Users/cell/dev/ninjamzap-core/tests/video-sync/harness/TestClient.cpp:120-176
       //         (sendVideoFrame + sendFakeSPSPPS).
       //
       //     SPS/PPS chunk (when present): no Annex-B start codes; Annex-B wrap is the
       //         decoder's job (Plan 21-02 sendAnnexB_ helper):
       //         [2B BE sps_len][SPS_NAL_bytes][2B BE pps_len][PPS_NAL_bytes]
       //
       //     Per-frame NAL chunk (common case): single NAL unit, no prefix, no start code:
       //         [NAL_bytes]
       //
       // This contract is the AUTHORITY for the parser in Plan 21-02
       // `Openh264Decoder::parseSlotAndFeed_`. The contract is unit-tested in Plan 21-01
       // Task 3 (test_marker_parse_extracts_guid_and_seq) and Plan 21-02 Task 3
       // (test_pushSlotView_full_ninjamzap_bytes — codex Cluster 7).
       struct VideoRecvBuffer { ... };
       ```

    2. **njclient.h:** Add `WDL_PtrList<VideoRecvState> m_video_streams;` and `WDL_Mutex m_video_recv_cs;` members alongside `m_rawdata_downloads` (line 1030). Public helpers go in the existing `public:` block near `RawDataSendBegin/Write` (line 767), with the four declarations cited above.

    3. **njclient.h:** Add four NEW private helper declarations (codex Cluster 5) in a `// Plan 21-01 Task 2 (codex Cluster 5): single-source state-machine helpers — called by production dispatchers AND test dispatchers AND audio-thread on_new_interval` comment block in the `private:` section:
       ```cpp
       void handleVideoRecvBegin_(const unsigned char guid[16], unsigned int fourcc,
                                   const char *username, int chidx);
       void handleVideoRecvWrite_(const unsigned char guid[16],
                                   const void *data, int dataLen);
       void handleVideoRecvEnd_(const unsigned char guid[16]);
       void runVideoReceiveBlock_();
       ```
       These four helpers each acquire `m_video_recv_cs` internally — callers must NOT pre-acquire.

    4. **njclient.h:** Inside the existing `#ifdef JAMWIDE_BUILD_TESTS` block, add timing instrumentation members + accessors (codex Cluster 1):
       ```cpp
       std::atomic<std::uint64_t> m_run_video_receive_block_max_nanos_{0};
       std::atomic<int>           m_run_video_receive_block_last_peer_count_{0};
       std::uint64_t getRunVideoReceiveBlockMaxNanosForTest() const noexcept;
       int           getRunVideoReceiveBlockLastPeerCountForTest() const noexcept;
       void          resetRunVideoReceiveBlockTimingForTest() noexcept;
       ```
       These are zero-cost in production builds (the entire JAMWIDE_BUILD_TESTS block compiles out).

    5. **njclient.cpp:** Implement `NJClient::findVideoStream / findOrCreateVideoStream / findVideoStreamByGUID / removeVideoStream` matching `ninjamzap-core/njclient.cpp:2124-2173` byte-for-byte. Use the existing `snprintf(vs->key, sizeof(vs->key), "%.*s:%d", nameLen, username, chidx)` pattern. Place helpers adjacent to existing 14.3 helpers (`IsVideoFourcc`, `DrainRawDataSendQueueForTest`); search `njclient.cpp` for `bool NJClient::IsVideoFourcc` to find the insertion point.

    6. **njclient.cpp:** Implement the JAMWIDE_BUILD_TESTS timing accessors as one-line bodies that load/store the atomics with `std::memory_order_relaxed`. The actual increment of `m_run_video_receive_block_max_nanos_` happens in Task 2's `runVideoReceiveBlock_` body via a scoped `std::chrono::steady_clock` measurement.

    7. **njclient.cpp:** In `NJClient::~NJClient()`, add `m_video_streams.Empty(true);` immediately after `m_rawdata_downloads.Empty(true);` (line 1320).

    8. **Verification:** `ninja -C build-juce njclient` builds cleanly; `nm build-juce/libnjclient.a | grep -E "findVideoStream|findOrCreateVideoStream|findVideoStreamByGUID|removeVideoStream"` shows the four symbols. Existing test suite green: `cd build-juce && ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder"` exits 0.
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder"</automated>
    <grep>grep -c "struct VideoRecvBuffer\|struct VideoRecvState\|WDL_PtrList&lt;VideoRecvState&gt; m_video_streams\|WDL_Mutex m_video_recv_cs" /Users/cell/dev/JamWide/src/core/njclient.h</grep>
    <grep>grep -c "NJClient::findOrCreateVideoStream\|NJClient::findVideoStreamByGUID\|NJClient::removeVideoStream" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "void handleVideoRecvBegin_\|void handleVideoRecvWrite_\|void handleVideoRecvEnd_\|void runVideoReceiveBlock_" /Users/cell/dev/JamWide/src/core/njclient.h</grep>
    <grep>grep -c "m_run_video_receive_block_max_nanos_\|getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/src/core/njclient.h</grep>
    <grep>grep -c "Wire-format contract for VideoRecvBuffer" /Users/cell/dev/JamWide/src/core/njclient.h</grep>
  </verify>
  <acceptance_criteria>
    - `grep -q "struct VideoRecvBuffer" /Users/cell/dev/JamWide/src/core/njclient.h` (struct present)
    - `grep -q "struct VideoRecvState" /Users/cell/dev/JamWide/src/core/njclient.h` (struct present)
    - `grep -q "WDL_PtrList&lt;VideoRecvState&gt; m_video_streams" /Users/cell/dev/JamWide/src/core/njclient.h` (member present)
    - `grep -q "WDL_Mutex m_video_recv_cs" /Users/cell/dev/JamWide/src/core/njclient.h` (mutex present)
    - `grep -q "data.Resize(4 \* 1024 \* 1024, true)" /Users/cell/dev/JamWide/src/core/njclient.h` (D-11 4 MB pre-allocation)
    - `grep -q "Wire-format contract for VideoRecvBuffer" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 6 contract comment present)
    - `grep -q "OUTER 4B BE prefix value for the marker frame MUST equal 20" /Users/cell/dev/JamWide/src/core/njclient.h` (Phase 20 regression-guard contract pinned)
    - `grep -q "void handleVideoRecvBegin_" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 5 helper declaration)
    - `grep -q "void handleVideoRecvWrite_" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 5 helper declaration)
    - `grep -q "void handleVideoRecvEnd_" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 5 helper declaration)
    - `grep -q "void runVideoReceiveBlock_" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 5 helper declaration)
    - `grep -q "m_run_video_receive_block_max_nanos_" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 1 timing counter declared)
    - `grep -q "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/src/core/njclient.h` (codex Cluster 1 timing accessor declared)
    - `grep -q "NJClient::findOrCreateVideoStream" /Users/cell/dev/JamWide/src/core/njclient.cpp` (helper implementation)
    - `grep -q "NJClient::findVideoStreamByGUID" /Users/cell/dev/JamWide/src/core/njclient.cpp` (helper implementation)
    - `grep -q "NJClient::removeVideoStream" /Users/cell/dev/JamWide/src/core/njclient.cpp` (helper implementation)
    - `grep -q "m_video_streams.Empty(true)" /Users/cell/dev/JamWide/src/core/njclient.cpp` (destructor cleanup)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder"` exits 0 (existing tests still green)
  </acceptance_criteria>
  <done>VideoRecvBuffer + VideoRecvState verbatim-ported with D-11 4 MB pre-allocation + explicit wire-format contract comment (codex Cluster 6); four single-source private state-machine helpers declared (codex Cluster 5) for Task 2 to body; JAMWIDE_BUILD_TESTS timing-counter atomics + accessors declared (codex Cluster 1); helpers compile and link; existing test suite green; njclient destructor properly cleans up m_video_streams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Body the four private helpers (handleVideoRecvBegin_/Write_/End_/runVideoReceiveBlock_) + wire dispatch sites + timing instrumentation + user-leave reset</name>
  <files>src/core/njclient.cpp, src/core/njclient.h</files>
  <read_first>
    - /Users/cell/dev/JamWide/src/core/njclient.cpp (lines 2292-2400 — existing BEGIN handler; lines 2458-2540 — existing WRITE/END handler; lines 5180-5300 — existing on_new_interval send-side video block at lines 5224-5260; existing user-leave handling around m_remoteusers Delete site — search for `delete theuser` to find it)
    - /Users/cell/dev/JamWide/src/core/njclient.h (Task 1 outputs — helper declarations + timing atomics)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1369-1409 (BEGIN handler raw-data branch — body for `handleVideoRecvBegin_`)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1439-1592 (WRITE/END handler raw-data branch — bodies for `handleVideoRecvWrite_` and `handleVideoRecvEnd_`)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:3084-3256 (audio-thread on_new_interval video receive block — body for `runVideoReceiveBlock_`)
    - /Users/cell/dev/ninjamzap-core/core/ninjamclient/libninjamcore/njclient.cpp:1300-1325 (user-leave video sync state reset — port VERBATIM into JamWide's m_remoteusers Delete site, immediately BEFORE the existing `delete theuser; m_remoteusers.Delete(x);` calls)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-14, D-15, D-16 — the verbatim port discipline)
    - /Users/cell/dev/JamWide/.planning/phases/15.1-rt-safety-hardening/15.1-CONTEXT.md (HIGH-2 mirror access pattern — m_remoteuser_mirror[s].next_ds[0]->guid is the audio-thread-accessible canonical DS pointer; the iteration MUST go through the mirror, NOT m_remoteusers — which is m_users_cs-protected and therefore CRITICAL on the audio thread)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md (codex Cluster 1 — timing instrumentation; codex Cluster 5 — single-source helpers)
  </read_first>
  <behavior>
    - `handleVideoRecvBegin_(guid, fourcc, username, chidx)` body: acquires `m_video_recv_cs`, calls `findOrCreateVideoStream(username, chidx)`, handles append-active cleanup, burst-detects via `vs->next.active`, resets vs->accumulating, populates {fourcc, chidx, username, guid, active=true, interval_seq=m_sync_interval_cnt}, releases the mutex — all per upstream :1369-1404 BYTE-FOR-BYTE. Strips SYNCLOG calls (D-16 hygiene). Does NOT call RawData_Callback (caller is responsible for that — the production dispatcher fires Callback BEFORE invoking this helper; the test dispatcher fires nothing).
    - `handleVideoRecvWrite_(guid, data, dataLen)` body: acquires `m_video_recv_cs`, executes GUID-routed accumulation logic per upstream :1452-1564 BYTE-FOR-BYTE (findVideoStreamByGUID, route into appending|playing|pending|accumulating slot, 4B BE length prefix multi-write reassembly via target->pending_remaining, frame complete → frameCount++ → 24B marker parse for accumulating.first-frame extracting `vs->accumulating.audio_guid` from bytes 4..19 inclusive of the payload and `vs->accumulating.sender_seq` from bytes 0..3 BE, mid-download startPlaying if accumulating ready and !next.active), releases mutex. Strips SYNCLOG.
    - `handleVideoRecvEnd_(guid)` body: acquires `m_video_recv_cs`, executes END logic per upstream :1568-1591 BYTE-FOR-BYTE (move accumulating → next if !wasAppending and frameCount > 1, reset accumulating; if wasAppending, snapshot expected_frames and clear append_active), releases mutex.
    - `runVideoReceiveBlock_()` body: acquires `m_video_recv_cs`, iterates m_video_streams, runs STAGE-1 promote + GUID-pair tree per upstream :3084-3256 BYTE-FOR-BYTE (with the Phase 15.1-07a HIGH-2 carve-out: read `m_remoteuser_mirror[s].next_ds[0]->guid` instead of `m_remoteusers[].channels[0].ds->guid`), updates prev_ds_guid, releases mutex. Strips SYNCLOG. **Per codex Cluster 1: under JAMWIDE_BUILD_TESTS, wraps the iteration in a steady_clock measurement and updates `m_run_video_receive_block_max_nanos_` (max of current and observed nanos) + `m_run_video_receive_block_last_peer_count_` (current count). Zero overhead in production — the entire timing block compiles out.**
    - **Per codex Cluster 1: timing instrumentation envelope.** Under `#ifdef JAMWIDE_BUILD_TESTS` only (zero production overhead), `runVideoReceiveBlock_` captures `auto t0 = std::chrono::steady_clock::now();` BEFORE acquiring `m_video_recv_cs`, captures `auto t1 = std::chrono::steady_clock::now();` AFTER releasing the mutex, computes `std::uint64_t elapsed_ns = std::chrono::duration_cast<std::chrono::nanoseconds>(t1 - t0).count();`, and updates the atomic max via a CAS loop: `for (auto cur = m_run_video_receive_block_max_nanos_.load(std::memory_order_relaxed); cur < elapsed_ns && !m_run_video_receive_block_max_nanos_.compare_exchange_weak(cur, elapsed_ns, std::memory_order_relaxed); ) {}`. Also stores the peer count (m_video_streams.GetSize()) into `m_run_video_receive_block_last_peer_count_`. Tests assert these counters report sane values (e.g., 5-peer max nanos < 5 ms under unit-test conditions; the absolute threshold is a soft signal — the hard signal is that the counter MOVES under load).
    - BEGIN-handler raw-data branch at `src/core/njclient.cpp:2360-2378`: after the existing `m_rawdata_downloads.Add(tracker);` (line 2374), call `handleVideoRecvBegin_(dib.guid, dib.fourcc, dib.username, dib.chidx);` BEFORE the existing `RawData_Callback(RawData_User, 0 /*begin*/, ...)` fire. The helper acquires `m_video_recv_cs` internally.
    - WRITE-handler raw-data branch at `src/core/njclient.cpp:2480-2509`: inside the existing `if (!memcmp(t->guid, diw.guid, sizeof(t->guid)))` block, AFTER the existing `config_debug_level>1 printf` and BEFORE the existing `RawData_Callback(RawData_User, 1 /*data*/, ...)` fire, call `if (diw.audio_data_len > 0 && diw.audio_data) handleVideoRecvWrite_(diw.guid, diw.audio_data, diw.audio_data_len);` Then if `diw.flags & 1` (END marker), call `handleVideoRecvEnd_(diw.guid);` BEFORE the existing `RawData_Callback(RawData_User, 2 /*end*/, ...)` fire and tracker deletion.
    - Audio-thread `on_new_interval`: add NEW code IMMEDIATELY after the existing Phase 20 send-side `{ WDL_MutexLock vlock(&m_video_cs); ... }` scope closing brace — exactly one line: `runVideoReceiveBlock_();`. The helper handles mutex acquisition + iteration + decision tree + release internally. NO writeLog / SYNCLOG / m_users_cs.Enter inside the helper.
    - User-leave (m_remoteusers Delete site): before `delete theuser; m_remoteusers.Delete(x);`, take m_video_recv_cs, iterate m_video_streams matching theuser->name on stream_username, reset prev_ds_guid + last_played_audio_guid + hold_count + empty_count + synced=false + last_played_sender_seq=-1 + next.reset() + pending.reset() + accumulating.reset() — all per upstream :1306-1322. This block does NOT live inside `handleVideoRecvBegin_/Write_/End_/runVideoReceiveBlock_` because user-leave is its own dispatch site; the planner pins it as inline at the m_remoteusers Delete site for fidelity to upstream :1306-1322.
    - All four edits compile cleanly under JAMWIDE_BUILD_TESTS=1; no new warnings (-Werror enforced); existing test suite remains green
  </behavior>
  <action>
    Per D-14/D-15/D-16 verbatim port from `ninjamzap-core/njclient.cpp` + codex Cluster 1 (timing instrumentation, NOT deferred) + codex Cluster 5 (single-source helpers):

    1. **Body `void NJClient::handleVideoRecvBegin_(const unsigned char guid[16], unsigned int fourcc, const char *username, int chidx)`** in njclient.cpp adjacent to the existing 14.3 helpers (search `bool NJClient::IsVideoFourcc` to find insertion point). Body is the byte-for-byte port of upstream `:1380-1404`: `m_video_recv_cs.Enter(); VideoRecvState *vs = findOrCreateVideoStream(username, chidx); if (vs->append_active) { ... }; if (vs->next.active) { ... burst-discard ... }; vs->accumulating.reset(); ... vs->accumulating.interval_seq = m_sync_interval_cnt; m_video_recv_cs.Leave();`. Use `lstrcpyn_safe` for username copy (JamWide convention). Strip the upstream `SYNCLOG(...)` calls.

    2. **Body `void NJClient::handleVideoRecvWrite_(const unsigned char guid[16], const void *data, int dataLen)`** in njclient.cpp. Body is the byte-for-byte port of upstream `:1452-1564` GUID-routed accumulation logic. Key elements: `m_video_recv_cs.Enter();`, `VideoRecvState *vs = findVideoStreamByGUID(guid); if (!vs) { m_video_recv_cs.Leave(); return; }`, bool `appending = vs->append_active && !memcmp(vs->append_guid, guid, 16);`, the cascade of VideoRecvBuffer* `target` selection (appending → vs->{pending,next,playing}; playing-guid-match → late frame append on playing; pending-guid-match → late frame append on pending; accumulating-guid-match → accumulating fresh data), the 4-byte BE length prefix multi-write reassembly via `target->pending_remaining`, the 24-byte marker parse INSIDE the `if (targetIsAccumulating && fc == 1) { ... }` branch (extract `vs->accumulating.audio_guid` from `m + 4 .. m + 19` (16 bytes after the 4-byte sender_seq) and `vs->accumulating.sender_seq` from `m+0..m+3` BE — **per the Plan 21-01 wire-format contract pinned in Task 1**, the 24-byte marker payload that lives in `data` is `[4B BE sender_seq][16B audio_guid]` (20 bytes — the outer prefix value MUST equal 20 was consumed by the WRITE handler)), and the mid-download startPlaying `if (targetIsAccumulating && !vs->append_active && fc >= 1 && !vs->next.active) { ... vs->next.copyFrom(vs->accumulating); ... vs->append_active = true; vs->append_to_next = true; ... }`. `m_video_recv_cs.Leave();` at end. Strip SYNCLOG calls.

    3. **Body `void NJClient::handleVideoRecvEnd_(const unsigned char guid[16])`** in njclient.cpp. Body is the byte-for-byte port of upstream `:1568-1591`: `m_video_recv_cs.Enter(); VideoRecvState *vs = findVideoStreamByGUID(guid); if (vs) { bool wasAppending = vs->append_active && !memcmp(vs->append_guid, guid, 16); if (wasAppending) { vs->expected_frames = ...; vs->append_active = false; } else { if (vs->accumulating.frameCount > 1) { vs->expected_frames = vs->accumulating.frameCount; vs->next.reset(); vs->next.copyFrom(vs->accumulating); } vs->accumulating.reset(); } } m_video_recv_cs.Leave();`.

    4. **Body `void NJClient::runVideoReceiveBlock_()`** in njclient.cpp.
       ```cpp
       void NJClient::runVideoReceiveBlock_()
       {
       #ifdef JAMWIDE_BUILD_TESTS
           const auto t0 = std::chrono::steady_clock::now();
       #endif
           {
               WDL_MutexLock vrlock(&m_video_recv_cs);
               // Byte-for-byte port of upstream njclient.cpp:3084-3256.
               // (STAGE-1 promote + GUID-pair tree + prev_ds_guid update; details below)
               // The ONE JamWide carve-out: read m_remoteuser_mirror[s].next_ds[0]->guid
               // instead of m_remoteusers[].channels[0].ds->guid (Phase 15.1-07a HIGH-2).
           }
       #ifdef JAMWIDE_BUILD_TESTS
           const auto t1 = std::chrono::steady_clock::now();
           const auto elapsed_ns =
               (std::uint64_t)std::chrono::duration_cast<std::chrono::nanoseconds>(t1 - t0).count();
           m_run_video_receive_block_last_peer_count_.store(
               m_video_streams.GetSize(), std::memory_order_relaxed);
           auto cur = m_run_video_receive_block_max_nanos_.load(std::memory_order_relaxed);
           while (cur < elapsed_ns &&
                  !m_run_video_receive_block_max_nanos_.compare_exchange_weak(
                       cur, elapsed_ns, std::memory_order_relaxed)) { }
       #endif
       }
       ```
       Inside the inner scope, the upstream `:3084-3256` body goes — STAGE-1 promote (`pending.active && pending.frameCount >= 1` → `playing.copyFrom(pending); pending.reset(); frame_idx = 0;`), then GUID-pair tree on `vs->next` (audioHasData via m_remoteuser_mirror[s] iteration matching vs->next.username, DS-match against `m_remoteuser_mirror[s].next_ds[0]->guid` → `vs->pending.copyFrom(vs->next); vs->next.reset(); vs->append_to_pending = true;`, PREV-match against `vs->prev_ds_guid` → `vs->playing.copyFrom(vs->next); vs->next.reset(); vs->frame_idx = 0;`, no-match → `vs->hold_count++; if (vs->hold_count >= kHoldCapDrop /* == 4 */) { vs->next.reset(); vs->synced = false; vs->hold_count = 0; vs->drop_resync_count++; }`), update `vs->prev_ds_guid` from `m_remoteuser_mirror[s].next_ds[0]->guid` at end. SYNCLOG sites stripped.

    5. **Implement the JAMWIDE_BUILD_TESTS accessor bodies** (declared in Task 1) in njclient.cpp:
       ```cpp
       #ifdef JAMWIDE_BUILD_TESTS
       std::uint64_t NJClient::getRunVideoReceiveBlockMaxNanosForTest() const noexcept {
           return m_run_video_receive_block_max_nanos_.load(std::memory_order_relaxed);
       }
       int NJClient::getRunVideoReceiveBlockLastPeerCountForTest() const noexcept {
           return m_run_video_receive_block_last_peer_count_.load(std::memory_order_relaxed);
       }
       void NJClient::resetRunVideoReceiveBlockTimingForTest() noexcept {
           m_run_video_receive_block_max_nanos_.store(0, std::memory_order_relaxed);
           m_run_video_receive_block_last_peer_count_.store(0, std::memory_order_relaxed);
       }
       #endif
       ```

    6. **BEGIN handler dispatch site augmentation** (`src/core/njclient.cpp:2363-2378`):
       AFTER the existing `m_rawdata_downloads.Add(tracker);` (line 2374) and BEFORE the existing `RawData_Callback(RawData_User, 0 /*begin*/, ...)` (line 2375), insert ONE LINE: `handleVideoRecvBegin_(dib.guid, dib.fourcc, dib.username, dib.chidx);`. The helper does the rest (mutex + state-machine work).

    7. **WRITE handler dispatch site augmentation** (`src/core/njclient.cpp:2480-2509`):
       Inside the existing `if (!memcmp(t->guid, diw.guid, sizeof(t->guid)))` block, AFTER the existing `config_debug_level>1 printf` (line 2486-2490) and BEFORE the existing `RawData_Callback(RawData_User, 1 /*data*/, ...)` fire (line 2493), insert: `if (diw.audio_data_len > 0 && diw.audio_data) handleVideoRecvWrite_(diw.guid, diw.audio_data, diw.audio_data_len);`. Then if `diw.flags & 1` (END marker, at lines 2497-2502), BEFORE the existing `RawData_Callback(RawData_User, 2 /*end*/, ...)` fire, insert: `handleVideoRecvEnd_(diw.guid);`. NOTE: the existing tracker delete + m_rawdata_downloads.Delete still happens AFTER the RawData_Callback fire — preserve that ordering.

    8. **Audio-thread receive block** (`src/core/njclient.cpp` `on_new_interval`, after the existing Phase 20 send-side `{ WDL_MutexLock vlock(&m_video_cs); ... }` scope closing brace — search for `// Plan 20-02 Task 1: NinjamZap-literal send-side video state machine.` to find the start, then locate the closing brace of that scope):
       Add ONE LINE: `runVideoReceiveBlock_();`. The helper does everything.

    9. **User-leave video state reset** (search `src/core/njclient.cpp` for `delete theuser` to find the m_remoteusers Delete site):
       Immediately BEFORE the existing `delete theuser;` (and `m_remoteusers.Delete(x);`), insert the byte-for-byte port of upstream `:1306-1322`: `m_video_recv_cs.Enter(); for (int vi = 0; vi < m_video_streams.GetSize(); vi++) { VideoRecvState *vs = m_video_streams.Get(vi); if (vs && !strcmp(vs->stream_username, theuser->name.Get())) { memset(vs->prev_ds_guid, 0, 16); memset(vs->last_played_audio_guid, 0, 16); vs->hold_count = 0; vs->empty_count = 0; vs->synced = false; vs->last_played_sender_seq = -1; vs->next.reset(); vs->pending.reset(); vs->accumulating.reset(); } } m_video_recv_cs.Leave();`. Strip the SYNCLOG. **Do not remove the VideoRecvState from m_video_streams** — keep the state alive so a re-join can reuse the same key entry (matches upstream behavior). Plan 21-03 may add an aggressive remove via `removeVideoStream` if testing shows memory pressure; for Plan 21-01 the upstream's leave-alive behavior is faithfully preserved.

    10. **Verification:** rebuild target `JamWideJuce` and run existing test suite — must remain green. The new code is dormant until Plan 21-02 / 21-03 produces a peer broadcasting H.264 against the test surface; for now the dispatchers act as no-ops at runtime (m_video_streams stays empty because no peer broadcasts video yet).
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder"</automated>
    <grep>grep -c "void NJClient::handleVideoRecvBegin_\|void NJClient::handleVideoRecvWrite_\|void NJClient::handleVideoRecvEnd_\|void NJClient::runVideoReceiveBlock_" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "handleVideoRecvBegin_(dib.guid\|handleVideoRecvWrite_(diw.guid\|handleVideoRecvEnd_(diw.guid\|runVideoReceiveBlock_();" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "kHoldCapDrop = 4\|pending.copyFrom(vs-&gt;next)\|playing.copyFrom(vs-&gt;next)\|playing.copyFrom(vs-&gt;pending)" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "m_run_video_receive_block_max_nanos_\|compare_exchange_weak" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "memset(vs-&gt;prev_ds_guid, 0, 16)" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
  </verify>
  <acceptance_criteria>
    - `grep -q "void NJClient::handleVideoRecvBegin_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (codex Cluster 5 helper body present)
    - `grep -q "void NJClient::handleVideoRecvWrite_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (codex Cluster 5 helper body present)
    - `grep -q "void NJClient::handleVideoRecvEnd_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (codex Cluster 5 helper body present)
    - `grep -q "void NJClient::runVideoReceiveBlock_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (codex Cluster 5 helper body present)
    - `grep -q "handleVideoRecvBegin_(dib" /Users/cell/dev/JamWide/src/core/njclient.cpp` (dispatch site wired in BEGIN handler)
    - `grep -q "handleVideoRecvWrite_(diw" /Users/cell/dev/JamWide/src/core/njclient.cpp` (dispatch site wired in WRITE handler)
    - `grep -q "handleVideoRecvEnd_(diw" /Users/cell/dev/JamWide/src/core/njclient.cpp` (dispatch site wired in END handler)
    - `grep -q "runVideoReceiveBlock_();" /Users/cell/dev/JamWide/src/core/njclient.cpp` (dispatch site wired in on_new_interval AND test wrapper)
    - `grep -q "kHoldCapDrop = 4" /Users/cell/dev/JamWide/src/core/njclient.cpp` (GUID-pair tree present)
    - `grep -q "vs-&gt;pending.copyFrom(vs-&gt;next)" /Users/cell/dev/JamWide/src/core/njclient.cpp` (DS-match defer path present)
    - `grep -q "vs-&gt;playing.copyFrom(vs-&gt;next)" /Users/cell/dev/JamWide/src/core/njclient.cpp` (PREV-match immediate path present)
    - `grep -q "vs-&gt;playing.copyFrom(vs-&gt;pending)" /Users/cell/dev/JamWide/src/core/njclient.cpp` (STAGE-1 promote present)
    - `grep -q "m_remoteuser_mirror" /Users/cell/dev/JamWide/src/core/njclient.cpp` (audio-thread reads via mirror)
    - `grep -q "m_run_video_receive_block_max_nanos_" /Users/cell/dev/JamWide/src/core/njclient.cpp` (codex Cluster 1 timing instrumentation landed)
    - `grep -q "compare_exchange_weak" /Users/cell/dev/JamWide/src/core/njclient.cpp` (CAS loop for atomic max-nanos update)
    - `grep -q "memset(vs-&gt;prev_ds_guid, 0, 16)" /Users/cell/dev/JamWide/src/core/njclient.cpp` (user-leave reset present)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder"` exits 0 (no regression)
  </acceptance_criteria>
  <done>Four private state-machine helper bodies landed (codex Cluster 5 — single source of truth: production dispatchers + test dispatchers + audio-thread on_new_interval all call the same code). Dispatch sites in BEGIN, WRITE, END handlers each reduce to a single helper invocation. Audio-thread `runVideoReceiveBlock_()` carries the upstream :3084-3256 logic with the Phase 15.1-07a HIGH-2 carve-out for `m_remoteuser_mirror[s].next_ds[0]->guid`. Per codex Cluster 1: JAMWIDE_BUILD_TESTS-gated timing instrumentation wraps the receive block with steady_clock measurement and updates the atomic max-nanos counter via a CAS loop — zero overhead in production builds. User-leave reset block landed at the m_remoteusers Delete site per upstream :1306-1322. Existing test suite remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add JAMWIDE_BUILD_TESTS dispatcher helpers + write test_video_recv_state.cpp covering 11 WIRE-02 sub-tests (nine original + two malformed-length) + wire CMake</name>
  <files>src/core/njclient.h, src/core/njclient.cpp, tests/test_video_recv_state.cpp, CMakeLists.txt</files>
  <read_first>
    - /Users/cell/dev/JamWide/tests/test_video_state_machine.cpp (entire file — Plan 21-01 mirrors this test-style: TEST/PASS/FAIL macros, NJClient instantiation in unique_ptr, MAX_NJ_FOURCC redefinition, JAMWIDE_BUILD_TESTS guarded helpers)
    - /Users/cell/dev/JamWide/tests/test_video_fourcc.cpp (Phase 14.3-03 substrate test — uses AddTestRemoteUser / DispatchTestServerDownloadIntervalBegin/Write pattern)
    - /Users/cell/dev/JamWide/src/core/njclient.h (Task 1+2 outputs — handleVideoRecvBegin_/Write_/End_/runVideoReceiveBlock_ helpers + timing atomics)
    - /Users/cell/dev/JamWide/src/core/njclient.cpp (Task 2 outputs — helper bodies)
    - /Users/cell/dev/JamWide/CMakeLists.txt (lines 504-541 — test_video_state_machine + test_video_fourcc add_executable + add_test pattern, ESPECIALLY the JAMWIDE_BUILD_TESTS=1 target_compile_definitions and add_test wiring)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-VALIDATION.md (lines 38-65 — Per-Task Verification Map: 9 sub-tests sub-test names ds_match_defers_to_pending, prev_match_plays_immediately, no_match_holds_then_drops_at_4, pending_promotes_to_playing_on_next_swap, mid_download_start_playing, user_leave_resets_video_sync_state, split_frame_reassembles_via_length_prefix, marker_parse_extracts_guid_and_seq, burst_begin_discards_stale_next)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-RESEARCH.md (lines 1283-1308 — Phase Requirements → Test Map: sub-test names AND scenario descriptions)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md (codex Cluster 10 — malformed-length tests: zero-length frame, prefix > 4 MB; codex Cluster 1 — timing instrumentation assertion in tests)
    - /Users/cell/dev/ninjamzap-core/tests/video-sync/scenarios/02_video_one_interval_early.cpp (the scenario that DS-match defers must protect against)
    - /Users/cell/dev/ninjamzap-core/tests/video-sync/scenarios/03_late_join.cpp (mid-stream join → burst-BEGIN-discards-stale-next coverage)
    - /Users/cell/dev/ninjamzap-core/tests/video-sync/scenarios/20_drop_resync_recovery.cpp (kHoldCapDrop=4 drop_resync_count behavior)
    - /Users/cell/dev/ninjamzap-core/tests/video-sync/scenarios/22_audio_then_video.cpp (audioHasData=false path + audio arriving later)
  </read_first>
  <behavior>
    - **Test dispatchers (codex Cluster 5 — call into production helpers, NO state-machine duplication):** the JAMWIDE_BUILD_TESTS test-helper functions are THIN WRAPPERS that delegate to the same private helpers production code calls. They do NOT inline a copy of the BEGIN/WRITE/END logic.
      - `void DispatchTestVideoRecvBegin(const unsigned char guid[16], unsigned int fourcc, const char *username, int chidx) { handleVideoRecvBegin_(guid, fourcc, username, chidx); }` — one-line forwarder
      - `void DispatchTestVideoRecvWrite(const unsigned char guid[16], const void *data, int dataLen, bool isEnd) { if (data && dataLen > 0) handleVideoRecvWrite_(guid, data, dataLen); if (isEnd) handleVideoRecvEnd_(guid); }` — forwards write then conditionally end
      - `void DispatchTestVideoRecvEnd(const unsigned char guid[16]) { handleVideoRecvEnd_(guid); }` — forwarder
      - `void RunOnNewIntervalReceiveBlockForTest() { runVideoReceiveBlock_(); }` — forwarder (also picks up the timing instrumentation under JAMWIDE_BUILD_TESTS automatically)
      - `VideoRecvState* GetVideoStreamForTest(const char *username, int chidx) { return findVideoStream(username, chidx); }` — read-only inspection
      - `void AddTestRemoteUserMirrorWithDs(int slot, const char *username, int channel, const unsigned char guid[16])` — populates m_remoteuser_mirror[slot] for the GUID-pair test (slot active, username copied, next_ds[0] pointing at a heap-allocated DecodeState with the given guid). Test owns the DecodeState; helper bumps a destruction counter so the test can verify proper cleanup.
      - `void DispatchTestUserLeaveForVideoReset(const char *username)` — runs the user-leave reset block from Task 2 (acquire m_video_recv_cs, iterate m_video_streams, reset matching VideoRecvState's scalars + slots, release). Replicates the inline user-leave block from Task 2 step 9.
    - **test_video_recv_state.cpp** contains 11 sub-tests (9 original + 2 malformed-length per codex Cluster 10) with PASS/FAIL/TEST macros mirroring test_video_state_machine.cpp:
      1. `test_marker_parse_extracts_guid_and_seq` — Begin → Write 20-byte marker payload `[BE swap_count=42][16B audio_guid=0xAA pattern]` wrapped with `[4B BE outer=20]` outer prefix → assert vs->accumulating.audio_guid bytes match + sender_seq==42 — codex Cluster 6 contract verification: the OUTER prefix value is 20, the PAYLOAD size in `data` is 20 bytes.
      2. `test_split_frame_reassembles_via_length_prefix` — Begin → Write `[4B BE prefix=20][8B of payload]` chunk1 → Write `[remaining 12B of payload]` chunk2 → assert frameCount==1, target->data.GetSize()==20, pending_remaining==0 (per codex Cluster 6 contract: data holds the 20-byte payload, NOT including the outer prefix).
      3. `test_mid_download_start_playing` — Begin → Write 20-byte marker frame complete → assert vs->next.active==true, vs->accumulating.active==false, vs->append_to_next==true (the WRITE handler promotes accumulating → next as soon as first frame completes)
      4. `test_burst_begin_discards_stale_next` — Begin → Write marker → End (next becomes populated) → IMMEDIATELY Begin again with new GUID → assert vs->next.active==false (burst-detection cleared stale next)
      5. `test_pending_promotes_to_playing_on_next_swap` — Manually populate vs->pending (via helper or DispatchTestVideoRecvBegin+Write+End with deferred-to-pending state) → RunOnNewIntervalReceiveBlockForTest() → assert vs->playing.active==true, vs->playing.frameCount matches, vs->pending.active==false **AND** `getRunVideoReceiveBlockMaxNanosForTest()` returns > 0 (codex Cluster 1 timing instrumentation actually firing)
      6. `test_ds_match_defers_to_pending` — Populate vs->next via Begin+Write(20B marker payload with audio_guid=0xAA, outer prefix=20)+End → AddTestRemoteUserMirrorWithDs(slot=0, "alice", ch=0, guid=0xAA pattern matching the marker's audio_guid) → RunOnNewIntervalReceiveBlockForTest() → assert vs->next.active==false, vs->pending.active==true, vs->pending.frameCount matches, vs->append_to_pending==true (DS-match defers 1 swap)
      7. `test_prev_match_plays_immediately` — Populate vs->next via Begin+Write+End with audio_guid=0xBB → manually set vs->prev_ds_guid = 0xBB pattern → RunOnNewIntervalReceiveBlockForTest() → assert vs->next.active==false, vs->playing.active==true, vs->pending.active==false (PREV-match plays immediately, no 1-swap defer)
      8. `test_no_match_holds_then_drops_at_4` — Populate vs->next with audio_guid=0xCC; populate m_remoteuser_mirror with a DIFFERENT guid 0xDD → loop: RunOnNewIntervalReceiveBlockForTest 3 times → assert vs->hold_count == 3 each step and vs->next.active still true → 4th call → assert vs->next.active==false (force-resync), vs->hold_count==0, vs->synced==false, vs->drop_resync_count==1
      9. `test_user_leave_resets_video_sync_state` — Populate VideoRecvState for "alice:1" with prev_ds_guid + hold_count + synced + next + pending → call DispatchTestUserLeaveForVideoReset("alice") → assert all fields cleared: prev_ds_guid all-zero, hold_count=0, synced=false, accumulating/next/pending all .active==false
      10. **(NEW per codex Cluster 10) `test_zero_length_frame_drops_cleanly`** — Begin → Write `[4B BE prefix=0]` outer prefix indicating zero-length frame body → no body bytes → assert vs->accumulating.frameCount remains 0 (the WRITE handler sees pending_remaining=0, increments frameCount to 1 with frame size 0, or rejects the zero-length frame — assert the EXACT behavior matches the upstream :1452-1564 byte-for-byte; specifically the upstream `if (target->pending_remaining > 0)` check at upstream :1502-1514 short-circuits zero-length frames). No crash, no out-of-bounds read, target->data.GetSize() unchanged from pre-state.
      11. **(NEW per codex Cluster 10) `test_oversize_prefix_clamps_at_4mb_cap`** — Begin → Write `[4B BE prefix=0xFFFFFFFF]` (4 GB declared) followed by 128 bytes of body → assert no crash, no allocation beyond the pre-allocated 4 MB cap. Expected behavior: the upstream pattern (target->pending_remaining = bodyLen; target->data.Resize(curSize + take, false) where take = min(writeLeft, pending_remaining)) clamps allocation to the existing 4 MB pre-allocation since Resize within capacity is realloc-free. After the 128 bytes are accumulated, target->pending_remaining still ~4 GB - 128, frameCount still 0 (frame not yet complete). The test then Writes End → asserts the partial frame is discarded (because frameCount > 1 not satisfied, accumulating gets reset on End). Specifically asserts: `vs->accumulating.active == false` after End, no abort, no out-of-memory crash. (This test guards T-21-01 / T-21-02 mitigations.)
    - CMakeLists.txt grows a test_video_recv_state add_executable + add_test entry (lines mirror test_video_state_machine at 504-511)
    - `ctest -R video_recv_state -V` exits 0 with all 11 sub-tests passing (tests_run == tests_passed == 11)
  </behavior>
  <action>
    Per codex Cluster 5 (single-source helpers) + Cluster 10 (malformed-length tests) + Cluster 1 (timing assertion):

    1. **njclient.h:** Inside the existing `#ifdef JAMWIDE_BUILD_TESTS` block at line 795 (right after the existing `DispatchTestServerDownloadIntervalWrite` declaration at line 833), declare:
       ```cpp
       void DispatchTestVideoRecvBegin(const unsigned char guid[16], unsigned int fourcc,
                                       const char *username, int chidx);
       void DispatchTestVideoRecvWrite(const unsigned char guid[16],
                                       const void *data, int dataLen, bool isEnd);
       void DispatchTestVideoRecvEnd(const unsigned char guid[16]);
       void RunOnNewIntervalReceiveBlockForTest();
       VideoRecvState* GetVideoStreamForTest(const char *username, int chidx);
       void AddTestRemoteUserMirrorWithDs(int slot, const char *username, int channel,
                                          const unsigned char guid[16]);
       void DispatchTestUserLeaveForVideoReset(const char *username);
       ```

    2. **njclient.cpp:** In the existing `#ifdef JAMWIDE_BUILD_TESTS` region (find by `grep -n "void NJClient::DispatchTestServerDownloadIntervalBegin"`), add the seven implementations adjacent to the existing dispatcher functions. **Per codex Cluster 5 — these MUST be thin forwarders, NOT duplicated state-machine logic:**
       - `DispatchTestVideoRecvBegin` body: ONE line — `handleVideoRecvBegin_(guid, fourcc, username, chidx);`
       - `DispatchTestVideoRecvWrite` body: `if (data && dataLen > 0) handleVideoRecvWrite_(guid, data, dataLen); if (isEnd) handleVideoRecvEnd_(guid);` — two-line forwarder
       - `DispatchTestVideoRecvEnd` body: ONE line — `handleVideoRecvEnd_(guid);`
       - `RunOnNewIntervalReceiveBlockForTest` body: ONE line — `runVideoReceiveBlock_();` (also automatically picks up the timing instrumentation under JAMWIDE_BUILD_TESTS)
       - `GetVideoStreamForTest` body: ONE line — `return findVideoStream(username, chidx);`
       - `AddTestRemoteUserMirrorWithDs` body: populates m_remoteuser_mirror[slot] with `active=true`, copies username to the mirror's name field (look up the mirror struct's field name in src/core/njclient.h), sets `next_ds[0]` to a new DecodeState with `decode_codec=nullptr`, `decode_buf=nullptr`, and `memcpy(decode_state.guid, guid, 16)`. Heap-allocates a DecodeState which the test must own; do NOT defer-delete via the SPSC here (the test is single-threaded inline). The mirror remains live until the next AddTestRemoteUserMirrorWithDs(slot, ...) call overwrites it.
       - `DispatchTestUserLeaveForVideoReset` body: replicates the user-leave reset block from Task 2 step 9 (acquire m_video_recv_cs, iterate m_video_streams, reset matching VideoRecvState's prev_ds_guid + last_played_audio_guid + hold_count + empty_count + synced + last_played_sender_seq + next.reset + pending.reset + accumulating.reset, release).

    3. **tests/test_video_recv_state.cpp:** New file. Header block mirrors test_video_state_machine.cpp:
       - Include `core/njclient.h`, `wdl/heapbuf.h`, standard headers
       - `static constexpr int MAX_ENC_BLOCKSIZE = 8192 + 1024;` (replication of the file-static)
       - `#define MAKE_NJ_FOURCC(A,B,C,D) ((A) | ((B)&lt;&lt;8) | ((C)&lt;&lt;16) | ((D)&lt;&lt;24))`
       - Static tests_run / tests_passed counters + TEST / PASS / FAIL macros (same shape as test_video_state_machine)
       - Helper `make_guid(unsigned char pattern)` returning `unsigned char[16]` of a recognizable bit pattern (e.g., all bytes = `pattern`)
       - Helper `write_be_u32(unsigned char *out, uint32_t v)` for the 4-byte BE length prefix
       - Helper `make_20b_marker_payload(uint32_t sender_seq, const unsigned char audio_guid[16])` returning a 20-byte vector (codex Cluster 6 contract: payload IS 20 bytes; OUTER prefix value is 20)
       - Helper `make_wire_frame_bytes(const std::vector<unsigned char>& payload)` returning `[4B BE outer=payload.size()][payload]` — the BYTES that the WRITE handler will accumulate, with the outer prefix as it appears on the wire

       Then eleven `static void test_<sub_test_name>()` functions (one per sub-test, names from behavior section above). Each:
       - Calls `TEST("<sub_test_name>")`
       - Instantiates `std::unique_ptr&lt;NJClient&gt; client_owned(new NJClient);`
       - Drives the state machine via DispatchTestVideoRecvBegin/Write/End and helpers
       - Asserts via inline `if (cond) { FAIL("explanation"); return; }` then `PASS();`
       - Cleans up: test-owned DecodeState pointers freed before client_owned destructs

       For `test_pending_promotes_to_playing_on_next_swap`: AFTER the runVideoReceiveBlock call, ALSO assert `client_owned->getRunVideoReceiveBlockMaxNanosForTest() > 0` (codex Cluster 1 — verifies the timing instrumentation is actually firing).

       For `test_zero_length_frame_drops_cleanly` (NEW): construct a wire frame with `[4B BE outer=0]` and zero body bytes; pass via DispatchTestVideoRecvWrite; assert vs->accumulating.frameCount remains 0 OR (if upstream allows zero-length entries) frameCount==1 and frameOffsets[1] == frameOffsets[0]. Read upstream :1502-1514 to determine the exact correct assertion; cite the upstream line number in the test comment.

       For `test_oversize_prefix_clamps_at_4mb_cap` (NEW): construct a wire chunk with `[4B BE outer=0xFFFFFFFF]` followed by 128 bytes of body. Pass via DispatchTestVideoRecvWrite. Assert no crash. The test should also call DispatchTestVideoRecvEnd and assert vs->accumulating gets reset (frame never completes). Time-bound the test (loop-guard with timeout) so a runaway never hangs ctest.

       `int main(void)` runs all eleven in sequence, then `return tests_run == tests_passed ? 0 : 1;`

    4. **CMakeLists.txt:** After the existing `add_test(NAME video_state_machine COMMAND test_video_state_machine)` (line 511), add a parallel block:
       ```cmake
       # Plan 21-01 Task 3: receive-side state-machine + GUID-pair tree test (11 sub-tests).
       # JAMWIDE_BUILD_TESTS-gated pure-C++ test; mirrors test_video_state_machine.
       add_executable(test_video_recv_state tests/test_video_recv_state.cpp)
       target_include_directories(test_video_recv_state PRIVATE
           ${CMAKE_CURRENT_SOURCE_DIR}/src
           ${CMAKE_CURRENT_SOURCE_DIR}
       )
       target_compile_definitions(test_video_recv_state PRIVATE JAMWIDE_BUILD_TESTS=1)
       target_link_libraries(test_video_recv_state PRIVATE njclient)
       add_test(NAME video_recv_state COMMAND test_video_recv_state)
       ```

    5. **Verification:** `./scripts/build.sh test_video_recv_state` builds the new target cleanly. `cd build-juce && ctest -R video_recv_state -V` runs the test executable; output shows all eleven sub-tests PASSING. Existing suite green: `ctest -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state"` exits 0.
  </action>
  <verify>
    <automated>cd /Users/cell/dev/JamWide &amp;&amp; ./scripts/build.sh test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state &amp;&amp; cd build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state"</automated>
    <grep>grep -c "test_ds_match_defers_to_pending\|test_prev_match_plays_immediately\|test_no_match_holds_then_drops_at_4\|test_pending_promotes_to_playing_on_next_swap\|test_mid_download_start_playing\|test_user_leave_resets_video_sync_state\|test_split_frame_reassembles_via_length_prefix\|test_marker_parse_extracts_guid_and_seq\|test_burst_begin_discards_stale_next\|test_zero_length_frame_drops_cleanly\|test_oversize_prefix_clamps_at_4mb_cap" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp</grep>
    <grep>grep -c "DispatchTestVideoRecvBegin\|DispatchTestVideoRecvWrite\|RunOnNewIntervalReceiveBlockForTest\|GetVideoStreamForTest\|AddTestRemoteUserMirrorWithDs" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "handleVideoRecvBegin_(guid, fourcc\|handleVideoRecvWrite_(guid, data\|handleVideoRecvEnd_(guid)\|runVideoReceiveBlock_()" /Users/cell/dev/JamWide/src/core/njclient.cpp</grep>
    <grep>grep -c "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp</grep>
    <grep>grep -c "add_test(NAME video_recv_state COMMAND test_video_recv_state)" /Users/cell/dev/JamWide/CMakeLists.txt</grep>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp` (file exists)
    - `grep -c "static void test_" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp` returns at least 11 (all eleven sub-tests defined)
    - All eleven sub-test names appear in the file: `for n in test_ds_match_defers_to_pending test_prev_match_plays_immediately test_no_match_holds_then_drops_at_4 test_pending_promotes_to_playing_on_next_swap test_mid_download_start_playing test_user_leave_resets_video_sync_state test_split_frame_reassembles_via_length_prefix test_marker_parse_extracts_guid_and_seq test_burst_begin_discards_stale_next test_zero_length_frame_drops_cleanly test_oversize_prefix_clamps_at_4mb_cap; do grep -q "$n" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp || exit 1; done` exits 0
    - `grep -q "DispatchTestVideoRecvBegin" /Users/cell/dev/JamWide/src/core/njclient.h` (declaration present)
    - `grep -q "void NJClient::DispatchTestVideoRecvBegin" /Users/cell/dev/JamWide/src/core/njclient.cpp` (implementation present)
    - `grep -q "handleVideoRecvBegin_(guid" /Users/cell/dev/JamWide/src/core/njclient.cpp` (test dispatcher forwards to production helper per codex Cluster 5)
    - `grep -q "void NJClient::RunOnNewIntervalReceiveBlockForTest" /Users/cell/dev/JamWide/src/core/njclient.cpp` (test wrapper for audio-thread block present)
    - `grep -q "runVideoReceiveBlock_()" /Users/cell/dev/JamWide/src/core/njclient.cpp` (RunOnNewIntervalReceiveBlockForTest body delegates to single-source helper)
    - `grep -q "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp` (codex Cluster 1: at least one test asserts the timing counter is actually accumulating > 0)
    - `grep -q "add_test(NAME video_recv_state COMMAND test_video_recv_state)" /Users/cell/dev/JamWide/CMakeLists.txt` (CMake wiring)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "^video_recv_state$"` exits 0 (test executable green; tests_run == tests_passed == 11)
    - `cd /Users/cell/dev/JamWide/build-juce &amp;&amp; ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state"` exits 0 (whole suite green, no regression)
  </acceptance_criteria>
  <done>11 WIRE-02 sub-tests pass against the Task 2 implementation: nine original behavior tests plus two new malformed-length tests (zero-length frame, oversize prefix) per codex Cluster 10. All test dispatchers are thin forwarders to the same private helpers production code calls (codex Cluster 5 — no state-machine duplication). At least one sub-test (test_pending_promotes_to_playing_on_next_swap) asserts the JAMWIDE_BUILD_TESTS timing counter is actually accumulating values > 0 (codex Cluster 1 — instrumentation is live, not vestigial). test_video_recv_state is wired into CTest.</done>
</task>

<task type="auto">
  <name>Task 4: Write Phase 21 audit-allowlist envelope into .claude/agents/realtime-audio-reviewer.md with explicit parity-only scope (codex Cluster 1)</name>
  <files>.claude/agents/realtime-audio-reviewer.md</files>
  <read_first>
    - /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md (entire file — Phase 20 envelope structure at lines 164-244 is the template Phase 21 mirrors)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-CONTEXT.md (D-16 envelope categories: m_video_recv_cs Enter/Leave, WDL_PtrList<VideoRecvState> iteration, scalar VideoRecvBuffer reads, copyFrom 4 MB memcpy, m_remoteuser_mirror[s].next_ds[0]->guid access, no writeLog)
    - /Users/cell/dev/JamWide/.planning/phases/20-h-264-encoder-send-pipeline/20-CONTEXT.md (D-09 envelope template — Phase 21's is the parallel inverse)
    - /Users/cell/dev/JamWide/src/core/njclient.cpp (the actual line numbers where `runVideoReceiveBlock_` and the user-leave reset block live, AS COMMITTED IN TASK 2 — use grep after Task 2 commits to find the real line numbers)
    - /Users/cell/dev/JamWide/.planning/phases/21-h-264-decoder-receive-pipeline/21-REVIEWS.md (codex Cluster 1 — envelope wording must distinguish 'parity-only' from 'safe in general'; codex Cluster 1 — timing instrumentation reference)
  </read_first>
  <action>
    Per D-16 + codex Cluster 1 (parity-only wording; timing-counter reference), write the Phase 21 envelope entries into `.claude/agents/realtime-audio-reviewer.md` as a NEW section appended after the existing `### Auditor zero-CRITICAL gate scope` block (the last section before EOF). Section header: `## Phase 21 audit allowlist envelope` (parallel to the existing `## Phase 20 audit allowlist envelope` at line 164).

    **Decision authority:** CONTEXT.md D-14 / D-15 / D-16 / D-20-mirror + REVIEWS.md codex Cluster 1.
    **Status:** Published 2026-MM-DD by Plan 21-01 Task 4; line-number-refreshed by same commit (Task 2 + Task 3 lines are stable after this task lands).

    Subsections (mirror Phase 20's structure):

    ### Envelope scope statement (Cluster 1 PARITY-ONLY clause — REQUIRED FIRST PARAGRAPH)

    Insert this verbatim as the first paragraph of the Phase 21 envelope section:

    > **The Phase 21 envelope is accepted ONLY as parity with the upstream NinjamZap source port** (`ninjamzap-core/njclient.cpp:3084-3256`). The envelope MUST NOT be treated as a general permission for similar audio-thread work in unrelated subsystems. Specifically:
    >
    > - The `m_video_recv_cs` acquire on the audio thread is accepted BECAUSE the upstream NinjamZap audio-thread `on_new_interval` does the equivalent acquire and JamWide is a byte-for-byte port. Any FUTURE audio-thread mutex acquire in another subsystem requires a SEPARATE design review — it does not inherit acceptance from Phase 21.
    > - The `VideoRecvBuffer::copyFrom` up to 4 MB on the audio thread is accepted BECAUSE it is bounded (one per peer per swap; pre-allocated; realloc-free per D-11) AND because upstream does the equivalent. Any FUTURE audio-thread memcpy in another subsystem requires its own bounded-cost analysis.
    > - The `m_remoteuser_mirror[s].next_ds[0]->guid` read is accepted via the Phase 15.1-07a HIGH-2 carve-out which already underwent its own design review.
    >
    > **Timing instrumentation requirement (codex Cluster 1):** the `runVideoReceiveBlock_` helper carries a JAMWIDE_BUILD_TESTS-gated steady_clock timing wrapper (Task 2). Tests are required to read `getRunVideoReceiveBlockMaxNanosForTest()` and assert sane bounds under simulated peer load. Phase 21-03 UAT MUST report a 3-peer max-nanos number; if it exceeds 1 ms (1,000,000 ns) per swap under steady-state, escalate to a follow-up design review.

    ### src/core/njclient.cpp `NJClient::runVideoReceiveBlock_()` (Plan 21-01 Task 2 — single source of truth for audio-thread video receive)

    For each accepted carve-out site, write a `- src/core/njclient.cpp:<line> — <description>` entry with the concrete line number obtained from the actual committed code. Required entries:
    - The `WDL_MutexLock vrlock(&m_video_recv_cs)` opening (D-15, NinjamZap-literal whole-block serialization parallel to D-08 send-side)
    - The `m_video_streams.GetSize()` + `m_video_streams.Get(vi)` iteration calls (read-only structural traversal under m_video_recv_cs)
    - The `m_remoteuser_mirror[s].next_ds[0]` field read for `senderDs->guid` GUID-pair comparison (Phase 15.1-07a HIGH-2 carve-out reused; ONLY the .guid field, no other DecodeState member)
    - `VideoRecvBuffer::copyFrom(...)` calls inside STAGE-1 promote, DS-match defer, PREV-match immediate play (D-16 accepts Resize + memcpy up to 4 MB per peer per swap; cost ~0.4 ms per peer at HD per RESEARCH §Pitfall 6)
    - Scalar reads/writes on VideoRecvBuffer fields (frameCount, active, audio_guid memcmp, sender_seq, frameOffsets reads)
    - Scalar writes to VideoRecvState fields (hold_count, empty_count, synced, last_played_sender_seq, last_played_audio_guid, drop_resync_count, prev_ds_guid memcpy)
    - **(JAMWIDE_BUILD_TESTS-only)** steady_clock measurement and atomic max-nanos / peer-count update around the inner scope (codex Cluster 1 — zero overhead in production builds; entire block compiles out)
    - **(Forward reference)** Slot-snapshot push onto VideoRecvState's pre-allocated 4-buffer ring — Plan 21-02 redesign per codex Cluster 2 replaces the previously-envisioned SpscRing<VideoRecvBufferView, 4> with VideoRecvState-owned slots; the audio-thread cost remains one bounded memcpy per peer per swap (~0.4 ms at HD, within the copyFrom allowlist).

    ### src/core/njclient.cpp on the audio-thread receive path

    **No `writeLog` / `SYNCLOG` / `printf` calls remain.** Plan 21-01 Task 2 stripped upstream SYNCLOG sites. NinjamZap source has SYNCLOG which is a no-op-able macro; JamWide port does NOT carry over the macro nor any equivalent — symmetric to Phase 20 R2 H7 + R4 M12.

    **No `WaitableEvent::signal()` calls remain (codex Cluster 1).** Plan 21-02 redesign moves the decoder-thread wake-up off the audio thread entirely — Plan 21-02's pushSlotSnapshot uses an atomic sequence counter that the decoder thread polls with a short timed wait, NOT a `juce::WaitableEvent::signal()` from the audio thread. See Plan 21-02 for the implementation.

    ### Auditor zero-CRITICAL gate scope (UPDATE existing)

    Update the existing `### Auditor zero-CRITICAL gate scope` section's wording to read: "All other audio-path violations remain CRITICAL — these are the only carve-outs accepted under the **D-09 (Phase 20)** + **D-16 (Phase 21, parity-only)** envelopes. The auditor zero-CRITICAL gate applies OUTSIDE these envelopes. Phase 21's envelope explicitly forbids generalization-by-analogy to other subsystems."

    Line-number population: after Task 2 + Task 3 commits, run `grep -n "WDL_MutexLock vrlock(&m_video_recv_cs)\|m_remoteuser_mirror\[.*\]\.next_ds\[0\]\|playing\.copyFrom\|pending\.copyFrom\|kHoldCapDrop = 4\|prev_ds_guid, 0, 16\|m_video_streams\.GetSize" /Users/cell/dev/JamWide/src/core/njclient.cpp` and use the actual line numbers in the envelope entries — do NOT leave `<line-placeholder>` tokens. Phase-close grep gate (parallel to Phase 20 R4 M13) is: `! grep -E "<line-placeholder>|TODO Plan 21" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` must exit non-zero (no placeholders left).
  </action>
  <verify>
    <automated>! grep -E "&lt;line-placeholder&gt;|TODO Plan 21" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</automated>
    <grep>grep -c "## Phase 21 audit allowlist envelope" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</grep>
    <grep>grep -c "parity with the upstream NinjamZap source port\|generalization-by-analogy" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</grep>
    <grep>grep -c "m_video_recv_cs\|m_remoteuser_mirror\|VideoRecvBuffer::copyFrom" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</grep>
    <grep>grep -c "WaitableEvent::signal" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</grep>
    <grep>grep -c "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md</grep>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Phase 21 audit allowlist envelope" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (section header present)
    - `grep -q "parity with the upstream NinjamZap source port" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (codex Cluster 1 — parity-only clause)
    - `grep -q "generalization-by-analogy" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` OR `grep -q "does not inherit acceptance from Phase 21" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (codex Cluster 1 — explicit anti-generalization clause)
    - `grep -q "WDL_MutexLock vrlock(&amp;m_video_recv_cs)" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (mutex acquire site flagged)
    - `grep -q "VideoRecvBuffer::copyFrom" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (copyFrom carve-out flagged)
    - `grep -q "m_remoteuser_mirror" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (HIGH-2 carve-out flagged)
    - `grep -q "No \`writeLog\`" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (writeLog hygiene gate)
    - `grep -q "No \`WaitableEvent::signal()\`" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (codex Cluster 1 — explicit no-signal clause referencing Plan 21-02 redesign)
    - `grep -q "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` (codex Cluster 1 — timing instrumentation referenced)
    - `! grep -E "&lt;line-placeholder&gt;|TODO Plan 21" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md` exits 0 (no placeholders left)
    - Every audit-allowlist entry references a real file:line in src/core/njclient.cpp that exists in the committed code (manual spot-check ≥3 lines)
  </acceptance_criteria>
  <done>Phase 21 envelope landed in .claude/agents/realtime-audio-reviewer.md with concrete line numbers (no placeholders). Auditor's zero-CRITICAL scope updated to reference both D-09 and D-16 envelopes. **Parity-only clause** explicitly states the envelope is acceptance-by-source-port, not general permission; **anti-generalization clause** forbids inheriting Phase 21's envelope by analogy in unrelated subsystems (codex Cluster 1). **No-WaitableEvent-signal clause** documents that Plan 21-02 moves the decoder wake-up off the audio thread (codex Cluster 1). **Timing instrumentation reference** points at `getRunVideoReceiveBlockMaxNanosForTest()` as a required UAT measurement surface (codex Cluster 1). writeLog hygiene call-out preserved (no audio-thread log calls on the receive path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NINJAM server → run thread | Untrusted: peer-controlled video bytes arrive over TCP. WRITE handler accumulates bytes into VideoRecvBuffer slots; 24-byte marker + 4-byte BE length prefix parsing must be robust to malformed input (drop frame, no crash). |
| Run thread → audio thread (via m_video_recv_cs) | Per-peer VideoRecvState shared between run-thread WRITE-handler and audio-thread on_new_interval SWAP. m_video_recv_cs serialises; Phase 21 D-16 envelope-accepts the audio-thread acquire AS PARITY WITH UPSTREAM SOURCE PORT (codex Cluster 1 — not generalizable to other subsystems). |
| Audio thread → Phase 15.1-07a mirror | Audio thread reads m_remoteuser_mirror[s].next_ds[0]->guid for the GUID-pair decision. HIGH-2 carve-out already accepted in Phase 15.1-07a. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | WRITE-handler 4B BE length-prefix parser | mitigate | If `bodyLen > 4MB - currentSize`, the per-slot 4 MB pre-allocation (D-11) clamps the Resize via the upstream pattern; `target->data.Resize(curSize + take, false)` is bounded by `take = min(writeLeft, pending_remaining)`. Adversary cannot drive allocation beyond the cap. Verified by test_split_frame_reassembles_via_length_prefix AND test_oversize_prefix_clamps_at_4mb_cap (codex Cluster 10 new test). |
| T-21-02 | Denial of Service | WRITE-handler accumulating slot 4 MB cap | mitigate | D-11: pre-allocated WDL_HeapBuf at slot ctor; subsequent Resize within 4 MB is realloc-free. If wire bytes attempt to exceed 4 MB, the upstream's `pending_remaining` clamp + take-min logic naturally bounds the accumulation — frame is silently truncated, frame complete check fails, frame is silently dropped. No allocation in the WRITE handler beyond the pre-allocated cap. Verified by Task 1 ctor pattern + Task 3 test_oversize_prefix_clamps_at_4mb_cap (codex Cluster 10). |
| T-21-03 | Tampering | 24-byte marker parser | mitigate | Marker is parsed ONLY when `targetIsAccumulating && fc == 1 && frameSize == 20` (per codex Cluster 6 contract: payload is 20 bytes, outer prefix value is 20). Strict bounds check; no out-of-bounds read possible since frameSize is computed from frameOffsets entries we wrote. Verified by test_marker_parse_extracts_guid_and_seq. |
| T-21-04 | Information Disclosure / Crash | m_remoteuser_mirror[s].next_ds[0]->guid audio-thread read | accept | Phase 15.1-07a HIGH-2 carve-out: DecodeState ownership crosses thread boundaries via SPSC handoff with deferred-delete; the `.guid` field is fixed at DecodeState construction and never written after — safe to read on audio thread under the existing carve-out. Audit-allowlist entry added by Task 4. |
| T-21-05 | Spoofing | (username, chidx) keying in findOrCreateVideoStream | mitigate | Username comes from NJClient's authenticated peer identity (server-vouched). Cross-channel collision (same peer on chidx 1 AND chidx 2) just creates two states — not a security issue (same-peer can broadcast on multiple channels by design). |
| T-21-06 | Denial of Service | m_video_streams growth (peer never leaves) | accept | Upstream's leave-alive behavior is preserved by Plan 21-01 Task 2's user-leave reset (state stays alive after a leave, fields reset). Memory cost per VideoRecvState in Plan 21-01: 4 slots × 4 MB = 16 MB per peer. After Plan 21-02 redesign (codex Cluster 2), VideoRecvState also gains 4 owned decoder slot snapshots × ~4 MB = additional ~16 MB per peer = ~32 MB total per active peer. At realistic populated NINJAM sizes (≤8 peers), bounded at ~256 MB — predictable and documented per RESEARCH §Pitfall 6 / §Security Domain. Phase 21-03 + Phase 24 will profile under HD-scale; if cap pressure shows, removeVideoStream lands as a later optimization. |
| T-21-07 | Crash | VideoRecvBuffer::copyFrom on audio thread under HD × N peers | mitigate | D-11 pre-allocation makes the underlying Resize realloc-free; the memcpy cost is bounded at ~0.4 ms per peer at HD per RESEARCH §Pitfall 6. Audit-allowlist envelope (Task 4) accepts the copyFrom under parity-only scope. **NEW per codex Cluster 1:** `runVideoReceiveBlock_` is now instrumented under JAMWIDE_BUILD_TESTS with steady_clock max-nanos tracking; Phase 21-03 UAT MUST report a 3-peer max-nanos number under load. If it exceeds 1 ms per swap, escalate to follow-up design review. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 21 installs zero new packages (all deps vendored as Phase 14.3-01 .dylibs / .DLLs). Slopcheck N/A. Per RESEARCH §Package Legitimacy Audit. |
</threat_model>

<verification>
After all four tasks land, run the full Phase 20+21 test suite to verify no regression:

```bash
cd /Users/cell/dev/JamWide && ./scripts/build.sh test_rawdata_send test_video_state_machine test_video_fourcc test_video_encoder test_video_recv_state JamWideJuce_Standalone
cd build-juce && ctest --output-on-failure -R "rawdata_send|video_state_machine|video_fourcc|video_encoder|video_recv_state"
```

All five test executables must exit 0 (test_video_recv_state runs 11 sub-tests; tests_run == tests_passed == 11). The JamWideJuce_Standalone build verifies the audio-thread receive block compiles cleanly under -Werror inside the full plugin target.

Audit-allowlist line-number freshness:
```bash
! grep -E "<line-placeholder>|TODO Plan 21" /Users/cell/dev/JamWide/.claude/agents/realtime-audio-reviewer.md
```

D-16 hygiene gate (no writeLog / SYNCLOG inside the audio-thread receive block):
```bash
awk '/void NJClient::runVideoReceiveBlock_/,/^\}$/' /Users/cell/dev/JamWide/src/core/njclient.cpp \
  | grep -v '^#\|^//' | ! grep -E "writeLog|SYNCLOG|m_users_cs\.Enter"
```

Codex Cluster 1 gate (timing instrumentation is wired):
```bash
grep -q "m_run_video_receive_block_max_nanos_" /Users/cell/dev/JamWide/src/core/njclient.cpp \
  && grep -q "compare_exchange_weak" /Users/cell/dev/JamWide/src/core/njclient.cpp \
  && grep -q "getRunVideoReceiveBlockMaxNanosForTest" /Users/cell/dev/JamWide/tests/test_video_recv_state.cpp
```

Codex Cluster 5 gate (no state-machine duplication — test dispatchers are forwarders only):
```bash
# DispatchTestVideoRecvBegin body must contain exactly one call to handleVideoRecvBegin_
awk '/void NJClient::DispatchTestVideoRecvBegin/,/^\}$/' /Users/cell/dev/JamWide/src/core/njclient.cpp \
  | grep -c "handleVideoRecvBegin_"  # must be 1
```
</verification>

<success_criteria>
- VideoRecvBuffer + VideoRecvState verbatim-ported with 4 MB pre-allocation per D-11 AND explicit wire-format contract comment (codex Cluster 6: marker payload size is 20 bytes, outer prefix value is 20)
- BEGIN handler raw-data branch acquires m_video_recv_cs and populates accumulating per upstream :1369-1404 (via private `handleVideoRecvBegin_` helper — codex Cluster 5)
- WRITE handler raw-data branch implements 4B BE length-prefix reassembly + 24B marker parse + mid-download startPlaying per upstream :1452-1564 (via private `handleVideoRecvWrite_` helper)
- WRITE handler END branch moves accumulating → next per upstream :1568-1591 (via private `handleVideoRecvEnd_` helper)
- Audio-thread `on_new_interval` receive block executes STAGE-1 promote + GUID-pair tree (DS/PREV/HOLD with kHoldCapDrop=4) per upstream :3084-3256 (via private `runVideoReceiveBlock_` helper)
- **Codex Cluster 5 (NEW):** Four private helpers are SINGLE SOURCE OF TRUTH — production dispatchers AND test dispatchers AND audio-thread on_new_interval all call the same code; test dispatchers are one-line forwarders with NO state-machine duplication
- **Codex Cluster 1 (NEW):** JAMWIDE_BUILD_TESTS-gated steady_clock timing instrumentation wraps `runVideoReceiveBlock_` and updates the atomic max-nanos counter via CAS loop; zero overhead in production; at least one Task 3 sub-test asserts the counter is actually accumulating > 0
- Audio-thread block reads m_remoteuser_mirror[s].next_ds[0]->guid (Phase 15.1-07a HIGH-2 carve-out preserved)
- User-leave video state reset matches upstream :1306-1322 (prev_ds_guid + slots cleared without removing the VideoRecvState)
- **11 WIRE-02 sub-tests** in tests/test_video_recv_state.cpp all pass under `ctest -R video_recv_state` — nine original behavior tests + two new malformed-length tests (codex Cluster 10): test_zero_length_frame_drops_cleanly, test_oversize_prefix_clamps_at_4mb_cap
- .claude/agents/realtime-audio-reviewer.md gains a `## Phase 21 audit allowlist envelope` section with concrete file:line entries (no `<line-placeholder>` tokens), an explicit **parity-only clause** (codex Cluster 1: "accepted ONLY as parity with the upstream NinjamZap source port"), an **anti-generalization clause** ("does not inherit acceptance from Phase 21" for future audio-thread work in unrelated subsystems), and a reference to the timing-instrumentation accessor `getRunVideoReceiveBlockMaxNanosForTest()` as the required UAT measurement surface
- No writeLog / SYNCLOG / m_users_cs.Enter inside the audio-thread receive block (D-16 hygiene)
- No `WaitableEvent::signal()` on the audio thread — Plan 21-02 redesign (codex Cluster 1 / Cluster 2) handles decoder wake-up off the audio thread
- Pre-existing test suite remains green (no regression in rawdata_send / video_state_machine / video_fourcc / video_encoder)
</success_criteria>

<output>
Create `.planning/phases/21-h-264-decoder-receive-pipeline/21-01-SUMMARY.md` when done.
</output>
